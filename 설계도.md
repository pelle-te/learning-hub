# 러닝 허브 — 설계도 (Architecture Blueprint)

> **문서 버전 v3.2** · 작성일 2026-06-28 · 최종 갱신 2026-06-28(**설계 철학 전환: "의존성 0" 교리 → "적응 우선"** §2.2. 이전: ESM 전환·유지율 추세·완료 렌더 일원화·ICS 신선도·due 역연동).
> 현재 코드 기준의 **있는 그대로의** 설계 문서. 목적: 이 문서를 바탕으로 개선·수정 지점을 찾기 위함. "이래야 한다"가 아니라 "지금 이렇다"를 기록한다.
> 짝 문서: 사용 관점은 `README.md`, 학습 이론은 `학습방법론.md`.
> **정기 점검:** 이 시스템을 12개 축으로 전수 평가하려면 `감사_템플릿.md`(복사해 채움) → 최근 결과는 `감사_리포트_2026-06-28.md`. 감사가 §14 후보를 갱신한다.

> **변경 이력**
> - **v3.2 (2026-06-28)**: **설계 철학 전환 — "의존성 0"을 비목표/경쟁우위로 못 박던 교리를 폐기하고 "적응 우선(§2.2)"으로 대체**. 다년 생존을 *불변(변화에 영향 안 받음)*이 아니라 *적응(변화에 잘 대응)*으로 재정의 — 기술이 바뀌어도 그대로면 생존이 아니라 도태. 의존성·빌드·기술 교체는 명분(역량·수명·유지비)이 서면 도입(도입 규율: 잠금 회피·rot 관리·로컬/오프라인 불변식·한 명령 재현). §2.1 비목표에서 "빌드/프레임워크/npm" 행 제거, §14 F-09 차단 사유 해제. *코드 변경 없음*(현재 필요한 의존성이 없어 기본값 유지).
> - **v3.1 (2026-06-28)**: ESM 단일 진입점 전환(`main.js`) 후 재감사(`감사_리포트` 🔄 갱신 2) 발견 반영 — ① 유지율(retention) 추세선(F-05, `retentionLog`·`recordRetentionSnapshot`/`retentionTrend`·통계 스파크라인), ② 콜드스타트 온보딩 구현 확인(F-07, `setupGuideCard`), ③ 완료 렌더 일원화(F-08, ui-kit 공용 `doneCheckbox`/`toggleDoneAt`), ④ ICS 신선도 가시화(F-10, `planSignature`·`_icsExport`·`icsFreshnessNote`), ⑤ due→시간예산 역연동(`ankiDueBudget`, 축 K 델타). 테스트 36→**44**(scheduler 20·state 11·ui-smoke 9·esm-smoke 4). 남은 F-09·F-11은 §14에 *방향*으로 유지.
> - **v3 (2026-06-28)**: 베이스라인 감사(`감사_리포트_2026-06-28.md`) 발견 반영 — ① 런타임 스캔 캐시를 내보내기/볼트백업에서 분리(F-01, §5·§13.2), ② 미배치·과적 복습을 경고로 노출(F-02, §6 6단계, "no silent caps"), ③ 백지 복습을 *단원(챕터) 단위*로 정밀화(F-03, §6 6.5단계·§9), ④ 백지 통과/막힘 명시 기록 + 막힘→CBMS 자동 연결(F-04, §4·§8.1), ⑤ CBMS 확신도(찍어맞음) 플래그(F-06, §4·§8.1). 테스트 36개로 확장(§12). 남은 발견(F-05·07·08·09·10·11)은 §14에 *설계만* 등록(다음 사이클).
> - **v2 (2026-06-28)**: 닫힌 피드백 루프 도입 — ① 적응형 용량(완료율→미래 계획, §6.4), ② 인출 증거 북극성 지표(§8.5), ③ 볼트 자동 백업·아카이빙(§5), ④ 3문장 요약·오답→Anki 카드 생성(§8.8·§9), ⑤ 에너지 인식 배치(§7), ⑥ 복습 이중계상 방지 옵션(§6). 비목표(§2.1)·데이터 손실 시나리오(§5.1)·스케일 예산(§13.1) 신설. 테스트 30개로 확장(§12).
> - **v1 (2026-06-28)**: 최초 as-is 블루프린트.

---

## 목차
1. [개요와 목적](#1-개요와-목적)
2. [설계 철학과 제약](#2-설계-철학과-제약)
3. [전체 아키텍처](#3-전체-아키텍처)
4. [데이터 모델 (state v3)](#4-데이터-모델-state-v3)
5. [영속화·백업·복구](#5-영속화백업복구)
6. [스케줄링 엔진](#6-스케줄링-엔진)
7. [하루 타임라인 & 내보내기(ICS)](#7-하루-타임라인--내보내기ics)
8. [탭별 설계 (9개)](#8-탭별-설계-9개)
9. [학습방법론 ↔ 앱 매핑](#9-학습방법론--앱-매핑)
10. [횡단 관심사](#10-횡단-관심사)
11. [외부 연동](#11-외부-연동)
12. [테스트](#12-테스트)
13. [알려진 제약·주의점](#13-알려진-제약주의점)
14. [관찰된 개선 후보](#14-관찰된-개선-후보)

---

## 1. 개요와 목적

**러닝 허브**는 한 명의 사용자(공대생)가 **졸업까지의 학습을 한 곳에서 굴리는 로컬 단일 페이지 웹앱**이다. 세 가지를 묶는다:

1. **계획 자동화** — 과목의 *주당 목표 시간*과 *챕터*만 넣으면, 일과의 빈 시간을 **2h 모듈**로 쪼개 과목에 자동 배분하고 복습까지 잡는다.
2. **학습방법론 실행** — 블록 내부 4단계·3문장 요약·CBMS 오답·보충필요 백로그·주간 메타인지 리뷰를 앱이 직접 굴린다(2026-06-28 추가된 실행 레이어).
3. **현황 통합** — 옵시디언 볼트(노트/검증/Anki 상태)·AnkiConnect due·졸업 요건을 같은 화면에서 본다.

핵심 데이터 단위는 **챕터**다. "그날 어떤 챕터를 배웠나"가 복습·통계·타임라인 모두의 근거가 된다.

---

## 2. 설계 철학과 제약

이 앱은 **로컬 정적 서버(http)로 띄운다** — `node serve.js` → `http://localhost:8000/`.
(2026-06-28 전환: 옛 `file://` 더블클릭 가정을 폐기. `file://`은 CORS로 ES모듈·fetch·PWA가 막혀,
서버로 올리면 그 제약이 한 번에 풀린다. `serve.js`는 의존성 0·Node 내장만.)

| 제약/선택 | 귀결 |
|---|---|
| **ES 모듈(`type="module"`)** | `index.html`은 `js/main.js` 하나만 로드. main이 의존성 순서대로 사이드이펙트 `import`. 각 모듈은 strict·독립 스코프이고, 파일 끝 `Object.assign(globalThis,{…})`로 **공개 표면만** 전역에 노출(인라인 onclick·타 모듈 호출용 상호운용 층). |
| **번들러·트랜스파일 없음(현재 기본값)** | *지금은* 외부 의존성 없이 순수 브라우저 API + 네이티브 ESM으로 돈다(`main.js`의 import 순서가 곧 의존성 그래프). 단 이건 **교리가 아니라 현재의 기본값** — 명분(역량·수명·이식성)이 서면 빌드·의존성·기술 교체를 받아들인다(§2.2 적응 우선). |
| **단일 사용자·단일 기기** | 인증·동기화 없음. 상태는 **localStorage** 한 곳. 백업은 수동 JSON 내보내기 + 볼트 폴더 백업. |
| **볼트/Anki 접근** | `fetch` 대신 **File System Access API**(사용자가 폴더 선택). manifest/서비스워커는 `http(s)`라 이제 **자동 활성화**(PWA·오프라인 캐시). |
| **UI = 문자열 템플릿** | 가상 DOM 없음. 각 탭은 `innerHTML`에 통째로 HTML 문자열을 써넣고 다시 그린다. 이벤트는 **인라인 `onclick`**(globalThis에 노출된 함수 호출). |

> 결과적으로 **"렌더 = 상태를 읽어 HTML 문자열을 만들고 컨테이너에 주입"**, **"상호작용 = (전역 노출된)함수가 상태를 바꾸고 persist() 후 다시 렌더"**라는 단순 사이클이 앱 전체를 관통한다.
>
> **공유 가변 런타임 상태**(`state`·`TAB`·`RES`·`vaultHandle`)는 여러 모듈이 읽고 쓰므로(예: ui-*가 `RES=schedule()`) import 바인딩(읽기전용) 대신 **globalThis 슬롯**에 둔다.

### 2.1 비목표 (Non-Goals)

단일사용자·로컬 철학에서 **지금 일부러 하지 않는** 것들. 단 *영구 금지*가 아니라 *현재 트레이드오프상 보류*다(§2.2 적응 우선). 명분이 서면 재검토 대상이며, **의존성·빌드·기술 스택은 더 이상 비목표가 아니다** — 그건 §2.2의 적응 원칙으로 옮겼다.

| 비목표(현재 보류) | 이유 | 재검토 트리거 |
|---|---|---|
| **멀티기기·실시간 동기화/클라우드** | 서버·인증이 들어오면 로컬·오프라인·프라이버시 불변식이 깨짐. 백업은 수동 JSON·볼트 파일로 충분. | 다기기 사용이 실제 필요해지고, 종단암호화 등으로 프라이버시를 지킬 수 있을 때. |
| **실시간 양방향 캘린더(Google API 등)** | OAuth·서버 필요. `.ics` 일회성 내보내기로 대체(§7). | 양방향 동기 가치가 OAuth 복잡도를 넘을 때. |
| **AI 자동 카드/요약 생성(LLM 호출)** | *생성 효과*(사람이 직접 추리는 학습 이득)를 죽임. 카드는 *초안*만, 큐레이션은 사람이(§8.8). | 학습 효과를 해치지 않는 보조 역할(예: 분류 제안)로 한정될 때. |
| **SuperMemo식 incremental reading** | 복잡도가 단일사용자 학습 플래너의 효용을 초과. | — |
| **협업·공유·다중 사용자** | 단일 사용자 전제(상태 1개·localStorage 1곳). | — |

### 2.2 적응 우선 (Adaptability over immutability)

> **다년 생존 = 변화에 *영향을 안 받는 것*이 아니라 변화에 *잘 대응하는 것*.** 20~30년 뒤 기술이 완전히 바뀌었는데 코드가 그대로면 그건 생존이 아니라 도태다. 그래서 **"의존성 0"은 목표가 아니라 현재의 기본값**일 뿐이다(과거 v3까지는 이를 비목표·경쟁우위로 못 박았으나, v3.2에서 뒤집음).

- **기본값은 여전히 가볍게** — 명분 없이 의존성을 더하지 않는다. 가벼움 자체가 이식·이해·수명에 유리하고, 의존성을 *그냥* 잔뜩 받는 것도 적응이 아니다(rot·공급망 위험).
- **명분이 서면 받아들인다** — 다음 중 하나가 분명하면 라이브러리·빌드 도구·런타임·기술 교체를 도입한다.
  - **역량**: 자체 구현이 비현실적인 가치를 준다(예: 대용량 가상 스크롤 F-09, 검증된 접근성 컴포넌트, 실측 차트).
  - **수명·이식성**: 표준에 더 가까워지거나(웹 표준·웹컴포넌트 등) 미래 플랫폼으로 옮기기 쉬워진다.
  - **유지비**: 자체 구현을 떠안는 비용이 의존성 추적 비용을 넘는다.
- **도입 규율(부패 관리)** — ① **잠금 회피**(표준·교체 가능한 것 우선), ② 버전 핀 고정 + 정기 갱신으로 rot 관리, ③ **로컬·오프라인·프라이버시 불변식 유지**(축 I), ④ 빌드를 넣더라도 *소스→실행*이 한 명령으로 재현되게.
- **정기 재평가** — 학기/반기 감사(축 K·H)에서 "이 스택이 5년 뒤에도 옮겨갈 만한가, 지금 받아들일 의존성/표준은 없나"를 묻는다. 멈춰 있으면 그게 곧 도태 신호.

---

## 3. 전체 아키텍처

### 3.1 파일 구조

```
러닝허브/
├─ serve.js                ★로컬 정적 서버(의존성 0·Node 내장). `node serve.js` → http://localhost:8000/
├─ index.html              진입점: 헤더 + 빈 컨테이너(#nav·#page) + `<script type="module" src="js/main.js">` 하나
├─ manifest.webmanifest    PWA 매니페스트 (http 서빙이라 자동 주입)
├─ sw.js                   서비스워커 (http 서빙이라 자동 등록)
├─ icon.svg                앱 아이콘
├─ css/style.css           전체 스타일 (CSS 변수 토큰 시스템 · 다크/라이트)
├─ js/
│  ├─ main.js              ⓪ ★ESM 진입점: 모듈을 의존성 순서로 import + 마지막 app.js가 부팅
│  ├─ utils.js             ① 상수 + 날짜/시간/주(週) 유틸 + loadVaultIndex + 탭 공용 헬퍼(itemById·ddayInfo)
│  ├─ ui-kit.js            ② 공용 UI 프리미티브(toast·confirmModal·promptModal) — IIFE 캡슐화
│  ├─ tabs.js              ③ ★탭 레지스트리(registerTab) + 네비 + render() 디스패치 + 탭 전역(TAB·RES·vaultHandle)
│  ├─ state.js             ④ 핵심 전역 상태·기본값·영속화·migrate·완료추적·테마
│  ├─ data-methodology.js  ⑤ 방법론 데이터(요약·CBMS·백로그·백지·주간리뷰·Anki카드·볼트백업·아카이빙)
│  ├─ scheduler.js         ⑥ ★배분 엔진 + 하루 타임라인 + ICS
│  ├─ ui-today.js          ⑦ 🎯 오늘 학습 (실행 레이어)
│  ├─ ui-schedule.js       ⑧ 📅 주간 스케줄
│  ├─ ui-items.js          ⑨ 📝 학습 항목
│  ├─ ui-routine.js        ⑩ ⏰ 일과 & 가용시간 + 기본 설정
│  ├─ ui-stats.js          ⑪ 📊 통계
│  ├─ ui-review.js         ⑫ 🔄 주간 리뷰 (메타인지)
│  ├─ ui-vault.js          ⑬ 📚 볼트 현황
│  ├─ ui-anki.js           ⑭ 🃏 Anki 현황
│  ├─ ui-degree.js         ⑮ 🎓 졸업 계획
│  ├─ ui-command.js        ⑯ ⌘K 명령 팔레트 + 전역 단축키(탭 아님 — registerTab 안 함)
│  ├─ app.js               ⑰ 헤더 오버플로 메뉴 + 부팅(applyTheme·render)
│  └─ vendor/              ★벤더링 의존성(오프라인·핀·동적 import만): lit-html.js(키드 렌더 · §2.2·README)
└─ test/*.test.js          회귀 테스트 4종(scheduler·state·ui-smoke·esm-smoke · Node 내장만, 의존성 0)
```

> **ESM 모델**: 각 파일은 진짜 모듈(strict·독립 스코프). 파일 끝 `Object.assign(globalThis,{…})`로 *공개 표면*만 전역에 올려, 인라인 onclick과 타 모듈이 호출한다(내부 헬퍼는 노출 목록에서 빼면 비공개 = 진짜 캡슐화). `import` 키워드는 `main.js`에만 있어, 테스트는 파일을 그대로 평가해 검증할 수 있다.
>
> **각 ui-\*.js는 파일 끝에서 `registerTab({key,label,group,order,render})`로 자신을 등록한다.**
> 탭 추가 = ① 새 `ui-*.js` + `registerTab` 한 줄, ② `main.js`에 `import` 한 줄, ③ `sw.js` 캐시 라인 — **app.js·tabs.js는 안 건드림**.

### 3.2 로드 순서 (의존성 그래프)

`js/main.js`가 **이 순서로 import** 한다(앞이 뒤가 쓰는 전역을 먼저 노출):

```
utils → ui-kit → tabs → state → data-methodology → scheduler
      → ui-today → ui-schedule → ui-items → ui-routine → ui-stats
      → ui-review → ui-vault → ui-anki → ui-degree → ui-command → app
```

- `utils`는 다른 모두가 쓰는 상수·날짜 유틸·탭 공용 헬퍼 → **맨 처음**.
- `ui-kit`은 토스트/모달 프리미티브(state·ui-*가 씀).
- `tabs`는 `registerTab`을 정의 → **자신을 등록하는 ui-\* 보다 먼저** 와야 한다. 탭 전역 `TAB`/`RES`/`vaultHandle`도 여기서 선언.
- `state`는 `utils`의 `KEY`/`iso`/`rid` 등을 씀.
- `data-methodology`는 `state`의 `persist`/`exportSnapshot`/`SCHEMA_VERSION`을 씀 → state 다음.
- `scheduler`는 `state`·`utils`를 씀.
- `ui-*`는 `scheduler`·`state`·`data-methodology`·`utils`를 씀. **ui끼리는 순서 무관**(전역 노출 심볼은 호출 시점에 모두 정의돼 있음). 탭 간 공용 헬퍼(`itemById`·`ddayInfo`)는 utils로 올려 ui끼리 의존하지 않는다.
- `app`은 부팅(`applyTheme(); render();`) → **맨 마지막**. `render()`는 tabs.js의 레지스트리에서 현재 탭을 찾아 디스패치.

> 평가순서 안전장치: 각 모듈이 파일 끝에서 자신을 globalThis에 노출하므로, main.js가 위 순서로 import하면 뒤 모듈이 쓰는 전역이 평가 시점에 이미 존재한다(옛 `<script>` concat과 동일 거동).
>
> ⚠️ 새 ui 파일의 `main.js` `import` 라인을 빠뜨리면 `registerTab`이 안 불려 **그 탭이 네비에 안 나타난다**(부팅 시 바로 눈에 띔).

### 3.3 런타임 전역 변수

| 변수 | 정의 위치 | 역할 |
|---|---|---|
| `TAB` | tabs.js | 현재 탭. **기본값 `'today'`** |
| `RES` | tabs.js | 마지막 `schedule()` 결과 캐시 (여러 탭이 갱신·참조) |
| `vaultHandle` | tabs.js | 옵시디언 폴더 핸들 (세션 동안만, 비영속) |
| `TAB_REGISTRY` | tabs.js | 등록된 탭 정의 배열. `registerTab`이 채우고 `renderNav`/`render`가 읽음 |
| `weekOffset` / `schedView` / `schedSelDow` | ui-schedule | 주간뷰 상태. `schedView`만 localStorage `'sched_view'`에 영속 |
| `classDow` | ui-routine | 수업 편집기에서 보는 요일 |
| `openItems` / `openSems` | ui-items / ui-degree | 펼친 아코디언 id 집합 (세션 동안만) |
| `reviewWeekOffset` | ui-review | 주간 리뷰에서 보는 주 |
| `_chDrag` | ui-items | 챕터 드래그 상태 |

### 3.4 네비게이션 & 렌더 사이클

**단일 탭바(레지스트리 구동).** 각 탭은 `registerTab({key,label,group,order,render})`로 자신을 올리고, `tabs.js`는 레지스트리만 순회한다. `group`이 바뀌는 경계에만 구분선(`navsep`)을 넣어 그룹이 늘어도 자동 대응 — 현재는 `main`(8개)과 `degree`(학사) 2그룹이라 졸업 계획 앞에 구분선 하나(원래 IA 유지).

- `registerTab(def)` → 같은 `key`면 덮어쓰고, 아니면 추가(중복 누적 방지).
- `go(t)` → `TAB=t` 후 `render()`.
- `render()` → `renderNav()`(레지스트리 순서·그룹대로 버튼 생성) + 현재 `TAB`의 `render(pageEl())` 호출(없으면 첫 탭 폴백).
- **탭이 늘어날 때**: 그룹을 더 나누면(예 `'분석'`/`'자원'`) 구분선이 자동으로 늘고, 더 많아지면 `renderNav`에 오버플로(⋯) 분기만 추가하면 된다 — 등록부는 안 바뀜.

**렌더 사이클 (모든 탭 공통 패턴):**

```
사용자 입력 → 인라인 onclick → 전역 핸들러
   → state 변경 → persist() → render<Tab>(pageEl())  // 해당 탭만 다시 그림
```

대부분의 `render*`는 진입 시 `persist(); RES=schedule();`을 먼저 호출해 **항상 최신 스케줄로 그린다**(schedule()은 순수 계산이라 매번 호출해도 안전·저렴).

> **텍스트 입력 중 재렌더 회피 패턴**: 이름/메모 같은 텍스트 필드는 `oninput`에서 `...Silent()`(저장만)·`onchange`(blur 시)에서 전체 재렌더 → 타이핑 중 포커스 유실 방지(ui-routine의 `updBlockSilent` 등).

---

## 4. 데이터 모델 (state v3)

전역 `state` 객체 하나가 앱의 모든 영속 데이터다. `SCHEMA_VERSION = 3`.

### 4.1 최상위 스키마

```js
state = {
  schemaVersion: 3,
  theme: 'dark' | 'light',
  startDate: 'YYYY-MM-DD',        // 계획 시작일 (모든 날짜 인덱스의 기준)
  moduleLen: 120,                 // 모듈 1칸 = 분 (기본 2h)
  reviewRatio: 20,                // 가용시간 중 복습예산 비중(%)

  routine: [ Block, ... ],        // 고정 일과 블록 (수면/식사/취미/수업/기타)
  dayOverrides: { 'YYYY-MM-DD': 1.5 },  // 특정 날짜 가용시간(시간) 덮어쓰기

  items: [ Item, ... ],           // 학습 항목(과목) — 앱의 중심
  completions: { 'YYYY-MM-DD': { 'sid|type': {done:true, min:90} } },  // 실행 추적

  // ── 학습방법론 실행 레이어 (2026-06-28 추가) ──
  summaries: { 'YYYY-MM-DD': [ {id,sid,name,s1,s2,s3} ] },  // 3문장 요약
  cbms: [ {id,ds,sid,name,chapter,code,note,conf} ],        // 오답 분류 C/B/M/S/T (conf=찍어맞음/확신없음 · F-06)
  backlog: [ {id,ds,sid,name,topic,note,done,doneDs} ],     // 보충필요 백로그
  blankResults: [ {id,ds,sid,name,passed,note} ],          // 백지 통과/막힘 실측(9절·E4 · F-04). 막힘은 CBMS(C)로 자동 연결
  weekly: { '주월요일ISO': {checks:{backlog,cbms,plan,anki}, note} },  // 주간 리뷰
  rituals: { 'YYYY-MM-DD': {plan, shutdown, note} },        // 일일 의식(아침 계획·저녁 셧다운 · Stage 3)
  blankReviewWeekly: true,        // 백지 복습 자동 배치 on/off
  mockEveryWeeks: 0,              // 모의시험 N주마다 (0=끔)

  // ── 적응·배치 설정 (v2, 2026-06-28) ──
  adaptiveCapacity: true,         // 최근 14일 완료율로 미래 계획 용량 보정 (§6.4)
  peakStart: '', peakEnd: '',     // 각성도 최고 시간대(HH:MM) — new/mock 우선 배치 (§7). 빈값=끔
  reviewViaAnki: false,           // 복습을 Anki/FSRS에 위임 → 합성 rev 슬롯 생성 끔(시간 이중계상 방지)

  degree: { targetTotal, reqMajorReq, reqMajorSel, reqLiberal, semesters:[...] },
  anki: { source: 'file' },

  // ── 런타임 캐시(영속되긴 하나 계산 산출물) ──
  _vaultScan: { at, src, subjects:[...] },   // 볼트 스캔 결과
  _ankiFile:  { at, src, decks:[...] },       // Anki 파일 스캔
  _ankiLive:  { at, decks:[...] },            // AnkiConnect due
  _lastBackupAt: 'ISO datetime',  // 마지막 볼트 백업 시각(백업 노후 경고용 · §5)
  // _today: 'YYYY-MM-DD'  ← 테스트/시뮬레이션 시드. migrate가 실데이터에서 제거(§4 주의)
}
```

### 4.2 핵심 하위 타입

**Item (학습 항목)** — `items[]`의 원소:
```js
{
  id, source,          // source: '직접'|'볼트'|'Anki'|'수강'
  name, color,         // color: PALETTE에서 순환 배정
  mode: 'weekly' | 'daily',
  weeklyHours: 5,      // mode='weekly': 주당 목표 시간
  dailyMin: 30,        // mode='daily' (Anki 등): 매일 고정 분
  deadline: '',        // 선택 (시험일)
  chapters: [ {id, name, hours, done:false}, ... ]  // 순서대로 학습
}
```

**Block (일과 블록)** — `routine[]`의 원소:
```js
{ id, name, type, start:'HH:MM', end:'HH:MM', days:[0..6] }
// type ∈ BLOCK_TYPES: 수면·식사·취미·수업·기타. days: 일=0 .. 토=6
// '공부' 블록 개념은 폐지 — 가용시간은 '깨어있는 시간 − 블록'으로 자동 계산
```

**completions 키 규약**: `'YYYY-MM-DD'` → `{ 'sid|type': {done, min} }`. `type`은 `new|rev|anki|blank|mock`. 완료 체크 시 그 항목의 *계획 분*을 `min`에 기록 → 통계의 "실제 완료 시간"이 된다.

### 4.3 상수 (utils.js / 각 ui)

| 상수 | 값 | 위치 |
|---|---|---|
| `REVIEW_OFFSETS` | `[1,3,7,16]` | utils — 간격 복습일 |
| `PALETTE` | 8색 | utils — 과목 색 순환 |
| `BLOCK_TYPES` | `{수면,식사,취미,수업,기타}`→색 | utils |
| `SKIP` | `{attachments,.obsidian,.trash,...}` | utils — 볼트 스캔 제외 폴더 |
| `KEY`/`BACKUP_KEY`/`CORRUPT_KEY` | `study_planner_v3`(+`_backup`/`_corrupt`) | utils/state |
| `CBMS_INFO` | C/B/M/S/T → {label,tip,color} | state |
| `BLOCK_STAGES` | 4단계 [이름,시작%,끝%,행동] | ui-today |
| `WEEKLY_CHECKS` | 주간 점검 4항목 | ui-review |
| `CATS` | `[전공필수,전공선택,교양,기타]` | ui-degree |

---

## 5. 영속화·백업·복구

`state.js`가 전담. 데이터 손실 방지에 상당히 방어적으로 설계돼 있다.

- **boot()** — 부팅 시 `localStorage[KEY]`를 읽어 `migrate(JSON.parse(...))`. 실패하면 **원본 raw를 `CORRUPT_KEY`에 보존**한 뒤 `defaults()`로 시작(과거: 손상 → 기본값 → 첫 persist가 원본을 덮어써 영구 손실됐던 버그 P1-7 대응). `recoverCorrupt()`로 복구 시도 가능.
- **migrate(s)** — `validShape()`로 최소 구조(items/routine/degree.semesters/startDate) 검증 후, 누락 필드를 `defaults()`로 보강. **구버전 호환의 단일 창구** — 새 필드를 추가하면 여기에 보강 라인을 넣어야 한다. (예: `if(s.summaries==null) s.summaries={}`)
- **persist()** — `localStorage.setItem`(동기 1차 저장). 용량 초과 시 토스트 경고. + **IndexedDB write-through 미러**(`idbMirror`, Stage 5): 같은 스냅샷을 비동기로 IDB에 복제 → 사이트 데이터 삭제로 localStorage 전소 시 `restoreFromIDB()`로 복구(미지원/테스트는 no-op).
- **백업/되돌리기** — 파괴적 동작(초기화·가져오기) 직전 `backupOrConfirm()`가 `BACKUP_KEY`에 스냅샷. `undoLast()`로 1단계 되돌리기. 백업 실패 시 사용자에게 진행 여부 확인.
- **내보내기/가져오기** — `exportJSON()`(파일명 `러닝허브_<startDate>.json`)·`importJSON()`(migrate 통과 못 하면 거부). **`exportSnapshot()`가 런타임 스캔 캐시(`_vaultScan/_ankiFile/_ankiLive`)를 빼고 직렬화**(F-01) → 내보내기·볼트백업 파일이 가볍고 깨끗. 가져오기 시에도 들어온 파일의 옛 스캔 캐시는 폐기. *영속(localStorage)에는 캐시를 남겨* 로컬 새로고침 시 재스캔을 아낀다.
- **볼트 폴더 백업(v2)** — `backupToVault()`가 File System Access의 *쓰기*로 볼트 폴더에 `러닝허브_백업.json`을 떨군다(localStorage 전소 대비). 마지막 시각을 `_lastBackupAt`에 기록 → 일과 탭 유지보수 카드가 **7일 이상 미백업 시 경고**(`lastBackupDays()`).
- **오래된 기록 아카이빙(v2)** — `archiveOldData(6)`가 6개월 이전 completions/summaries/cbms/회수된 backlog를 **보관 파일(`러닝허브_보관_<cutoff>.json`)로 내려받고 앱에서 비운다** → 쿼터·재렌더 성능 보호(§13.1). `dataSizeKB()`/`recordCount()`로 현재 크기 표시.
- **v2 비호환** — 모델 변경으로 이전 버전 데이터와 호환 안 됨(처음 한 번 새로 입력).

> 새 실행 레이어 데이터(summaries/cbms/backlog/weekly)도 같은 `KEY`에 저장 → **내보내기 백업에 자동 포함**, 가져오기 시 `migrate`가 보강.

### 5.1 데이터 손실 시나리오 & 복구 스토리

| 시나리오 | 영향 | 복구 |
|---|---|---|
| **상태 손상(파싱 실패)** | 부팅 시 기본값으로 시작 | 원본을 `CORRUPT_KEY`에 보존 → `recoverCorrupt()`. (테스트 S6) |
| **파괴적 동작(초기화/가져오기)** | 직전 상태 덮어씀 | `backupOrConfirm()`→`undoLast()` 1단계 되돌리기. |
| **브라우저 캐시/사이트데이터 삭제** | localStorage **전소** | ⚠️ **마지막 수동 내보내기/볼트 백업 이후 전부 소실** → 정기 백업이 유일한 방어(유지보수 카드 경고). |
| **localStorage 쿼터 초과(다년 누적)** | persist 실패(alert) | 아카이빙(§5)으로 비우거나 내보내기 후 정리. |
| **다른 브라우저/기기에서 열기** | 데이터 없음(동기화 안 함, §2.1 비목표) | 내보낸 JSON을 가져오기. |

---

## 6. 스케줄링 엔진

`scheduler.js`의 `schedule()`이 앱의 심장. **순수 함수**(전역 `state`만 읽고 DOM 안 씀) → 매 렌더마다 호출되고 테스트도 쉽다.

### 6.1 입력·출력

- 입력: `state.startDate`, `items`, `routine`(→ 가용시간), `moduleLen`, `reviewRatio`, `dayOverrides`, `blankReviewWeekly`, `mockEveryWeeks`.
- 출력:
```js
{
  days: [ {ds,date,wd, studyMin,used,modLeft,revLeft, items:[PlanItem]} ],
  itemStat: [ 과목별 진행/완료/예상종료/마감 ],
  weekHours: { '주ISO': { sid: hours } },   // 통계 막대용 (anki·new만)
  chapterLog: [ {ds,date,name,color,chapters} ],  // 챕터 타임라인용 (new만)
  warnings: [ '⚠ ...' ],
  capUsed, capTotal, ML
}
```
**PlanItem**: `{type, sid, name, color, min, chapters?, mod?}`, `type ∈ new|rev|anki|blank|mock`.

### 6.2 가용시간 계산 (routine → 빈 시간)

"공부 블록"이라는 개념은 없다. 대신:
- `awakeBounds()` — **수면 블록**으로 깨어있는 구간 `[wake0,wake1]` 결정(없으면 0~1440).
- `freeWindowsForWeekday(wd)` — 깨어있는 시간에서 **수면 제외 모든 블록**(식사·취미·수업·기타)을 빼 **빈 구간(windows)**과 그 합 `freeMin`을 낸다. 겹치는 블록은 병합.
- `studyMinByWeekday()` — 요일별 빈 시간(분) 배열.
- `dayStudyMin(ds,wd,cap)` — **`dayOverrides` 우선**, 없으면 요일 기본값.

### 6.3 배분 알고리즘 (7단계 + 2)

```
1) horizon 결정   : 마감 최댓값 vs 주당 페이스로 모든 챕터 끝낼 주(최대 26주). 시작~종료 일수.
2) 일자 생성      : day마다 studyMin 계산.
3) daily 먼저     : Anki 등 mode='daily'를 매일 dailyMin 확보(가용 안에서). type='anki'.
   └ 남은 시간을 학습/복습으로 분리:
       learn = round(rem*(1-revFrac));  modLeft = floor(learn/ML);  revLeft = rem − modLeft*ML
4) 과목 상태 초기화: done 챕터 제외, 남은 챕터만 _chs로. 챕터 포인터(_cum,_idx). 마감 인덱스 _dlIdx.
5) 주(週) 단위 모듈 배분 :
     - 과목별 그 주 목표 모듈 = weeklyHours*60/ML, 분수 모듈은 _carry로 이월(과/미배정 방지).
     - 날짜순·모듈 슬롯마다: 후보 = (주간목표 미달 & 챕터 남음 & 마감 전).
       정렬: ① 마감 임박(_dlIdx−di) ② 덜 채운 과목. 같은 과목 연속 회피(인터리빙: lastSid≠pick).
     - advance()로 그 모듈이 덮는 챕터 계산 → PlanItem(type='new', chapters).
     - 복습 예약: REVIEW_OFFSETS(1·3·7·16)마다 reviewTask 생성(그날 배운 챕터 동반, min≥15·ML*0.25).
6) 복습 배치      : revLeft 우선 → 모듈 잔여 → 여유 큰 날. 같은 과목 복습은 같은 날에 병합(min·챕터 합침).
                    ★ 용량 부족으로 *미배치(revMissed)* 되거나 *여유 없는 날에 넘겨 끼운(revOver)* 복습은
                      warnings에 개수로 노출(F-02 · "no silent caps" — 사용자가 '복습 계획됨' 착각 방지).
6.5) 백지 복습    : blankReviewWeekly=true일 때. ★ *단원(챕터) 단위*(F-03) — 각 챕터의 '마지막 학습일' 직후
                    여유 날에 1칸(type='blank', chapters=[단원1개], min=max(30,ML*0.4)). 용량 없으면 건너뜀(의도된 캡).
6.6) 모의시험     : mockEveryWeeks>0일 때. (w+1)%N==0인 주말의 여유 날에 1모듈(type='mock', min=ML).
                    이전에 학습한 게 있어야 배치.
7) 통계 산출      : itemStat(진행률·예상종료·마감초과 warning), weekHours, chapterLog, capUsed/Total.
```

**불변식(테스트로 고정)**: 하루 `used ≤ studyMin(+1)`, 복습은 첫 학습 이후, daily는 매일 확보, 인접 같은 과목 금지, done 챕터 제외, blank/mock는 용량 초과 안 함.
> *예외(문서화됨)*: 복습이 창 내 모든 날을 채우면 마지막으로 '여유 큰 날'에 넘겨 끼워 `used`가 `studyMin`을 넘을 수 있다(revOver). 이는 침묵하지 않고 **warnings로 노출**(F-02)하므로 사용자가 인지·보정한다(테스트 T19).

> **분수 모듈 캐리오버** `_carry`: 주 1h·모듈 2h면 매주 0.5모듈씩 누적해 2주에 1모듈. 과목이 사라지지 않게 하는 장치.

> **복습 이중계상 방지(v2)** — `reviewViaAnki=true`이고 daily(Anki) 항목이 있으면 **5단계의 합성 간격복습(rev) 슬롯 생성을 끈다**. 매일 Anki가 이미 시간을 예약하고 실제 due는 FSRS가 소유하므로, 동일 활동(복습)을 시간 예산에서 두 번 빼지 않게 한다(테스트 T17). 기본은 off(기존 동작 유지).

### 6.4 적응형 용량 — "계획은 가설" (v2)

방법론 1·10절의 *"계획은 가설이다 — 어긋나면 다음 블록에서 보정한다"*를 코드로 닫는 피드백 루프. **데이터(`completions.min`)는 이미 있었으나 통계로만 흐르고 계획으로 되돌아오지 않던** 구멍을 메운다.

- **`adherenceFactor(start,horizon,capWd,today)`** — `today` 이전(과거) **최근 14일**의 `Σ실제 완료분 / Σ가용분`을 구해 `[0.5, 1.0]`로 클램프. 이력 부족(<3 활동일) 또는 `adaptiveCapacity===false`면 **1.0**.
- **적용**: 2단계 일자 생성 시 **오늘/미래 날(`ds ≥ today`)의 `studyMin`만** 그 비율로 축소(과거는 원본 유지). 상한 1.0이라 *가용 이상으론 절대 안 늘림* → 용량 불변식 보존.
- **효과**: 꾸준히 70%만 완료하면 다음 계획도 ~70%로 줄어 "실패할 계획"이 "굴러가는 계획"이 된다(Motion/Reclaim의 실측 재계획과 같은 원리, 서버 없이).
- **`today` 시드**: 순수성·테스트 결정성을 위해 `todayISO()`(=`state._today || iso(new Date())`)를 단일 출처로. 결과에 `adapt`·`adaptApplied` 노출(통계 표시용). (테스트 T15·T16)

---

## 7. 하루 타임라인 & 내보내기(ICS)

- **`layoutDay(day)`** — 그날의 PlanItem들을 **빈 구간(windows)에 시각 배정**. `{tl, free, freeMin, sessions}` 반환. `tl`은 일과 블록 + 학습 세션을 시각순으로 합친 행 목록. 빈 구간을 넘치면 `over`로 표시(미배치). 스케줄/오늘 탭 둘 다 이걸로 시각을 얻는다.
  - **에너지 인식 배치(v2)** — `peakStart`/`peakEnd`가 설정되면(방법론 1절 *"가장 어려운 새 학습을 가장 맑을 때"*) **고인지부하(`new`·`mock`)를 피크 구간에 먼저 배치**하고, 복습·Anki는 남은 시간에. `take(need, prefer)`가 빈 구간을 `subtractIntervals`로 깎아가며 할당(여러 구간으로 쪼갬 허용). **피크 미설정 시엔 입력 순서대로 이른 시각부터 채우는 기존 동작과 100% 동일**(회귀 안전). (테스트 T18)
- **`buildICS()`/`exportICS()`** — 학습/복습/Anki/백지/모의 세션을 **플로팅 로컬타임 VEVENT**로 변환해 `.ics` 다운로드. 라벨: 📘학습·🔁복습·🃏Anki·📝백지복습·🧪모의시험. 구글/애플/아웃룩에 그대로 가져옴.

---

## 8. 탭별 설계 (9개)

각 탭은 `render<Tab>(p)` 하나가 진입점이며 `p`(=`#page`)에 HTML을 주입한다.

### 8.1 🎯 오늘 학습 (`ui-today.js`) — 실행 레이어
- **목적**: "블록 안에서 어떻게 공부하나"를 굴린다. 계획(스케줄)이 *언제/무엇*이면 여기는 *어떻게*.
- **구성**: ①5원리 칩 + 부록C 축소판 안내 ②오늘의 블록(각 `new` 모듈에 **4단계 비중 막대 `stageBar`** + 70% 룰 + 과목 프리필 버튼; rev/anki/mock엔 절차 리마인더; **`blank`엔 통과/막힘 버튼**(F-04) — 막힘은 메모를 받아 CBMS(C)로 자동 연결) ③블록 흐름·체크리스트(접이식 참고) ④3문장 요약 입력·목록 ⑤CBMS 오답 입력·목록(칩+처방 + **확신없음(찍어맞음) 체크**, F-06) ⑥보충필요 백로그(추가·회수).
- **데이터**: `summaries`/`cbms`/`backlog`/`blankResults` ← state 헬퍼(`addSummary`/`addCbms(…,conf)`/`addBacklog`/`setBlankResult`/`toggle·del`). 완료 체크는 `toggleDoneToday`→`setDone`→재렌더. 백지 결과는 `blankPass`/`blankBlocked`→`setBlankResult`(막힘 시 `addCbms` 연쇄).
- **상수**: `BLOCK_STAGES`(4단계), `subjectOptions()`(과목 드롭다운), `itemById()`.

### 8.2 📅 주간 스케줄 (`ui-schedule.js`)
- **목적**: 한 주씩(◀▶) 일정을 본다. **개요뷰**(7일 막대 스트립 + 선택일 아젠다)와 **카드뷰** 전환(`schedView`, localStorage 영속).
- **핵심**: `dayParts(curMon,k)` — 하루치 머리글/진행막대/타임라인 행/꼬리를 계산(두 뷰 공용). `studyRow()` — 학습/복습/Anki/백지/모의 한 줄(완료 체크박스 포함). 날짜 칸에서 **가용시간 직접 덮어쓰기**(`setDayOverride`). `deadlineCard()` — D-day 카운트다운.
- **완료 추적**: 행마다 체크 → `setDone(ds,sid,type,plannedMin,on)`. 개요뷰 막대는 완료/잔여를 색 분리.

### 8.3 📝 학습 항목 (`ui-items.js`)
- **목적**: 과목별 *주당 목표 시간*·*챕터(순서·예상시간·완료)* 편집. **접이식 아코디언**(과목 많아도 짧게).
- **핵심**: `itemCard()`(접힘=요약 칩, 펼침=편집 그리드+챕터), `stepper()`(+/−), `chapterEditor()`(표+드래그 정렬 `chDragStart/chDrop`+일괄 붙여넣기 `bulkCh` "이름 | 시간"). CRUD: `addItemUI`/`addItem`(타 탭 호출용)/`updItem`/`updCh`/...
- **출처**: `addItem(name,{source,mode,weeklyHours,chapters})`을 볼트·Anki·졸업 탭이 공용 호출.

### 8.4 ⏰ 일과 & 가용시간 (`ui-routine.js`)
- **목적**: 기본 설정(시작일·모듈길이·복습비중 **+ 백지복습 토글·모의시험 주기 + 적응형 용량·복습 위임·피크 시간대**) + 고정 일과 블록 편집 → 요일별 공부 가능 시간 자동 표시.
- **핵심**: 수업은 **요일별 개별 블록**(`renderClassList`, 세그먼트로 요일 선택). 그 밖 블록은 `renderBlocks`(유형·시각·요일칩·평일/주말/매일 프리셋). `setSetting(k,v)`로 기본 설정 저장.
- **유지보수 카드(v2)** `maintenanceCard()`: 마지막 백업 노후 경고 + 저장 크기/기록 수 + **볼트 백업·파일 내보내기·오래된 기록 정리(아카이빙)** 버튼(§5).

### 8.5 📊 통계 (`ui-stats.js`)
- **목적**: 무엇을 얼마나 학습했나 + **얼마나 꺼낼 수 있게 됐나**(v2).
- **핵심**: KPI 4개(실제완료/계획·연속학습일 `studyStreak`·완료챕터·복습세션) + 과목별 진행 표 + **주별 학습시간 누적 막대**(`weeklyBars`, weekHours) + **챕터 타임라인**(`chapterTimeline`, chapterLog).
- **인출 증거 패널(v2)** `retrievalCard()` — *투입(시간) 아닌 출력 지표*(방법론 14절). ① **CBMS 주간 추세**(`cbmsTrend`: 지난주→이번주, 감소=약점 닫힘) ② **백지 복습 완료율**(계획된 blank 중 done) ③ **능동 인출 활동**(요약+완료 백지+완료 모의). "이해했다"를 "꺼낼 수 있다"로 바꾼 증거를 북극성으로.

### 8.6 🔄 주간 리뷰 (`ui-review.js`) — 메타인지
- **목적**: 주 1회 *공부 방식*을 점검(방법론 10절).
- **구성**: 주 선택(◀▶) + ①계획 대비 실제(`weekPlanActual`: 그 주 7일 계획/완료 분 합 + 요일별 미니 막대) ②CBMS 분포(`cbmsCounts` 막대 + 최다 코드 처방 힌트) ③보충필요 회수(열림/이번주 회수 수 + 열린 항목 닫기) ④주간 체크리스트(`WEEKLY_CHECKS` 4항목)+메모(그 주 `weekly[wk]`에 저장).

### 8.7 📚 볼트 현황 (`ui-vault.js`)
- **목적**: 옵시디언 전공 폴더 → 과목→챕터 노트수/검증/구버전/Anki 상태. 과목·챕터를 학습 항목에 주입.
- **핵심**: `scanVault()` — **정본 `_meta/감사/_index.json` 우선**(`loadVaultIndex`→`subjectsFromIndex`), 없으면 **`.md` 직접 스캔 폴백**(`scanVaultFromFiles`+`readFM`로 YAML frontmatter). 결과를 `state._vaultScan`에 캐시. `addSubjFromVault`/`addChapFromVault` → `addItem`. 노트수→예상시간 `estH=max(1,round(notes*0.5))`.
- **상태 버킷**: verified / wip(raw·drafted 진행중) / legacy(status 없는 구버전) / exported(anki_exported). 실전문제·MOC는 분모 제외.

### 8.8 🃏 Anki 현황 (`ui-anki.js`)
- **목적**: 덱별 카드 수(파일 기준) + AnkiConnect 실시간 due + **요약·오답 → 카드 초안 생성**(v2).
- **핵심**: `scanAnkiFiles()` — **`_index.json`의 `anki[]` 매니페스트 우선**, 없으면 **`anki`/`_anki` 자식 폴더의 `*.txt` 폴백**(⚠️ 폴백은 선택 핸들의 *직속 자식*만 봐서 4분할 이후 경로 주의 — README). `ankiLive()` — `ankiConnect`(localhost:8765, 3초 타임아웃)로 `deckNames`+`getDeckStats`. `addAnki` → daily 항목.
- **카드 생성(v2)** `buildAnkiCards(from,to)`/`exportAnkiCards(scope)` — 그동안 적은 **3문장 요약·반복 오답**을 Anki import용 **TSV(.txt) 초안**으로(요약: s1→front, s2+s3→back / 오답: 챕터·메모→front, 메모+처방→back). 헤더에 `#separator:Tab #html:true #tags column:3`. 오늘 학습 탭에도 "오늘 → 카드" 버튼. **시점(due)은 여전히 FSRS가 소유**, 생성물은 *초안*이며 ≤5장 큐레이션·왜/응용형 손질은 사람이(방법론 7절·생성 효과 보존). 이로써 *방법론이 약속하던 "자동 생성 카드 큐레이션"*의 생성 측을 앱이 실제로 채운다(과거: 미구현).

### 8.9 🎓 졸업 계획 (`ui-degree.js`) — *대학생활 섹션*
- **목적**: 학기별 수강(과목·학점·구분·상태·성적) + 졸업 요건 대비 진행률.
- **핵심**: 요건 입력(총/전공필수/전공선택/교양) + 카테고리별 KPI + 학기 아코디언(`semCard`). 상태 `예정/수강중/완료`. '수강중' 과목은 📥로 학습 항목에 보냄(`courseToItem`).

---

## 9. 학습방법론 ↔ 앱 매핑

`학습방법론.md`의 각 절이 앱 어디서 실행되는지의 대응표. **이 매핑이 두 문서의 정합성 계약**이다.

| 방법론 절 | 앱 구현 위치 |
|---|---|
| 1. 블록 단위 계획·버퍼 | 스케줄러 배분(주당시간→모듈) · 일과 탭 가용시간 |
| 2. 블록 내부 4단계 | **오늘 학습** `stageBar`(개념15/풀이63/스케치/요약) |
| 3. 3문장 요약 | **오늘 학습** 요약 입력 → `summaries` |
| 4. 이중부호화 노트 | (앱 밖 — 손노트) 오늘 학습 가이드에 안내만 |
| 5. 70% 룰·보충필요 백로그 | **오늘 학습** 70%룰 가이드 + 백로그 추가 → `backlog` |
| 6. CBMS 오답 분류 | **오늘 학습** CBMS 입력 → `cbms` · **주간 리뷰** 분포 |
| 7. Anki 큐레이션 | **카드 초안 생성**(`buildAnkiCards` — 요약·오답→.txt) + Anki 탭 + 오늘 학습 anki 블록 리마인더(≤5장). *생성은 앱, 큐레이션·due는 사람·FSRS* |
| 8. 3중 복습 루프 | 당일=스케줄 완료체크 / 간격=REVIEW_OFFSETS(`reviewViaAnki`로 Anki에 위임 가능) / 백지=blank 블록 |
| 9. 백지 복습 | 스케줄러 **blank 블록**(blankReviewWeekly, *단원 단위* · F-03) + 오늘 학습 통과/막힘 기록(F-04, 막힘→CBMS) |
| 10. 주간 리뷰 | **주간 리뷰 탭** 전체(체크리스트·CBMS분포·계획대비·백로그) |
| 11. 인터리빙 | 스케줄러 과목 인터리빙(lastSid 회피) — *문제 유형 섞기는 앱 밖* |
| 12. 모의시험 | 스케줄러 **mock 블록**(mockEveryWeeks) + CBMS의 T코드 |
| 13. 수면 | 일과 수면 블록(가용시간 산정의 토대) |
| 14. 동기·재시작 | 연속학습일(streak)·부록C 축소판 안내·백로그 정리 · **인출 증거 패널**(통계 §8.5) |
| 1·10. 계획은 가설 | **적응형 용량**(§6.4) — 완료율로 미래 계획 자동 보정 |
| 1. 어려운 새 학습을 맑을 때 | **에너지 인식 배치**(§7) — peak 시간대에 new/mock 우선 |

> ⚠️ **복습 슬롯 ≠ 실제 Anki due**: `REVIEW_OFFSETS`는 *계획용* 고정 휴리스틱. 실제 카드 due는 Anki/FSRS가 소유(방법론 7절·README 경고와 정합). `reviewViaAnki=true`면 합성 슬롯을 아예 끄고 due를 전적으로 FSRS에 위임(시간 이중계상 방지).

---

## 10. 횡단 관심사

- **테마**: `data-theme` 속성 + CSS 변수 토큰(`--bg/--panel/--acc/...`). `toggleTheme()`. **JS 인라인 스타일이 변수명을 참조**하므로 변수명은 보존 대상.
- **타임존 안전 `iso()`**: `toISOString()`(UTC) 대신 **로컬 연·월·일**로 포맷 → KST에서 하루 밀림 버그 방지(테스트 T1이 가드).
- **이스케이프**: `esc()`(HTML 본문/속성, 작은따옴표까지) / `jsq()`(인라인 핸들러 인자의 JS 문자열). 인라인 onclick 패턴이 많아 **사용자 입력을 넣을 때 필수**.
- **접근성**: `role=tablist/tab`·`aria-selected`·`aria-label`, 키보드 포커스(`:focus-visible`), 아코디언 헤더 Enter/Space, `prefers-reduced-motion` 존중.
- **완료 추적·스트릭**: `completions`가 통계·스트릭·주간 계획대비실제의 단일 출처. `totalDoneHours()`/`studyStreak()`.
- **반응형**: CSS 그리드 `auto-fit`·미디어쿼리(560/700/880px)로 모바일 대응.

---

## 11. 외부 연동

| 연동 | 방식 | 주의 |
|---|---|---|
| **옵시디언 볼트(읽기)** | File System Access API(`showDirectoryPicker`) | Chrome/Edge만. `vaultHandle`은 세션 동안만. |
| **볼트 백업(쓰기, v2)** | File System Access 쓰기(`createWritable`) → `러닝허브_백업.json` | `requestPermission({mode:'readwrite'})` 필요. localStorage 전소 대비(§5). |
| **정본 인덱스** | `_meta/감사/_index.json`(검사.sh --index 산출) 소비 | 없으면 `.md`/`.txt` 직접 스캔 폴백. |
| **AnkiConnect** | `fetch http://localhost:8765` | Anki 실행 + 애드온 + CORS(`*`/`null`) 필요. 3초 타임아웃. |
| **캘린더** | `.ics` 내보내기(플로팅 로컬타임) | — |
| **PWA** | `http(s)`로 서빙될 때만 manifest 주입 + sw 등록 | `file://`에선 둘 다 오류라 **건너뜀**(index.html 하단 가드). |

---

## 12. 테스트

네 스위트 모두 **의존성 0 · Node 내장(`vm`)만 · 실패 시 비-0 종료**(검사.sh/CI 연동). 총 **51개**(scheduler 20·state 11·ui-smoke 16·esm-smoke 4).

- **`test/scheduler.test.js` (T1~T18)** — `utils.js`+`scheduler.js`를 샌드박스에 로드해 mock state로 `schedule()` 검증. **타임존을 `Asia/Seoul`로 고정(자기 재실행)**해 `iso()` 회귀를 결정적으로.
  - 커버: 날짜키/연속성, done 제외, finished, 용량 불변식, 페이스·완료, 복습 배치, 마감 경고, daily 확보, 인터리빙, 빈 입력, dayOverride, 빈 구간, blank(T13)·mock(T14) 생성·용량, **적응형 용량 축소·정확배(T15)·이력없으면 1.0(T16)**, **reviewViaAnki rev 억제(T17)**, **피크 new 배치(T18)**, **복습 용량부족 경고(T19·F-02)**, **백지복습 단원 단위(T20·F-03)**.
- **`test/state.test.js` (S1~S10)** — `utils.js`+`state.js`를 mock localStorage 위에 로드. **migrate 보강·무효 거부·`_today` 제거·공부블록 제거·persist↔boot 라운드트립·손상 보존(P1-7)·Anki 카드 생성** + **확신도 플래그(S8·F-06)·백지 막힘→CBMS·통과율(S9·F-04)·exportSnapshot 캐시 제외(S10·F-01)** 검증.
- **`test/ui-smoke.test.js` (U1~U11)** — 모든 js를 DOM/localStorage stub 위에 index.html 순서로 로드. **부팅 렌더 + 9개 탭 전부 throw 없이 HTML 생성 + 실행레이어 핸들러(요약/오답/백로그/setDone/테마/백지통과)가 state 갱신** + 완료 일원화(U7)·유지율 스냅샷(U8)·ICS 신선도(U9)·**명령 팔레트 commands/filter/run(U10)·Undo 토스트(U11)** 확인.
- **`test/esm-smoke.test.js` (M1~M4)** — `js/main.js`를 진짜 ES모듈로 import해 strict·평가순서·전역 노출·9탭 렌더가 깨지지 않는지(브라우저 실경로) 검증.
- **실행**: `node test/scheduler.test.js`(스케줄러 수정 후 필수)·`node test/state.test.js`(state 수정 후)·`node test/ui-smoke.test.js`(ui 수정 후).

---

## 13. 알려진 제약·주의점

1. **복습 슬롯은 계획 보조일 뿐** 실제 FSRS due가 아니다(혼동 금지).
2. **`_vaultScan`/`_ankiFile`/`_ankiLive`가 state에 영속**된다(로컬 새로고침 캐시 목적). ~~내보내기/가져오기 오염~~ → **해소(F-01)**: `exportSnapshot()`가 내보내기·볼트백업에서 제외하고, 가져오기 시에도 폐기(§5).
3. **Anki 폴백 경로 함정**: `anki`/`_anki`를 *선택 핸들의 직속 자식*에서만 찾음 → 4분할 구조에선 `전공/`을 고르면 폴백이 못 찾음(정본 `_index.json` 경로 권장).
4. **v2 데이터 비호환** — 한 번 새로 입력 필요.
5. **로드 순서 의존** — ui 파일 추가 시 `main.js`의 `import` 라인 누락하면 그 탭이 **네비에 안 나타남**(`registerTab` 미호출). 부팅 즉시 누락이 보여 발견이 빠르다. `tabs.js`는 `registerTab` 정의용이라 import 순서상 ui-* 보다 먼저 와야 함.
6. **전역 네임스페이스 단일 평면** — 모든 함수가 전역. 이름 충돌 위험(예: 여러 탭이 `add*`/`upd*`/`del*` 동사 공유하나 현재 중복 0 — `grep -hoE '^(async )?function \w+' js/*.js | sort | uniq -d`로 점검). 탭 등록·렌더 디스패치는 `tabs.js` 레지스트리로 모았고, 탭 간 공용 헬퍼는 `utils`로 올려 ui끼리 의존을 없앴다(예전 `ui-review→ui-today.itemById` 결합 해소). 핸들러 자체의 모듈 네임스페이스화는 인라인 onclick을 전부 바꿔야 해 *처방 아님*으로 보류(§14).
7. **인라인 onclick + 문자열 템플릿** — XSS는 `esc`/`jsq`에 전적으로 의존. 새 출력에서 누락하면 취약.
8. **state.js / data-methodology.js 분리** — 핵심 상태·영속(state)과 방법론 기능 데이터(data-methodology)를 갈랐다. 기능이 늘면 *새 데이터 파일*을 추가해 state.js가 만물상이 되지 않게 한다. 테스트도 둘을 함께 로드(`test/state.test.js`).

### 13.1 스케일 가정 & 성능 예산

`file://` + `innerHTML` 전체 재렌더 + **무한 증가 배열**(completions/summaries/cbms)이라 다년 누적 시 한계가 있다. 가정:

| 항목 | 가정/한계 | 방어 |
|---|---|---|
| **localStorage 쿼터** | 브라우저당 약 5~10MB. 하루 ~몇 KB 누적이면 수년치는 수 MB 수준. | `dataSizeKB()` 표시 + 6개월 아카이빙(§5)으로 비움. |
| **재렌더 비용** | `schedule()`는 horizon 최대 26주로 상한 → 항상 저렴. 무거워지는 건 통계/타임라인이 **전체 completions/chapterLog 순회**할 때. | 수천 레코드까진 체감 무리 없음. 그 이상은 아카이빙으로 활성 데이터 축소. |
| **horizon** | 최대 26주(스케줄러 내부 상한). 그 이상 장기 계획은 안 그림. | 의도된 설계(주당 페이스 기반). |
| **권장 정리 주기** | 학기/반기마다 1회 아카이빙 + 백업. | 유지보수 카드 경고가 백업 노후를 알림. |

---

## 14. 개선 후보 (처리한 것 · 남은 것)

### 🚧 개선 사이클(2026-06-28~) — 퀄리티·확장성·다양성 (5단계 로드맵)
> 감사 후 "더 높은 퀄리티(확장성·기능·디자인)" 요청에 따른 다단계 개선. 각 단계 = 독립 커밋 + 테스트 green.
- **Stage 1 — 상호작용 레이어 ✅(2026-06-28)** — ① **⌘K 명령 팔레트**(`ui-command.js`: `paletteCommands`/`filterCmds`/`openPalette` — 탭 이동+핵심 액션, 퍼지 필터·키보드 네비, 헤더 `⌘K` 버튼) ② **Undo 토스트**(`toast()` 액션 버튼 + `toastUndo()` → 초기화/가져오기 뒤 '되돌리기') ③ 토스트 액션 인프라. *축 K 델타(Linear/Things)* 일부 해소. 테스트 44→**46**(ui-smoke U10·U11).
- **Stage 2 — 디자인/시각화 ✅(2026-06-28)** — ① **학습 스트릭 히트맵**(`streakHeatmap` — GitHub식 잔디, 최근 18주·하루 완료분 5단계 농도) ② **CBMS 오답 레이더**(`cbmsRadar` — 5축 펜타곤 SVG로 약점 '모양') ③ **세피아 테마** 추가 + 테마 3종 순환(`THEME_CYCLE` dark→light→sepia, 토큰 기반) ④ **모바일 하단 탭바**(≤700px 고정·엄지 도달·가로 스크롤). 테스트 46→**48**(ui-smoke U12·U13).
- **Stage 3 — 기능 심화 ✅(2026-06-28)** — ① **노트형 요약 내보내기**(`buildSummaryNotes`/`exportSummaryNotes` — 3문장 요약→옵시디언 `.md`, 카드(.txt)에 이은 *연결*용 · Traverse/RemNote 델타) ② **일일 의식 + 홈 대시보드**(오늘 탭 `ritualCard` — 크로스탭 KPI 4종 + 아침 계획/저녁 셧다운 체크 · Sunsama 델타. `state.rituals` 스키마+migrate). 테스트 48→**49**(ui-smoke U14). ③ **F-11(과목 내 유형 인터리빙)은 *방향* 유지** — 챕터는 *순서대로 학습*이 스케줄러 불변식이라 재배열은 데이터모델·제품결정 선행(감사 분류와 동일).
- **Stage 4 — 렌더 모델(A1·F-09) ✅(2026-06-29)** — ① **부분 렌더**(의존성 0): `chapterTimeline`이 최근 `TIMELINE_CAP=60`일만 DOM(초과분 생략 안내) → 대용량서도 상한. ② **lit-html 벤더링**(`js/vendor/lit-html.js` — `lit-html@3.3.3` esbuild 단일 번들 8.9KB·외부 import 0·오프라인, §2.2 도입 규율 충족) + 통계 탭 챕터 타임라인 **lit 키드 렌더**(`enhanceTimeline`). **테스트 안전장치**: lit은 최상위에서 `document`를 참조하므로 **main.js 정적 그래프에 안 넣고** *브라우저(진짜 DOM)에서만* `import('./vendor/lit-html.js')` 동적 로드 — 비DOM(concat-vm·esm-smoke·SSR)은 feature-detect로 빠져 **문자열 폴백** 유지(50테스트 green). 핀·재현·부패관리는 `js/vendor/README.md`. ⚠️ **lit 렌더 경로는 브라우저 검증 대상**(`#chTimeline[data-lit-rendered]` 마커). 다른 리스트 확장도 이 패턴.
- **Stage 5 — 영속(A3) ✅ write-through(2026-06-29)** — **IndexedDB write-through 미러**: localStorage가 *동기 1차 저장*(코어 동기 가정 그대로) + `persist()`마다 같은 스냅샷을 IDB에 *비동기* 미러(`idbMirror`). 브라우저 '사이트 데이터 삭제'로 localStorage가 전소해도 IDB가 남으면 `restoreFromIDB()`로 복구(유지보수 카드 ♻ 버튼). feature-detect(`typeof indexedDB`)로 미지원/테스트는 전부 no-op → 동기 경로·51테스트 불변. **'IDB를 1차 저장으로' 전환은 *방향* 유지** — persist/boot의 동기→비동기 리팩터가 선행이고 쿼터 천장은 아카이빙으로 완화됨(§13.1). ⚠️ IDB 미러·복구 경로는 브라우저 검증 대상. 테스트 50→**51**(ui-smoke U16).

### ✅ v3(2026-06-28)에서 처리됨 — 베이스라인 감사 발견 반영
- **F-01 런타임 캐시 export 분리** — `exportSnapshot()`로 `_vaultScan/_ankiFile/_ankiLive`를 내보내기·볼트백업·가져오기에서 제외(§5·§13.2). 영속 캐시는 유지.
- **F-02 미배치·과적 복습 경고** — "no silent caps": 복습이 용량 부족으로 미배치/넘겨끼움되면 warnings에 개수 노출(§6 6단계·T19).
- **F-03 백지 복습 단원(챕터) 단위** — 과목×주 통째 → 챕터별 '마지막 학습일' 기준 1칸(§6 6.5단계·§9·T20).
- **F-04 백지 통과/막힘 명시 기록** — 오늘 학습의 blank 블록에 통과/막힘 버튼, 막힘은 메모→CBMS(C) 자동 연결, `blankPassRate()`로 실측 통과율(§8.1·S9).
- **F-06 확신도(찍어맞음) 플래그** — `addCbms(…,conf)` + 오늘 학습 CBMS 폼 체크박스·목록 칩(§4·§8.1·S8).

### ✅ v2(2026-06-28)에서 처리됨
- **닫힌 피드백 루프** — 적응형 용량(§6.4): 완료율→미래 계획 자동 보정.
- **인출 증거 북극성 지표**(§8.5): 투입(시간) 아닌 출력(CBMS 추세·백지율·인출 활동).
- **데이터 손실 방어** — 볼트 자동 백업·노후 경고·아카이빙(§5·§5.1).
- **요약·오답 → Anki 카드 생성**(§8.8) — 방법론이 약속하던 자동 생성의 생성 측 구현.
- **에너지 인식 배치**(§7) — peak 시간대에 new/mock 우선.
- **복습 이중계상 방지**(`reviewViaAnki`, §6).
- **UI 스모크 테스트 리포 편입**(§12) + migrate 라운드트립 테스트.
- **스케일 예산·비목표·데이터손실 시나리오 문서화**(§13.1·§2.1·§5.1).

### ✅ v3.1(2026-06-28, ESM 전환 후)에서 처리됨 — 재감사(`감사_리포트` 🔄 갱신 2)
- **F-05 유지율(retention) 추세선** *(state·data-methodology·ui-stats·ui-anki)* — `retentionLog:[{wk,at,due,cards}]` 스키마+migrate, `recordRetentionSnapshot()`(ankiLive 성공 시 주별 due 스냅샷·같은 주 덮어씀·최근 26주)·`retentionTrend()`, 통계 `retentionSpark()` 스파크라인. 북극성을 '시간'에서 '유지율(due 빚 감소)'로 확장(U8).
- **F-07 콜드스타트 온보딩** *(ui-today)* — `setupGuideCard()` 3스텝 라이브 가이드(과목·목표·일과). 갖춰지면 자동 숨김. 빈 화면 해소(F1 충족).
- **F-08 오늘↔스케줄 완료 일원화** *(ui-kit·ui-today·ui-schedule)* — 공용 `doneCheckbox()`+`toggleDoneAt()`(ui-kit)로 마크업·핸들러 통합. 토글 후 `render()`로 *현재 탭*만 재렌더. 탭별 `toggleDoneToday`/`toggleDone` 제거(렌더 이중화 해소·U7).
- **F-10 ICS 신선도 가시화** *(scheduler·state·ui-schedule)* — `planSignature()`(계획 영향 입력 지문)+`exportICS`가 `_icsExport={at,sig}` 스탬프(런타임 캐시라 백업 제외, RUNTIME_CACHE_KEYS). 스케줄 탭 `icsFreshnessNote()`가 서명 불일치 시 "재내보내기" 배지 표시(U9).
- **due→시간예산 역연동(축 K 델타)** *(ui-anki)* — `ankiDueBudget()`: 오늘 풀 due 합을 '매일 복습' 분 예산으로 upsert(카드당 ~30초·10~180분). FSRS due를 스케줄러 용량에 역연동.

### ⏳ 남은 발견 — *방향*(데이터모델/아키텍처 선행). 감사 F-09·F-11

> v3.1까지 in-file·인접 파일 발견은 모두 처리. 아래 둘은 **선행 작업이 커서 방향으로 유지**(열린 P0/P1 0건).

- **F-09 대용량 부분 렌더** *(전 render\*)* — 통계/타임라인이 전체 `completions/chapterLog`를 순회·`innerHTML` 통째 재생성. 장기적으로 화면에 보이는 범위만 그리는 부분 렌더/가상 스크롤. ~~의존성 0 유지~~ → **적응 우선(§2.2)**: 자체 구현이 비현실적이면 검증된 가상 스크롤 라이브러리 도입도 후보(역량 명분). 당장은 아카이빙으로 완화.
- **F-11 문제유형(과목 내) 인터리빙** *(scheduler·ui-items)* — 현재 *과목* 인터리빙만. 챕터에 '유형 태그'를 달아 같은 과목 안에서 유형을 섞도록 배분. 데이터 모델(챕터 태그) 확장이 선행이라 장기.

### ✅ 모듈화 정리(2026-06-28) — 탭 증식 대비
- **탭 레지스트리** — `tabs.js`의 `registerTab()`로 각 ui-*가 자신을 등록. app.js의 TAB_LABEL/TAB_ORDER/render() 3중 수정 함정(§13.5) 제거. 탭 추가 = 파일 + 등록 한 줄(+index.html·sw.js 라인).
- **state.js 분리** — 방법론 데이터(요약·CBMS·백로그·백지·주간·Anki카드·백업·아카이빙)를 `data-methodology.js`로. state.js는 핵심 상태·영속만(만물상 해소).
- **탭 간 결합 해소** — `itemById`(공용)·`ddayInfo`(D-day 라벨 중복) 를 `utils`로 올림. `ui-review→ui-today` 의존 제거, ui-items/ui-schedule의 D-day 로직 중복 제거.
- **네비 그룹화** — `group` 경계마다 구분선. 그룹을 더 나누거나 오버플로를 붙이면 등록부 변경 없이 더 많은 탭을 수용.

**그 외 관찰(처방 아님)**
- **핸들러 모듈 네임스페이스화 보류** — 탭별 객체(`Today.submitSummary` 등)는 인라인 onclick 문자열을 전부 바꿔야(또는 이벤트 위임 전환) 해 비용 대비 이득이 작다. 탭 등록·공용 헬퍼 정리로 *실제* 충돌·결합은 이미 없앴으므로 현 단계에선 두지 않는다.
- **summaries/cbms/blankResults를 스케줄의 특정 session과 연결** — 지금은 과목/날짜 단위 자유 기록.
- **모의시험 기본값 자동 제안** — 과목 마감 기반(현재 0=끔).
- **옵시디언 볼트로 요약 노트 내보내기**(카드 .txt는 v2 · 노트형은 추후).

---

*이 문서는 코드 변경 시 함께 갱신한다. 특히 §4(스키마)·§6(배분 단계)·§9(방법론 매핑)은 기능 추가의 단일 점검 지점.*
