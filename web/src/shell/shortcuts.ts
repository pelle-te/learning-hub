/* ============================================================
   shell/shortcuts.ts — 전역 키보드 단축키 정의(단일 원천).
   네비게이션은 'g' 다음 키 시퀀스(Linear/GitHub 결): g→t = 오늘 학습.
   App이 이 표를 읽어 키 핸들링하고, ShortcutsHelp가 같은 표로 치트시트를 그린다.
============================================================ */
import { MOD_LABEL } from '@/lib/platform';
import { TABS } from './tabs';

export interface NavShortcut {
  /** 'g' 다음에 누르는 키 */
  seq: string;
  /** 이동할 탭 key(shell/tabs.ts) — 치트시트 라벨은 TabMeta.label에서 파생(SSOT, C-13). */
  tab: string;
}

/* ⚠⚠ **`g` 표는 이제 `role` 에서 파생된다(W5 · 2026-07-31).**

   D-4 가 "갈 수 있는 곳의 열거 다섯 벌"을 `role` 하나로 접었는데 **이 축만 손 유지 사본으로
   남아 있었다.** 그 대가는 조용했다 — 실측(2026-07-31): destination 7 중 `forecast`·`reads` 에
   `g` 가 **없고**(가장 최근에 승격된 목적지가 손가락에 없다), `g` 키 12 중 **6개가 lens** 라
   눌러 가면 **레일에 없는 곳에 선다**. 사본은 E13(forecast 승격)·H24(markets 강등)·E17(atlas
   강등)을 하나도 안 따라갔고, 옛 불변식은 _"seq 의 tab 이 실존한다"_ 만 봐서 전부 초록이었다.

   파생 규칙은 하나다: **destination 이면 `g` 를 갖는다 · `g` 는 destination 만 가리킨다.**
   손으로 유지하는 것은 **첫 글자가 겹칠 때의 예외 표 둘**뿐이고, 그 둘조차 불변식이 잠근다
   (충돌을 방치하면 seq 가 중복돼 "모든 destination 이 고유 g 를 갖는다"가 깨진다).

   ⚠ E24(모션)가 이미 푼 패턴이다 — *선언은 있는데 집행자가 없어 사본이 표류한다*. 처방도 같다:
   파생 + 불변식. 새 목적지를 추가할 때 이 파일을 고칠 일은 **첫 글자가 겹칠 때뿐**이다. */

/* ⚠⚠ **①-6(W5 · 2026-08-07) — `g` 가 렌즈까지 간다.** 종전 파생 규칙은 *"destination 이면 `g`
   를 갖고 `g` 는 destination 만 가리킨다"* 였고, 그 규칙이 옳았던 이유는 **렌즈가 레일에 없었기
   때문**이다(레일에 없는 곳으로 손가락이 데려가면 사용자가 자기 위치를 잃는다). N-14 가 레일을
   평탄화하면서 그 전제가 사라졌다 — 이제 렌즈도 레일에 자기 줄을 갖는다.
   → 규칙을 **한 단어만** 고친다: *레일에 서면 `g` 를 갖고, `g` 는 레일에 선 것만 가리킨다.*
   ⚠ 원래 로드맵은 이걸 "①-6 g키를 lens 까지"로 적었는데, 그 항목이 N-14 에 흡수된 이유가
   여기 있다 — **규칙이 아니라 로스터가 바뀐다.** 규칙 문장은 그대로다.

   ⚠ 대체 키가 늘어난 것은 첫 글자 충돌이 늘었기 때문이지 예외가 늘어난 게 아니다. 고유성은
   불변식이 잠근다(중복이면 `g` 하나가 다른 하나를 조용히 가린다). */
/** 첫 글자가 이미 쓰였을 때의 대체 키(손 유지 · 여기 없는 레일 탭은 `key[0]` 이 그대로 seq). */
const SEQ_OVERRIDE: Record<string, string> = {
  stats: 'a', // 's' 는 schedule(캘린더) — 통계는 analytics 의 a
  settings: 'e', // 's' 충돌 — 환경설정(environment)의 e
  /* W17 이 `review-run` 을 목적지로 올리며 'r' 이 겹쳤다 — **불변식이 그 자리에서 잡았다**
     (파생 + 고유성 단언이 손 유지 사본 시절엔 원리적으로 못 하던 일이다). 인출 축이 매일
     쓰는 쪽이라 'r' 을 갖는다.
     ⚠ `reads: 'd'` 가 여기 있었다 — 그 탭은 P10 W4 에서 `survey/` 필러로 갔다(사문 · 삭제). */
  /* ── ①-6 이 데려온 렌즈들의 키 ─────────────────────────────────────────────
     `r` 은 `review-run`(매일) 이 갖고, 주간 리뷰는 **weekly** 의 `w`.
     `d` 는 `day`(매일 여는 하루) 가 갖고, 졸업 계획은 **graduation** 의 `g`… 는 접두사와
     겹치므로(`g g` 는 칠 수 있지만 헷갈린다) **cap** 의 `c` 로 간다(그 탭 아이콘 이름과 같다).
     `alloc` 은 `a` 가 통계에 있으므로 **배분**의 `b`, `integrations` 는 `i` 가 과목에 있으므로
     **연동**의 `n`. 전부 *한국어 이름의 소리* 또는 *이미 코드에 있는 이름*에서 왔다 — 지어낸
     연상은 다음 사람이 못 따라온다. */
  review: 'w',
  degree: 'c',
  alloc: 'b',
  integrations: 'n',
};

/** 같은 목적지를 가리키는 **추가** 키(손가락 습관 보존 · 파생이 아니라 명시 예외).
 *  ⚠ `p`(계획)와 `s`(캘린더)가 같은 곳을 가리키는 것은 **의도된 것이다.** D-4 이전에도 `g p` 는
 *  plan-host 를 거쳐 `/schedule` 로 튕겼으니 두 키의 착지는 원래 같았다. 셸이 사라졌다고 한
 *  쪽을 지우면 그 손가락 습관만 깨지고 얻는 것이 없다(이 저장소는 사용자 1인이다). */
const SEQ_ALIASES: NavShortcut[] = [{ seq: 'p', tab: 'schedule' }];

/** g-시퀀스 네비게이션 — **레일 탭 전량**(파생 · ①-6). 치트시트 라벨은 `TabMeta.label` 파생(C-13).
 *  ⚠ 불변식(`test/invariants.test.ts`)이 셋을 잠근다: 실존 · 전부 레일 탭 · 레일 탭 전량. */
export const NAV_SHORTCUTS: NavShortcut[] = [
  ...TABS.filter((t) => t.role === 'destination' || t.role === 'lens').map((t) => ({
    seq: SEQ_OVERRIDE[t.key] ?? t.key[0]!,
    tab: t.key,
  })),
  ...SEQ_ALIASES,
];

/** 치트시트에 함께 보일 전역 단축키(시퀀스 외). 수정자 표기는 플랫폼 파생(lib/platform).
 *  ⚠ **여기 있는 것은 진짜로 어디서나 동작해야 한다.** N-16 에서 `,`/`.`(주 이동)를 뺐다 —
 *  전역이라 적혀 있었지만 실제 리스너는 2화면에만 있어 **12개 탭에서 거짓말**이었다. 그 항목은
 *  이제 `useKeymap` 이 화면과 함께 등록하고 치트시트 '이 화면' 섹션이 문맥에 맞게 그린다. */
export const GLOBAL_SHORTCUTS: { keys: string; label: string }[] = [
  { keys: `${MOD_LABEL} + K`, label: '명령 팔레트' },
  /* 근본① — **직전 편집**을 되돌린다(파괴적 동작 직전 백업이 아니다). 라벨이 그 단위를 말하는
     것이 중요하다: 옛 ⋯메뉴 '되돌리기'는 *마지막 파괴적 동작 시점*으로 갔고 그게 며칠 전일 수
     있었다. ⚠ 글자를 치는 중에는 안 먹는다(그때는 텍스트 되돌리기 · `App.tsx` 가 이유를 갖는다). */
  { keys: `${MOD_LABEL} + Z`, label: '직전 편집 되돌리기' },
  // D-2 — 팔레트 안에서만 유효하지만 여기 있는 이유: 캡처는 "떠올랐을 때" 쓰는 것이라
  // 팔레트를 열어 힌트 바를 읽고 나서 배우면 이미 늦다.
  { keys: `${MOD_LABEL} + Enter`, label: '팔레트에 친 문장을 그대로 캡처' },
  { keys: 'G 그다음 ↑표의 키', label: '탭으로 이동' },
  { keys: '[  /  ]', label: '이전 / 다음 탭' },
  { keys: '← → Home End', label: '탭 사이 이동(나브 포커스 시)' },
  { keys: '?', label: '이 도움말 열기 / 닫기' },
  { keys: 'Esc', label: '팔레트·모달·도움말 닫기' },
  /* E20 — **OS 전역**이다(앱이 포커스가 아니어도 먹는다). 위 항목들과 성격이 다르므로 라벨에
     그 사실을 적는다 — "어디서나"의 뜻이 여기서는 *앱 밖에서도* 이기 때문이다.
     ⚠ 등록이 실패할 수 있는 유일한 항목이기도 하다(다른 앱이 조합을 선점). 실패 여부는 연동 탭의
       HOTKEY 채널이 말한다 — 치트시트가 "된다"고 단정하지 않게 라벨에 조건을 남겼다. */
  /* ⚠ 라벨을 짧게 유지한다 — 키 칩(`Ctrl + Shift + Space`)이 넓어서 긴 라벨이 **3줄로 감긴다**
     (실렌더로 확인 · `shortcuts-help-dark` 스냅샷이 이제 그걸 지킨다).
     ⚠⚠ **문구가 "앱 밖에서도" 였는데 고쳤다**(2026-07-30 · 사용자 실측 후). 전역 단축키는 앱이
     **실행 중일 때만** 산다(OS 등록이 프로세스에 묶인다) — 사용자는 셸을 켜 놓고 살지 않으므로
     "앱 밖에서도"는 *앱이 꺼져 있어도* 로 읽힐 수 있고 그건 거짓이다. 정확한 범위는 **다른 창에
     포커스가 있을 때**이고, 실제 값이 나오는 자리는 집중 세션 중이다(세션을 시작하려면 앱을 열게
     되고, 그 뒤 PDF·강의 창으로 넘어간다 — 붙잡을 생각이 떠오르는 바로 그 구간).
     ⚠ "실행 중"이 조건 둘을 겸한다: 앱이 떠 있어야 하고 등록도 성공해야 한다. 후자의 실패 사유는
     연동 탭 HOTKEY 채널이 말한다 — 치트시트가 "된다"고 단정하지 않는 것이 E16 의 계약이다. */
  { keys: `${MOD_LABEL} + Shift + Space`, label: '다른 창에서도 캡처 — 앱 실행 중' },
];
