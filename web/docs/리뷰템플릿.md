# 리뷰 템플릿 — web 변경분/feature

> `/리뷰 코드 <대상>`(실패·구조·표면·성능 4축 팬아웃)이 쓰는 관점을 사람이 PR·변경분에 직접 적용할 때의 체크리스트. 심각도순으로 `파일:라인 · 실패 시나리오 · 수정 코드 · 비용` 형식으로 기록.
> 통과/실패 기계 검증은 `/게이트`. 화면·접근성은 `/리뷰 ux`. 발견을 실제로 고치는 것은 `/실행`이다.
> ⚠ 2026-08-20 개편: 옛 `/감사 빠른`·`/감사 … 점수`·`web-reviewer` 는 은퇴했다. 범위를 좁히려면
> 축은 그대로 두고 **대상 경로만** 좁힌다(`/리뷰 코드 web/src/features/today`).
> ⚠⚠ **바로 이 줄의 예시가 `/감사 코드` 였다**(V064 · 2026-08-31) — 은퇴를 선언하는 문장이
> 다음 호흡에서 그 은퇴한 명령을 시범 보이고 있었다.

## 1. 정확성 / 버그

- [ ] 경계값·null/undefined·빈 배열 처리
- [ ] async 레이스(중복 요청·언마운트 후 setState·순서 뒤집힘)
- [ ] 상태 무효화 정확성(특히 `useRuntime` 분리 캐시 vs `useApp` — §아키텍처 3)
- [ ] persist 계약 유지(flush 병합·split/merge 대칭 불변)
- [ ] 회귀: 기존 테스트가 이 변경을 덮나

## 2. 접근성 (a11y)

- [ ] 키보드 도달·포커스 순서·포커스 트랩(모달/팔레트)
- [ ] aria-current/label, 역할 정직성(가짜 tablist 금지)
- [ ] reduced-motion 백스톱, `fxLite` 존중
- [ ] 대비(토큰 기반), 터치 타깃 크기

## 3. 레이어 경계 / 재사용

- [ ] boundaries 단방향 준수(역방향 import 없음)
- [ ] 컴포넌트 내 인라인 계산 → `lib` 순수함수로 뺄 것
- [ ] 중복 로직(이미 lib/utils·scheduler 등에 있나)
- [ ] 새 전역 CSS 클래스 신설 금지 — 스타일은 **JSX Tailwind 유틸리티 / 공유 `ds-*` / `global/`**
      셋 중 하나다. ⛔ **`*.module.css` 는 저장소에 0개이고 `check:tokens` 가 신설을 막는다**
      (여기 «ds + module» 이라 적혀 있었다 · V064)

## 4. 성능

- [ ] 불필요 재렌더 유발(Rules of React 위반 → 컴파일러 bail)
- [ ] 렌더 경로의 무거운 계산(메모/lib 이관)
- [ ] 번들/청크 영향(`npm run budget`)
- [ ] 리스트 key·가상화 필요 여부

## 마감

- [ ] `npm run verify` + (시각 변경 시) `npm run e2e` 녹색
- [ ] 확신 없는 지적은 "PLAUSIBLE"로 표시, 과잉 지적 금지
