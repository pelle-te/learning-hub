# 원장 아카이브 — 닫힌 항목

`web/docs/원장.md` 는 **열린 것만** 진다. `/실행`이 닫으면 여기로 온다.
닫기 노트 형식: `[무엇을 고쳤나(파일) → 검증 → 처방과 달라진 점 → 남은 것]`

---

## 2026-08-21 · ux 축 1회차 (U001~U038 · 38/38 종결)

리뷰 원문 `리뷰/2026-08-21-ux/`. 게이트 결과는 각 항목이 아니라 회차 말미에 한 번 적는다.

| ID   | 상태 | 무엇을 고쳤나                                                                                                                                                                         | 검증                                                                                                                                                                     | 처방과 달라진 점                                                                                                                                                                                                                                                                                                                                   |
| ---- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| U001 | 닫힘 | `check-tokens.mjs` 에 **미선언 사다리 칸** 검사 신설 — `text-*`·`rounded-*` 가 `@theme` 에 없는 이름을 쓰면 실패한다(= Tailwind 기본 rem 으로 조용히 실리는 통로)                     | 임시 파일에 `text-9xl rounded-3xl` 를 심어 **실제로 빨간불이 뜨는 것** 확인 후 제거. 실측 4종(`text-base`·`rounded-xs`·`text-3xl`·`text-4/5xl`)이 그 상태였다            | ⚠ 처방은 «분수 접미사를 막아라» 쪽으로 읽혔는데 **반대로 갔다**: 분수 칸 737곳은 규약 위반이 아니라 «사다리에 칸이 없다»는 신고였다. 막는 대신 `--spacing: 4px` 로 **같은 사다리에 들였다**(U002). 집행자가 겨누는 것은 표기가 아니라 **단위**다                                                                                                   |
| U002 | 닫힘 | `tokenBridge.css` 에 `--spacing: 4px` + `--text-base/3xl/4xl/5xl`·`--radius-xs` 를 px 로 못박았다                                                                                     | `[재현]` 루트 20px 에서 재측정: `p-5.5` 27.5→**22px** (`p-6` 24px 아래) · `text-base` 20→**16px** (`text-xl` 20px 아래) — 역전 해소                                      | 처방대로. ⚠ 기본 루트(16px)에서는 **한 픽셀도 안 바뀐다**(0.25rem = 4px) — 스냅샷 재생성이 이 항목 때문에 필요하지 않았다                                                                                                                                                                                                                          |
| U003 | 닫힘 | a11y 게이트에 **미측정(incomplete) 래칫** — 다크 전 탭을 돌며 `결과.incomplete` 노드를 세고 상한을 넘으면 실패. 실패 메시지가 규칙별 내역을 준다                                      | 실측 **624노드**(`color-contrast` 623 · `form-field-multiple-labels` 1)로 래칫 고정                                                                                      | ⚠ 리포트의 «1,144» 는 두 테마·오버레이까지 합산한 수였다. 이 케이스는 **다크 경로 로스터**만 돌아 624 다 — 분모를 바꿔 적기보다 **재는 범위를 명시**했다. ⚠ `toBe` 가 아니라 `≤` 다: 무관한 UI 추가마다 숫자를 올리게 만들면 그 래칫은 곧 형식이 된다                                                                                              |
| U004 | 닫힘 | `CloudCard` 의 동기화 토스트를 `syncToast()` 하나로 모으고 **네 갈래를 전부** 말한다(`blocked` 는 push 축까지 본다)                                                                   | 타입 수준 — `SyncResult.status` 4종 + `push.status` 를 분기가 전부 소비한다(옛 코드는 `failed` 하나만 보고 나머지를 `else` 초록으로 접었다)                              | 처방대로 + **첫 동기화 경로도 함께**(`connect()` 가 같은 함수를 쓴다 — 종전엔 거기만 `warn` 톤이라 같은 사실을 두 얼굴로 말했다)                                                                                                                                                                                                                   |
| U005 | 닫힘 | `UpdateCard` 의 실패 토스트 둘에 톤 명시(`'bad'`)                                                                                                                                     | 코드 — `toast()` 기본 인자가 `'ok'` 라 톤 생략이 곧 초록 ✓ 였다                                                                                                          | 처방대로. ⚠ 저장소 전수로 넓혀 확인: 같은 형태(실패 문구 + 톤 생략)는 이 둘뿐이었다                                                                                                                                                                                                                                                                |
| U006 | 닫힘 | `lib/tauri.ts` 의 `vaultScan` 이 **던진다**. `app/VaultSync` 가 받아 쿼리 캐시(`VAULT_ERROR_KEY`)에 적고 `VaultPanel` 이 경고 상자로 그린다. 미설정(`isVaultUnset`)은 실패가 아니다   | `test/tauriBoundary.test.ts` 의 옛 케이스를 뒤집고 이유를 그 자리에 적었다(판례 D002 형태). 셸 없는 브라우저 경로는 그대로 `null`                                        | 처방대로. ⚠ 주석이 지목하던 `capabilities.vaultWatchError` 는 **감시자만** 세운다 — 그 채널을 넓히지 않고 **스캔 축을 따로** 세운 이유는, 감시 실패와 읽기 실패가 사용자에게 다른 사실이기 때문                                                                                                                                                    |
| U007 | 닫힘 | `disconnectCloud()` 가 `localCleared` 를 함께 돌려주고, 화면이 **그 갈래를 가장 먼저** 말한다                                                                                         | 코드 — `execDb` 는 실패를 `false` 로 접는 계약이라 반환값을 안 보면 «한 줄도 못 지웠는데 끊었다고 말하는» 상태가 된다                                                    | 처방대로. ⚠ 문구가 결과를 말한다: _"다음 부팅에 연결이 되살아납니다"_ — 사용자가 유령으로 읽지 않게                                                                                                                                                                                                                                                |
| U008 | 닫힘 | `artifactErrorCopy()` 신설(번역 + **원문 병기**) · 화면 넷(`Ledger`·`Mastery`·`PathView`·`ExamSheet`)이 그것을 쓴다. 원문만 돌려주던 `artifactErrorMessage` 는 은퇴                   | `test/artifactState.test.ts` 케이스 교체(500·404·403·TypeError·`Error:` 접두 벗기기·사유 없음)                                                                           | ⚠ 처방은 «옛 `artifactErrorCopy` 를 되살려라» 였는데 **다른 물건을 만들었다**: 옛 것은 콜드 게이트 *문장*이고 여기 필요한 것은 실패 원문의 *번역*이다. ⚠ 원문을 지우지 않는다 — H23 이 «사유를 한 문장으로 뭉개지 말라»고 못박은 반대편으로 가지 않기 위해                                                                                         |
| U009 | 닫힘 | 마이크로카피 넷(`Mistakes`·`StatsDetail`×2·`AvailRail`)의 탭 이름을 실물에 맞추고, **불변식 ⑰** 신설 — `<b>이름</b> 탭` 이 살아 있는 탭(=`role!=='retired'`)을 가리키는지 기계가 본다 | `npx vitest -t "⑰"` 통과 + HEAD 판본의 `Mistakes.tsx` 에 같은 정규식을 돌려 «학습 기록»이 걸리는 것 확인                                                                 | ⚠ 리포트가 든 넷 중 하나(`TodayBlocks`)는 U010 과 같은 자리라 함께 고쳤고, **리포트에 없던 다섯째**(`AvailRail` 의 «배치 탭»)를 집행자가 찾아냈다 — 이 항목이 코드 수정이 아니라 **집행자 신설**이어야 했다는 증거                                                                                                                                 |
| U010 | 닫힘 | `TodayBlocks` 빈 상태가 «과목이 있나»로 갈린다 — 있으면 `오늘 계획 짜기 →`(뷰 전환 + 딥링크), 없으면 `학습 항목 설정 →`                                                               | 코드 — `TodaySignature` 의 PL-1 분기와 **같은 술어·같은 목적지**를 쓴다                                                                                                  | 처방대로                                                                                                                                                                                                                                                                                                                                           |
| U011 | 닫힘 | `hooks/useHashTarget` 신설(조상 `<details>` 펼치기 → 스크롤 → 포커스 · 퍼센트 인코딩 디코드) · `Subject` 가 소비한다                                                                  | 코드 — `.hash` 를 읽는 코드가 저장소에 **0건**이었다(그래서 «착지한다»는 주석이 그 사실의 유일한 기록이었다)                                                             | ⚠ 처방에 없던 것 둘을 넣어야 실제로 작동했다: ① 챕터 표는 `<details open={chs.length===0}>` 라 **접혀 있다** ② SPA `navigate()` 는 문서 로드가 아니라 브라우저가 프래그먼트를 처리하지 않는다                                                                                                                                                      |
| U012 | 닫힘 | 복습 완주 화면에 `복습 예보 →` 버튼(들어가는 문) + ⌘K 의 은퇴 탭 힌트를 `은퇴` → **호스트 탭 이름**으로                                                                               | 코드 — 같은 화면의 문장이 _"예보 탭에서 되돌릴 수 있어요"_ 라 말하는데 갈 방법이 없었다(그 문장도 함께 고쳤다)                                                           | 처방대로. ⚠ 힌트 문구는 내부 어휘(`role:'retired'`)의 노출이었다 — 사용자에게 «없어진 화면»으로 읽힌다                                                                                                                                                                                                                                             |
| U013 | 닫힘 | `Ledger` 챕터 상세에서 `aria-modal="true"` 제거(비모달 dialog)                                                                                                                        | 코드 — 배경 히트맵 칩 22개가 보이고 클릭된다(칩을 눌러 상세를 갈아 끼우는 것이 이 화면의 사용법)                                                                         | ⚠ 포커스 트랩·Esc·복원은 **남겼다**. 트랩은 «왔던 칩으로 돌려보내는 장치»이고 Esc 가 있으므로 2.1.2 위반이 아니다 — 거짓이던 것은 «배경이 없다»는 선언 하나였다                                                                                                                                                                                    |
| U014 | 닫힘 | 오버레이 a11y 로스터에 둘 신설(`overlay-stats-detail` = `DetailDrawer` · `overlay-day-buffer`) + 로스터 주석의 거짓 두 줄 정정                                                        | 두 케이스 실행 — axe 통과. ⚠ `DayBuffer` 는 ⌘K 로 열 때 **질의를 먼저 넣어야** 한다(첫 화면은 최근·상위만 보여 준다 — 그걸 모르고 짜면 타임아웃이 제품 결함처럼 읽힌다)  | ⚠ 주석이 든 «못 덮는 것 1종»(`Markets` AI 브리핑)은 **사문**이었다 — 그 화면은 P10 W4 에서 `survey/` 로 갔다. 지금 못 덮는 오버레이는 0이고, 문단은 «다음에 그런 것이 생기면 여기 적는다»로 남겼다                                                                                                                                                 |
| U015 | 닫힘 | `a11y.spec.ts` 머리주석 정정(axe 에 포커스 표시 규칙이 **없다**) + 그 층의 집행자를 시각 회귀에 신설 — `focus-ring · desktop                                                          | phone`(Tab 한 번 누르고 촬영 · 포커스가 실제로 들어갔는지 단언)                                                                                                          | 두 케이스 통과 · 베이스라인 2장 생성(스킵 링크에 링이 보인다)                                                                                                                                                                                                                                                                                      | ⚠ 처방은 «주석을 고쳐라» 였는데 **커버리지를 세우는 쪽**까지 갔다: 못 보는 층을 적어 두기만 하면 다음 회차가 같은 것을 다시 발견한다. 데스크톱·폰 둘 다 찍는 이유는 포커스 스타일이 **다른 파일에 살기** 때문(폰은 `global/components.css` 를 안 문다) |
| U016 | 닫힘 | 숙달 램프 명도 양 끝을 다시 골랐다 — 다크 58/62 → **86/36**, 라이트 44/32 → **16/32**. 집행자는 `test/masteryRamp.test.ts`(1형·2형 시뮬 ΔE · 양 끝 휘도비 · 각 단계의 면 대비)        | 계산 8케이스 통과. 실측 변화: 라이트 D형 ΔE **1.93 → 47.9** · 양 끝 휘도비 1.31 → 4.28                                                                                   | ⚠ 처방(«hue 도메인을 좁혀라»)을 안 썼다 — 적록 축은 좁혀도 CVD 에서 무너진다. 대신 **명도 축**을 벌렸다. ⚠ 「명도 단조성」은 계약에서 뺐다: sRGB 에서 노랑이 휘도 봉우리라 0°→120° 램프는 **원리적으로** 단조일 수 없다. ⚠ 방향이 테마마다 반대인 것이 결과: 두 테마 모두 **약점이 배경에서 가장 튄다**                                            |
| U017 | 닫힘 | `--anno` 믹스 72% → **다크 84% · 라이트 94%** + `test/annoContrast.test.ts`(네 면 × 두 테마 4.5:1 · 그리고 «`--mut` 과 여전히 다른 평면인가»)                                         | 계산 10케이스 통과. 실측: 최악 면(`panel2`)에서 다크 3.54→**4.54** · 라이트 2.98→**4.69**                                                                                | ⚠ 처방은 «비텍스트 3:1 이면 된다는 전제를 재검토» 였고 그 전제가 **틀렸다**로 결론났다 — 이 층이 나르는 것은 문장이다(1.4.3). ⚠ 값을 올리면 세 번째 평면이 사라질 위험이 있어 **그 사실도 테스트가 잡는다**(휘도차 하한)                                                                                                                           |
| U018 | 닫힘 | 9px 범례 스와치·히트맵 셀의 모서리를 `--radius-cell`(3px)로 통일(`AvailRail`·`mastery/classes`)                                                                                       | 코드 — 같은 역할·같은 크기인데 `rounded-xs`(2px)와 `rounded-cell`(3px)로 갈려 있었다                                                                                     | 처방대로                                                                                                                                                                                                                                                                                                                                           |
| U019 | 닫힘 | **합치지 않았다.** 대신 `ds.css` 에 판정 근거를 못박았다(다음 회차가 다시 열지 않게)                                                                                                  | 실측 — 세 축이 다르다: 글자 `11px` **절대** vs `0.78em` **부모 상대** · 테두리 `border`(박스 +1px) vs `inset shadow`(박스 그대로) · 색 상속 vs `--mut` 고정              | ⚠⚠ **처방이 틀렸다.** `em` 이 결정적이다 — 칩은 자기가 얹힌 줄의 밀도를 따라가고 알약은 어디서나 11px 인 독립 배지다. 합치면 둘 중 하나를 잃는다(24곳이 문맥마다 흔들리거나, 16곳이 밀도 높은 행에서 튄다). 그리고 테두리 기법 차이는 **레이아웃 차이**다                                                                                          |
| U020 | 닫힘 | 중량 사다리 신설(`--font-weight-title/heading/label/body`) · 데스크톱 전역 `h1/h2/h3` 와 폰 헤딩이 **같은 이름**을 쓴다                                                               | `check-tokens` 의 브리지 고아 검사가 네 칸의 소비를 확인(`font-weight` 네임스페이스를 그 표에 추가)                                                                      | ⚠ **값은 한 픽셀도 안 바꿨다** — 이름을 주는 것이 1단계다. 바뀐 것은 폰 `<h2>` 600 → 700(데스크톱과 같아진다) 하나뿐이고, 그것이 이 항목의 발견 자체였다                                                                                                                                                                                           |
| U021 | 닫힘 | 워드마크에 `flex-none`(+ 각 조각 `whitespace-nowrap`)                                                                                                                                 | `[재현]` h1 높이 실측: `schedule`·`alloc` **80px → 40px**(다른 탭과 같아짐)                                                                                              | 처방대로. ⚠ 시각 회귀 88장이 이걸 못 잡은 이유가 이 항목의 진짜 교훈이다 — **그 그림이 베이스라인으로 굳어 있었다**(오버레이 §1-C 에 적었다)                                                                                                                                                                                                       |
| U022 | 닫힘 | `tokens.css` 머리주석 정정 — 「4의 배수 간격 스케일」 → **바닥 단위 4px + 이름 있는 여섯 칸 + 1px 단위 자동 생성 칸**(실측 인용)                                                      | 실측 — 자동 생성 칸 737곳 중 4의 배수 21%                                                                                                                                | ⚠ 처방(«사다리를 실측에 맞춰 다시 선언») 대로. 다만 **선언만 고치지 않았다**: 같은 회차에 `--spacing: 4px` 으로 그 칸들을 진짜 같은 사다리에 들였다(U002) — 안 그러면 선언은 정직해지고 값은 여전히 갈린다                                                                                                                                         |
| U023 | 닫힘 | ① 모바일 하단 탭바를 **스크롤 컨테이너**로(`justify-start` + `overflow-x-auto`) + 현재 탭 `scrollIntoView` ② `TopBar` 액션 묶음이 좁은 창에서 줄바꿈                                  | `[재현]` 8경로 320px 재측정 — 문서 폭 **372/352 → 전부 320**. 게이트 신설: `a11y.spec.ts` 의 `reflow · 320px · <화면>` 25케이스 + 탭바 도달성 1                          | ⚠⚠ **발견이 더 컸다.** 리포트는 «320px 에서 가로 스크롤»이라 적었는데 실측하면 그 원인이 **탭바가 탭을 화면 밖에 두고 스크롤도 안 되던 것**이었고, 그건 **390px(폰 베이스라인)에서도 7/16**이었다 — 즉 확대만의 문제가 아니라 좁은 창에서 앱 절반이 사라지고 있었다. ⚠ 실측 경로도 6/8 이 아니라 **3/8** 이었다(리포트는 dev 서버 · 이쪽은 빌드본) |
| U024 | 닫힘 | 「찾기」 결과 0 상태의 `terminal` → 매뉴얼 버튼(첫 진입 상태와 **같은 행동**)                                                                                                         | 코드 — 그 문(매뉴얼)은 이 화면에만 있다(은퇴 탭은 나브에 안 선다)                                                                                                        | 처방대로. ⚠ 화면이 스스로 적어 둔 근거가 자기 형제 상태에는 적용되지 않은 형태였다                                                                                                                                                                                                                                                                 |
| U025 | 닫힘 | `졸업요건` 빈 상태에 실제 행동(`졸업 계획 입력하러 가기 →` = `?view=` 제거)                                                                                                           | 코드 — 뷰가 `useState`→URL 로 간 뒤(W9) 「소유하지 않아 조작 못 한다」는 근거가 사라졌다                                                                                 | 처방대로. ⚠ **근거가 사문이 되면 결론도 다시 본다**는 것이 이 항목의 형태다                                                                                                                                                                                                                                                                        |
| U026 | 닫힘 | 빈 로그 문구가 `isToday` 로 갈린다(과거면 «이 날엔 …» + 백필 안내)                                                                                                                    | 코드 — 컴포넌트가 이미 `isToday` 를 받고 있었고 제목만 그걸 썼다                                                                                                         | 처방대로                                                                                                                                                                                                                                                                                                                                           |
| U027 | 닫힘 | 약점 히트가 **행 앵커**로 착지한다(`/review#weak-<sid\|챕터>`) — 행에 `id` 부여 + `Review` 가 `useHashTarget` 소비                                                                    | 코드 — 목적지 화면에 그 좌표를 쓰는 코드가 생겼다                                                                                                                        | ⚠ 처방(`?sid=` 로 좁혀 보내기)을 **안 썼다**: `/review` 에는 과목 필터가 없어 `?sid=` 를 읽는 코드가 0이고(넣으면 U011 이 지적한 «아무도 소비하지 않는 딥링크»를 하나 더 만든다) 약점은 과목이 아니라 **과목×챕터**라 sid 로는 좁혀지지도 않는다                                                                                                   |
| U028 | 닫힘 | 온보딩 2단계가 과목이 **하나뿐이면** 그 과목 화면으로 간다(여럿이면 갤러리 유지)                                                                                                      | 코드 — 1단계에서 막 만든 사람이 자기 과목을 목록에서 다시 찾던 경로                                                                                                      | 처방대로 + 조건 하나(여럿이면 고르는 것은 사람의 일)                                                                                                                                                                                                                                                                                               |
| U029 | 닫힘 | 등록 코드 칸에 Enter 제출(조건은 버튼 `disabled` 와 동일)                                                                                                                             | 코드 — 같은 관용구 17곳 중 여기만 빠져 있었다                                                                                                                            | 처방대로. ⚠ 조건을 버튼과 **같게** 맞춘 것이 요점(다르면 Enter 가 버튼이 막는 상태에서 요청을 보낸다)                                                                                                                                                                                                                                              |
| U030 | 닫힘 | `Ledger`·`Mastery` 의 `Setup` 을 `components/State` 로 옮기고 **`next` 를 채웠다**(원장 재빌드 · 볼트에서 불러오기)                                                                   | 코드 — `State` 는 `next` 를 타입으로 강제한다. 그 밖에 있어서 두 화면의 유일한 출구가 빈 상태 안에 없었다(`Mastery` 는 화면 위쪽을 손가락으로 가리키고 있었다)           | 처방대로                                                                                                                                                                                                                                                                                                                                           |
| U031 | 닫힘 | `TABS_EMPTY` 배열을 **지웠다** — 빈 상태 스냅샷이 `TABS` 를 시드만 바꿔 돈다(5탭 → 13탭)                                                                                              | `knip` 이 별칭(`TABS_EMPTY = TABS`)을 **중복 export** 로 잡았고 그 지적이 옳아 이름을 없앴다                                                                             | ⚠ 처방(«로스터를 파생시켜라»)의 첫 판은 별칭이었는데 게이트가 더 나은 형태를 가리켰다 — 이름이 둘이면 «둘은 다르다»로 읽힌다                                                                                                                                                                                                                       |
| U032 | 닫힘 | ics **주소 재발급·폐기**에 `confirmIrreversible`(③단)                                                                                                                                 | 사다리 판정 — 둘 다 `mutate` 를 안 거치고(⌘Z 밖) **앱 밖 상태**를 바꾼다(구독 중인 캘린더가 그 순간부터 못 읽는다)                                                       | ⚠ **갱신(`publish()`)은 그대로 뒀다** — 같은 주소의 내용만 새로 쓰는 것이라 잃는 것이 없다. 확인창의 가치는 희소성에서 나온다는 사다리의 규율                                                                                                                                                                                                      |
| U033 | 닫힘 | 배분 보드 치트시트에서 `Alt + ↑/↓` 줄 삭제                                                                                                                                            | `[재현]` 실측 — 그 격자는 Alt 조합을 **비켜 주기만** 하고(`if (e.altKey…) return`) 실제 구현은 **다른 화면**(`ChapterEditor`)에 있다                                     | 처방대로. ⚠ 이 파일이 M-13 에서 고친 결함(«경로는 있는데 발견 가능성이 없다»)의 **거울상**이 같은 자리에 생겼다는 사실을 주석에 남겼다                                                                                                                                                                                                             |
| U034 | 닫힘 | CSS 안 생 px `font-size` **23건을 토큰으로** 치환(`ds.css`·`global/*.css`)                                                                                                            | `lint:css`·`check:tokens` 통과 + 값이 같은 칸으로만 바꿨다(픽셀 변화 0)                                                                                                  | ⚠ 남은 1건(`.ds-roNum` 24px)은 **사다리에 칸이 없다**(22 ↔ 26). 반올림하면 위계가 붙거나 카드 높이가 바뀌므로 «사다리에 칸을 더할 것인가»의 문제로 남기고 그 사실을 그 줄에 적었다                                                                                                                                                                 |
| U035 | 닫힘 | `tokens.css` 머리주석을 **「금지」 → 「늘리지 않는다 + 줄일 땐 내림」** 으로 고치고 `check-tokens.mjs` 에 **반픽셀 래칫**(현재 5) 신설                                                | `check:tokens` 가 `반픽셀 5/5` 를 보고한다                                                                                                                               | 처방(«방향을 못박는다») 대로 + 집행자까지. ⚠ 지우는 쪽으로 안 간 이유는 `stylelint.config.js:17-22` 의 실사고(정수 **상향** → 월 캘린더 칩 잘림)                                                                                                                                                                                                   |
| U036 | 닫힘 | U023 의 `scrollIntoView`(`block:'nearest'`)가 **세로 축도 함께** 해결한다 + 게이트 신설(최소 창 높이 600 에서 현재 탭이 레일 안에 보이는가)                                           | 실측 1440×{600,720,805,900} 네 점 — 레일 내용 814px 이 600 창에서 스크롤되고 현재 탭(`설정`)이 보인다                                                                    | ⚠ 처방은 «레일 높이를 줄여라» 계열이었는데 **도달성 문제로 다시 읽었다**: 넘치는 것 자체는 죄가 아니고 못 닿는 것이 죄다. 그러면 U023 과 **같은 한 줄**로 닫힌다                                                                                                                                                                                   |
| U037 | 닫힘 | 하루 백필에 `type="date"` 입력(±1 스테퍼 옆 · `max=오늘`)                                                                                                                             | 코드 — 7일 전이 7클릭이었다. 네이티브 컨트롤이라 키보드·SR 경로가 그대로 온다                                                                                            | 처방대로                                                                                                                                                                                                                                                                                                                                           |
| U038 | 닫힘 | 오늘 버퍼 `<pre>` 에 `tabIndex={0}` + `role="group"`                                                                                                                                  | `[추측]` 이었던 것을 코드로 확인 — `overflow:auto` 만으로는 포커스를 못 받고, 이 오버레이엔 다른 포커스 대상이 위쪽 버튼 둘뿐이라 키보드로 본문을 스크롤할 방법이 없었다 | ⚠ `role="region"` 이 아니라 **`role="group"`** 이다 — `eslint.config.js` 가 스크롤 컨테이너의 표식으로 정해 둔 어휘이고, 이름을 주지 않는 것까지가 그 규약이다                                                                                                                                                                                     |

### 뒤따라 닫힌 것 둘 (같은 날 · 실행 중 발견해 그 자리에서 닫았다)

| ID   | 상태          | 무엇을 고쳤나                                                                                                                                                       | 검증                                                                                                 | 처방과 달라진 점                                                                                                                                                                                                                                                                                               |
| ---- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V027 | **부분 닫힘** | 불변식 ⑩의 지역 파일 워커를 `test/_sources.ts` 의 공용 `tsxFiles`+`strip` 으로 접었다                                                                               | `vitest -t "⑩"` 2케이스 통과                                                                         | ⚠ 실제 결함이 하나 있었다: ⑩만 **블록 주석만** 걷어서 줄 주석 안의 `animate-[live-*]` 를 위반으로 셌다(m-16 이 경고한 역인센티브 형태). ⚠ **남은 셋(⑧·⑪·⑬)은 안 건드렸다** — 그쪽은 주석 제거가 이미 옳아 보일러플레이트 중복일 뿐이고, 게이트가 잡는 명제를 바꿀 위험이 이득보다 크다. 원장에 Nit 으로 남긴다 |
| V028 | 닫힘          | `hub/CLAUDE.md` 의 a11y 절에서 **수를 전부 지웠다**(「26 → 64」·「오버레이 6종」·「폰 5화면」) + 이 회차가 늘린 축 둘(리플로우 320px · 미측정 래칫)을 목록에 넣었다 | 그 문단의 네 줄 아래가 스스로 «화면 수를 여기 적지 않는다»고 말한다 — 이제 문단이 자기 규칙을 지킨다 | 갱신이 아니라 **삭제**다. 수를 고쳐 적으면 다음 회차에 또 낡는다(그 문서가 이미 두 번 겪었다)                                                                                                                                                                                                                  |

**회차 총평.** 38건 중 재현 불가 **0건**. 처방을 그대로 쓴 것 20건 · **다르게 고친 것 18건**
(위 표에서 ⚠ 로 시작하는 「처방과 달라진 점」이 그것들이다 — 절반 가까이가 갈렸다는 사실 자체가
이 회차의 신호다: 리포트는 «무엇이 틀렸나»에는 정확했고 «어떻게 고치나»에는 자주 한 겹 얕았다).
새로 선 집행자 **일곱**: 불변식 ⑰(탭 이름) · 미선언 사다리 칸 · 반픽셀 래칫 · incomplete 래칫 ·
320px 리플로우 26케이스 · 포커스 링 스냅샷 2장 · 색 계산 테스트 둘(`masteryRamp`·`annoContrast`).
**그중 여섯이 「선언은 있는데 집행자가 없다」를 닫은 것**이고, 그것이 이 회차 근본원인 R1 의 형태였다.

⚠ **실측이 리포트와 갈린 곳 셋**(다음 회차의 읽는 법):
① U023 은 6/8 이 아니라 **3/8**(리포트는 dev 서버, 이쪽은 빌드본) — 대신 **더 큰 것**이 나왔다(390px 에서 탭 7개 실종).
② U003 의 미측정 노드는 1,144 가 아니라 **624**(재는 범위가 다르다 — 다크 경로 로스터).
③ U019 는 **중복이 아니었다**(`em` vs `px`). 리포트가 «드리프트 기준 미만»이라 부른 것이 실제로는 서로 다른 계약이었다.

---

## 2026-08-21 · 데이터 축 1회차 (D001~D019 · 19/19 종결)

리뷰 원문 `리뷰/2026-08-21-데이터/`. 게이트 결과는 아래 각 항목이 아니라 회차 말미에 한 번 적는다.

| ID   | 상태 | 무엇을 고쳤나                                                                                                                                                                                                                                                                                                                      | 검증                                                                                                                                                 | 처방과 달라진 점                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---- | ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D001 | 닫힘 | 폰 스키마 이행을 `db/migrateSchema.ts` 로 갈라내고 **각 마이그레이션을 BEGIN/COMMIT/ROLLBACK 으로** 감쌌다(워커 `batch` 핸들러와 같은 관용구). 갈라낸 이유는 테스트가 붙을 자리를 만들려고 — 워커는 모듈 최상위에서 `self`·wasm 을 잡아 노드에서 못 부르고, 그래서 이 저장소는 폰 마이그레이션을 **한 번도 실행해 본 적이 없었다** | `test/phoneMigrate.test.ts` (`node:sqlite` 로 실제 적용 · 중단→재개 왕복)                                                                            | ⚠ **리포트가 든 예 009 로는 재현되지 않는다** — 그 파일이 마지막에 `summaries_v9`→`summaries` rename 을 해서 우연히 재실행 가능하다. 실제로 브릭되는 것은 `CREATE TABLE` 로 **끝나는** 007·008·010·011 이고(`table route_visits already exists` 실측) 테스트는 007 로 잠갔다                                                                                                                                                                                                       |
| D002 | 닫힘 | `parseInboundBatch` 의 관용 필터를 `known` → `knownRow`/`knownTomb` 으로 갈라 **기본키·데이터 열 개수**까지 경계에서 본다                                                                                                                                                                                                          | `test/cloudSchema.test.ts` 에 `parseInboundBatch` 케이스 5종 신설(그 함수엔 테스트가 **0건**이었다)                                                  | 처방대로. ⚠ **기존 테스트 하나를 뒤집었다** — `cloudClient.test.ts` 의 «아는 테이블의 내용이 다르면 던진다» 는 뒤집기 전 정책이었다. 뒤집은 근거를 그 자리에 적었다. ⚠ 남는 트레이드오프: 버린 행은 `upto` 가 전진하므로 **업데이트 뒤에도 다시 안 받는다**(전체 재동기화가 필요하다). 이건 H16 이 `tbl` 축에서 이미 받아들인 대가이고, 대안(배치 거부)은 수신 영구 정지라 더 나쁘다. 관측은 이미 있다 — `dropped` 가 텔레메트리 + «업데이트가 필요할 수 있어요» 토스트로 올라간다 |
| D003 | 닫힘 | localStorage→SQLite 이관에 **완료 마커**(`sync_state` 의 `migration:localStorage`)를 뒀다. 쓰기 **전에** 세우고 성공 후 지운다. 마커가 남아 있으면 부분 DB 를 기준선으로 세우고 이관을 **재개**한다(diff 가 나머지만 채우므로 멱등)                                                                                                | `test/dbBoot.test.ts` 4케이스(재개·무마커·성공시제거·실패시보존)                                                                                     | 처방대로(`meta` 가 아니라 `sync_state` 인 이유까지). 기존 설치는 마커가 없어 거동 변화 0                                                                                                                                                                                                                                                                                                                                                                                           |
| D004 | 닫힘 | 네 줄 관용구를 **`store/settleBeforeExit.ts` 하나**로 모으고 세 종료 경로(창 닫기·트레이 종료·**업데이트 설치**)가 전부 그것을 태운다                                                                                                                                                                                              | 불변식 ⑯(`test/invariants.test.ts`) — `installUpdate`·`shellQuit` 를 부르는 파일은 `settleBeforeExit` 를 함께 부른다(비공허 단언 포함)               | 처방은 «UpdateCard 에 같은 4줄을 넣어라» 였는데 **사본을 늘리지 않았다** — 근본원인 ③이 «관용구가 경로마다 붙었지 한 곳에 없다» 였으므로 자리를 만드는 쪽이 처방의 뜻이다. 네 번째 경로는 불변식이 잡는다                                                                                                                                                                                                                                                                          |
| D005 | 닫힘 | ① `backupPayload` 에 `_docs`(표 **전량** · 미지 키 포함) ② `docDelete` — **툼스톤을 먼저** 내고 실패하면 행을 안 지운다 ③ `unknownDocKeys()` + 설정 › 진단의 `OrphanDocs` 리드아웃(보고만 · 앱이 스스로 안 지운다)                                                                                                                 | `test/docs.test.ts` 5케이스(회수·보고·툼스톤 선행·툼스톤 실패시 미삭제·가져오기는 아는 키만)                                                         | **실 데이터는 안 지웠다**(사용자 결정). 51,793 B 삭제는 사용자가 진단 화면에서 직접 — 되돌릴 수 없는 것을 자동으로 실행하지 않는다. ⚠ 덤: `ics:feed`(살아 있는 세입자)도 **어떤 백업에도 없었다** — `_docs` 가 그것도 함께 구한다                                                                                                                                                                                                                                                  |
| D006 | 닫힘 | `DB_OK_KEY`(마지막 정상 개방 시각) + 세션 플래그 `dbStaleSince()`, `StorageBanner` 에 ③ 가지 신설 — 낡은 사본 위에 떠 있다는 사실을 **화면이** 말한다                                                                                                                                                                              | `test/dbBoot.test.ts` 3케이스. 핵심은 «한 번도 못 연 기기(트랙 A·첫 실행)는 조용하다» — 그 조건이 없으면 시각 스냅샷 전량에 오폭한다                 | 처방대로                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D007 | 닫힘 | `exportObservations`/`importObservations` 를 **4표**로(`route_hops`·`idle_spells` 추가 · 합치기 규칙 `MAX`). 그리고 **집행자**: `updated_at` 열이 없는 표를 실제 마이그레이션 적용 후 스키마에서 역산해 백업 키 집합과 대조 + 사유 붙은 면제표                                                                                     | `test/observations.test.ts` — 역산 4케이스 + 왕복 2케이스                                                                                            | 처방대로. ⚠ 스키마 역산을 **정규식으로** 짰다가 14표 중 5표만 찾았다(한 줄 DDL·들여쓰기·`ALTER ADD COLUMN`·rename 이 섞여 있다) → `node:sqlite` 로 **실제 적용해서** 읽는다(`dbMigrations.test.ts` 와 같은 근거)                                                                                                                                                                                                                                                                   |
| D008 | 닫힘 | 아카이브 대상을 `ARCHIVE_ARRAY_SPECS` **한 곳**에 선언하고 `archivableCount`·`archiveOldData` 가 둘 다 거기서 돈다. 규율 밖이던 슬라이스를 편입: `retrievals`·`events`·`tasks`·`questions`·`jolAsks`. 나머지는 사유 붙은 `ARCHIVE_EXEMPT`                                                                                          | `test/methodology.test.ts` — `ARRAY_SLICES` 와 대조하는 역산 케이스 + `retrievals` 실제 회수 + «미완료 할 일은 안 걷는다»                            | ⚠ 처방의 «`retrievals` 에 90일 창» 은 **안 했다**. 자매 넷(관측 원장)의 90일은 기기 로컬·동기화 밖이라 조용히 지워도 되지만, `retrievals` 는 `records` 슬라이스라 **동기화되는 사용자 데이터**다 — 배경 프루닝은 툼스톤을 전 기기로 보낸다. 같은 축(보존)을 **사용자가 아는 경로**(정리 6개월)에 붙이는 것이 이 앱의 규율에 맞다. ⚠ `tasks` 는 `done` 인 것만 걷는다(미완료·인박스는 기록이 아니다)                                                                                |
| D009 | 닫힘 | 레이트 버킷의 **가변 경로를 접는다**(`/api/ics/<토큰>` → `/api/ics/*`). 일반화 정규식이 아니라 **선언 표**(`가변경로`)인 이유: 정규식으로 마지막 세그먼트를 지우면 `/api/token` 과 `/api/enroll` 이 같은 버킷이 되어 의도한 분리가 조용히 사라진다                                                                                 | `server/test/roundtrip.test.ts` 2케이스 — 다른 토큰 25회에 429 · 그리고 그것이 인증 라우트 버킷을 휩쓸지 않는다                                      | 처방대로. 검증 케이스는 «지금 통과하는 것이 곧 결함» 형태로 썼다                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D010 | 닫힘 | `src-tauri/deny.toml` + `npm run tauri:deny` + 게이트(`mode:'cargo'`) + CI(windows 러너라 `cargo install` · rust-cache 가 `~/.cargo/bin` 을 캐시한다)                                                                                                                                                                              | `cargo deny check` 전량 통과. **그리고 옛 락(event-listener 5.4.1)에 겨눠 실제로 빨간불이 나는 것까지 확인**                                         | ⚠⚠ 처방이 반쪽이었다: `cargo deny` 기본값은 `informational = "unsound"` 를 **리포트하지 않는다**(실측 — 그 crate 가 그래프 안인데 `advisories ok`). `[advisories] unsound = "all"` 을 명시해야 D016 이 잡힌다. ⚠ `[graph] targets` 필터가 계약(락은 피처·타깃 무관이라 mysql·rsa 트리까지 적는다). ⚠ `publish = false` 를 `Cargo.toml` 에 넣었다 — 없으면 라이선스 게이트가 **우리 자신을** unlicensed 로 막는다                                                                   |
| D011 | 닫힘 | 폰 `migrateSchema` 에 다운그레이드 가드 — 적용된 최대 버전 > 번들이면 **던진다**(데스크톱 C2 `is_downgrade` 와 같은 술어)                                                                                                                                                                                                          | `test/phoneMigrate.test.ts` 2케이스                                                                                                                  | 처방대로                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D012 | 닫힘 | `dbMigrations.test.ts` 에 폴더 ↔ `db.rs` 목록 대조 케이스                                                                                                                                                                                                                                                                          | 폴더에만 있는 `.sql` 을 심어 **실제로 빨간불이 뜨는 것** 확인 후 제거                                                                                | 처방대로                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D013 | 닫힘 | `idle_spells` 청소를 **쓰기 경로**(`bump`)로 옮기고 `_prunedOn` **날짜** 가드를 달았다. 읽기(`idleSummary`)는 읽기만 한다                                                                                                                                                                                                          | `test/idleLedger.test.ts` 3케이스(첫 기록이 청소를 부른다 · 같은 날 1회 · 자정 넘기면 다시)                                                          | 처방대로. ⚠ 목 대역을 `calls`/`deletes` 두 채널로 갈랐다 — 한 통이면 기존 «한 부재는 한 번만 센다» 단정이 청소 때문에 흔들려 **검사가 재는 명제가 조용히 바뀐다**                                                                                                                                                                                                                                                                                                                  |
| D014 | 닫힘 | `phone.html` 에 CSP meta                                                                                                                                                                                                                                                                                                           | **실 Chromium 으로 확인**: OPFS 풀 생성(`.learning-hub`) + 콘솔 오류 0 — 즉 wasm·워커·SAH 가 전부 살았다                                             | ⚠ `'wasm-unsafe-eval'` 이 필수다(없으면 폰 로컬 저장이 통째로 죽는다). ⚠ `frame-ancestors` 는 meta 에서 **무시된다**(사양) — 그건 `_headers` 가 자산 워커에 실려야 해서 배포 경로가 다르다(별건으로 남긴다)                                                                                                                                                                                                                                                                        |
| D015 | 닫힘 | 런북 **§5-1b** — «유출된 것 / 즉시 / 그다음 / 잃는 것» 표 5행 + 히스토리에 안 남기는 curl                                                                                                                                                                                                                                          | 표의 사실 둘을 코드로 재확인: 리프레시 검증은 D1 의 SHA-256 해시만 쓰고 `HUB_SIGNING_KEY` 와 무관(→ 서명키 회전은 거의 공짜) · 등록 코드도 해시 저장 | 처방대로. **순서**(키 재발급 → 코드 무효화)를 표에 명시했다 — 뒤집으면 공격자가 곧바로 재발급한다                                                                                                                                                                                                                                                                                                                                                                                  |
| D016 | 닫힘 | `cargo update -p event-listener` → 5.4.2                                                                                                                                                                                                                                                                                           | `cargo deny check` 통과 + `tauri:build:fast` 재빌드 성공 + 트랙 B 9/9                                                                                | 처방대로(`Locking 1 package` · 코드 변경 0). ⚠ 이제 **게이트가 이 부류를 잡는다**(D010 의 `unsound = "all"`) — 월간 `lockFileMaintenance` 의 우연에 안 맡긴다                                                                                                                                                                                                                                                                                                                      |
| D017 | 닫힘 | `server/package.json` 의 `allowScripts` **삭제**(사유는 그 자리에 주석으로)                                                                                                                                                                                                                                                        | `npm run verify`(server) 통과                                                                                                                        | 「지우거나 셋을 함께 세운다」 중 **지우는 쪽**. `@lavamoat/allow-scripts` 도입은 새 공급망 도구 도입이라 별건이고, 지금 상태(집행자 없는 목록)는 «통제 없음»보다 나쁘다 — 인수인계받는 사람이 그것을 통제로 읽는다                                                                                                                                                                                                                                                                 |
| D018 | 닫힘 | 루트 `package.json` 에 `audit` + 빈 `audit-allowlist.json` + 게이트 `root audit` + CI 한 줄                                                                                                                                                                                                                                        | `npm run audit`(루트) 통과 — 실측 취약 0건                                                                                                           | 처방대로. 근거를 한 줄 더 못박았다: 세 트리 중 **비밀(`TAURI_SIGNING_PRIVATE_KEY`)을 보는 유일한 트리**가 게이트 밖이었다                                                                                                                                                                                                                                                                                                                                                          |
| D019 | 닫힘 | `capabilities/default.json` 에서 `global-shortcut:allow-{register,unregister,is-registered}` + `core:window:allow-show` 제거(22→18) + 그 자리에 사유 기록                                                                                                                                                                          | **릴리스 exe 로 실동작 확인**: `tauri:build:fast` → 트랙 B 9/9 → 프로브로 `hotkey: true · hotkeyError: null`                                         | 처방대로 + `core:window:allow-show`(부수 지적)까지 함께. 되살릴 조건을 파일에 적었다                                                                                                                                                                                                                                                                                                                                                                                               |

**회차 총평.** 19건 중 재현 불가 0건. 처방을 그대로 쓴 것 15건 · 다르게 고친 것 4건(D004·D008·D010·D017).
발상 항목 셋(D020~D022)은 **열려 있다** — 이 회차의 수정 대상이 아니다.

---

## 2026-08-22 · 발상 축 1회차 (I001~I053 · 진행 중)

리뷰 원문 `리뷰/2026-08-22-발상/`. 이 축은 **결함이 아니라 아직 없는 것**을 담아, 닫기의 뜻이
다른 축과 다르다: 「고쳤다」가 아니라 **「최싼검증을 실제로 세웠거나, 그 검증이 가능해졌다」**다.
그래서 닫기 노트에 *무엇을 재게 됐는가*를 함께 적는다.

### 1단계 — 선행 사슬(리포트 §5 의 상충 판정 셋이 여기 걸려 있다)

| ID   | 상태 | 무엇을 고쳤나                                                                                                                                                                                                                                                | 검증                                                                                                                                                                   | 처방과 달라진 점                                                                                                                                                                                                                                                                                                                                                           |
| ---- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I030 | 닫힘 | **점검 모드**를 세웠다 — `ui.inspectDs`(기기별 영속) → `lib/visits.setInspectDs` 미러 → `recordVisit`·`recordHop` 이 그 날짜를 통째로 건너뛴다. 스위치는 `features/settings/VisitLedger.tsx`(원장 바로 옆)                                                   | `test/visits.test.ts` 5케이스 — 방문·홉·**보존 청소까지** 안 돈다 / **날짜가 다르면 기록한다** / 끄면 되돌아온다                                                       | ⚠ **처방은 「플래그 한 줄」이었는데 부울을 안 썼다.** 부울이면 켜 놓은 것을 잊어 관측이 조용히 영원히 멈추고, 그러면 다음 판정자가 **0 을 「안 쓴다」로 읽는다** — 이 원장이 막으려는 그 순환을 노브 자신이 재현한다. **날짜**를 들어 자정에 스스로 꺼지게 했다. ⚠ 이미 들어간 오염 행은 **소급 삭제하지 않는다**(분모까지 줄어 표본 판정이 뒤집힌다 · 90일 창이 밀어낸다) |
| I034 | 닫힘 | `shell/tabs.ts` 에 `visitKeyOfLocation()` 신설 — `?view=` 로 흡수된 화면이 **자기 key** 로 집계된다. `App.tsx` 의 원장 이펙트만 그것을 쓴다                                                                                                                  | `test/visitKey.test.ts` 5케이스 — 흡수 화면 3종이 자기 key / **로스터에 없는 뷰 값은 호스트로 떨어진다**(카디널리티) / 루트는 `today` / 이름 판정과 같은 표를 읽는다   | ⚠ 처방(«`routeKey` 산정»)을 그대로 하면 **레일 활성·fill 프레임·`useMarkSeen` 이 함께 바뀐다**. 그쪽은 «지금 어느 호스트인가»가 맞는 질문이라 `routeKey` 는 **그대로 두고** 원장 키만 갈랐다 — H-12 가 이름 축에서 한 것과 같은 형태(그 함수의 짝이다). ⚠ 전환기 90일 동안 **호스트 수치가 과소 집계된다**는 사실을 그 머리주석에 못박았다                                 |
| I023 | 닫힘 | `lib/aiUsage.ts` 신설(기기-로컬 KV · 날짜별 카운터 + **관측 시작일**) · `lib/api.ts` 의 전송 분기 둘(`aiCall`·`embedTexts`)이 센다 · 리드아웃은 `VisitLedger` 의 「로컬 LLM」 줄                                                                             | `test/aiUsage.test.ts` 6케이스 — **안 쟀으면 `since===null`**(0 과 다른 상태) / 7일 미만이면 `ready` 거짓 / 쓴 날 수를 따로 / 보존 창 밖은 잊는다 / 손상돼도 안 던진다 | ⚠ **표를 늘리지 않았다.** 관측 원장이 이미 넷이고 같은 회차의 I032 가 그것을 줄이자고 올라와 있다 — 한 주짜리 질문에 다섯째 표를 파면 질문이 끝나도 표가 남는다(이 저장소가 «영원히 유예»라 부르는 형태). KV 한 칸이라 **질문이 끝나면 키를 지우면 철거**다                                                                                                                |
| I033 | 닫힘 | 폰에 **내보내기 문**(`phone/ExportBackup.tsx` · 홈에서만) + 데스크톱에 **관측만 합치기**(`VisitLedger` 의 파일 입력). 그러려고 `backupPayload` 를 `shell/actions.ts` → **`lib/backup.ts`** 로 옮겼다(`src/phone/**` 은 `shell` 배럴을 물 수 없다 · §13-0·H7) | 트랙 A 재생성 2장으로 실렌더 확인(버튼이 캡처바와 탭바 사이에 정상 배치) · `verify` 189파일 2,105케이스                                                                | ⚠⚠ **처방(«백업 왕복 1회»)을 그대로 하면 앱 데이터가 덮인다.** `importJSON` 은 `loadState` 로 상태를 **교체**하는데, 앱 데이터의 정본은 클라우드 동기화다 — 폰 백업으로 그걸 하면 두 정본이 서로를 덮는 경로를 손으로 여는 셈이다. 그래서 **`_obs` 만 꺼내 upsert 하는 별도 입구**를 팠다(방문 수 `MAX` 규칙은 `lib/observations` 가 이미 소유)                            |

**1단계 게이트**: `verify` ✅(189파일 · 2,105케이스) · 트랙 A ✅(216) · `e2e:a11y` ✅(94) ·
`e2e:motion` ✅(5) · `budget` ✅(4축 전부). 시각 베이스라인 **2장 재생성**(폰 홈에 버튼이 하나 늘었다).

### 2단계 — 코드만으로 닫히는 저비용 묶음

| ID   | 상태 | 무엇을 고쳤나                                                                                                                                                                                                                                                                     | 검증                                                                                                                                                                                                                          | 처방과 달라진 점                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---- | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I020 | 닫힘 | `spacedReview.chapterFuzz()` — 챕터 정체성(`sid\|chapter`) 해시로 **결정적 ±1일** 흔들기. `riskOf` 가 다섯째 인자로 받고, 오늘 화면과 시험 앵커가 **같은 흔들기**를 쓴다. 해시는 `utils.hash32` 로 뽑아 `colorForId` 와 한 집                                                     | `test/spacedReview.test.ts` 5케이스 — 결정적 / ±1 상한 / **같은 날 무리가 사흘에 걸쳐 넘어간다** / 막힌 챕터는 안 탄다 / 사다리 역전 방어                                                                                     | ⚠ 최싼검증(«일별 due 히스토그램»)은 **실 데이터로 못 했다** — 이 회차가 실측한 대로 학습 표가 전부 0행이라 히스토그램의 분자가 없다. 대신 몰림을 **구조로** 증명했다(임계가 전부 상수 → 같은 `lastDs` 는 같은 날 전이). 그 증명이 곧 회귀 테스트다. ⚠ `failing` 은 안 흔든다(`coef` 와 같은 안전 방향) ⚠ 경계에 정확히 앉힌 옛 fixture 셋을 **한 칸 밖으로** 옮겼다 — 그 케이스들이 재려던 것은 경계가 아니라 필터·경과일이었다 |
| I018 | 닫힘 | `lib/scheduler/simulate.ts` — `simulate(state, patch)` + `shortfallDelta()`. **소비처를 함께 붙였다**: `CutCard` 의 «이만큼 빼면 닫혀요»가 산술(`합 ≥ 부족분`)에서 **엔진의 답**으로 바뀌고, «대신 다른 과목이 부족해져요»(부수 피해)를 새로 말한다                               | `test/simulate.test.ts` 7케이스 — **입력 불변** / 결정적(엔진 부작용 없음 = 이 항목의 전제) / 제안대로 빼면 실제로 닫힌다 / 부수 피해 / 예산. **실측 20회 7.3ms(회당 0.37ms)** — 문턱(슬라이더 50ms)을 두 자릿수 여유로 통과  | ⚠ 처방은 «미리보기 한 줄»이었는데 **기존 문장을 바꿨다**: 근사를 남긴 채 옆에 진짜 값을 놓으면 화면이 같은 질문에 두 답을 준다. ⚠ 캐시를 안 뒀다 — 캐시는 «무엇이 입력인가» 목록을 한 벌 더 만들고, 0.37ms 는 그걸 살 이유가 없다                                                                                                                                                                                               |
| I040 | 닫힘 | `reviewSnooze` + `reviewPause()`(**유일한 판정자**) + 러너의 동사 둘(「오늘은 빼기」·「당분간 빼기」). 큐 필터가 `state.reviewHold` 직접 읽기에서 `reviewPause` 로 옮겼다                                                                                                         | `test/reviewHold.test.ts` 7 + `test/reviewQueue.test.ts` 1 — 날짜 스코프 / **배타(hold 가 이긴다)** / 덮어쓰기 / 선반에 안 뜬다 / **큐가 실제로 먹는다**(이게 없으면 «버튼은 눌리는데 아무 일도 안 일어나는» 실패가 통과한다) | ⚠ 전제(«타입으로 못박을 수 있다»)를 **부분만** 지켰다: 영속 형태가 이미 `reviewHold` 라 태그드 유니온으로 못 바꾸고, 배타를 **설정자 + 단일 판정자**가 진다. 그 사실을 그 파일 머리주석에 적었다. ⚠ 스누즈엔 되돌리기 UI 를 **안 만들었다** — 자동 만료가 있는 결정에 되돌리기를 붙이면 대상이 하루 만에 사라져 유령 버튼이 된다                                                                                                |
| I041 | 닫힘 | leech 전용 배지·본문(`chapterCopy`)과 **처방 둘**(막힌 지점 적기 → `bl` 프리필에 챕터를 실어 `/day` · 챕터 쪼개기 → `/subject/:id`). 그 김에 러너의 배지 삼항을 `chapterCopy().badge` 로 합쳤다                                                                                   | `test/reviewQueue.test.ts` 3 — 다른 배지 / 본문이 «자료»를 말한다(«망각곡선 리셋»이 아니다) / 유지 문구 우선                                                                                                                  | ⚠⚠ **원장보다 나빴다.** 원장은 «표식은 있는데 갈 자리가 없다»였는데 실측하면 **`leech` 를 읽는 화면이 0개**였다 — 표식조차 안 그려지고 있었다(`spacedReview` 가 A-4 에서 _"UI 는 이걸 다른 문구로 그려야 한다"_ 고 적어 둔 조건이 한 번도 충족된 적 없다). ⚠ 그리고 러너 배지가 `chapterCopy` 의 배지를 **손으로 재현**하고 있었다(폰은 `copy.badge` 사용) — H14 가 본문에서 고친 「말이 두 벌」이 배지 축에 남아 있었다        |
| I038 | 닫힘 | `lib/snapshots.ts` — 되돌리기 백업이 **3세대 사다리**가 됐다(0세대 키 = 옛 `BACKUP_KEY` 그대로 · 마이그레이션 0). `undoTo(key)` + 설정의 「되돌릴 수 있는 지점」 목록(시각 표시 + **한계 문구**)                                                                                  | `test/snapshots.test.ts` 8케이스 — **연달아 두 번 밀어도 첫 상태가 산다**(이 항목의 전부) / 옛 키 호환 / 세대가 서로 다른 값(앞에서 밀면 전부 같아진다) / 되돌린 지점은 당겨 채우지 않는다 / 저장 실패는 false                | ⚠ 처방(«위험 작업 직전 파일 복사»)을 **안 썼다**: 이 앱의 파괴적 동작은 이미 직전에 백업을 찍고 있었고, 없던 것은 *시점*이 아니라 **세대**였다(두 번째 동작이 첫 번째의 백업을 덮는다 — 회수 경로가 자기를 덮는 형태로, H19·H2 가 두 번 고친 그 부류). ⚠ 밖의 문서가 자기 페이지에 적는 한계(«같은 기기라 기기가 부서지면 무의미»)를 **화면에 그대로** 적었다                                                                   |
| I039 | 닫힘 | `db.rs` 의 부팅 가드가 **체크섬 드리프트**까지 판정한다(`applied_checksums_at` + 순수 술어 `drifted_versions`). 프런트는 같은 문(`dbDowngrade`)으로 받고 `DowngradeScreen` 이 **원인을 갈라** 말한다. 그 김에 sqlx 변환기를 테스트 지역 사본에서 프로덕션 `bundled_sqlx()` 하나로 | `cargo test --lib` 76 통과(신규 `번호는_같은데_내용이_다르면_열기_전에_잡는다` 4단언 — 평소 조용함 / 흔든 버전을 잡는다 / 미적용 버전은 아니다 / **SQL 한 글자로 체크섬이 바뀐다**는 sqlx 계약) + `test/dbBoot.test.ts` 1     | ⚠⚠ **처방(«`PRAGMA table_info` 해시»)을 안 썼다 — 탐지는 이미 있었다.** sqlx 가 SHA-384 로 매 부팅에 대조하고 그 사실은 `db.rs` 가 스스로 적어 뒀다. 없던 것은 **보고**다: 그 실패가 `load()` Err → C1 조용한 폴백으로 흘러 «뜨는데 데이터가 옛날 것»이 됐고, `is_downgrade` 는 이 경우 거짓이라 명시적 화면이 안 떴다. 해시를 새로 만들면 같은 사실에 판정자가 둘이 된다                                                       |
| I043 | 닫힘 | `buildAdhocQueue()` + `AdhocNav`(`preview` **필수**) + `/find` 결과의 「이 결과 N개로 지금 세션」. preview 면 `advance()` 가 앵커를 **안 옮긴다**(한 줄 · 호출부마다 기억시키지 않는다) 상단 크롬이 「임시 세트 · 사다리 무변경」을 말한다                                        | `test/reviewQueue.test.ts` 5 — 부른 것만 / 빈 목록은 빈 큐(정규 큐로 조용히 안 떨어진다) / **보류·스누즈 무시** / 회상·착각 안 섞임 / 모르는 키는 조용히 빠진다                                                               | ⚠ 밖의 매뉴얼이 자기 페이지에 적는 _"반복 사용에 부적절"_ 을 **설계 조건으로** 옮겼다: 그래서 `preview` 가 옵셔널이 아니라 필수다(옵셔널이면 «안 적으면 앵커를 옮긴다»가 기본이 되고, 그게 정확히 그 문서가 경고하는 실패다)                                                                                                                                                                                                    |

**2단계 게이트**: `verify` ✅(191파일 · 2,142케이스) · 트랙 A ✅(216) · `e2e:a11y` ✅(94) ·
`e2e:motion` ✅(5) · `budget` ✅ · `cargo test --lib` ✅(76). 시각 베이스라인 **2장 재생성**
(`/find` 결과 위에 세션 버튼 한 줄).

### 3단계 — 순수 은퇴 9건 일괄 (I044~I052 · 사용자 승인 후 재확인)

⚠⚠ **근거가 두 종류였고, 그 사실을 사용자에게 먼저 알렸다.** 셋(I044 시각 실물 · I045 TDR 실사고 ·
I050 무인증 공개 GET)은 **구조적** 근거이고, 나머지 여섯은 «해당 표가 0행»에 매달린다 — 그런데
같은 회차의 1단계가 고친 것이 정확히 «그 0 을 근거로 쓰면 순환»이었다(순회 트래픽 오염 · 폰 원장
미도달). 그 상충을 제시했고 **사용자가 전량 삭제로 재확인**했다. 되돌리기는 git 한 줄이다.

| ID   | 상태 | 무엇을 지웠나                                                                                                                                                                                              | 검증                                                                         | 함께 걷은 것 · 남긴 대가                                                                                                                                                                                                                                                                                                                                                                                      |
| ---- | ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I044 | 닫힘 | `features/graph/`(6파일 1,655줄) + 테스트 4 + `/items` 의 구조도 뷰 토글 + 탭 로스터 항목 + `STRUCTURE_VIEW`                                                                                               | `verify` · 트랙 A(케이스 2 + 베이스라인 2장 제거) · a11y 로스터 1 제거       | `icon:'graph'` · `--backdrop-graph` · 브리지 12칸 · `.ds-srOnly` · `semanticChapterEdges` · 카드 원장 1행. **불변식 ③-c 의 바닥을 4→3 으로 내렸다**(그 수는 로스터 사본이 아니라 «0이 아님»의 바닥이다)                                                                                                                                                                                                       |
| I045 | 닫힘 | `components/AmbientCanvas.tsx`(373줄 · WebGL 오로라) + 필름 그레인 오버레이 + `--grain`                                                                                                                    | 트랙 A **전량 재생성**(배경이 전 화면에 걸린다) · 실렌더 확인                | 배경은 `--ambient`(옛 폴백 3줄)가 진다. `smoke` 의 H19 케이스(컨텍스트 손실→복구)와 `motion` 의 캔버스 은닉 규칙도 함께 갔다. ⚠ `fxLite` 의 뜻이 좁아졌다(이제 CSS 애니만 끈다)                                                                                                                                                                                                                               |
| I046 | 닫힘 | 게이미피케이션 3종 — 18주 히트맵(`StreakHeatmap`) · 「연속 학습일」 · 「최장 연속(개인 기록)」. 상단 크롬의 `연속` 칸은 **「밀린 복습」**이 받았다                                                         | `verify` · `appStateTabs` 단언 교체                                          | `icon:'trophy'` · `--color-hm-l*` · 잔디 표기 18칸. ⚠ **오늘 축의 스트릭은 안 건드렸다**(범위 밖) — `I054` 로 원장에 새로 열었다                                                                                                                                                                                                                                                                              |
| I047 | 닫힘 | 명사 주소 셋(`/chapter/*`·`/week/:ws`·`/exam/:sid`) + `app/NounRoutes.tsx`                                                                                                                                 | `verify` · 불변식 ②의 파일 목록 정정                                         | ⚠ 근거가 «`route_visits` 5키 0행»만은 아니었다: **저장소 전수로 그 셋을 향해 `navigate`·`<Link>` 하는 코드가 0**이었다(리다이렉트 자신이 유일한 소비처). `subject`·`mini` 는 호출부가 실재해 남는다                                                                                                                                                                                                           |
| I048 | 닫힘 | `role:'retired'` **어휘** → `role:'view'`(호스트 안의 이름 있는 뷰). `journal` 탭은 **삭제**(그 `to` 가 `/day` 자기 자신이라 두 번째 이름이었다)                                                           | `verify` · 불변식 두 케이스 이름·술어 교체 · `/journal` → `/day` 테스트 이동 | ⚠⚠ **부분이다 — 그리고 그게 판단이다.** 「두 갈래만: 레일에 선다 / 없다」 중 `forecast`·`mastery` 를 지금 어느 쪽으로도 못 보낸다: 레일에 올리려면 자기 라우트·로더가 필요하고(W9/A-16/A-19 를 되돌리는 IA 변경), 로스터에서 빼면 **H-12(이름)와 I034(집계)가 함께 회귀**한다. 지운 것은 «영원히 유예»라는 **뜻**이고, `view` 는 유예가 아니라 확정된 배치다. 자리 이동은 I025 가 데이터와 함께 판정할 일이다 |
| I049 | 닫힘 | 말 걸기 채널 셋 — 작업표시줄 배지(`useTaskbarBadge`+`shellBadge`) · 하루 한 번 알림(`useDailyReminder`+`shouldFire`/`reminderBody`) · **상주 트레이**(`tray.rs` 168줄 + 자동 시작 + 닫기 가드의 hide 분기) | `verify` · `cargo test --lib` 73 · 권한 5종 제거 후 clippy·fmt               | 트레이가 함께 간 이유는 그 파일이 자인했다: _"T-3 → T-6 은 원리적 선행"_. 남은 채널은 **레일 배지 하나**. `shellNotify`(집중 세션 종료)는 남는다 — 그건 같은 수를 두 번 말하는 채널이 아니다                                                                                                                                                                                                                  |
| I050 | 닫힘 | `.ics` 살아 있는 구독 피드 — 서버 라우트 + `lib/icsFeed.ts` + `IcsFeedPanel` + 왕복 케이스                                                                                                                 | `server verify`(24케이스) · `verify`                                         | ⚠ 이 앱의 **유일한 무인증 공개 GET** 이 사라졌다. 캘린더로 가는 길은 **1회 내보내기**가 남는다. D009 의 `가변경로` 표는 비었지만 **지우지 않았다**(다음 가변 경로가 그 한 줄을 다시 필요로 한다)                                                                                                                                                                                                              |
| I051 | 닫힘 | 실시간 poke — `live.rs`(258) + `cloud/live.ts`(268) + `syncHub.ts`(DO) + 라우트·브로드캐스트·`liveClosed`·`currentAccessToken`                                                                             | `server verify` · `cargo` · `verify`                                         | ⚠ `wrangler.jsonc` 에 **`deleted_classes` 태그를 추가**했다(바인딩만 지우면 배포가 거절된다). 옛 태그는 이력이라 안 지웠다. H15 케이스는 채널과 함께 갔다                                                                                                                                                                                                                                                     |
| I052 | 닫힘 | 텔레메트리 — `lib/telemetry.ts`(221) + `/api/log` + 스키마 + 전역 오류 훅 + `collectWebVitals`                                                                                                             | `server verify` · `verify` · `dbMergeWindow` 목을 콘솔로 교체                | ⚠⚠ **대가를 축소하지 않는다**: 렌더 밖 부팅 실패(OPFS·wasm·청크 로드)는 `ErrorBoundary` 가 원리적으로 못 잡고 **폰에서는 이제 아무 데도 안 남는다**. 리포트가 이 항목의 근거를 스스로 «약하다»고 적은 것이 이 때문이다. 되살릴 조건: I033 이 «폰이 실제로 쓰인다»를 내는 것                                                                                                                                   |

**3단계 게이트**: `verify` ✅(184파일 · 2,063케이스) · 트랙 A ✅(209+5 · **2회 연속**) ·
`e2e:a11y` ✅(90) · `budget` ✅(데스크톱 js 516.7→492.4 · 폰 308.1→303.3) ·
`cargo test --lib` ✅(73) · `cargo clippy -D warnings` ✅ · `server verify` ✅ · `audit` ✅.

⚠ **하네스도 한 줄 고쳤다**: 트랙 A 를 `workers: 1` 로 못박았다. 베이스라인을 `--workers=1` 로
**만들면서 검사는 병렬로** 하고 있었고(생성 조건 ≠ 검사 조건), 레일이 한 줄 줄자 그 어긋남이
넉 장에서 표면화됐다. 계측 결과 폰트는 두 조건에서 동일했고 갈린 것은 서브픽셀 스크롤 상태였다.
`settle()` 에는 `readyState==='complete'` 와 폰트 강제 로드를 더했다 — 네이티브 폼 컨트롤이
그려지기 전에 찍히는 상이 실제로 있었다.

### 4단계 — 입력 파이프(묶음 A/B) · 1차

| ID   | 상태 | 무엇을 세웠나                                                                                                                                                                                             | 검증                                                                                                                                                          | 처방과 달라진 점                                                                                                                                                                                                                                                                                                                                                                      |
| ---- | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I002 | 닫힘 | `ankiReviewedTodayBySubject()` — `findCards {rated:1}` + `cardsInfo` 로 **오늘 실제로 답한 카드**를 덱→과목 귀속. today 블록의 rev·anki 행이 「Anki 에서 오늘 N장 했어요 · 완료로 반영」 줄을 받는다      | `test/ankiReviewed.test.ts` 6 — 귀속 / `rated:1` 질의 / **귀속 안 된 카드를 숨기지 않는다** / 못 물어보면 `null`(0 아님) / 물어봤고 0 / 계약 깨짐도 `null`    | ⚠ **조용히 안 쓴다.** 처방은 「완료를 파생한다」였는데 자동 기록으로 만들지 않았다: 매칭이 이름 부분문자열이라(I035) 덱 이름이 바뀐 날 **틀린 완료가 조용히 기록된다**. 승인 줄 한 개가 그 위험을 0으로 만든다. ⚠ Rust 변경 0(`ankiConnect` 가 액션 이름을 받는 범용 통로)                                                                                                            |
| I005 | 닫힘 | 완료를 켜는 그 자리에 **실제 소요 칩 넷**(계획대로·−15·+15·+30). `toggleDone` 의 여섯째 인자가 이미 그 자리였다                                                                                           | 코드 — `actualMin` 은 **집중 세션 완주 경로에만** 실렸고(그 파일이 자인) 손 체크 경로엔 없었다. 소비처는 이미 있었다(`Subject`·`TodaySignature` 의 추정 대조) | ⚠ 분 단위 입력을 안 만들었다 — 이 값의 쓸모는 `estimateCalibration` 의 **배율**이고 거기에 분 정밀도가 필요 없다. ⚠ 닫는 버튼이 없다(Q-1 커밋 줄과 같은 규율): 안 누르고 지나가면 종전대로 계획 분이 쓰인다                                                                                                                                                                           |
| I004 | 닫힘 | `Ritual.did` — **계획을 참조하지 않는 한 줄**. 일일 의식이 받고, `/day` 가 그 날짜의 것을 되읽는다                                                                                                        | `test/ritualDid.test.ts` 4 — 날짜 키 / `stopWhy` 와 다른 칸 / 무마이그레이션 / 다른 날 무간섭                                                                 | ⚠ 처방(«새 표면 · 하루 되짚기 한 장»)을 **안 만들었다**: 이 앱은 이미 하루를 말하는 표면이 일곱~아홉이고(같은 회차 I024) 여덟째를 더하는 것은 그 진단과 정면으로 충돌한다. 없던 것은 화면이 아니라 **질문의 방향**이라, 기존 카드에 칸 하나로 뒤집었다. ⚠ 되읽히는 자리를 함께 만들었다 — 소비처 없는 기록은 기록이 아니다                                                            |
| I006 | 닫힘 | **볼트 노트를 연다.** `Note.file`(경계에서 버려지던 필드) + `files::open_in_vault`(경로 판정은 순수 `resolve_in_vault`) + `tauri-plugin-opener` + `lib/vault.noteRelFor` + today 블록의 「N장 노트 열기」 | `cargo test --lib` 74(신규 `볼트_밖은_열지_않고_없는_파일도_열지_않는다` — 부모 참조·절대경로·없는 파일·폴더) + `test/noteRel.test.ts` 6                      | ⚠ **유일할 때만 연다.** 여럿이면 «대상을 연다»가 아니라 «아무거나 연다»다 — 그래서 해석 실패는 조용한 폴백이 아니라 **버튼 부재**로 표현된다(안 풀리면 매칭을 먼저 고치라는 신호다). ⚠ 플러그인 권한을 **JS 에 주지 않았다** — 경로 검증은 Rust 가 `vault::safe_join`(노트 읽기와 **같은 경계**)으로 하고, D019 가 걷어낸 형태(«유일한 호출자가 웹뷰의 임의 JS»)를 다시 만들지 않는다 |

**4단계-1차 게이트**: `verify` ✅(187파일 · 2,081케이스) · 트랙 A ✅(209+5) · `e2e:a11y` ✅(90) ·
`budget` ✅ · `cargo test --lib` ✅(74) · `cargo clippy -D warnings` ✅.

### 4단계 — 2차(관측·매칭·게이미피케이션 잔여)

| ID   | 상태 | 무엇을 했나                                                                                                                                                                                       | 검증                                                                                                                                                                 | 처방과 달라진 점                                                                                                                                                                                                                                                                                                             |
| ---- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I054 | 닫힘 | 오늘 축의 스트릭 전량 은퇴 — PL-9 마일스톤 축하 · 상단 `연속` 리드아웃 · 영속 마커 `_lastStreakCele` · `lib/persistence.studyStreak` 자체                                                         | `verify` · 시각 베이스라인 1장 재생성(`today · onthisday`) · 모션 1장                                                                                                | I046(통계 탭) 실행 중 **범위 밖으로 남기고 원장에 새로 연** 항목이었다 — 같은 근거(`completions` 0행)라 같은 날 닫았다. ⚠ 빈 자리를 **급히 안 채웠다**: 이 화면은 히어로가 이미 44px 로 «지금 무엇을»을 말한다                                                                                                               |
| I035 | 닫힘 | `matchSubjectKind()` — 붙었다/안 붙었다 **두 칸을 셋으로**(정확 · **추정** · 없음). `JoinReport.partial` 이 추정 목록을 나르고, 연동 화면이 리드아웃(`9/12 · 추정 2`)과 본문에서 그 이름을 말한다 | `test/subjectMatch.test.ts` 16(신규 4 — exact/partial/none + **규칙 불변 대조**)                                                                                     | ⚠ **규칙을 안 바꿨다.** 처방은 «id 바인딩»이었지만 첫 조각은 «매칭기를 제안기로 강등»이고, 그건 규칙이 아니라 **보고**를 바꾸는 일이다. 규칙(길이차 최소 부분문자열)은 배분을 구동하며 검증돼 온 것이라 손대면 그 전체가 흔들린다. ⚠ 이 항목이 I002 의 안전장치이기도 하다(거기서 자동 완료 기록을 안 한 이유가 이 추측이다) |
| I031 | 닫힘 | `lib/observationsQuery.ts` — `observationsReady(question)`. 네 질문(탭 은퇴 · 두 페인 · 하루 국면 · 유휴 트리거)마다 **「지금 답해도 되는가」**를 답하고, 설정의 원장 옆에 그 목록이 선다         | `test/observationsQuery.test.ts` 8 — **못 쟀다 ≠ 부족하다 ≠ 답 나왔다** / 문턱은 `visits.SAMPLE_MIN` 그대로 / **왕복쌍 0은 답이다** / 유휴 판정은 `idleVerdict` 소유 | ⚠ **답을 내지 않는다** — 「답할 수 있는가」만 말한다. 판정은 사람의 일이고, 이 저장소가 물린 것은 «분자만 보고 미만이면 삭제»였다. ⚠ 질문마다 **자기 원장만** 본다: 합친 「전체 표본」을 만들면 한쪽이 충분하다는 이유로 다른 쪽을 판정하게 된다                                                                             |

**4단계-2차 게이트**: `verify` ✅(188파일 · 2,087케이스) · 트랙 A ✅(209+5) · `e2e:a11y` ✅(90) ·
`budget` ✅ · `cargo test --lib` ✅(74).

### 4단계 — 3차(미래를 보는 층 · 캡처 트리아지)

| ID   | 상태 | 무엇을 세웠나                                                                                                                                                                                                                        | 검증                                                                                                                                                                          | 처방과 달라진 점                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I019 | 닫힘 | `lib/loadCurve.ts` — `dueForecast` 의 **사다리를 인자로 빼고**(기본값 그대로 · 소비처 무변경) 현행 대 「한 칸 늘림」 두 곡선을 같은 함수로 뽑는다. 복습 예보 화면이 두 스파크라인 + **대가 문장**을 그린다                           | `test/loadCurve.test.ts` 6 — 같은 눈금(`peak`) / 늘림 규칙이 사다리 자신에서 / **늘리면 앞쪽이 실제로 가벼워진다** / 0으로 안 나눈다                                          | ⚠ **노브를 안 만들었다.** 처방이 «노브는 곡선 뒤에»였고, 곡선만 먼저 세우는 것이 그 순서의 실행이다 — 곡선 없이 노브를 두는 것이 이 항목이 막으려는 형태다. ⚠ 대가 문구(`TRADEOFF`)를 **lib 이 소유**한다: 화면마다 다르게 말하면 그 경고는 곧 장식이 된다                                                                                                                                                                                     |
| I042 | 닫힘 | `Backlog.triaged`·`snoozeUntil` + `untriagedBacklog`/`triageBacklog`/`snoozeBacklog`. 캡처는 세 입구 공통 함수(`fileCapture`)에서 **판정 전**으로 들어오고, 보충 카드에 「미분류」와 나가는 길 셋(지금 볼 것 · 7일 뒤 · 버림)이 선다 | `test/backlogTriage.test.ts` 8 — 미분류는 **별도 목록이 아니다** / 옛 항목 무마이그레이션 / 손 보충은 처음부터 판정됨 / 스누즈가 스스로 돌아온다 / **미루기가 판정도 끝낸다** | ⚠ **별도 목록을 안 만들었다.** 밖의 표본이 함께 적는 대가가 «판정 노동이 의무가 된다»이고, 플래그로 두면 판정을 건너뛰어도 그 항목은 여전히 열린 보충이다 — 트리아지가 새 백로그가 되지 않는 유일한 방법. ⚠ 미루기가 판정을 함께 끝내는 것이 핵심이다: 안 그러면 돌아올 때마다 다시 미분류라 판정 노동이 영구화된다. ⚠ 날짜 없는 `openBacklog()` 호출부(스무 곳)는 종전 그대로다 — 그쪽이 세는 것은 «열린 것의 총량»이지 «오늘 볼 것»이 아니다 |

**4단계-3차 게이트**: `verify` ✅(190파일 · 2,101케이스) · 트랙 A ✅(209+5) · `e2e:a11y` ✅(90) · `budget` ✅.

---

## 2026-08-22 · 발상 축 **실행 2회차** (남은 21건을 이어서)

1회차가 29건을 종결하고 21건을 남겼다. 이 회차는 그 21건을 **최싼검증부터** 다시 집었다 —
발상 축의 닫기는 「고쳤다」가 아니라 **「그 전제가 참인지 실제로 쟀다」**이므로, 재는 것이
착수보다 먼저다. 그 결과 **여섯 건이 코드를 한 줄도 안 쓰고 끝났다**(전제가 거짓이거나 분모가 0).

⚠ 실측의 출처는 두 곳이다: **실 DB**(`%APPDATA%/dev.jin.learninghub/learning-hub.db`)와
**WebView2 로컬스토리지**(`lh_ui_v1`). 앱 데이터와 UI 취향이 다른 저장소에 사는 것이
이 저장소의 계약이라(CLAUDE.md), 둘 중 하나만 보면 절반만 재게 된다.

### 1단계 — 측정으로 끝난 것 (코드 0 · 전제가 답을 냈다)

| ID   | 상태              | 무엇을 쟀나                                                                                                                                       | 그래서                                                                                                                                                                                                                                                                                                        |
| ---- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I011 | 기각              | 전제 «사진 한 장이 텍스트 4칸보다 자주 남는다» → **사용자 확인: 안 찍는다**(2026-08-22)                                                           | 앱 전체 이미지 필드 0 은 사실이지만 **채울 것이 없다.** D1 계약(전량 불투명 JSON)을 바이너리로 처음 깨는 비용을 수요 0 에 치르지 않는다. ⚠ 되살릴 조건: 사진 습관이 생기는 것                                                                                                                                 |
| I014 | 기각              | 전제 «점수와 **틀린 문항 번호**를 돌려받는다» → **사용자 확인: 총점만 안다**                                                                      | 이 항목의 값은 «챕터별 실점 ↔ `chapterStrength` 순위 상관»이었고 그건 **문항 단위 정답지가 있어야** 성립한다. 총점만이면 그 검증이 원리적으로 불가능하고, 총점의 집은 **이미 있다**(`Course.grade` — 실 DB 에 44과목 전량 채워져 있다). 즉 새 필드는 두 번째 집이 된다                                        |
| I016 | 기각              | 전제 «재료가 있다(I015·I011 선행)» → 재료 둘 중 **하나가 죽었다**(I011)                                                                           | 「모의시험에 내용과 결과를 준다」의 결과 쪽 그릇은 I014 였고 그것도 기각됐다. 남은 것은 I015(문제풀이 세션) 하나뿐인데, 그것만으로는 «모의시험»이 아니라 그냥 세션이다. ⚠ **I015 가 서면 그때 다시 연다** — 그때의 형태는 이것과 다르다(결과 축이 없다)                                                       |
| I012 | 보류(전제 미성립) | 전제 «시험 종료 시각이 앱에 있다» → 실 DB 5과목 중 `exams` 를 가진 것 **0개**, 그리고 `ExamSchema` 는 `date` 만 있고 **시각 필드가 아예 없다**    | 전제가 **두 겹으로** 거짓이다. 폰에 회수 시트를 놓아도 열릴 조건이 영원히 안 온다. ⚠ 선행은 I033(닫힘)이 아니라 **시험이 실제로 입력되는 것**이고, 그 길은 이번 회차의 I008·I010 이 열었다 — 그 뒤에 다시 재라                                                                                                |
| I017 | 보류(분모 0)      | 최싼검증 «최근 backlog 중 과목 태그를 가진 비율» → 실 DB `records`·`completions` **0행**, backlog 슬라이스도 비어 있다                            | 비율의 **분모가 0**이라 판정 자체가 불가능하다. ⚠ 이건 «안 쓴다»가 아니라 «아직 못 잰다»다(I031 이 세운 구분). ✅ 다만 **선행 하나가 이번에 풀렸다** — 항목이 「I008 사실상 선행」이라 적은 시간표 인입이 섰다                                                                                                |
| I028 | 기각              | 최싼검증 «다섯 스키마 필드를 겹쳐 차집합 ≤3이면 확정» → **차집합 6**(Task 전용 `sid`·`deadline`·`done`·`doneDs`·`repeat` · PlanEvent 전용 `note`) | 항목이 적은 «`done?` 유무가 유일 차이»가 **틀렸다.** 항목 자신의 게이트(≤3)에 걸린다. ⭐ 대신 **낡은 주석 하나를 찾았다**: `PlanEventSchema` 가 «가용시간을 깎는 것은 일정만»이라 말하는데 2026-08-07 의 N-1 이 **시각 박힌 과제도 같은 축**으로 넣었다 — I028 의 처방이 그 낡은 문장 위에서 나왔다. 정정했다 |

### 2단계 — 실행한 것

| ID   | 상태            | 무엇을 세웠나                                                                                                                                                                                                                                                                                                                       | 검증                                                                                                                                                   | 처방과 달라진 점                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I008 | 닫힘            | **`.ics` 를 거꾸로 읽는다** — `lib/icsParse.ts`(순수) + `features/degree/CalendarIntake.tsx`. 주 반복(`RRULE:FREQ=WEEKLY`)은 **일과 블록**으로, 단발은 **학기 눈금**으로. 판정 어휘는 새로 안 짓고 `syllabusIntake` 에서 `classifyLine()` 을 꺼내 **두 경로가 같은 표를 읽게** 했다                                                 | `test/icsParse.test.ts` 16케이스 — 접힌 줄 · 격주/서수 배제 · `Z` 환산 · BYDAY 없는 주 반복 폴백 · 분모 있는 `unparsed`                                | ⚠ 표면이 처방과 다르다: 원장은 «`integrations` 한 줄»이라 적었지만 **`/degree` 인입 뷰**에 놓았다. 그 탭이 지는 것은 *바깥 도구 연결*이고 `.ics` 는 *학사 입력*이라 강의계획서 붙여넣기와 **같은 질문**에 답한다 — 갈라 두면 그 질문의 답이 두 탭에 나뉜다. ⚠ 착수 전 전제 확인함(«학교가 `.ics` 를 준다» → **준다**) — 안 줬으면 이 파일은 안 지어졌다                                                                                                                         |
| I010 | 닫힘            | **인입구가 상시가 됐다** — ① `SyllabusIntake.apply` 의 `if (!item) return` 제거(눈금은 학기의 것인데 **과목 게이트가 그 위에 얹혀 있었다**) ② 과목 0 에서도 화면이 안 닫힌다 ③ 뷰 이름 «계획서 인입» → **«학기 인입»** · 문구를 *공지 한 줄*로 ④ **오늘 배지** — `buildBeyondStrip` 에 학사일정 그룹(7일 창)                        | `test/syllabusIntake.test.ts` +2케이스(공지 한 줄 → 눈금 / 시험 연기 → 시험 우선) · 트랙 A                                                             | ⚠⚠ **처방이 놓친 것을 실행이 찾았다**: 「상시 인입구」의 진짜 장벽은 화면 문구가 아니라 **`if (!item) return` 한 줄**이었다 — «4/6 휴강»을 붙여도 과목을 고르지 않으면 아무 일이 안 일어났다. ⚠ 배지 창이 7일인 이유: 졸업탭 국면판은 21일을 보지만 그건 «학기가 어디쯤인가»의 축이고 오늘 줄은 «오늘 무엇이 다른가»다. ⭐ 배지를 세우자 **낡은 라벨이 드러났다** — 눈금 라벨이 짧은 날짜를 안 걷어 «4/6 휴강 D-3」으로 두 번 읽혔다(과제 제목은 이미 걷고 있었다). 함께 고쳤다 |
| I027 | 닫힘            | **레일 조립 노브 은퇴** — `features/settings/RailAssembly.tsx`(114) · `shell/railLayout.ts`(102) · `test/railLayout.test.ts` · `ui.railHidden`/`railOrder` · `setRailLayout` · 설정 한 절. 레일은 이제 `navGroups()` 그 자체다. **꼬리로 모션 어휘 둘도 함께 갔다**(`shift` A-17 · `deny` A-18 — 소비처가 이 화면 **하나**뿐이었다) | 실측 2회: 실 DB `settings` 에 레일 키 **없음** + `lh_ui_v1` 에 `railHidden:[]`·`railOrder:[]`. 출하 2026-08-07 → 15일 · 그사이 설정 방문은 원장에 있다 | ⚠⚠ **처방의 절반만 했고, 나머지 절반은 이 측정이 막았다.** 항목은 «레일을 줄인다»였고 접기 노브는 그 근거가 되어 주기로 한 것이었다(«무엇을 접었는지가 곧 답»). 그 답이 **빈 배열**이라 무엇을 줄일지는 여전히 모른다 — 그래서 노브만 걷고 레일은 15줄 그대로 뒀다. 줄이려면 오염 없는 방문 원장(I030 뒤)이 먼저다. ⚠ 어휘 축소는 집행자(불변식 ⑥ `VOCAB`)까지 좁혔다 — 안 좁히면 **없는 마디로 키프레임을 지을 수 있게 된다**                                                  |
| I022 | 보류(측정 대기) | 최싼검증을 **세웠다** — `undoStack.preImageTally()`(세션 누계) + 설정의 「되돌릴 수 있는 지점」 밑 한 줄. ⚠ **축출된 것까지 더해서** 센다(저널은 버리지 않으므로 스택 크기를 읽으면 비용을 과소평가한다)                                                                                                                            | `test/undoStack.test.ts` +3케이스 — 빈 pre-image 는 누계에도 안 듦 / **예산 축출과 무관** / 다시실행은 안 셈                                           | ⚠ **표를 안 팠다.** 이 수를 영속하는 순간 그것이 곧 저널의 첫 조각이 되어, 재려던 것을 재기 전에 지은 셈이 된다(I023 이 같은 자리에서 내린 판단과 동형). 그래서 세션 누계이고 화면이 «껐다 켜면 0부터»라고 말한다. **하루 실사용 뒤에 이 항목을 다시 연다**                                                                                                                                                                                                                     |

### 3단계 — 인입이 열리자 성립한 것

| ID   | 상태 | 무엇을 세웠나                                                                                                                                                                                                                                                                                                                                     | 검증                                                                                                                                                                        | 처방과 달라진 점                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I013 | 닫힘 | **수업이 어느 과목인지 앱이 안다** — `RoutineBlockSchema.sid?`(옵셔널 · `settings.routine` 불투명 JSON 안이라 **D1 계약 변경 0**) · `.ics` 인입이 `matchLectureSid` 로 자동 연결(규칙은 `lib/subjectMatch` 재사용) · 화면이 「수업 N · 과목 연결 M」을 **반영 전에** 말한다 · 읽는 쪽은 `weeklyLectureMin` → 과목 화면 리드아웃 「주간 강의 N h」 | `test/icsParse.test.ts` +4(길이차 최소 매칭 · 못 맞추면 `sid` 키 자체를 안 만듦) · `test/scheduler.test.ts` +3(요일 오버라이드 존중 · **`sid` 를 붙여도 배치가 안 바뀐다**) | ⚠⚠ **최싼검증이 처방보다 먼저 답을 냈다.** 항목의 검증은 «실 DB `routine` 값에 과목명이 이미 들어 있나»였고 답은 **아니오**였다 — 블록 넷이 전부 이름이 그냥 「수업」이라 **자동으로 맞출 재료 자체가 없었다.** 그래서 이 항목은 I008(`.ics` 인입)이 이름을 넣어 준 **뒤에야** 성립했다. 순서가 뒤집혔으면 «이름이 없으니 못 한다»로 닫혔을 것이다. ⚠ 표면도 좁혔다: 항목은 «새 표면(과목 안 수업 축) + 수업 직후 폰 한 탭»이었지만 **새 표면 0**으로 끝냈다 — 리드아웃 한 칸이면 «주 10h 가 안 보인다»가 해소되고, 폰 탭은 그 뒤에 수요를 보고 판단할 일이다 |

### 4단계 — 정본 원장이 계획에 닿는다 + 남은 최싼검증

| ID   | 상태              | 무엇을 세웠나 / 무엇을 쟀나                                                                                                                                                                                                               | 검증                                                                                                                                                           | 처방과 달라진 점 · 그래서                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---- | ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I001 | 닫힘              | **「계획에 반영」 줄**(`/ledger` 우측 최상단) — `ledgerPending`/`pendingCount`/`pendingPrompt` 신설. 원장이 `carded` 라 말하는데 앱에 안 찍힌 챕터를 세어 **한 번 묻고** `applyCardedDone` 으로 찍는다                                    | `test/ledgerSeed.test.ts` +5(이미 찍힌 것 제외 · 0건 과목은 목록에 없음 · 원장에만 있는 과목을 만들지 않음) · **트랙 A 신규 2장**(`ledger-apply-{dark,light}`) | ⚠⚠ **다리는 이미 있었다 — 놓인 자리가 틀렸을 뿐이다.** `lib/ledgerSeed.ts` 는 2026-07-31 부터 있었고 규율(«자동으로 안 찍는다»)도 옳았는데, 소비처가 **`importVaultSubject` 하나**였다. 임포트는 1회이고 원장은 계속 자라므로, 손으로 만든 과목이나 임포트 때 「아니요」를 고른 사람에겐 그 다리가 영원히 안 놓인다 — **I010 과 글자 그대로 같은 형태**(«개강 1회»)가 다른 축에서 반복된 것이다. 그래서 새로 지은 것은 판정이 아니라 **묻는 시점**뿐이다. ⚠ 커버리지 0으로 태어나지 않게 e2e 를 **먼저** 팠다(기본 픽스처는 과목 이름이 안 맞아 이 줄이 정의상 안 그려진다 · §15-4) |
| I003 | 보류(전제 미성립) | `진행.py list` **1회 실행**: 진행 중 챕터 **1개**(선형대수/06 · **마지막 20일 전**, 그 자신이 «비용 재검토 중»이라 적어 뒀다). 최근 2주 `knowledge/` 커밋 27건 중 **챕터 파이프라인 회차는 0건**(전부 파생물 갱신·학습원장 append·교양축) | 부모 저장소 읽기 전용(도구 1회 + `git log`)                                                                                                                    | 항목의 전제는 «`진행.py list` 출력이 안정적 · **주당 유의미한 시간을 먹는다**» 였다. 앞은 참(출력은 구조적이고 안정적)이지만 **뒤가 지금 거짓**이다 — 주당 0챕터다. ⚠ 이건 «못 만든다»가 아니라 **«지금 계상할 노동이 없다»** 이고, 없는 것을 계상하는 화면은 0을 그린다. 다시 열 조건: `진행.py list` 가 2주 연속 회차를 내는 것                                                                                                                                                                                                                                                   |
| I007 | 보류(분모 0)      | 최싼검증 «교재 챕터별 문자 수 vs **실측 소요**의 상관». 교재 쪽은 잴 수 있다(선형대수 교재 156만자 · h1/h2 369절 · `1.1 …` 형태의 절 구조). **실측 소요 쪽이 0이다** — `completions` 0행                                                  | 실 DB + `sources/` 읽기                                                                                                                                        | 상관의 **한쪽 축이 통째로 비어 있어** 항목 자신의 기각 조건(«상관 없으면 안 전체가 무너진다»)을 시험할 수 없다. ⚠ 그 분모를 채우는 것이 **I005**(실제 소요 되돌려넣기 · 1회차에 닫힘)이므로 이 항목은 그 데이터가 쌓인 뒤가 자리다 — 지금 지으면 «상관 없음»을 표본 0으로 판정하게 된다                                                                                                                                                                                                                                                                                             |
| I037 | 보류(분모 0)      | 최싼검증 «같은 `(table,key)` 를 두 기기가 같은 날 쓴 건수». 실측: `completions` **0행** · `tombstones` **0행** · `devices` **0행**(클라우드 연결 자체는 살아 있다 — `sync_state` 에 baseUrl·deviceId·워터마크가 있다)                     | 실 DB 읽기                                                                                                                                                     | 항목은 «0이거나 전부 `completions` 면 참»이라 적었는데, 실측 0의 정체가 **«충돌이 드물다»가 아니라 «그 표에 쓰기가 아직 0»** 이다. 즉 I017 과 같은 부류(분모 0)다. ⚠⚠ 사용자 승인이 이미 있지만 **승인은 착수 조건이 아니라 승인일 뿐**이다 — 이 저장소의 `/실행` 규율은 «재현 안 되면 고치지 마라» 이고, 되돌리기 비용이 큰 동기화 코어에서 그 규율을 먼저 접으면 안 된다. 다시 열 조건: `completions` 에 두 기기 쓰기가 실제로 생기는 것                                                                                                                                          |

### 5단계 — 보류 하나를 되살렸다 (전제가 실측으로 확인됐다)

| ID   | 상태 | 무엇을 세웠나                                                                                                                                                    | 검증                                                                                                                                                                              | 처방과 달라진 점                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---- | ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I053 | 닫힘 | 배분판의 **「전공 밖」 레인** — `state.outsideAlloc`(weekMon → 7요일[분]) + `outsideVec`/`setOutsideCell` + 보드 한 행. 푸터의 «배분 / 가용» 분모에 **더해진다** | `test/weekAlloc.test.ts` +4(**승격 안 됨** · 전부 0이면 키 삭제 · 읽기는 사본 · **스케줄 산출 불변**) · `test/allocBoard.test.tsx` 구조 4건 갱신 · 트랙 A 4장 재생성(실렌더 확인) | ⚠⚠ **보류 사유였던 결합을 저장 자리로 풀었다.** 항목은 「`alloc` 보드 레인 하나」였는데, 그것을 `weekAlloc` 의 합성 sid 로 두면 칸에 값을 넣는 순간 `setAllocCell → ensureWeekAlloc` 이 **그 주를 managed 로 승격**시킨다(«교양 2시간을 적었다»가 그 주 배치를 자동→수동으로 뒤집는다). 처방을 그대로 하면 그 사고가 난다 — 그래서 **별도 최상위 키**로 갔다. `ROW_SLICES` 밖이라 `settings` 한 행으로 떨어지고 **새 표 0 · D1 DDL 0**이다. ⚠ 전제도 실측했다: 최근 2주 `survey/` 커밋 **131건** — 항목의 기각선(«주 2h 미만이면 한 줄 표시»)을 크게 넘는다. ⚠ 스케줄러는 이 값을 안 읽는다(계약 0 유지) |

### 6단계 — IA 축 넷을 실측으로 갈랐다 (지은 것 하나 · 기각 둘 · 보류 셋)

⚠ 이 단계의 요지: **네 항목이 전부 「표면이 너무 많다」를 말했는데, 실측하면 셋은 세는 법이
틀렸고 하나만 참이었다.** 세는 법을 먼저 고치지 않으면 처방은 옳은 것을 지운다.

| ID   | 상태                 | 무엇을 쟀나 / 세웠나                                                                                                                                                                                                                                                                                                                                             | 검증                                                                                                                               | 그래서                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---- | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I025 | 닫힘                 | 최싼검증(«경로+쿼리 전 조합에 이름·⌘K·링 도달을 표로 찍는다»)을 실제로 찍었다 → **문법은 이미 옳고 12개 중 2개에만 적용돼 있었다.** 로스터에 `role:'view'` **다섯**을 등재(`intake`·`close`·`req`·`path`·`guide`) + `viewsOfHost()`/`viewValueOf()` 신설 + **`Degree.tsx` 의 `const VIEWS` 5칸을 로스터에서 파생** + **불변식 ⑱**(`?view=` 화면이 로스터에 있다) | `test/invariants.test.ts` 불변식 ⑱ 3케이스(소스가 보내는 `?view=` 전부 대조 · 면제 표 사문화 검사 · 자기 무효화 방지) · 트랙 A 209 | ⚠⚠ **처방(«라우트 하나로 재설계»)을 안 했다 — 실측이 다른 답을 냈다.** ✗ 인 다섯은 셋을 한꺼번에 잃고 있었다: 아나운서가 **호스트 이름**을 읽고(H-12 가 이름 축에서 고친 결함의 재발 경로) · ⌘K 에 문이 없고 · **방문 원장이 호스트로 접어 집계한다**(I034 가 «그 화면은 자기 몫의 수를 다시는 갖지 못한다»고 적은 그 상태). 즉 살릴 근거도 지울 근거도 못 얻는 고정 상태였다. ⭐ **덤: `segLabel` 이 N-14 이후 소비처 0이었는데 여기서 부활**했다(바에서만 짧게 · ⌘K 는 긴 이름). ⚠ `/subject/:id?view=sheet` 는 **사유 있는 면제**다(매개변수를 가진 명사 안이라 정적 `to` 가 없다) |
| I024 | 기각                 | 최싼검증(«「오늘 학습」과 `/day/오늘` 을 나란히 놓고 **같은 질문에 다른 답을 주는 칸**을 센다») → **0칸**. 두 화면의 리드아웃 교집합이 공집합이다(`/day` = 오답·열린 보충 · `/today` = 용량(조건부)뿐)                                                                                                                                                           | 소스 대조                                                                                                                          | 항목이 든 「일곱~아홉 표면」은 **components 를 screens 로 셌다.** 근거로 인용된 `DayBufferOverlay` 머리주석은 _"하루를 **여섯 표면**으로 말한다(히어로·흐름·막대·스트립·레일·리드아웃)"_ 인데 그 여섯은 전부 **`TodaySignature` 한 화면의 부품**이다. 실제로 하루를 말하는 *화면*은 넷이고(`/today` 지금 무엇을 · `/day` 무엇을 남겼나 · `/schedule` 일 뷰 시간표 · 버퍼 오버레이 훑기) **시제와 질문이 다르다.** 수렴하면 네 질문이 한 화면에서 섞인다                                                                                                                               |
| I026 | 기각                 | 실측: 크롬을 세우는 화면 **19** 중 `primary: null` **9**(47%). ⭐ 재는 중에 **결함**이 나왔다 — `GlanceMode` 가 `readouts` 를 `primary` 분기 **안에** 중첩해, 대표 수가 없는 아홉은 보조 수가 있어도 **사과문만** 봤다. 셋을 갈랐다(대표 수 · 보조 수 · 둘 다 없을 때만 안내)                                                                                    | `verify` · 트랙 A(오버레이 로스터에 이미 있다)                                                                                     | 처방(«계약 축소 + `⇧G` 제거»)의 전제는 «절반이 `null` 이므로 계약이 안 지켜진다» 인데, 이 저장소에서 `primary: null` 은 **위반이 아니라 정직한 선언**이다(`Find.tsx` 가 그 자리에 _"잊은 것이 아니라 없다고 정한 것"_ 이라 적어 뒀다). 그 선언을 결함으로 읽어야만 축소가 성립하고, 그건 `null` 에 부여한 뜻과 반대다. ⚠ 그리고 «⇧G 가 값을 내는가»는 **이 결함 때문에 한 번도 제대로 측정된 적이 없다** — 고친 뒤에 다시 물어야 한다                                                                                                                                                 |
| I021 | 보류(분모 0)         | 전제 «절반 분할이 학습을 망치지 않는다» → 분할할 과목이 **5**(하나는 자리표시자)이고, 4주 뒤 **비교할 결과**(`completions`)가 **0행**                                                                                                                                                                                                                            | 실 DB                                                                                                                              | 카나리는 «두 갈래를 나중에 비교한다»가 본체인데 비교의 재료가 0이다. 항목 자신이 «표본이 작으면 비율이 안 맞는다»를 이미 자인했고, 실측은 그보다 나쁘다(표본이 작은 게 아니라 **없다**). 다시 열 조건: `completions` 가 4주치 쌓이는 것                                                                                                                                                                                                                                                                                                                                               |
| I015 | 보류(전제 확인 불가) | 최싼검증(«한 세트를 그 형식으로 옮겨 적어 본다 · 30초 넘으면 형식이 틀렸다»)은 **사용자의 실제 문제 세트**가 있어야 돈다. 인접 실측: 이미 있는 「주당 과제 시간」(`choreWeeklyH`)을 채운 과목 **0/5**                                                                                                                                                            | 실 DB + 스키마 주석                                                                                                                | ⚠ 두 가지를 갈라 적는다: 이 항목이 원하는 것은 *시간 예산*이 아니라 **무엇을 풀었나의 원장**이라 `choreWeeklyH` 와 다른 것이 맞다. 다만 `SessionType` 을 6번째로 늘리는 것은 스케줄러·`.ics`·통계·`completions` 키를 **전부** 건드리는 변경이고, 형식이 옳은지는 실물 한 세트로만 판정된다. 사용자 확인이 선행                                                                                                                                                                                                                                                                        |
| I009 | 보류(실행 불가)      | 최싼검증(«테스트 덱에 `addNote` 1회») → **AnkiConnect 무응답**(`127.0.0.1:8765` HTTP 000). 그리고 항목이 적은 후보 공급 둘 중 **I011 이 이번 회차에 기각**됐다(사진 인입)                                                                                                                                                                                        | 실측 프로브                                                                                                                        | 앱→Anki **쓰기**는 되돌리기가 사용자 컬렉션에 남는 경로라, 「돌려 보지 않고 짓는다」를 이 저장소가 가장 경계하는 부류다(`/실행` 도구 도입 경로 ②: _"실물로 한 번 돌려 보기 전엔 닫지 마라"_). 다시 열 조건: Anki 기동 + 후보 공급(I004 는 이미 섰다)                                                                                                                                                                                                                                                                                                                                  |

### 7단계 — 사용자 판단이 필요했던 둘 (2026-08-22)

| ID   | 상태              | 실측과 판단                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---- | ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I015 | 기각              | 최싼검증의 전제 «번호만으로 나중에 그 문제를 찾을 수 있다» → **사용자 확인: 없다 / 문제풀이를 거의 안 한다**(2026-08-22). `SessionType` 을 여섯째로 늘리는 것은 스케줄러·`.ics`·통계·`completions` 키를 **전부** 건드리는 변경인데 그 대가를 치를 수요가 없다. ⚠ 되살릴 조건: 문제풀이가 실제 루틴이 되고, 세트를 30초 안에 옮겨 적을 수 있는 형식이 생기는 것. ⚠ 인접 사실을 함께 남긴다 — 이미 있는 「주당 과제 시간」(`choreWeeklyH`)도 채운 과목이 **0/5** 다                                                                                                                                                                                                                                                                                           |
| I029 | 보류(재측정 대기) | ⚠⚠ **승인은 있었지만 실측이 그 *방향*을 반증했고, 사용자가 재측정을 골랐다**(2026-08-22). 실측 셋: ① `courses[]` 중 `itemId` **0/44** ② 그런데 **44코스 전량에 학점·성적이 기입돼 있다**(38 완료 + 6 예정) — 회계 축은 **사용자가 유지하는 살아 있는 데이터**다 ③ 정작 죽은 것은 **학기 국면 축**이다(학기 3개 중 `startDs` **0개** → `activeSemester` 가 늘 null → 국면판·학기 목표·결산·학사 눈금·`phaseRamp` 전부 무동작). 처방대로 `Course` 를 `Item` 에 얹으면 **과목 목록이 5행 → 44행**이 되어 제품 정의(「한눈에」)의 반례가 되고, 손으로 채운 성적이 이관 대상이 된다. **판단: 학기 하나에 개강일을 넣어 국면 축을 실제로 굴린 뒤 다시 잰다** — 이번 회차의 인입구(`.ics`·공지)가 정확히 그 축을 채우려는 것이라, 지금 걷으면 그 페이오프를 버린다 |

---

# 코드 축 1회차 — 실행 (2026-08-22) · `C033`~`C066`

> 리포트: [`리뷰/2026-08-22-코드/리포트.md`](리뷰/2026-08-22-코드/리포트.md) · 원장에서 이관.
>
> **결함 18 · 진보 8 · 잔재 8 중 33건 종결 · 1건 유지 판정 · 2건은 새 항목으로 쪼갬.**
> 재현 불가 **0건**(리포트의 `[측정]` 비율이 높았다는 신호다) · 처방과 다르게 고친 것 **7건**.
>
> ## ⭐⭐ 이 회차가 남기는 것 — 근본 원인 셋이 전부 집행자를 얻었다
>
> · **R1**(집행자가 자기 문법만 막는다) → 불변식 ⑰ 이 **둘째 문법**(`<Tab>`)을 안다. 그리고
> 그 처방의 나머지 절반은 «문자열 대신 키»였다 — 문법을 늘리는 것은 임시방편이고 **표기를
> 하나로 만드는 것**이 근본이다.
> · **R2**(비동기 실패가 어느 층에도 안 걸린다) → **타입 인지 린트 두 룰**이 켜졌다(위반 114 → 0).
> ⚠ **절반만 닫혔다** — `onClick={async …}` **69곳**은 여전히 아무 데도 안 걸린다(새 항목 `C067`).
> · **R3**(새 표면이 규칙을 `.tsx` 콜백에 두고 테스트를 안 붙인다) → 불변식 ㉓ 이 그 크기를
> 래칫한다. 실측 **23 → 20**(C036 이 11줄을 `lib` 으로 내렸다).
>
> ⚠⚠ **가장 값비싼 발견은 고치는 중에 나왔다**: `C051` 의 자동 `void` 삽입이 `run: void A.resetAll`
> 셋을 만들었고, 그건 **함수를 버리고 `undefined` 를 넘긴다**(`run?` 이 옵셔널이라 타입도 안 잡는다).
> ⌘K 액션 셋이 조용히 무동작이 될 뻔했다 — 자동 수정은 **위치가 아니라 표현식**을 봐야 한다.

## 결함 (18건 전량 닫힘)

| ID   | 상태 | 무엇을 고쳤나                                                                                                                      | 검증                                                                                                                                                           | 처방과 달라진 점                                                                                                                                                                                                                                                                                                                                                       |
| ---- | ---- | ---------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C033 | 닫힘 | `find/GuideView.tsx` 의 `Tab` 이 **로스터 키**를 받는다(`tabByKey` → `label`) · 불변식 ⑰ 에 둘째 문법 + 키 대조 케이스 셋          | 죽은 이름 `발견` 을 되넣어 ⑰이 **실제로 빨간불**이 되는 것을 확인 → 되돌림                                                                                     | ⚠ **처방의 `k: TabKey` 유니온을 못 썼다 — 그런 타입이 없다**(`TabMeta.key` 가 `string` · `TABS` 가 `as const` 아님). 바꾸면 로스터를 읽는 곳 전부가 딸려 온다(범위 초과). 같은 보장을 **게이트 층**에 뒀다: ⑰이 `<Tab k="…" />` 를 긁어 로스터와 대조 + 컴포넌트의 `throw` 가 마지막 그물. 표시는 `segLabel` 이 아니라 **`label`** 이다(팔레트가 그 이름으로 검색된다) |
| C034 | 닫힘 | `test/shellContract.test.ts` 에 **양방향** 로스터 대조(TS 호출 ↔ `generate_handler![…]`) · 파싱 실패는 `throw`                     | `db::db_version_guard` 를 일부러 개명 → **양쪽 케이스가 동시에** 빨간불 → 되돌림                                                                               | ⚠ 추출 정규식이 **두 관용구**를 봐야 한다: 처음 짠 것이 `call<T>('…')` 의 제네릭을 못 받아 «Rust 에만 9건» 이라는 **거짓 드리프트**를 냈다. 실측 결과 실제 드리프트는 **0**(양쪽 24개 일치)                                                                                                                                                                            |
| C035 | 닫힘 | `icsParse.weeklyDays` 가 `COUNT=n`(유한 반복)을 거부 — 두 줄 위 `INTERVAL !== 1` 과 같은 논거                                      | `test/icsParse.test.ts` +2(`COUNT=3` → `lectures` 0 · `unparsed` 1 / **`UNTIL` 은 통과**)                                                                      | ⚠ 픽스처 제목에 **눈금 어휘를 쓰지 마라** — 「3주 보강」으로 짰더니 단발 눈금으로 착지해 `unparsed` 가 0이 됐고, 그게 «고쳐졌다»처럼 보였다                                                                                                                                                                                                                            |
| C036 | 닫힘 | `lib/semester.applyMarks` 신설 — 두 인입구의 11줄 사본 둘을 한 줄 호출로                                                           | **`test/applyMarks.test.ts` 신설 7건**(id 부여 · 재인입 0 · 같은 날 같은 종류 1건 · 다른 종류 2건 · 기존 보존 · 빈 입력이 `marks` 를 안 만듦)                  | 처방대로. ⚠ `row` 헬퍼(13줄 사본)는 **안 옮겼다** — 그건 표현이고 이 항목은 «규칙»이다(옮기면 한 커밋에 두 종류가 섞인다). ⚠ 남은 20줄(강의 블록 중복 판정)은 **같은 형태**라 새 항목 `C068` 로 쪼갰다                                                                                                                                                                 |
| C037 | 닫힘 | `shell/actions.importAnkiDeck` 신설 — `AnkiPanel`·`VaultImport` 의 지역 `addAnki` 삭제(H22 가 볼트 쪽에 만든 자리 옆)              | **`test/importAnkiDeck.test.ts` 신설 4건** — 특히 «두 입구를 오가면 `dailyMin` 이 두 번 예산에 든다»                                                           | 처방대로. `boolean` 반환을 더했다(지금 둘 다 안 쓰지만 호출부가 후속 UI 를 정할 수 있게)                                                                                                                                                                                                                                                                               |
| C038 | 닫힘 | `lib/utils.minuteSegments` 한 벌 — 자정 걸침 분할 사본 **셋** 제거(`scheduler/windows.ts` 둘 · `layout.ts` 하나)                   | `test/utils.test.ts` +4(두 구간 · **`s===e` 는 폭 0** · 총 길이 보존)                                                                                          | ⚠ **처방의 위치가 틀렸다**: _"`toMin` 이 사는 곳 = `scheduler/windows.ts`"_ 라 적혀 있었지만 `toMin` 은 **`lib/utils.ts`** 에 산다. 거기 두면 `layout.ts` 가 이미 `../utils` 를 import 하고 있어 새 결합이 0이다                                                                                                                                                       |
| C039 | 닫힘 | `seenAt`→**`seenDs`** · `doneAt`→**`doneHm`** · **불변식 ⑳**(zod `*At` 은 epoch ms · 면제 사유 필수)                               | `seenAt` 을 되넣어 ⑳이 빨간불 → 되돌림 · `uiState`·`persistence`·`since` 63건                                                                                  | ⚠ **`doneAt` 은 동기화되는 앱 데이터라 개명 전에 실 DB 를 쟀다** — `completions` **0행**(읽기 전용 사본 · 2026-08-22)이라 이관이 0이다. 값이 쌓인 뒤였다면 이 개명은 마이그레이션을 요구했을 것이다                                                                                                                                                                    |
| C040 | 닫힘 | `PACE_WEEKS_MIN=8`·`PACE_WEEKS_MAX=26` 에 이름을 주고 `PACE_HORIZON_MAX` 주석을 **「마감 경로 전용」**으로 정정                    | **`test/scheduler.test.ts` +2** — 마감 없는 3000시간 계획이 ≤189일 · 먼 미래 시험이 있으면 ≤547일(>200)                                                        | 처방대로(값은 안 바꿨다 — «26이 옳은가»는 별개 판단이다). ⚠ 주석 정정만으로 끝내지 않고 **산수를 실행 가능하게** 못박았다: 근거가 안 돌면 다음 회차에 또 표류한다                                                                                                                                                                                                      |
| C041 | 닫힘 | `applyPull({readback:false})` + `refreshBaseline()` — 되읽기가 드레인 **밖으로 한 번**                                             | `test/cloudDrain.test.ts` 재작성(회차 5여도 `refreshBaseline` **1회** · 회차마다 `readback:false` · 받은 것 0이면 0회) + **`server test:roundtrip` 22건 녹색** | ⚠ **사용자 승인**(2026-08-22). ⚠ 되읽기를 뺀 회차도 **기준선 무효화 + 병합 창**은 그대로 연다 — 안 그러면 DB 는 병합-후·기준선은 병합-전이라 flush 가 받아온 행을 되돌린다(C-1 이 고친 형태). ⭐ **부수 이득이 실현됐다**: C-1 의 실패 형태 ⓐ·ⓑ 가 한 갈래로 합쳐져 루프 안 `else setDiffBaseline(null)` 이 사라졌다                                                   |
| C042 | 닫힘 | `DOC_KEYS` 를 `[]` 로 되돌리고 ✅「세입자가 돌아왔다」 문단 삭제(`lib/icsFeed.ts` 는 I050 이 걷어 **파일이 없다**)                 | `test/docs.test.ts` 10건(레지스트리 0 · 복원이 0 · 미지 키 보고는 유지) · `backup.ts` 주석 정정                                                                | 처방대로. ⚠ 테스트 둘의 **의미**를 함께 고쳤다 — 없는 세입자를 흉내 내면 그건 테스트가 아니라 무대장치다(그 파일 머리주석의 규율)                                                                                                                                                                                                                                      |
| C043 | 닫힘 | `since.ts` 저널 카운터가 `utils.iso()`(로컬) 사용 · **불변식 ⑲**(UTC 날짜 자르기 금지) · **TZ 매트릭스**(C052)                     | 옛 코드로 되돌려 실측: **`TZ=UTC` 는 8/8 통과**(판별력 0) · 매트릭스는 **양방향 모두 실패**                                                                    | 처방대로 + 짝을 둘 세웠다. ⚠ 이 실측이 이 회차에서 가장 값진 한 줄이다: **CI(UTC)와 개발 머신(KST)이 서로 다른 것을 재면서 둘 다 «전량 녹색»** 을 보고하고 있었다                                                                                                                                                                                                      |
| C044 | 닫힘 | `installSyncTriggers` 에 `_again` 플래그 — 도는 중에 온 트리거를 **한 번으로 접어** `finally` 에서 잇는다                          | **`test/syncTriggerRequeue.test.ts` 신설 3건** · 플래그를 빼면 2건이 빨간불이 되는 것 확인                                                                     | 처방대로. ⚠ **큐가 아니라 부울**인 것을 테스트가 명시적으로 잰다 — 큐면 30초 드레인 뒤에 열 번이 줄 선다                                                                                                                                                                                                                                                               |
| C045 | 닫힘 | `readTouched` 가 **키 열마다** `IN`(곱집합) — `records`·`ds_map` 의 슬라이스 전량 인출 제거                                        | **`test/dbQueryContract.test.ts` 신설 6건**(C055) · 첫-키만으로 되돌리면 **3건이 빨간불**                                                                      | 처방대로. ⚠ 그 자리 주석의 거짓말(_"과다 인출의 상한은 손댄 첫 키 수"_ = 바인딩 상한이지 **반환 행 상한이 아니다**)을 함께 정정했다                                                                                                                                                                                                                                    |
| C046 | 닫힘 | `hash01` → `(hash32(id)%10000)/10000` · `hashStr` ×31 둘 → `utils.rotateSeed` 한 벌 · **불변식 ㉑**(`Math.imul` 은 `lib/utils` 안) | **20만 입력 대조로 비트 동일** 확인(스냅샷 재생성 0) · 해시 관련 32건                                                                                          | 처방대로. ⚠ **두 축을 합치지 않았다** — `hash32`(분포를 산다)와 `rotateSeed`(결정성만 산다)는 다른 성질이라, 합치면 색 분포 튜닝이 「오늘의 창」 회전을 함께 흔든다. ㉑이 그 분리도 함께 잠근다                                                                                                                                                                        |
| C047 | 닫힘 | 루트 `/` → `/phone` **302**(`wrangler.jsonc` 의 `run_worker_first` 에 `"/"` + Hono 라우트)                                         | `server/test/assets.test.ts`(라우팅) + **`roundtrip.test.ts` 신설 3건**(실 Hono 로 302·Location·다른 경로 미삼킴)                                              | ⚠ **사용자가 ②(리다이렉트)를 골랐다.** ⚠ 부수 발견: `/index.html` 은 자산 워커가 `/` 로 **307 정규화**하는데 그 307 을 `dispatchFetch` 가 조용히 따라가고 있었다 — 그래서 「번들 참조」 케이스가 이제 `/today` 로 읽는다. 그 사실은 어디에도 안 적혀 있었다                                                                                                            |
| C048 | 닫힘 | `lib/contentAnchors.ts` 신설(목적지 한 관용구·한 자리) · **불변식 ㉒**(로스터 경로와 글자 그대로 대조)                             | `day: '/journal'` 로 일부러 갈라 ㉒가 빨간불 → 되돌림                                                                                                          | ⚠ **처방의 «로스터에서 읽는다»가 레이어에 막힌다**(`lib → lib` 만 허용). 그 경계는 옳다(순수 규칙이 셸을 끌면 폰 번들·테스트가 무거워진다) → 같은 보장을 **테스트 층**에 뒀다(테스트는 레이어 밖이라 둘 다 본다)                                                                                                                                                       |
| C049 | 닫힘 | `tabs.ts` 삭제 주석에 «지금의 선례는 «지운다»가 아니라 «`role:'view'` 로 내린다»» 문단 추가                                        | 문서 정정(게이트 대상 아님)                                                                                                                                    | 처방대로                                                                                                                                                                                                                                                                                                                                                               |
| C050 | 닫힘 | `eslint.config.js` 의 `atlasData.ts` 면제 블록 삭제 + 근거 문장 정정                                                               | `npm run lint` 녹색(래칫 구멍이 닫혔다)                                                                                                                        | 처방대로                                                                                                                                                                                                                                                                                                                                                               |

## ⭐ 진보 (8건 전량 닫힘)

| ID   | 상태 | 무엇을 세웠나                                                                                                        | 실측                                                                                       | 처방과 달라진 점                                                                                                                                                                                                                                                                             |
| ---- | ---- | -------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C051 | 닫힘 | **타입 인지 비동기 린트 두 룰**(`no-floating-promises` + `no-misused-promises`) · `verify` 안                        | 위반 **114 → 0**. 내역: floating 34 · 속성 69 · 프로퍼티 9 · 인자 2. 린트 전량 **19~22초** | ⚠ **`checksVoidReturn.attributes: false`** 로 69건(60%)을 범위 밖에 뒀다 — 그 축은 «전역 `unhandledrejection` 한 겹»으로 훨씬 싸게 덮이고, 대가를 `C067` 로 원장에 적었다. ⚠⚠ **자동 `void` 삽입이 셋을 무동작으로 만들 뻔했다**(위 머리말 참조)                                             |
| C052 | 닫힘 | **`vitest.tz.config.ts`** — 날짜 파일 9개를 `Asia/Seoul`·`America/Los_Angeles` 두 프로젝트로. `verify` 안(`test:tz`) | 534건 · **0.6초**                                                                          | ⚠ **처방의 «`vitest --project` 둘»을 별도 설정 파일로** 뺐다 — 루트 설정에 `projects` 를 넣으면 커버리지 임계(래칫)의 분모가 흔들린다. ⚠ **`resolve.alias` 는 프로젝트마다** 줘야 한다(루트에 두면 18파일이 통째로 죽는다 · 실측)                                                            |
| C053 | 닫힘 | (C034 와 같은 산출물 — `shellContract.test.ts` 케이스 넷)                                                            | 개명 주입으로 검출력 확인                                                                  | 처방대로(생성기가 아니라 케이스)                                                                                                                                                                                                                                                             |
| C054 | 닫힘 | **`test/mergeConvergence.test.ts`** — 병합 SQL **전수 순열**(6연산 = 720)이 한 상태로 수렴하는가                     | 6케이스 · 0.3초. 동점(같은 ms) 삭제↔편집 · 오래된 편집의 부활 시도 · 다중 키               | ⚠⚠ **`fast-check` 를 안 썼다**(처방과 다름). ① 상태 공간이 유한·작아 **전수 열거가 랜덤 100표본보다 강하다**(축소도 불필요) ② 더 중요한 것: 모델을 TS 로 다시 쓰면 «내 모델이 내 모델과 같다»를 증명한다 → `contract.ts` 의 **진짜 SQL** 을 Node 내장 `node:sqlite` 에 먹인다. **새 의존 0** |
| C055 | 닫힘 | **`test/dbQueryContract.test.ts`** — 카운팅 `Db` 스텁으로 «몇 개 질의로 무엇을 긁는가»                               | 6케이스. `C045` 회귀를 3건으로 잡는다                                                      | 처방대로. ⚠ 실 DB 가 24행이라 **시간으로는 아무것도 안 보인다** — 그래서 시간이 아니라 **SQL 모양**을 잰다                                                                                                                                                                                   |
| C056 | 닫힘 | **`test/dbPartialWrite.test.ts`** — k번째 `execute` 에서 던지는 결함 주입                                            | 5케이스. `_last = null` 을 빼면 「기준선 무효화」 케이스가 빨간불                          | 처방대로 + 축을 하나 더 팠다: **«DELETE 가 upsert 보다 앞서지 않는다»** — 머리주석의 _"DB 가 비는 순간이 없다"_ 가 실제로 뜻하는 것이 그것인데 아무도 안 재고 있었다                                                                                                                         |
| C057 | 닫힘 | **불변식 ㉓** — `.tsx` 의 `mutate(…)` 콜백 줄 수 래칫                                                                | 리뷰 실측 **23** → C036 뒤 **20**. 6줄 초과 10건                                           | ⚠ **임계를 16 으로 적었다가 되돌렸다** — `format` 이 C036 의 한 줄 호출을 5줄로 접었고 **이 래칫이 그것을 잡았다.** 임계는 포맷 **뒤**의 실측이어야 한다(그 사실을 상수 주석에 남겼다)                                                                                                       |
| C058 | 닫힘 | **불변식 ㉔** — `src/` 의 dev/prod 분기 0을 잠근다                                                                   | `rg` 실측 **0건**                                                                          | 처방대로. ⚠ 빌드 설정(`vite.config.ts` 등)은 대상이 아니다 — 막는 것은 **런타임 코드가 자기 모드를 아는 것**                                                                                                                                                                                 |

## 🧹 잔재 (8건 삭제 · 1건 유지)

> **사용자 승인**(2026-08-22 · 목록 전체를 한 번에). 복구는 전부 `git show <커밋>:<경로>`.

| ID   | 상태            | 무엇을 지웠나                                                                                                    | 지운 뒤 깨진 것                                                                                                                                       |
| ---- | --------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| C059 | 닫힘            | `GuideView.tsx` §③「수집·발견」절 + 도구 표 2행 + §④의 죽은 `How` 하나                                           | ⭐ **둘이 깨졌고 둘 다 게이트가 잡았다**: `icons.test.ts`(`discovery` 글리프가 죽은 아이콘이 됨 → 함께 삭제) · `guideTab.test.tsx`(세 축 → **두 축**) |
| C060 | 닫힘            | `bootFallbackScreen.ts`·`App.tsx` 의 텔레메트리·`unhandledrejection`·`reportError` 언급                          | 없음. ⚠ 그 주석이 **R2 의 구멍을 가리고 있었다** — 지우면서 «지금 아무 층도 안 본다»를 명시했다                                                       |
| C061 | 닫힘            | `AmbientCanvas` 현재형 주석 **6곳**(`base.css`·`motion.css`·`tokenBridge.css`·`visibility.ts` + `invariants` 둘) | 없음. ⚠ 리포트는 5곳이라 했는데 **테스트 파일 둘이 더 있었다**(불변식 ④·⑦의 예시)                                                                     |
| C062 | 닫힘            | `lib.rs` 은퇴 트레이 doc comment + 오적용된 `#[cfg(desktop)]`                                                    | 없음(`cargo` 단계 녹색)                                                                                                                               |
| C063 | 닫힘            | `tabs.ts` 「안내(guide) … fill 없음」                                                                            | 없음                                                                                                                                                  |
| C064 | 닫힘            | `quickCapture.ts` 의 도달 불가 `try/catch`                                                                       | 없음 — ⚠ **원칙 ②(절대 throw 금지)를 관측 가능한 자리로 옮겼다**: `test/quickCapture.test.ts` 에 병적 입력 10종 + 정규식 메타 과목명 케이스           |
| C065 | 닫힘            | `components/hud/index.ts`(export 1개 배럴) — `App.tsx` 가 직수입                                                 | 없음(`knip` 녹색)                                                                                                                                     |
| C066 | 보류(유지 판정) | `retrievalStats`·`slowButPassing` — **지우지 않는다**                                                            | 리포트의 유지 판정을 그대로 승계. 다시 열 조건: «실 DB `records` 100행 초과인데 그리는 화면이 여전히 0»                                               |

### 코드 축 **실행 2회차** (2026-08-22) — 1회차가 스스로 연 둘

> 1회차가 «범위를 넘지 않으려고» 쪼갠 `C067`·`C068` 을 이어서 닫았다. 둘 다 근본 원인의
> **남은 절반**이라, 이 회차의 산출물은 새 기능이 아니라 **R2·R3 가 이제 관측된다**는 것이다.

| ID   | 상태 | 무엇을 세웠나                                                                                                                                                                                                    | 검증                                                                                                                                                                             | 처방과 달라진 점                                                                                                                                                                                                                                                                                                         |
| ---- | ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| C067 | 닫힘 | **트랙 A 전량이 비동기 실패를 본다** — `e2e/_test.ts` 가 `test` 를 한 겹 감싸 `pageerror` + `unhandledrejection` 을 듣고, 선언되지 않은 실패가 있으면 **그 케이스를 실패시킨다**. 스펙 9개가 전부 이 래퍼를 쓴다 | **트랙 A 213케이스 전량 + 모션 5 + 폰 4 에서 잡히지 않은 비동기 실패 0건.** 검출력은 `asyncErrors.spec.ts` 가 **일부러 둘을 던져** 확인한다(선언 `expectAsyncFailures(page, 2)`) | ⚠⚠ **처방(전역 `unhandledrejection` 핸들러)을 안 만들었다.** 항목이 적은 최싼검증이 _"0이면 표면을 안 만들어도 되고, 0이 아니면 그 수가 곧 처방의 근거다"_ 였고 **실측이 0**이었다 — 없는 실패를 위해 표면을 만들면 그게 이 저장소가 반복해 만든 «있는 것처럼 보이는 배관»이다. ⚠ 대신 **0을 잠갔다**(C058 과 같은 형태) |
| C068 | 닫힘 | `lib/icsParse.applyLectureBlocks` — 강의 블록 중복 판정을 `CalendarIntake` 의 `mutate` 콜백에서 내렸다. `C036` 의 짝                                                                                             | **`test/applyLectureBlocks.test.ts` 신설 8건** — 특히 «`sid` 는 판정 키가 아니다»(연결이 나중에 붙으면 같은 시간에 블록 둘이 생긴다)                                             | 처방대로. ⚠ 집은 `lib/semester` 가 아니라 **`lib/icsParse`** 다 — `lectureToBlock` 이 거기 살고, `routine` 은 학기의 속성이 아니다(`matchLectureSid` 가 같은 파일에 있는 선례)                                                                                                                                           |

> ## ⭐⭐ 이 회차가 남기는 것 셋
>
> · **R2 가 닫혔다.** 근거 셋(타입 인지 린트 off · `unhandledrejection` 0 · e2e 가 `pageerror`
> 미검사)이 전부 채워졌다. ⚠ 그리고 **셋째가 가장 값쌌다**: `C051` 이 범위 밖에 둔 69곳
> (`onClick={async …}`)은 **눌러야** 거부가 나는데, 린트로 69번 편집하는 대신 **누르는 층**에
> 감시를 다니 한 번에 덮였다. 처방이 «린트를 더 켠다»였다면 69번의 편집과 0의 이득이었다.
> · **R3 의 관측치가 22 → 14 다**(주석 제외 실측). `C036`(눈금) + `C068`(강의 블록)이 같은
> 콜백에서 규칙 둘을 내렸고, 불변식 ㉓ 이 그 자리를 계속 가리킨다.
> · ⚠⚠ **래칫이 자기 세는 법을 두 번 틀렸다** — ① 포맷 **전** 실측(16)으로 잡아 `format` 이
> 그것을 깼고 ② 그 뒤에도 **주석을 세고 있어서**, 규칙을 내리고 «왜 내렸는지» 적으면 그
> 문단이 줄 수를 도로 올렸다(**근거를 적는 쪽이 벌을 받는 형태**). 두 번 다 「임계가 최댓값에
> 붙어 있다」 케이스가 잡았다. 크기 래칫을 새로 만들 때 이 둘을 먼저 확인하라.

### 규약 축 이월 셋 (2026-08-22 · `/실행` 3회차) — `V023`·`V032`·`V033`

> 코드 축 1회차가 **판정만 해 두고** 넘긴 것들이다. 셋 다 「주석이 사실과 다르다」로 올라왔는데,
> 실측하니 **하나만 진짜였고** 나머지 둘은 다른 것이었다 — 그 갈림이 이 회차의 산출물이다.

| ID   | 상태 | 무엇을 고쳤나                                                                                                                                                         | 검증                                                                                                                                                | 처방과 달라진 점                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---- | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V023 | 닫힘 | 셋 중 **하나만** 실제 결함이었다 — `GlanceMode.tsx:41` 의 「오버레이 **6종**이 쓴다」(실측 8). **수를 고치지 않고 뺐다** + 세는 법(`rg -l useFocusTrap src`)을 적었다 | `tabs.ts` 도달성 셋 실측 · `MiniHud:58` 시제 확인                                                                                                   | ⚠⚠ **`tabs.ts` 를 한 번 틀리게 판정할 뻔했다.** `/guide` 딥링크를 `rg "'/guide'"` 로 찾아 «없다 → 주석이 거짓» 이라 읽었는데, 그 라우트는 **손으로 안 적혀 있다**: `App.tsx:173` 이 `role:'view'` 인 로스터 행에서 `<Route path={'/' + t.key}>` 를 **생성**한다. 코드 축의 「이미 참」이 맞았다 — 생성되는 것을 리터럴로 찾으면 없다고 나온다. ⚠ `MiniHud:58` 은 **과거시제**라 처음부터 위반이 아니었다(거짓 양성)                                                                                                                         |
| V032 | 닫힘 | **`scripts/_hexcheck.mjs`** — TS 문자열 속 생 hex 검사 + 사유·만료일 원장. `check:tokens` 에 편입                                                                     | `lib/utils.ts` 에 `'#ff00ff'` 를 심어 **빨간불** 확인 → 되돌림. 리포트 실측 「3파일」은 **주석 포함**이었고, 주석을 걷으면 살아 있는 것은 **2파일** | ⚠ 주석을 안 본다 — 매치 대부분이 _"그때 그 값이 틀렸다"_ 를 적는 근거 문단이었다. 근거에 옛 hex 를 쓰는 것이 위반이 되면 그건 이 저장소가 다섯 번 못박은 역인센티브다. ⚠ `styles/` 는 대상 밖(토큰의 **정의**가 사는 곳)                                                                                                                                                                                                                                                                                                                    |
| V033 | 닫힘 | `ThemeProvider.tsx:16`·`Settings.tsx:116` 의 폴백 근거를 **정정**했다 — 「SSR·초기 프레임」은 이 앱에 없는 상황이다                                                   | 두 함수가 **`useEffect` 안에서만** 불리는 것을 확인(토큰은 이미 서 있다)                                                                            | ⚠⚠ **충돌은 진짜였고, 그래서 «둘 중 하나가 틀렸다»가 아니라 «근거가 틀렸다»였다.** `getPropertyValue` 가 빈 문자열을 주는 **실제 조건은 «그 토큰이 없다» 하나**이고 그건 폴백이 덮을 일이 아니라 시끄럽게 실패할 일이다(`lib/ledger.ts` 가 기록한 `--panel-2` 사고와 같은 형태). 집행자는 이미 있다 — **불변식 ④**. ⚠ **폴백 자체는 안 지웠다**: 지우면 `theme-color` 가 `content=""` 로, 스와치가 색 없이 떨어진다 — «시끄럽게 실패»의 **형태를 고르는** 별개 판단이라 사용자 몫으로 남기고 `_hexcheck` 원장에 만료일(2026-11-30)로 잠갔다 |

> ## ⚠ 이 회차가 남기는 것 — 「주석이 거짓이다」는 세 가지 다른 것이었다
>
> 셋 다 같은 제목으로 올라왔지만 처방이 전부 달랐다:
> · **손으로 적은 수**(`GlanceMode`) → 고치지 말고 **뺀다** + 세는 법을 적는다.
> · **집행자 없는 규약**(`V032`) → 주석이 아니라 **검사기**가 답이다.
> · **근거가 틀린 옳은 코드**(`V033`) → 코드는 두고 **근거를 고치고**, 진짜 판단은 만료일로 예약한다.
>
> ⚠⚠ **그리고 하나는 애초에 거짓이 아니었다**(`MiniHud:58` · 과거시제). 「주석이 사실과 다르다」
> 류를 받으면 **시제**부터 확인하라 — 이 저장소의 주석은 대부분 «그때 그랬다»의 기록이다.

### 규약·ux·코드 이월 (2026-08-22 · `/실행` 4회차) — `V024`~`V031` · `U039`·`U040` · `C025`·`C029`

> 「효율적인 순서로 전부」 지시로 남은 열린 항목을 축별로 묶어 처리했다. **측정이 처방을 두 번
> 갈랐다**(`V025` 재현 불가 · `V026` 방향 반대 · `V029` 대상 축소) — 그 셋이 이 회차의 요지다.

| ID   | 상태            | 무엇을 했나                                                                                                                                      | 검증                                                                                                                                                     | 처방과 달라진 점                                                                                                                                                                                                                                                                                                                                                                                     |
| ---- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V024 | 닫힘            | **`e2e/typography.spec.ts`** — 베이스라인 없는 관계 검사: 워드마크 조판이 **모든 화면에서 같고** 조각이 자기 상자를 안 넘친다                    | 폭을 `1ch` 로 조여 **넘침이 실제로 관측되는지** 확인하는 케이스를 같은 파일에 뒀다                                                                       | ⚠⚠ **세는 법을 두 번 틀렸다.** ① 「조각이 한 줄인가」로 쟀는데 `MARK_PART` 가 `whitespace-nowrap` 이라 **원리적으로 줄바꿈하지 않는다** — 영원히 초록인 검사였다. 현 구조에서 «눌림»의 증상은 **넘침**이다. ② `flex-none` 제거로는 **더 이상 재현되지 않는다**(U021 이후 `BAR` 조판이 바뀌었다) → «그 커밋을 잡았겠는가»를 주장하지 않고 **«눌리면 빨간불인가»**를 증명했다                          |
| V025 | 기각            | —                                                                                                                                                | `git ls-files dist` → **0** · `.gitignore` 에 `web/dist/` 존재 · `git log -- web/dist` **이력 없음**                                                     | ⚠ **재현 불가.** 「`dist/` 가 커밋돼 있다」는 사실이 아니었다(한 번도 추적된 적이 없다). 리뷰가 근거를 안 재고 올린 항목이다                                                                                                                                                                                                                                                                         |
| V026 | 닫힘            | 카드 표면 래칫 **25 → 21**(실측 최댓값)                                                                                                          | `check:tokens` 가 `21/21` 로 보고                                                                                                                        | ⚠⚠ **방향이 반대였다.** 항목은 _"여유 0이라 다음 리팩터가 즉시 막힌다"_ 였는데 실측은 **여유 4**(코드 축 1회차의 삭제들이 표면 넷을 걷었다). 즉 «막힌다»가 아니라 **래칫이 4만큼 헐거워졌다** — `eslint` `max-lines` 가 H18 에서 21줄 느슨했던 것과 같은 형태                                                                                                                                        |
| V027 | 닫힘            | 불변식 ⑧·⑪·⑬의 **지역 파일 워커 셋**을 공용 `sources()` 로. ⑪의 지역 `strip` 도 제거                                                             | 82종 전량 통과                                                                                                                                           | ⭐ **사본이 실제로 갈려 있었다**: ⑪의 지역 `strip` 은 **줄 맨 앞 줄 주석만** 지워 줄 끝 주석을 못 걷었고, ⑬은 m-16 까지 아예 원문을 봤다. 「보일러플레이트 중복일 뿐」이라던 Nit 판정이 실제로는 **동작 차이**였다                                                                                                                                                                                   |
| V029 | 닫힘            | 끝난 발주서 **둘** 삭제(`착수계획-감사근본` 44KB · `착수계획-전역undo-아이콘` 12KB) + 그것을 가리키던 **네 곳**의 포인터를 복구 명령과 함께 정정 | `rg` 로 잔여 참조 0 확인                                                                                                                                 | ⚠⚠ **처방(「1.7MB 삭제 · `로드맵.md` 685KB 포함」)을 실행하지 않았다.** `로드맵.md` 는 **인바운드 참조 71건**(19개 문서)이라 지우면 그 인용이 전부 매달린다 — 제안이 참조를 안 재고 나온 것이다. ⚠ `예약봉투-3일검증.md` 도 남겼다: 「완료」 선언이 없는 **가설 검증 문서**라 지우면 살아 있는 아이디어가 사라진다                                                                                   |
| V030 | 기각(중복 아님) | 모션 스펙 머리주석에 **반증 표**를 못박았다                                                                                                      | `--dur-slow` 340→204(길이 **값**) → 불변식·스냅샷 **둘 다** 잡는다 / `--pulse-r` 10→6px(키프레임 **기하**) → 불변식 **82종 전량 통과** · 스냅샷만 잡는다 | ⚠ 길이 값을 정적 층이 잡은 것은 «막아서»가 아니라 `--dur-slow` 가 `COMMIT_MS` 와 **짝이라는 별도 계약** 때문이다. 짝 없는 토큰이면 정적 층은 아무 말도 안 한다 → **겹치지 않는다.** 줄이면 잃는 것은 «중복»이 아니라 **기하 축 전부**                                                                                                                                                                |
| V031 | 닫힘            | 루트 `REVIEW.md`·`review-manifest.md` → **`web/docs/리뷰/2026-08-20-코드/{리포트,대장}.md`** · `렌즈-오버레이.md` §1-E 정정                      | 이동 후 `/리뷰` 의 나열 대상과 재지적 필터의 입력이 **한 곳**이 됐다                                                                                     | 처방대로. ⚠ 과거 리포트들의 언급은 **안 고쳤다** — 그것들은 이력이고, 이력을 다시 쓰면 «그때 무엇을 봤나»가 사라진다. 고친 것은 **살아 있는 SSOT**(오버레이) 하나다                                                                                                                                                                                                                                  |
| U039 | 닫힘            | `--fs-3xl: 24px` 칸 신설 → `.ds-roNum` 이 토큰을 쓴다(**값 무변경**)                                                                             | `check:tokens` 녹색 · CSS 의 생 px `font-size` **0**(`em` 축소 3단은 다른 축)                                                                            | 처방대로. ⚠ 이름이 `display-*` 가 아닌 이유: `display-*` 는 «값 자체가 화면인» 자리이고 리드아웃 숫자는 `xl`·`2xl` 과 같은 가족이다                                                                                                                                                                                                                                                                  |
| U040 | 닫힘            | `README §실행` 에 **`PYTHON` 환경변수** 절(예시 두 줄 + `python3` 없음 경고)                                                                     | —                                                                                                                                                        | ⚠ **설정 UI 에는 안 넣었다**: 프로세스 환경변수라 앱이 자기 실행 뒤에 바꿔도 **이미 뜬 셸에 안 걸린다**. 문서는 「미리」, 기존 에러 메시지(n-7)는 「지금」 — 둘 다 필요하고 서로를 대체하지 않는다                                                                                                                                                                                                   |
| C025 | 닫힘            | **불변식 ㉕** — `useKeymapDoc` 이 광고한 키를 그 파일이 **키로서** 다루는가                                                                      | `Ledger` 에 없는 키 `Q` 를 광고시켜 빨간불 확인 → 되돌림                                                                                                 | ⚠⚠ **첫 판이 아무것도 안 재는 검사였다.** 「그 파일이 언급하는가」로 쟀더니 단일 문자(`'q'`)가 `params.get('q')` 같은 곳에 흔해 **검출력이 0**이었다. 키 관용구를 **다섯**(`.key ===` · `case` · `k:` · `keys:[]` · `*_KEY =`) 읽는 추출기로 바꿨고, 그중 둘은 오탐이 나서야 알았다. ⚠ 관용구가 늘면 눈이 머는 것을 막으려고 **「광고하는 파일마다 처리 집합이 비어 있지 않다」**를 분모로 함께 잰다 |
| C029 | 닫힘            | `schedView` 를 **주소로** 올렸다 — `?span=day\|week\|month`(영속값은 주소가 말이 없을 때의 기본). `goPlanToday` 의 `setSchedView` 한 줄 제거     | 딥링크에서 `span` 을 빼면 `planLanding` 2건이 **일 뷰를 못 찾는다**(옛 결함 재현)                                                                        | ⚠ **이름이 `view` 가 아니다.** `?view=` 는 이 저장소에서 «호스트 안의 다른 화면»이고 불변식 ⑱이 로스터 등재를 요구한다 — 일·주·월은 **같은 화면의 다른 기간**이라 셋을 등재하면 ⌘K·방문 집계가 부풀고, 면제로 빼면 ⑱이 헐거워진다. ⚠ 어휘의 집은 `shell/tabs.ts` 다(보내는 쪽·받는 쪽이 **다른 feature** 라 서로 import 불가)                                                                        |

> ## ⭐ 이 회차가 남기는 것 — 「측정이 처방을 가른다」가 세 번 반복됐다
>
> `V025`(재현 불가) · `V026`(방향 반대) · `V029`(대상이 1.7MB 가 아니라 56KB)는 전부 **리뷰가
> 근거를 안 재고 올린 항목**이었다. 셋 다 «지적이 틀렸다»가 아니라 **«수를 안 세고 적었다»**다.
> ⚠ 다음 회차의 규약 축은 «집행자가 있는가»만큼 **«그 수를 무엇으로 셌는가»**를 물어라.
>
> ⚠⚠ 그리고 이 회차에서 **내가 만든 검사 둘이 처음엔 아무것도 안 쟀다**(`V024` 의 줄바꿈 · `C025`
> 의 언급). 둘 다 **검출력 확인 케이스**를 같은 파일에 두고 나서야 드러났다 — 새 검사에는
> «일부러 깨뜨려 보는 케이스»를 같이 넣어라. 그게 없으면 초록은 증거가 아니다.

### 데이터·성능·보류 (2026-08-22 · `/실행` 5회차) — `D020`~~`D024` · `P023`~~`P027` · 보류 넷

> 남은 열넷을 축별로 묶어 끝냈다. **성능 팩은 「재는 것」이 곧 산출물**이라, 닫은 것보다
> **실측치**가 이 회차의 본체다 — 다섯 중 셋이 «비용이 관측되지 않는다»로 끝났다.

| ID                        | 상태                 | 무엇을 했나 / 무엇을 쟀나                                                                                                                                                                                     | 검증                                                            | 처방과 달라진 점                                                                                                                                                                                                                                                                                                                                                                      |
| ------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D023                      | 닫힘                 | `rowsToState` 가 `meta.present` 의 **모르는 이름을 버린다**                                                                                                                                                   | `test/dbRows.test.ts` +4 · 가드를 빼면 2건이 빨간불             | ⚠⚠ 위험이 「선언과 실물의 불일치」보다 구체적이었다: 유령 이름이 `out[k]={}` 로 상태에 들어오면 → 다음 쓰기가 `ROW_SLICES` 밖으로 보아 **`settings` 한 행**으로 만들고 → `settings` 는 `sync:true` 라 **D1 과 모든 기기로 영구히 퍼진다.** 자기 증식이라 한 번 생기면 안 사라진다. ⚠ **읽는 쪽에서 거르므로 이미 오염된 DB 도 다음 부팅에 낫는다**                                    |
| D024                      | 닫힘                 | `noteMergedTombs` — 에코 억제를 **툼스톤에도** 건다(행과 같은 규칙 · 이름공간 분리)                                                                                                                           | `test/cloudOutbox.test.ts` +3 · 필터를 빼면 1건이 빨간불        | 처방대로. ⚠ **이름공간을 갈랐다**(`tomb#`): 같은 `(tbl,k1,k2)` 에 대해 «행을 받았다»와 «그 삭제를 받았다»는 **다른 사실**이라, 안 가르면 하나가 다른 하나의 기록을 덮는다(그 케이스를 따로 세웠다). ⚠ 첫 판은 구분자로 NUL 을 썼다가 **불변식 ⑮**(원시 NUL 금지)에 걸렸다                                                                                                             |
| D021                      | 닫힘                 | `src-tauri/migrations/README.md` 에 **Expand / Migrate / Contract** 절                                                                                                                                        | —                                                               | 처방대로. ⚠ **기계 집행자를 안 만들었다(의도)**: 제거가 아직 0건이라 잠글 대상이 없고, 없는 것을 막는 린트는 다음 사람에게 «이런 일이 있다»고 거짓말한다. 첫 Contract 커밋이 집행자를 함께 가져오는 것이 맞다                                                                                                                                                                         |
| D022                      | 닫힘                 | `lib/methodology.recordsSince` + 백업 배너에 **「N건이 앱에만」** 꼬리                                                                                                                                        | `test/methodology.test.ts` +5                                   | ⚠ **날짜 축을 하나로 안 접었다** — `completions` 는 키가 `ds`, `summaries` 는 `at`(epoch ms), 나머지는 `ds` 필드다. 접으면 「시각 없는 것」을 자정으로 반올림하게 되고 그게 `C043` 이 물린 부류다(같은 날 오전/오후를 가르는 케이스로 잠갔다). ⚠ **0건이면 수를 안 붙인다** — 「0건이 로컬에만」은 안심이 아니라 소음이다                                                             |
| D020                      | 보류(표면 결정 선행) | ⭐ **비-자명한 절반을 확인했다**: 스키마 버전 개념이 **이미 양쪽에 공유돼 있다** — `server/wrangler.jsonc` 의 `migrations_dir` 가 **`../src-tauri/migrations`** 라 로컬 SQLite 와 D1 이 같은 원천을 쓴다      | —                                                               | ⚠ 그런데 남은 것이 「헤더 1줄」이 아니다: 서버가 **먼저 경고**하려면 «오류가 아닌 경고» 채널이 필요한데 지금은 `PermanentPushError`(= 실패 뒤)뿐이다. ⚠ 그리고 **해로운 방향은 이미 덮여 있다**: 송신은 `SCHEMA_SKEW` 가 400+처방 문구로 잡고, 수신은 관용 파서(H16)가 견딘다. **다시 열 조건: 「오류가 아닌 동기화 경고」를 놓을 자리가 정해지는 것**                                |
| P023                      | 기각                 | **[측정]** 실 지식 산출물 = 과목 4 · 개념 666 → `HEAT_CAP=60` 적용 시 `<i>` **191개**(캡 없으면 666)                                                                                                          | `_지식상태.json` 실측                                           | 「수백 개 DOM 폭주」가 **관측되지 않는다.** 캡이 제 일을 하고 있고 191 노드는 비용이 아니다. ⚠ 전부 펼쳐도 666 이라 그것도 문제가 아니다 — 다시 볼 축이 있다면 노드 수가 아니라 **셀마다의 `box-shadow` 글로우 페인트**이고, 그건 다른 측정이다                                                                                                                                       |
| P024                      | 기각                 | **[측정]** `stateToRows` + `toTableData` 의 flush 당 비용: n=100 **0.2ms** · 1,000 **1.0** · 5,000 **4.0** · 10,000 **6.1ms**(선형)                                                                           | 순수 함수 벤치                                                  | 저장소가 스스로 적은 설계 목표(10,380행)에서 **6.1ms/flush** 다. 디바운스가 400ms 이므로 **1.5%** 이고 상호작용 경로 밖이다. m-5 가 절반을 없앴고 나머지 절반은 재구조화할 값이 없다                                                                                                                                                                                                  |
| P025                      | 닫힘(측정 완료)      | **[측정]** `rowsToState` 곡선: n=1,000 **0.8ms** · 10,000 **5.6ms**(선형 · 1000행당 ~0.56ms). 저장소 기록치(«2년 근사에서 JS 9ms»)와 정합                                                                     | 같은 벤치                                                       | ⚠ **이 항목의 존재 이유가 사라졌다**: _"`C041` 의 배수를 시간으로 환산할 유일한 방법"_ 이었는데 **C041 이 그 배수를 없앴다**(N회 되읽기 → 1회). 남은 단발 되읽기는 위 수치대로 싸다                                                                                                                                                                                                   |
| P027                      | 닫힘                 | `cloud/undo.ts:currentImages` 가 **`readTouched` 를 재사용**한다(행마다 순차 `selectDb` → 표마다 질의 하나)                                                                                                   | `test/cloudUndo.test.ts` +2(질의 수 계약) · 되돌리면 2건 빨간불 | ⭐ **사본을 안 만들었다** — 「방금 쓴 행만 되읽는다」 규칙은 `C045` 가 고친 `readTouched` 하나가 소유한다(이 회차가 `C036`·`C037`·`C038` 에서 세 번 만난 형태). ⚠ **둘째 절반(`server` 본문 두 번 읽기)은 기각**: [측정] 1KB=10.8ms · 64KB=11.4 · 512KB=**13.6ms** — 512KB 에서 총 2.8ms(3% 미만)이고, 그 두 겹은 _"헤더 하나에 기대는 방어는 방어가 아니다"_ 라는 근거가 있는 설계다 |
| P026                      | 보류(측정 도구 선행) | 폰 워커(wasm+OPFS) 왕복 실지연을 재려 했으나 **DB 핸들이 페이지에서 도달 불가**하다(테스트 전용 전역을 심는 것은 프로덕션 표면을 늘리는 일이라 안 했다)                                                       | —                                                               | ⚠ `CALL_TIMEOUT_MS = 30_000` 의 근거(_"wasm SQLite 연산은 밀리초대"_)는 **아직 [추측]** 이다. 다만 그 상수는 **행 감지기**(워커가 멎으면 영원히 대기하는 것을 끊는다)라 값이 커도 해롭지 않다 — 위험은 「너무 길다」가 아니라 「근거 없이 적혀 있다」다. **다시 열 조건: 폰 하네스가 왕복 시간을 관측 가능하게 노출하는 것**                                                          |
| C066 · I022 · I032 · I036 | 보류 유지            | **[측정] 재개 조건이 넷 다 아직 아니다**(2026-08-22 실 DB 읽기 전용 사본): `records` **0행**(C066 은 100행 초과가 조건) · `completions` **0행** · `route_visits` 72 · `day_signals` 8 · `idle_spells` **0행** | 실 DB                                                           | ⚠ `I032`(관측 원장 축소)의 선행인 I030 이 **오늘** 착지했으므로 오염 없는 관측 1주는 정의상 아직이다. `I036`(저장 엔진 통합)은 트랙 A 시딩 재작성이 선행이고 착수 안 했다                                                                                                                                                                                                             |

> ## ⭐ 이 회차가 남기는 것 — **성능 지적의 절반은 「재면 없다」**
>
> 성능 팩 다섯 중 **셋이 기각/측정완료**로 끝났고(`P023`·`P024`·`P025`), 하나는 절반만 참이었다
> (`P027` — 클라이언트 쪽은 진짜, 서버 쪽은 3% 미만). 공통점은 **아무도 재지 않았다**는 것이다.
> ⚠ 다음 성능 회차는 지적을 적기 전에 **그 수를 먼저 내라** — 이 회차의 다섯 개 측정은 전부
> 10분 안에 끝났고(순수 함수 벤치 · 실 산출물 파싱 · roundtrip 하네스), 그 10분이 없어서
> 네 항목이 몇 주를 열린 채 있었다.
>
> ⚠⚠ 그리고 **닫은 셋(`D023`·`D024`·`P027`)은 전부 「대칭이 안 맞는 자리」였다**: 행에는 있고
> 툼스톤에는 없던 에코 억제 · 읽기에는 있고 `present` 에는 없던 이름 검증 · `readTouched` 에는
> 있고 `currentImages` 에는 없던 배치화. **한쪽만 고쳐진 짝**을 찾는 것이 이 저장소에서
> 가장 수확이 좋은 렌즈다(코드 축 1회차의 `C036`~`C038` 도 같은 형태였다).

---

## 운영 축 1회차 실행 (2026-08-22 · `/실행`) — `O001`~`O034` + 이월 아홉

> 리포트: [`리뷰/2026-08-22-운영/리포트.md`](리뷰/2026-08-22-운영/리포트.md)
>
> **34건 중 32건 종결 · 1건 기각(O015 재현 불가) · 1건 열림(O029 미승인).**
> 이월 아홉(`C069`·`U041`·`U042`·`D025`·`D026`·`V034`~`V039`)도 함께 정리했다.
> 재현 불가 **1건** · 처방과 다르게 고친 것 **아홉**.
>
> ⚠⚠ **이 회차는 성격이 다르다 — 고친 것보다 「잴 수 있게 만든 것」이 본체다.**
> 근본 원인 R1(«시간 상수가 전부 상한이고 하한은 둘뿐»)이 뜻하는 바가 그것이다: 이 축의 결함은
> 대부분 **코드가 틀린 것이 아니라 아무도 안 보고 있던 것**이었고, 그래서 처방도 수정이 아니라
> **집행자**였다(`렌즈-오버레이.md §0-B` 에 여덟 개가 추가됐다).

| ID                                                           | 상태            | 무엇을 했나                                                                                                                                                                                                                                                                                                                                                                                                                                             | 검증                                                                                                                                                                           | 처방과 달라진 점                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------ | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| O001                                                         | 닫힘            | `release-verify.mjs` 에 **서명 키 신원** 축 — `tauri.conf.json` 의 `pubkey` 에서 key id 를 뽑아 매니페스트 서명의 key id 와 대조한다(네트워크 **앞**에서 죽는다)                                                                                                                                                                                                                                                                                        | 실물 배포로 통과(`e0def2444769d896`) · **음성 시험**: pubkey 의 key id 한 바이트를 뒤집자 즉시 실패 · 서명 0개인 매니페스트도 실패(분모 단언)                                  | ⚠⚠ **리포트의 `keyIdOf` 스니펫이 틀렸다.** 공개키에 `.pop()`, 서명에 `[1]` 을 쓴 것을 `.pop()` 하나로 일반화했더니 **멀쩡한 키를 불일치라 신고**했다 — 서명 블록은 3줄이라(`trusted comment: …`) 마지막 줄이 base64 본체가 아니다. 줄 번호가 아니라 **주석 줄을 이름으로 걸러** 뽑는다                                                                                                                                                                                                            |
| O002                                                         | 닫힘            | `build-test` 에 폭포 차단 — `build` 에 `id` 를 주고 하류 넷을 `!cancelled() && steps.build.outcome=='success'` 로 게이팅                                                                                                                                                                                                                                                                                                                                | YAML 파싱 + 잡 구조 확인. ⚠ **원격 판정은 다음 push 의 런**이 낸다                                                                                                             | 처방대로. ⚠ `always()` 가 아니라 `!cancelled()` 다 — 새로 넣은 `concurrency`(O017)가 취소할 때는 **따라 멈춰야** 한다. `always()` 면 취소된 런이 끝까지 돌아 O017 이 아끼려던 과금분을 그대로 쓴다                                                                                                                                                                                                                                                                                                |
| O003                                                         | 닫힘            | **셋 다** 했다: ① CI 세 잡이 `node-version-file: web/.nvmrc` 를 읽는다 ② `.nvmrc` 22 → **24**(로컬 실측과 일치) ③ `observations.test.ts` 의 jsdom 프라그마 제거(`window` 두 곳 → `globalThis.window`)                                                                                                                                                                                                                                                   | 그 테스트 11 passed · **불변식 ㉖ 신설** + 음성 시험 2회(프라그마 되살림 → `observations.test.ts: node:sqlite` 검출 · `node-version:` 되살림 → 2건 실패)                       | ⚠ 국소 처방을 리포트의 `server.deps.external` 대신 **jsdom 제거**로 했다: 전자는 지금 러너에서 **검증할 수 없고**(로컬은 Node 24 라 원래 통과한다) 후자는 로컬에서 즉시 재현·검증된다. 근본 처방(패리티)은 그대로 했다. ⚠ 그리고 집행자를 **값이 아니라 문법**에 걸었다 — 「22 인가」가 아니라 「선언이 한 곳인가」를 잰다                                                                                                                                                                        |
| O004                                                         | 닫힘            | **Dependabot 으로 교체**(사용자 판단). `renovate.json` 삭제 · `.github/dependabot.yml` 신설(생태계 4 = actions + npm 루트/web/server) · 서술 3곳 정정(`ci.yml` ×2 · `audit-gate.mjs`)                                                                                                                                                                                                                                                                   | YAML 파싱 · 그룹/ignore 구조 확인. ⚠ **첫 실행 증명은 다음 월요일**이고 `freshness` 의 「봇 활동」 행이 그것을 읽는다(보류 · 재검토 2026-09-01)                                | ⚠ **고른 이유가 기능이 아니라 「실행 흔적」이다.** Renovate 설정은 오히려 더 정교했다(그룹 9 · `pinDigests` · `lockFileMaintenance`) — 죽은 이유는 **GitHub App 이라 실행이 저장소 밖에 걸려 있었고, 그래서 「설정돼 있다」와 「돌고 있다」를 구분할 수 없었다**는 것이다. 대가를 파일 머리주석에 명시했다: 자동 머지 없음 · `lockFileMaintenance` 없음 · 0.x 조건부 규칙 없음                                                                                                                    |
| O005                                                         | 닫힘            | `schedule` cron(KST 05:00) + **`freshness` 잡 신설**(npm 축 SCA 셋 + 신선도). 나머지 네 잡에 `if: github.event_name != 'schedule'`                                                                                                                                                                                                                                                                                                                      | YAML 파싱 · 잡 5개 조건 확인                                                                                                                                                   | ⚠ 무거운 것을 스케줄에서 **뺐다**(windows 2배 과금). `tauri:deny` 를 넣고 싶었지만 windows 툴체인을 끌어오므로 제외했다 — npm 축 셋만으로 실측 사각(12일)의 대부분이 덮인다                                                                                                                                                                                                                                                                                                                       |
| O006 · O030 · O031 · O032                                    | 닫힘            | **`web/scripts/freshness.mjs` 하나로 넷을 닫았다** — 기대 주기를 **표(데이터)** 로 두고(O031 · `gate.mjs` 의 `ALL` 과 같은 관용구) 배포 도달·CI 마지막 성공·봇 활동을 잰다. CI 행이 **미해결 빨간불**과 **큐에 갇힌 런**까지 본다(O032·O030)                                                                                                                                                                                                            | 실행 결과가 실측 셋을 그대로 재현: 미배포 **15커밋/14일** · CI 최신 런 failure · 봇 0건. 게이트 `full` + CI `freshness` 잡에 배선                                              | ⚠⚠ **처방을 하나 더 넣었다: 기한부 `보류` 원장.** 이 부류 게이트는 켜는 순간 빨갛고(세 축 전부 참), 그때 끄면 영영 안 돌아온다 — `audit-allowlist` 와 같은 관용구(사유+재검토일)에 **역래칫**(해소됐는데 남아 있으면 실패)을 붙였다. ⚠ 그리고 첫 판이 **0커밋**을 냈다: `git log -- web/src` 를 `web/` 에서 돌려 경로가 cwd 기준으로 풀렸다(→ `ROOT` 명시)                                                                                                                                        |
| O007                                                         | 닫힘            | **셋 다** 붙였다: `@tauri-apps/plugin-log` 의존성 · `capabilities` 에 `log:default` · `lib/log.ts` 의 `bridgeConsole()` 을 `main.tsx` 부팅 첫 줄에서 동적 import                                                                                                                                                                                                                                                                                        | `test/logBridge.test.ts` **7** · 번들 영향 실측 **+1.5 KB gz**(데스크톱 504.0/555) · `cargo check` 로 ACL 유효                                                                 | ⚠ **38곳을 고치지 않았다** — 호출부를 바꾸면 39번째가 다시 샌다. `console.error`·`warn` 을 **감싸는** 한 겹으로 전부 덮었다(C067 이 `onClick` 69곳을 「누르는 층」에서 덮은 것과 같은 형태). ⚠ `Error` 는 **스택째** 나른다(`String(e)` 는 스택을 접어 진단의 본체를 지운다) · 객체는 JSON(`[object Object]` 는 있으나 마나) · 싱크가 던져도 콘솔 출력이 안 사라진다                                                                                                                              |
| O008                                                         | 닫힘            | `sync_state` 에 `cloud:lastOkAt`(기기 로컬 · `MAX` 로 단조) + `Ledger.staleDays` + 3일 넘으면 문구가 **「N일째 안 되고 있어요」**                                                                                                                                                                                                                                                                                                                       | `test/syncLedger.test.ts` +7(경과·임계·우선순위·「모른다」) · 연결 해제가 이 키도 지운다                                                                                       | ⚠ **새 발명이 아니라 베낀 것이다** — 볼트 백업(`_lastBackupAt` 영속 + 임계 + 「무엇을 잃나」)이 같은 질문에 이미 답하고 있었고 동기화만 그 짝이 없었다(R3). ⚠ **임계를 7일이 아니라 3일**로 잡았다: 백업은 사람이 눌러야 도는 경로지만 동기화는 편집·포커스·복귀마다 도는 경로라, 3일이면 트리거 넷이 전부 헛돌았다는 뜻이다. ⚠ **실패 횟수를 안 센다** — 재시도 정책에 따라 뜻이 바뀐다                                                                                                          |
| O009                                                         | 닫힘            | 「성공했는가」 판정을 **`lib/syncLedger` 로 올렸다**(`okAt`·`blockedReason`·`attemptFailed`) — `useSyncLedger` 와 `CloudCard` **둘 다** 그것을 쓴다. 카드가 중단 사유도 말한다                                                                                                                                                                                                                                                                          | `test/syncLedger.test.ts` +6(핵심: **push blocked 인데 바깥 status 는 ok**) · 27 passed                                                                                        | ⚠ **국소 수정이 아니라 판정 이동이다.** 처방은 `CloudCard` 를 고치는 것이었는데, 그러면 **같은 판정이 여전히 두 벌**이고 세 번째 소비처가 또 자기 판정을 짓는다. 이 파일 머리주석이 이미 규율을 적어 뒀다 — **판정=lib · 읽기=store · 그리기=components**. 판정이 store 에 갇혀 있던 것이 결함의 원인이었다                                                                                                                                                                                       |
| O010                                                         | 닫힘            | notify 의 `Err` 를 나르는 채널 신설(`hotkey.rs` 관용구) + 첫 대기를 `dead_check` 타임아웃 루프로(무한 대기 → 사망 확인). `watch_with_every(dir, dead_check, …)` 로 주기 주입                                                                                                                                                                                                                                                                            | `cargo test` 신규 1 · **음성 시험**: 옛 무한 대기로 되돌리면 5초 타임아웃으로 실패 · 전체 76 passed **×3회 안정**                                                              | ⚠⚠ **`Path::exists()` 로 재면 Windows 에서 거짓 음성이다** — 감시 중인 폴더를 지우면 열린 핸들 때문에 「삭제 보류」가 되고 **이름은 남는다.** 실제로 첫 판이 그것으로 죽었다. 물어야 할 것은 「이름이 있나」가 아니라 **「아직 읽을 수 있나」**(`read_dir`)이고, 그게 이 감시의 유용성 자체다. ⚠ **연속 2회**를 요구한다 — 일시적 잠금으로 감시를 걷으면 스스로 낫는 상태에 사람을 부른다. ⚠ 주기를 인자로 뺀 이유는 `AppHandle` 을 뺀 이유와 같다(안 그러면 이 검사에 30초가 든다)               |
| O011                                                         | 닫힘            | `server/package.json` 에 `postdeploy` = `release:verify` + `릴리스.md §2-4 ④` 를 「훅이 자동으로 돈다」로                                                                                                                                                                                                                                                                                                                                               | 훅 정의 확인 · `release:verify` 실물 통과                                                                                                                                      | 처방대로. ⚠ 이제 `npm run deploy` 한 줄이 predeploy → deploy → postdeploy 를 돈다 — 「⚠ 이 줄을 건너뛰지 마라」라는 **문장**이 지키던 것을 의존이 지킨다(C1 의 나머지 절반)                                                                                                                                                                                                                                                                                                                       |
| O012 · O013 · O014 · O016 · O024 · O025 · O027 · O028 · V038 | 닫힘            | **런북·릴리스 문서 일괄**: wrangler 직접 호출 3곳 → `npm run deploy` · DB 이름 `hub-prod/hub-dev` → 실물 `learning-hub`(§3-4 는 **스크립트를 가리키게** 해 이름을 문서에서 뺐다) · §7-1 「환경은 하나다」로 재작성 + `--env prod` 전량 제거 · **§2-5 「이 배포는 되돌릴 수 없다」 신설** · `.gitignore 59행` → 절 이름 · 실측 404 예시 URL → 실물 형태 · 은퇴한 DO 절 삭제 · 만족 불가 체크리스트 2줄 삭제 · `curl` 두 줄 삭제 · 절 번호 §7-6/§7-7 교정 | `wrangler.jsonc` 실물 대조(`env` 블록 0 · `database_name`) · `server/src` 3파일에 `syncHub` 0건 · `.gitignore` 62행 실측 · `db_version_guard`·`migrations/README.md` 실재 확인 | ⚠ **O014 는 「환경을 만든다」가 아니라 「문서를 실물에 맞춘다」로 갔다** — 환경을 가르면 D1·시크릿·확인 절차가 한 벌씩 늘고 1인 앱에서 그 값은 §0-1 이 기각한 「인위적 노동」이다. **대신 대가를 명시했다**: `npm run deploy` 는 사용자 데이터 정본을 쥔 워커로 바로 나가고, 안전망은 환경 분리가 아니라 predeploy·postdeploy·rollback 셋이다. ⚠ **O016 은 결함이 아니라 「의도가 안 적힌 것」이었다** — 전진 전용은 설계이고, 문제는 그 사실이 *릴리스를 누르기 전에 읽는 자리*에 0글자였다는 것 |
| O017 · O018 · O020 · O022                                    | 닫힘            | `concurrency`(같은 ref 앞선 런 취소) · `cache-on-failure: true` + `cargo install cargo-deny` → **버전 핀 + 릴리스 바이너리 + 체크섬 대조**(`secret-scan` 관용구) · `paths` 에 루트 lock·SCA 원장 · 잡별 `timeout-minutes`(20/10/15/75)                                                                                                                                                                                                                  | YAML 파싱 · **cargo-deny 내려받기·체크섬·tar 를 실물로 1회 실행**(0.20.2 windows-msvc · `strip-components=1` 로 exe 위치 확인)                                                 | ⚠⚠ **리포트의 `sha256sum -c` 형태는 죽는다** — 그 프로젝트의 `.sha256` 은 **해시 한 덩어리만**(대문자·파일명 없음) 담는데 그 도구는 두 열을 요구한다. 줄을 직접 조립했다. **gitleaks 가 이름 불일치로 도입 이래 0회 돌았던 것과 같은 함정의 다른 얼굴**이고, 실물로 안 돌려 봤으면 그대로 심었다. ⚠ O017 의 대가(중간 커밋 미검증)를 주석에 명시했다                                                                                                                                              |
| O019                                                         | 닫힘            | `bundle-budget.mjs` 에 **축 ⑤ 저장소 바이트**(`git count-objects -v` · 래칫 700 MiB)                                                                                                                                                                                                                                                                                                                                                                    | 실행 결과 **645 MiB / 700(92%)** — 리포트 실측(pack 608.1 + loose 37.0)과 일치                                                                                                 | ⚠ **예산이 아니라 래칫이라고 적었다**: ①~④는 초과해도 다음 커밋에서 되돌리면 사라지는데 이 축은 **append-only 라 「나중에 정리」가 성립하지 않는다.** 그래서 처방도 「줄여라」가 아니라 **「무엇이 이력에 들어가는지 보라」**다. ⚠ git 이 없으면 「모른다」라 말하고 넘어간다(조용한 통과를 안 만든다)                                                                                                                                                                                            |
| O021                                                         | 닫힘            | `VaultTouched.unreadable` 신설(루트 실패 포함) → TS 경계(`looseObject` 라 옵셔널) → `AbsenceOutside.unreadable` → 문장이 **`노트 5+`**                                                                                                                                                                                                                                                                                                                  | `cargo test` 신규 1(못 읽는 볼트 vs 멀쩡한 볼트 **양쪽**) · `test/absence.test.ts` +2                                                                                          | ⚠ **`+` 한 글자가 처방의 전부다** — 재는 것은 「몇 건인가」가 아니라 **「이게 전부라고 주장하는가」**다. ⚠ 짝은 `lib/anki.ts` 가 이미 갖고 있었다(부분 실패 카운트) — 관용구는 있었고 한쪽에만 적용돼 있었다(R3)                                                                                                                                                                                                                                                                                  |
| O023                                                         | 닫힘            | `src-tauri/Cargo.toml` 에 `[profile.dev]` + `[profile.dev.package."*"]`(우리 코드는 line-tables-only · 의존 crate 는 debuginfo 0)                                                                                                                                                                                                                                                                                                                       | `cargo check`·`cargo test` 76 passed·`fmt`·`clippy -D warnings` 전부 녹색                                                                                                      | 처방대로. ⚠ **대가를 적었다**: 의존 crate **내부** 패닉의 줄번호가 사라진다(우리 코드는 남는다). 그리고 **되돌릴 수 있는 결정**이다 — 디스크와 달리                                                                                                                                                                                                                                                                                                                                               |
| O026                                                         | 닫힘            | 🧹 `commit-guard.sh` 를 **살아 있는 표적으로 교체**(사용자 판단) — 도달 불가 조건(형제 폴더) → **시크릿이 `git add -f` 로 스테이징됐는가**(`.updater-key`·`.dev.vars`)                                                                                                                                                                                                                                                                                  | 임시 저장소에서 **양방향 실행**: 정상 파일만 → 통과 · 시크릿 강제 스테이징 → deny JSON 출력                                                                                    | ⚠ **지우지 않았다**(처방은 삭제였다). 조건이 503커밋 이력에 0건이라 도달 불가인 것은 맞지만, 리포트 자신이 _「막으려던 진짜 실패 모드는 살아 있다 — 지우지 말고 그것을 겨누게 고치는 편이 낫다」_ 고 적었다. 새 표적의 근거: `.gitignore` 는 **추적되지 않는 파일만** 가리고 `-f` 는 그것을 뚫으며, gitleaks 는 **push 뒤**에 잡는다(`릴리스.md`: _「push 된 시점에 이미 유출」_)                                                                                                                 |
| O033                                                         | 닫힘            | `/api/health` 에 `recent5xx`(count·firstAt·lastAt·lastMsg) — `onError` 가 세고 400/429 분기에서 되돌린다                                                                                                                                                                                                                                                                                                                                                | `server test:roundtrip` +1(**필드 이름까지** 계약으로 잠근다) · 23 passed                                                                                                      | ⚠ **isolate 지역 메모리다** — D1 에 안 쌓는 이유 셋은 그 라우트가 이미 적어 뒀다(마이그레이션 공유 · 오류 폭주 = 쿼터 소진 · 조인 불필요). 그래서 **`0` 은 「사고 없음」이 아니라 「이 isolate 는 못 봤다」**이고 그 한계를 주석에 명시했다. 값이 있는 이유: **장애는 대개 지속되므로** 이 수가 겨누는 것은 사후 집계가 아니라 「지금 무슨 일이 벌어지는가」다                                                                                                                                    |
| O034                                                         | 닫힘            | 🧹 운영 사고 **넷**을 `판례.md` 로(gitleaks 0회 · `cloud-server` 13일 · 배포 폴백 HTML · e2e 174 skipped) + 이 회차가 물린 **여섯**                                                                                                                                                                                                                                                                                                                     | `판례.md` 83 → 93행                                                                                                                                                            | 처방대로(새 문서 0). ⚠ 이 축의 재지적 필터가 **사실상 0**이었던 것이 이번 회차가 「이미 기각된 제안을 다시 실었을 가능성」을 배제 못 한 이유다 — 그 부재를 메운다                                                                                                                                                                                                                                                                                                                                 |
| O015                                                         | 기각            | **재현 불가.** `installSyncTriggers` 가 트리거 등록 뒤 **`run()` 을 즉시 부른다**(`syncController.ts:411` · 주석 _「설치 즉시 1회 — 열었다 = 최신을 보고 싶다」_)                                                                                                                                                                                                                                                                                       | `git log -S` 로 그 줄이 **2026-07-24**(`797e441`)부터임을 확인 — 리뷰보다 한 달 전                                                                                             | 리포트가 트리거 **등록 목록**(`visibilitychange`·`focus`·`online`·편집)만 보고 그 아래 즉시 호출을 놓쳤다. ⚠ 「트리거가 전부 전이 이벤트다」류는 **그 함수의 끝까지** 읽어라                                                                                                                                                                                                                                                                                                                      |
| O029                                                         | 열림(미승인)    | 🧹 큐에 **16일 갇힌 런** `31123359986`(2026-08-06T17:31Z · jobs 0 · status `queued` — 오늘 재확인)                                                                                                                                                                                                                                                                                                                                                      | —                                                                                                                                                                              | ⚠ **사용자가 잔재 목록에서 이 항목을 빼고 승인했다.** `gh run cancel` 은 저장소 밖으로 나가는 행동이라 임의로 하지 않는다. 다시 올릴 때는 「잃을 것이 0」(잡 0개 → 산출물도 로그도 없다)을 근거로                                                                                                                                                                                                                                                                                                 |
| U041                                                         | 닫힘            | a11y 「대비 미측정」 래칫의 타임아웃을 **로스터 길이에서 유도**(화면 수 × 8초 + 20초)                                                                                                                                                                                                                                                                                                                                                                   | 로컬 15.7초 통과(옛 상한 30초 · 새 예산 124초). ⚠ 이것이 `shell-build` 2런 연속 실패의 **직접 원인**이었다(`page.evaluate: Test timeout of 30000ms exceeded`)                  | ⚠ **더 큰 상수로 안 올렸다** — 30초가 틀린 값이 된 *이유*가 「로스터가 자랐다」라 상수를 상수로 바꾸면 같은 실패를 뒤로 미룰 뿐이다. ⚠ 케이스를 쪼개지도 않았다: 이 지표는 **전 화면의 합**이라 쪼개면 래칫이 아니게 된다                                                                                                                                                                                                                                                                         |
| C069                                                         | 닫힘            | `PYTHON` → **`LEARNING_HUB_PYTHON`**(옛 이름은 하위 호환으로 계속 읽고, 둘 다 있으면 새 이름이 이긴다) + `README.md` 갱신                                                                                                                                                                                                                                                                                                                               | `cargo check`·`cargo test` 76 passed                                                                                                                                           | ⚠ 규율은 이미 있었다 — `paths.rs:25` 가 `LEARNING_HUB_E2E_DATA_DIR` 을 두며 _「사용자의 평상 환경에 우연히 존재할 이름이 아니다」_ 라 적었다. **한쪽에만 적용돼 있었다**(R3). ⚠ 옛 이름을 안 지운 이유: README 가 그 이름으로 안내해 왔으므로 조용히 무시하면 «어제까지 되던 것이 안 된다»가 된다                                                                                                                                                                                                 |
| D025                                                         | 닫힘            | 한도 판정 정규식 옆에 **표류를 관측하는 경로**를 적었다 — 문구가 바뀌면 500 으로 떨어지고, 그 실제 문자열이 이제 `/api/health` 의 `recent5xx.lastMsg` 에 남는다(O033)                                                                                                                                                                                                                                                                                   | `server verify` 녹색                                                                                                                                                           | ⚠ **정규식을 안 고쳤다** — 이 휴리스틱의 진짜 문제는 「틀릴 수 있다」가 아니라 **「틀린 것을 알 방법이 없다」**였다(증상이 「느려짐」이라 아무도 안 본다). 실패 방향은 이미 안전한 쪽으로 잡혀 있었고(못 알아보면 500), 남은 것은 관측이었다                                                                                                                                                                                                                                                      |
| D026                                                         | 닫힘            | `server/.dev.vars`(347 B) **내용 감사** — 값 셋 다 로컬 전용(`local-…` ×2 · localhost) · `git check-ignore` 확인 · **`git log --all` 로 이 경로가 한 번도 커밋된 적 없음** 확인                                                                                                                                                                                                                                                                         | 위 셋 · 값은 길이·접두 4자만 봤다(전문을 로그에 남기지 않는다)                                                                                                                 | 결함 없음 — 확인 후 닫는다. ⚠ 다음 회차가 다시 열지 마라: 재현 절차가 이 줄에 있다(`awk -F=` 로 이름·길이·접두만 뽑는다)                                                                                                                                                                                                                                                                                                                                                                          |
| V034                                                         | 닫힘            | `결정로그.md §열린 결정` 이 백로그 SSOT 로 **`원장.md`** 를 가리킨다(+ 폐루프 한 줄)                                                                                                                                                                                                                                                                                                                                                                    | —                                                                                                                                                                              | ⚠ 이 문서는 「왜 이렇게 결정했나」를 찾을 때 여는 자리라, 여기서 아카이브(`로드맵.md` 685KB)로 보내면 **닫힌 항목 수백 개를 열린 백로그로** 읽게 된다 — 다음 회차의 착수 대상이 통째로 틀어진다                                                                                                                                                                                                                                                                                                   |
| V035                                                         | 닫힘            | **불변식 ㉗ 신설** — `tauri.conf.json` ↔ `Cargo.toml` ↔ `latest.json` 버전이 서로 같다                                                                                                                                                                                                                                                                                                                                                                  | 2 passed · **음성 시험**: Cargo 를 0.5.0 으로 내리자 즉시 실패                                                                                                                 | ⚠ **지적이 「원천 셋」이었는데 실제 결함은 다른 것이었다.** 루트·web 의 `0.0.0` 은 `private` 패키지의 관용 표기라 결함이 아니다(그 사실도 주석에 적었다). 진짜 결함은 `릴리스.md §2-1` 이 _「두 곳이 같아야 한다」_ 고 적고 **집행자가 없던 것**이고, 어긋나면 업데이터가 잘못 말하거나 **조용히 침묵한다**(H-13 의 형태)                                                                                                                                                                         |
| V036                                                         | 닫힘            | `gate.mjs` 주석이 가리키던 **존재하지 않는 단계** `release:sign` → 실제 자리(`tauri:build` + 그 파일 하단의 `env` 조립)                                                                                                                                                                                                                                                                                                                                 | 저장소 전체 grep → 코드 0건                                                                                                                                                    | 처방대로                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| V037                                                         | 닫힘            | `릴리스.md §4-1` 신설 — **「CI 녹색은 「업데이트를 낼 수 있다」를 뜻하지 않는다」**(일회용 키는 안전장치이고, 그 축을 실제로 판정하는 것은 `release:verify` 의 키 신원 축이다)                                                                                                                                                                                                                                                                          | —                                                                                                                                                                              | ⚠ 사실 자체는 `ci.yml` 주석에 **이미 정확히** 적혀 있었다. 결함은 「없다」가 아니라 **「릴리스를 내는 사람이 여는 문서에 없다」**였다 — O001 이 그 축을 실제로 만들었으므로 이제 그 처방까지 함께 적힌다                                                                                                                                                                                                                                                                                          |
| V039                                                         | 기각            | `wrangler.jsonc` 의 은퇴한 DO `migrations` 태그 둘                                                                                                                                                                                                                                                                                                                                                                                                      | 파일 실측                                                                                                                                                                      | **지우면 안 된다.** 그 자리 주석이 이미 근거를 적어 뒀다 — 태그 목록은 **누적 이력**이고, 지우면 이미 그 태그를 적용한 배포와 어긋난다. `deleted_classes` 태그는 «명시 삭제»를 선언하는 **살아 있는 계약**이지 잔재가 아니다                                                                                                                                                                                                                                                                      |
| U042                                                         | 열림(측정 보강) | **[측정]** 30일간 스냅샷 PNG 쓰기 **1,807건 / 67커밋** · 상위 커밋들이 **110장 중 ~100장**을 다시 쓴다                                                                                                                                                                                                                                                                                                                                                  | `git log --name-only` 집계                                                                                                                                                     | ⚠ **분모가 바뀌었다**: 문제는 파일당 크기(무손실 재압축 8.1%)가 아니라 **한 번의 토큰·레이아웃 변경이 100장을 다시 쓴다**는 것이다 — 즉 「요소 범위 스냅샷으로 churn 을 가둘 수 있나」가 정확한 질문이 맞다. ⚠ 지금 전량 재압축하면 **그 커밋 자체가 이력에 29MB 를 더한다**(축 ⑤가 그것을 잰다) — 착수는 «찍는 범위»를 줄인 뒤다                                                                                                                                                                 |

> ## ⭐⭐ 이 회차가 남기는 것 — **「짝을 찾아라」가 여덟 번 청구됐다**
>
> 리포트가 근본 원인 **R3** 로 «한쪽만 고쳐진 짝» 11쌍을 셌고, 닫으면서 **그 규율을 매번 물었다.**
> 결과가 이 회차 처방의 형태를 결정했다 — 여덟 건이 *국소 수정이 아니라 판정·관용구의 이동*으로
> 끝났다: `O009`(판정을 lib 으로) · `O008`(볼트 백업을 동기화에 베낌) · `O010`(`hotkey.rs` 채널을
> `vault.rs` 로) · `O021`(`anki.ts` 부분 실패 카운트를 볼트로) · `O018`(`secret-scan` 관용구를
> `shell-build` 로) · `C069`(`paths.rs` 이름공간 규율을 `tools.rs` 로) · `O002`(C2 폭포 차단을
> `build-test` 로) · `O011`(`predeploy` 의 짝).
> ⚠ **다음 회차가 쓸 것**: 처방을 적기 전에 «이 관용구가 이미 어딘가에 있나»를 먼저 grep 해라.
> 있으면 발명하지 말고 옮겨라 — 그게 이 저장소에서 수확이 가장 좋은 렌즈다.
>
> ⚠⚠ **그리고 「재현 불가 1건」을 «리포트가 정확했다»로 읽지 마라.** 이 축의 지적은 대부분
> `[측정]` 이라 재현이 쉬웠다. 대신 **처방이 틀린 것이 아홉 건**이었고, 그중 셋은 **그대로 썼으면
> 새 결함을 심었을 것**이다: O001 의 `keyIdOf`(멀쩡한 키를 불일치로 신고) · O018 의 체크섬 대조
> (그 형태로는 죽는다) · O006 의 `git log` 경로(0을 조용히 낸다).
> **리포트의 코드는 처방이지 정답이 아니다 — 실물로 한 번 돌려 보기 전엔 게이트에 심지 마라.**
>
> ⚠ **열린 채로 남는 것 둘**: `O029`(사용자가 승인 목록에서 뺐다) · `U042`(측정만 보강 — 착수는
> «찍는 범위»를 줄인 뒤다). 그리고 `freshness` 의 **보류 셋**은 항목이 아니라 **기한부 판단**이다
> (배포 2026-09-05 · CI 2026-08-29 · 봇 2026-09-01) — 그날이 지나면 게이트가 깨진다.

---

## 운영 축 1회차 **잔여 종결** (2026-08-23) — `O029` · `U042`

> 1회차가 남긴 열린 둘을 닫는다. **열린 항목 0.**

| ID   | 상태 | 무엇을 했나                                                                                                                                                                                                                                                                            | 검증                                                                                                                                                                                                                                                                           | 처방과 달라진 점                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---- | ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| O029 | 닫힘 | 🧹 큐에 **16일 갇힌 런** `31123359986` 취소                                                                                                                                                                                                                                            | `gh run view` → `completed / cancelled`                                                                                                                                                                                                                                        | ⚠ 2026-08-22 에는 사용자가 잔재 승인 목록에서 **뺐던** 항목이다. 다음 지시(«남은 거 다»)에 포함해 처리했다 — 잡이 **0개**라 산출물도 로그도 잃은 것이 없다                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| U042 | 닫힘 | 시각 베이스라인의 **찍는 범위를 좁혔다**: 본문 샷 전량을 `page`(fullPage) → **`#main`** 으로, 그리고 크롬 커버리지를 **`nav[aria-label="주요 메뉴"]`·`header`** 전용 샷 넷으로 세웠다. 전체 페이지로 남은 것은 **오버레이·시트·폰**뿐이다(크롬 자체가 대상이라 좁히면 검사가 사라진다) | **[측정] 레일 행 높이를 흔들어(`py-2→py-4`) 재현**: 종전이면 전 화면이 움직이는 변경인데 **9장만 깨졌다**(그중 `chrome · dark/light` 는 **의도된 발화** · 나머지 일곱은 오버레이·시트). 베이스라인 **28.2 → 20.7 MB(−27%)** · 파일 110 → 114(크롬 넷 추가) · 게이트 16/16 녹색 | ⭐ **처방이 「재압축」이 아니었다.** 원장은 무손실 재압축(8.1%)을 부수 처방으로 들었지만, 실측하면 문제는 파일 크기가 아니라 **churn** 이었다 — `5acace3`(IA 재편)이 **소스 7파일·스타일 0파일**을 고치고 **PNG 104장**을 다시 썼다. 전체 페이지 샷이 공유 크롬을 물고 있었기 때문이다. ⚠⚠ **그리고 첫 판의 크롬 잠금은 아무것도 안 잠갔다** — 전체 페이지 샷은 `maxDiffPixelRatio` 가 **이미지 전체 비율**이라 레일 변화를 임계 아래로 희석시킨다(실측: 교란에도 둘 다 통과). 크롬을 **자기 크기로** 찍고서야 발화했다. 그 논리는 이 저장소에 이미 있었다(`motion.spec` 의 맥동이 자기 클립을 쓰는 이유) |

> ## ⭐ 이 회차가 남기는 것 — **「가둔다」는 크기가 아니라 경계의 문제였다**
>
> U042 는 원장에 «요소 범위 스냅샷으로 churn 을 가둘 수 있나»라는 **질문**으로 올라와 있었고,
> 답은 **가둘 수 있다**(110 → 9 · −92%)였다. 다만 그 과정에서 세 번 물렸고 셋 다 판례에 있다:
> ① 크롬 잠금이 희석으로 무력했다 ② 교란이 truncate 에 먹혀 의도한 것을 안 일으켰다
> ③ 교란을 건 채 베이스라인을 재생성해 **깨진 상태를 정답으로 굳힐 뻔했다.**
> ⚠ 셋의 공통점: **「검사를 세웠다」와 「그 검사가 겨눈 것을 실제로 잡는다」는 다른 사실이다.**
> 새 래칫·새 잠금을 세우면 **해소 경로를 한 번 밟아라** — 이 회차가 같은 교훈을 두 번(역래칫 ·
> 크롬 잠금) 얻었다.
