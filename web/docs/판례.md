# 판례 — 러닝허브

`/실행`이 항목을 닫을 때 append 한다. **명령·렌즈팩 문서에는 적지 않는다** —
거기 쌓이면 규율 준수 자체가 작업이 되어 실제 사고 예산을 잡아먹는다.

한 줄 형식: `날짜 · 무엇을 하려다 · 뭐에 물렸나 · 교훈`

| 날짜                                                                                                                                                                                                                                                                                                                                        | 하려던 것                                                                                                                                 | 물린 것                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 교훈                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-20                                                                                                                                                                                                                                                                                                                                  | 옛 명령·에이전트 은퇴                                                                                                                     | 보관소 `.claude/_은퇴-*/` 를 만들었다가 지웠다 — «참고용 보존본» 은 잔재를 만드는 합리화다(검색·스캔 경로에 남아 계속 비용을 낸다)                                                                                                                                                                                                                                                                                                                                                                 | **보관소를 만들지 마라. git 이 이력이다.** 복구: `git show f1af4f8:.claude/commands/감사.md` · `git show ed358dc:.claude/agents/web-reviewer.md` · `bd31741:.claude/agents/web-ideator.md` · `86bc12d:.claude/commands/게이트.md` ⚠ **hub 은 서브모듈** — hub 저장소에서 찾는다                                                                                                                                                                                             |
| 2026-08-21                                                                                                                                                                                                                                                                                                                                  | `/리뷰 데이터` 의 §진보에서 「최고 수준엔 있고 우리에겐 없는 것」을 세려 했다                                                             | `time-travel`(하이픈·소문자)로 grep 해 0건을 얻고 **「Time Travel 없음」·「복원 훈련 없음」·「키 회전 절차 없음」 셋을 격차로 올렸는데 셋 다 이미 있었다** — 실물 표기가 `Time Travel`(공백·대문자)이었고, `restoreDrill.test.ts` 와 `릴리스.md:25-30` 도 있었다                                                                                                                                                                                                                                   | **부재를 주장할 땐 `-i` + 표기 변형 2개 이상.** 한 패턴 0건은 `[측정]`이 아니라 `[추측]`이다. 이 실패는 §진보에서 특히 비싸다 — `ⓓ=없음` 은 «이미 있는 것을 다시 만들자»가 되어 `/실행`이 중복을 짓는다                                                                                                                                                                                                                                                                     |
| 2026-08-21                                                                                                                                                                                                                                                                                                                                  | 같은 회차에서 「지우는 경로가 없는 데이터」를 찾으려 했다                                                                                 | 코드만 읽어서는 **원리적으로 안 보였다** — 실 SQLite 를 열자 `docs` 표에 은퇴한 화면이 남긴 **3행 51,793 B** 가 있었고 읽을 수도 지울 수도 없는데 이미 D1·폰까지 밀려 있었다                                                                                                                                                                                                                                                                                                                       | **데이터 축은 실행 중인 저장소를 열어야 한다.** ⚠ 반드시 **임시 복사 후 읽기 전용** — 이 저장소엔 검증이 검증 대상을 망가뜨린 전력이 있다(트랙 B 가 실 DB 의 `updated_at` 을 0 으로 깎았다)                                                                                                                                                                                                                                                                                 |
| 2026-08-21                                                                                                                                                                                                                                                                                                                                  | D001(폰 마이그레이션 비원자)의 재현 — 리포트가 든 예 `009` 로 재현 스크립트를 짰다                                                        | **재현되지 않았다.** 009 는 마지막에 `summaries_v9` 를 `summaries` 로 rename 해서 **우연히 재실행 가능**하다. 실제로 브릭되는 것은 `CREATE TABLE` 로 **끝나는** 007·008·010·011 쪽이었다(`table route_visits already exists` 실측)                                                                                                                                                                                                                                                                 | **결함의 부류가 맞아도 리포트가 든 예가 맞는 건 아니다.** 재현 단계를 건너뛰고 그 예로 회귀 테스트를 썼다면 **아무것도 안 잠그는 녹색 테스트**가 남았을 것이다 — 이 회차에서 실제로 한 번 그렇게 쓸 뻔했다                                                                                                                                                                                                                                                                  |
| 2026-08-21                                                                                                                                                                                                                                                                                                                                  | D010 — `cargo deny` 를 게이트에 넣고 D016(event-listener 5.4.1 · RUSTSEC-2026-0221)이 잡히는지 확인했다                                   | **`advisories ok` 가 나왔다.** 그 crate 가 실 빌드 그래프 안인데도(`-s` 로 재확인: 0 errors/warnings/notes). 원인은 `informational = "unsound"` 인 권고가 **v2 기본에서 리포트되지 않는 것** — `[advisories] unsound = "all"` 을 명시해야 한다(`unmaintained` 와 같은 enum)                                                                                                                                                                                                                        | **도구를 들였으면 「그 도구를 들인 계기」로 한 번 겨눠 봐라.** 안 겨눴으면 «게이트 있음»이라 적힌 채 정확히 그 부류를 못 잡는 게이트가 남는다 — 이 저장소가 세 번 물린 «있는 줄 알았는데 안 돌고 있었다» 의 변종이다                                                                                                                                                                                                                                                        |
| 2026-08-21                                                                                                                                                                                                                                                                                                                                  | D019 — JS 소비처 0인 `global-shortcut` 권한 3종 + `core:window:allow-show` 삭제                                                           | 아무것도 안 깨졌다. 릴리스 exe + 트랙 B 프로브로 `capabilities.hotkey === true`·`hotkeyError === null` 확인                                                                                                                                                                                                                                                                                                                                                                                        | **Tauri 권한(capabilities)은 웹뷰 `invoke` 만 통제한다 — Rust 가 부르는 경로는 ACL 을 안 탄다.** 그래서 "Rust 가 쓰니까 권한도 필요하겠지"는 틀린 추론이고, 그 추론으로 켜 둔 권한의 유일한 사용자는 **침해된 웹뷰의 임의 JS** 다                                                                                                                                                                                                                                           |
| 2026-08-21                                                                                                                                                                                                                                                                                                                                  | D002 — pull 관용을 열 개수 축까지 넓혔다                                                                                                  | 기존 테스트 하나(`cloudClient.test.ts` "아는 테이블의 내용이 계약과 다르면 여전히 던진다")와 **정면 충돌**했다. 그 테스트는 뒤집기 전 정책을 정확히 인코딩하고 있었다                                                                                                                                                                                                                                                                                                                              | **게이트가 빨개졌을 때 «내 수정이 깼다»와 «옛 계약을 갱신해야 한다»는 다르다.** 후자면 테스트를 고치되 **뒤집힌 이유를 그 자리에 적는다** — 안 적으면 다음 회차가 옛 근거를 읽고 되돌린다                                                                                                                                                                                                                                                                                   |
| 2026-08-21                                                                                                                                                                                                                                                                                                                                  | `/리뷰 ux` 의 §진보에서 표본(Linear·Vercel)의 CSS 를 받아 `font-size` 고유값을 셌다                                                       | 표본이 **27종·79종**으로 나왔다 — 우리(28)보다 많다. 그 수만 보면 「우리는 양호」라는 **정반대 결론**이 나온다. 원인은 제품 사이트라 **서드파티 CSS 가 섞인** 것이고, 실제로 비교 가능했던 것은 **토큰 «정의» 층**이었다(`--radius-4/6/8/12` vs `--radius-swatch/chip/rail-chip`)                                                                                                                                                                                                                  | **표본의 «총량»은 거의 항상 비교 불가다. 비교하는 것은 «규율의 형태»다.** 여기서 나온 것은 개수가 아니라 **이름 짓는 법**이었다 — 값으로 이름 지으면 off-ladder 값은 이름을 못 지어 사다리가 유한해지고, 호출부로 이름 지으면 사다리가 컴포넌트 수만큼 자란다                                                                                                                                                                                                               |
| 2026-08-21                                                                                                                                                                                                                                                                                                                                  | 같은 회차에서 「레일이 720px 에서 잘린다」를 Major 로 올리려 했다                                                                         | **배포 기본 창은 1440×900**(`src-tauri/tauri.conf.json:17`)이라 기본 상태에서는 다 들어간다. 720 은 **Playwright 뷰포트**(`devices['Desktop Chrome']`)였고, 그것을 사용자 창으로 읽었다                                                                                                                                                                                                                                                                                                            | **베이스라인이 보여 주는 것은 「사용자가 보는 화면」이 아니라 「테스트가 보는 화면」이다.** 창 크기를 근거로 삼기 전에 `tauri.conf.json` 의 `width`·`minWidth` 를 열어라. ⚠ 다만 발견이 사라진 것은 아니고 **좁아졌다** — `minHeight` 가 600 이고 검증망이 그 대역(720)에서 돈다는 사실은 남는다                                                                                                                                                                            |
| 2026-08-21                                                                                                                                                                                                                                                                                                                                  | 같은 회차에서 반픽셀 `--fs-*` 5칸을 「자기 규약 위반」으로 올리려 했다                                                                    | `stylelint.config.js:17-22` 에 **근거 있는 예외**가 있었다 — 정수로 일괄 **상향** 했더니 월 캘린더 `.cell`(고정 행 높이 + `overflow:hidden`)에서 과목 칩이 통째로 잘린 실사고                                                                                                                                                                                                                                                                                                                      | **예외의 «범위»를 재라 — 근거가 있다고 전부 덮는 것이 아니다.** ① 그 예외는 _리터럴_ 에 대한 것인데 C-7 이 리터럴을 전부 **사다리 칸으로 승격**시켰다(같은 파일이 「사다리에 대한 규약」이라 못박은 그 자리로 들어갔다) ② 실패 모드는 **상향에만** 있다 — `[재현]` 레일에서 올려도 +3px·생략 0건이고, **내림으로 고정하면 그 실패가 원리적으로 불가능하다**. 뒤집는 것이 아니라 **방향을 못박는 것**이 처방이었다                                                           |
| 2026-08-21                                                                                                                                                                                                                                                                                                                                  | U021 — 워드마크가 `schedule`·`alloc` 에서 네 줄로 무너지는 것을 고치고 **곧바로 재측정**했다                                              | 값이 안 바뀌었다(h1 높이 80px 그대로). 원인은 코드가 아니라 **측정 대상**이었다: 트랙 A 는 `vite preview` 로 **`dist` 를 서빙**하는데 `npm run build` 를 안 돌렸다                                                                                                                                                                                                                                                                                                                                 | **트랙 A 로 무언가를 재기 전에 빌드하라.** 절대규칙 #1(«셸이 prebuilt dist 를 로드한다»)은 앱 실행에만 해당하는 줄 알기 쉬운데, **e2e 도 같은 dist 를 본다** — 안 그러면 «고쳤는데 안 고쳐졌다»가 되고, 그 다음 수순은 멀쩡한 처방을 의심하는 것이다                                                                                                                                                                                                                        |
| 2026-08-21                                                                                                                                                                                                                                                                                                                                  | U023 — 「320px 에서 가로 스크롤」을 고치려고 넘치는 요소를 찾았다                                                                         | 범인이 본문이 아니라 **하단 탭바**였고, 더 나쁜 사실이 함께 나왔다: 그 바는 **390px(폰 베이스라인)에서도 탭 7개를 화면 밖에 두고 스크롤조차 안 됐다**. 즉 「확대 시 리플로우」로 접수된 것이 실제로는 **상시 도달성 결함**이었다                                                                                                                                                                                                                                                                   | **넘치는 것과 못 닿는 것을 가려라.** 문서 폭만 재면 `position:fixed` 안의 넘침은 안 잡힌다(뷰포트 스크롤에 안 실린다) — 그래서 이 결함은 리플로우 지표에서 **부분적으로만** 보였다. 게이트를 세울 때 «넘쳤는가»와 «닿는가»를 각각 물어라                                                                                                                                                                                                                                    |
| 2026-08-21                                                                                                                                                                                                                                                                                                                                  | U016 — 숙달 램프를 색각이상에서도 갈리게 하려고 «명도 단조성 + 양 끝 대비 + 면 대비» 셋을 계약으로 걸었다                                 | **단조성은 원리적으로 불가능했다.** sRGB 휘도는 빨강(낮음)→노랑(최고)→초록(중간)이라 0°→120° 램프의 중간이 항상 봉우리다. 셋을 다 걸면 통과하는 조합이 **하나도 없다**                                                                                                                                                                                                                                                                                                                             | **계약을 걸기 전에 그 계약이 만족 가능한지 먼저 풀어 봐라.** 여기서는 격자 탐색 한 번(2분)이 «어떤 계약을 포기해야 하는가»를 알려 줬고, 그 덕에 포기한 이유를 테스트 머리주석에 근거와 함께 적을 수 있었다 — 못 지킬 계약을 걸어 두면 다음 사람이 그것을 «지킬 수 있는데 안 지킨 것»으로 읽는다                                                                                                                                                                             |
| 2026-08-21                                                                                                                                                                                                                                                                                                                                  | 여러 항목에서 «현재 위치로 데려간다»(`scrollIntoView`)를 붙였다                                                                           | **컴포넌트 테스트 70건이 통째로 죽었다** — jsdom 에 그 메서드가 없다. 제품 코드는 멀쩡한데 게이트가 빨갛다                                                                                                                                                                                                                                                                                                                                                                                         | **없는 API 는 환경이 채운다 — 제품 코드에 `typeof … === 'function'` 가드를 심지 마라.** 그 가드는 곧 «왜 방어하지?»가 되고, 진짜 이유(테스트 환경의 한계)는 아무 데도 안 남는다. `test/_setup.ts` 가 그 자리다                                                                                                                                                                                                                                                              |
| 2026-08-21                                                                                                                                                                                                                                                                                                                                  | U031 — 손으로 적힌 빈 상태 로스터를 `export const TABS_EMPTY = TABS` 별칭으로 파생시켰다                                                  | `knip` 이 **중복 export** 로 잡았다                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **게이트가 더 나은 형태를 가리킬 때가 있다.** 이름이 둘이면 다음 사람이 «둘은 다르다»로 읽는다 — 별칭을 지우고 소비처가 원본을 쓰게 하는 쪽이 옳았다                                                                                                                                                                                                                                                                                                                        |
| 2026-08-21                                                                                                                                                                                                                                                                                                                                  | U030 — 두 화면의 셋업 안내를 `components/State` 로 옮겼다                                                                                 | 옮기고 나니 **빈 상태 스냅샷 넷이 타임아웃**으로 죽었다. `State` 의 제목이 `<div>` 라 그 화면들의 `#main` 안에 **머리글이 하나도 없었고**, 회귀망의 「본문이 떴는가」 단언이 `h2` 를 찾고 있었다                                                                                                                                                                                                                                                                                                   | **검사가 멈춘 자리가 제품의 구조 결함일 수 있다.** 단언을 느슨하게 하는 것이 첫 반응이지만, 여기서는 **제목에 `role="heading"` 을 주는 것**이 옳았다(SR 머리글 탐색이 18개 빈 화면에서 함께 살아났다). ⚠ `<h2>` 태그로 바꾸는 것은 오답이다 — 언레이어드 전역 `h2 {}` 가 18곳의 조판을 통째로 바꾼다                                                                                                                                                                        |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I039 — 「번호는 같은데 내용이 다른 마이그레이션」을 잡으려고 `PRAGMA table_info` 해시 가드를 만들려 했다                                  | **탐지는 이미 있었다.** sqlx 가 SHA-384 로 매 부팅에 대조하고, `db.rs` 자신이 _"주석 한 글자만 바뀌어도 기존 DB 를 못 연다"_ 고 적어 뒀다. 없던 것은 **보고**다 — 그 실패가 `load()` Err → C1 조용한 폴백으로 흘러 «뜨는데 데이터가 옛날 것»이 되고, `is_downgrade` 는 이 경우 거짓이라 명시적 화면이 안 떴다                                                                                                                                                                                      | **「없다」고 적힌 발견을 만들기 전에 「있는데 안 들리는 것 아닌가」를 물어라.** 해시를 새로 만들었다면 같은 사실에 판정자가 둘이 되고, 둘은 반드시 갈린다. 이 회차에서 같은 형태가 **넷**이었다(I039·I041·I038·I023) — 발상 축의 «없는 것»은 자주 «탐지는 있고 끝이 없는 것»이다                                                                                                                                                                                            |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I041 — 「leech 표식은 있는데 자료를 바꾸러 갈 자리가 없다」를 고치려 했다                                                                 | **표식조차 안 그려지고 있었다** — `leech` 를 읽는 화면이 저장소에 0개(`spacedReview` 가 A-4 에서 _"UI 는 이걸 다른 문구로 그려야 한다"_ 고 적어 둔 조건이 한 번도 충족된 적 없다). 그리고 러너 배지가 `chapterCopy().badge` 를 **손으로 재현**하고 있었다(폰은 `copy.badge` 사용 = 이미 갈릴 준비 완료)                                                                                                                                                                                            | **주석이 「UI 가 …해야 한다」고 적으면 그 소비처를 grep 해서 확인하라.** 판정만 흐르고 끝이 없는 층은 게이트가 원리적으로 못 잡는다(계산은 옳고 테스트도 녹색이다). H14 가 본문에서 고친 「말이 두 벌」이 **배지 축에 남아 있던 것**도 같은 방식으로만 보인다                                                                                                                                                                                                               |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I020 — 복습 due 에 챕터별 ±1일 흔들기를 넣었다                                                                                            | **유닛 3건이 깨졌다.** fixture 가 사다리 칸(16일)에 **정확히** 앉아 있어서, 그 챕터의 흔들기 부호를 그대로 검사하게 됐다                                                                                                                                                                                                                                                                                                                                                                           | **경계값 fixture 는 「경계를 재는 케이스」에만 둬라.** 저 셋이 재려던 것은 완료/미래 필터와 경과일이었고 경계는 우연이었다 — 한 칸 밖으로 옮기면 의도가 그대로 남는다. ⚠ 반대로 **정말 경계를 재는 케이스는 그대로 둔다**(riskOf 직접 호출 5건은 안 건드렸다)                                                                                                                                                                                                               |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I030 — 점검 트래픽을 원장에서 빼는 「플래그 한 줄」을 처방대로 부울로 만들려 했다                                                         | 부울이면 **켜 놓은 것을 잊어 관측이 조용히 영원히 멈춘다.** 그러면 다음 판정자가 그 0 을 「안 쓴다」로 읽는데, 그건 이 원장이 막으려는 바로 그 순환이다 — 노브가 자기 목적을 재현하는 형태                                                                                                                                                                                                                                                                                                         | **관측을 끄는 노브에는 만료를 붙여라.** 날짜 스코프면 자정에 스스로 꺼지고, 켜져 있다는 사실이 리드아웃에 날짜로 보인다. ⚠ 그리고 **행이 없어도 스위치는 그린다** — 안 그러면 첫날부터 켠 사용자는 끄는 문에 도달할 수 없다                                                                                                                                                                                                                                                 |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I033 — 폰 원장을 판정자에게 가져오려고 처방(«백업 왕복 1회»)대로 폰 백업을 데스크톱에서 가져오려 했다                                     | `importJSON` 은 `loadState` 로 **앱 상태를 교체**한다. 폰 백업으로 그걸 하면 폰 데이터가 PC 를 덮는데, 앱 데이터의 정본은 **클라우드 동기화**다 — 두 정본이 서로를 덮는 경로를 손으로 여는 셈                                                                                                                                                                                                                                                                                                      | **「그 파일을 가져온다」와 「그 파일의 한 조각을 합친다」는 다른 동작이다.** `_obs` 만 꺼내 upsert 하는 별도 입구를 파는 것이 옳았다(방문 수 `MAX` 규칙은 `lib/observations` 가 이미 소유하고 있었다)                                                                                                                                                                                                                                                                       |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I049 — 트레이·알림을 지운 뒤 `check:tokens` 가 **쓰고 있는 토큰**(`--color-learning`)을 고아로 잡았다                                     | 검출기의 주석 제거 정규식이 `<Cmd>exports/*.txt</Cmd>` 의 **글롭을 주석 시작으로** 읽어 `GuideView` 의 절반을 공백으로 지우고 있었다 — 그 구간의 소비가 통째로 안 보였다                                                                                                                                                                                                                                                                                                                           | **문자열 속 `/*` 를 주석으로 읽지 마라 — 앞 글자를 봐라.** 이 부류는 «다른 소비처가 하나라도 있으면 조용하다»: 실제로 그 화면이 삭제되기 전까지 몇 달을 지나갔다. 고친 형태는 `(^                                                                                                                                                                                                                                                                                           | [^A-Za-z0-9_])\/\*` 다(진짜 주석은 줄머리·공백·`{` 뒤에 온다)                                                                                                                                                                                                                    |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I044~I052 실행 뒤 트랙 A 넉 장(`settings`·`integrations` × 2테마)이 **병렬에서만** 빨개졌다                                               | 처음엔 웹폰트 스와프로 읽고 `fonts.load`·`fonts.ready`·레이아웃 안정 대기를 차례로 넣었는데 **전부 안 닫혔다.** 프로브가 그 가설을 죽였다 — 폰트는 두 조건에서 동일했고(`check` 참 · 항목 높이 38px 동일) 갈린 것은 레일 nav 의 `scrollTop`(40↔43) 같은 서브픽셀 상태였다                                                                                                                                                                                                                          | **베이스라인을 만드는 조건과 재는 조건이 다르면 그 차이만큼은 원리적으로 못 닫는다.** 이 저장소는 `e2e:update` 를 `--workers=1` 로 못박아 두고 검사는 `fullyParallel` 로 돌리고 있었다 — 평소엔 `retries:1` 이 흡수했고, 레일이 한 줄 줄자(I048) 표면화됐다. 처방은 임계 완화(드리프트를 삼킨다)도 재시도(어긋남을 남긴다)도 아니라 **조건을 맞추는 것**이었다                                                                                                              |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | 같은 회차에서 `settings · light` 한 장이 재시도까지 실패했다                                                                              | diff 를 보니 **네이티브 폼 컨트롤(체크박스·날짜 입력 아이콘)이 한쪽에만 그려져** 있었다 — 폰트도 레이아웃도 아니고 «아직 다 안 그렸다» 였다                                                                                                                                                                                                                                                                                                                                                        | **«정착했나»를 물을 때 `readyState` 를 빠뜨리지 마라.** rAF 두 번은 _스크립트가 만든_ 변화만 넘기고, 브라우저가 네이티브 위젯을 그리는 시점은 그 뒤일 수 있다                                                                                                                                                                                                                                                                                                               |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I048 — 「`role:'retired'` 어휘를 지운다」를 문자 그대로 실행하려 했다                                                                     | 그 항목이 규정한 두 갈래(레일에 선다 / 없다) 중 **어느 쪽도 지금 고를 수 없었다**: 레일에 올리려면 자기 라우트·로더가 필요하고(W9·A-16·A-19 를 되돌리는 IA 변경), 로스터에서 빼면 H-12(이름)와 I034(집계)가 **함께 회귀**한다                                                                                                                                                                                                                                                                      | **어휘를 지우는 것과 그 어휘가 붙은 것을 옮기는 것은 다른 결정이다.** 여기서 지울 수 있었던 것은 «영원히 유예»라는 **뜻**이고(그래서 `view` = 확정된 배치), 자리 이동은 데이터가 붙는 항목(I025)의 몫이다. 부분 실행이면 **무엇을 안 했는지와 왜인지를 닫기 노트에 적는다**                                                                                                                                                                                                 |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I036(저장 엔진 하나로) — 승인을 받았으니 바로 착수하려다 **최싼검증부터** 돌렸다                                                          | `enableBrowserDb()` 를 켜고 트랙 A 를 돌리니 시각 스냅샷은 **1장**만 깨졌는데(원장이 걱정한 비용) `plan.spec` 이 **쓰기가 아예 안 남아** 실패했다 — 트랙 A 의 시드가 `localStorage` 에 심기는데 정본이 OPFS 로 바뀌면 그 시드가 통째로 무시된다                                                                                                                                                                                                                                                    | **승인은 착수 허가지 전제 확인이 아니다.** 이 항목의 전제(«트랙 A 가 OPFS 위에서 돈다»)는 거짓이었고, 진짜 비용은 «깨진 스냅샷 수»가 아니라 **시딩 기질 재작성**이다. 5분짜리 프로브가 그것을 말해 줬다 — 안 돌렸으면 스냅샷 209장을 재생성한 뒤에 알았을 것이다                                                                                                                                                                                                            |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I032(관측 원장 축소) — 「`idle_spells` 0행」을 근거로 지우려 했다                                                                         | 같은 원장의 `idleVerdict` 가 스스로 _"관측 N일 — 3일 미만이면 판정하지 말 것(0 은 「없다」가 아니다)"_ 라 적어 두고 있었다. 그리고 이 항목의 전제는 «I030 뒤에도» 인데 **I030 이 그날 착지했다**                                                                                                                                                                                                                                                                                                   | **항목의 전제에 「무엇 뒤에」가 적혀 있으면 그 무엇이 언제 끝났는지를 봐라.** 선행이 방금 끝났으면 그 항목은 아직 **잴 수 없는 상태**이지 열려 있는 상태가 아니다. 보류에 사유를 적어 두면 다음 회차가 같은 0을 다시 근거로 쓰지 않는다                                                                                                                                                                                                                                     |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I053(배분판의 「전공 밖」 레인) — 「데이터 계약 0」이니 안전하다고 보고 착수하려 했다                                                     | 계약은 정말 0이었다(스케줄러는 `weekly.forEach` 로 실제 과목만 훑으므로 합성 sid 를 무시한다). 그런데 **쓰는 쪽**이 걸렸다: `setAllocCell` → `ensureWeekAlloc` 이 그 주를 **managed 로 승격**시킨다 — 「교양 2시간」을 적으면 그 주 배치가 통째로 자동에서 수동으로 바뀐다                                                                                                                                                                                                                         | **「읽는 쪽 계약 0」과 「쓰는 쪽 부작용 0」은 다른 말이다.** 항목이 «데이터 계약 0» 이라 적었을 때 검토된 것은 읽기 방향뿐이었다. 새 값을 기존 저장 구조에 얹을 때는 **그 구조를 쓰는 함수가 무엇을 함께 하는지**를 봐라                                                                                                                                                                                                                                                    |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I027(레일을 줄인다) — 「무엇을 접었는지가 곧 답」이라기에 접기 원장을 읽으러 갔다                                                         | **읽을 곳이 실 DB 가 아니었다.** UI 취향은 `settings` 표가 아니라 WebView2 의 `lh_ui_v1`(로컬스토리지 leveldb)에 산다 — 앱 데이터와 UI 를 다른 저장소에 두는 것이 이 저장소의 계약이라, 실 DB 만 열면 «키가 없다 → 기본값이다»를 **추론**하게 되고 그건 실측이 아니다. 교훈: **UI 노브의 실사용을 재려면 `%LOCALAPPDATA%/dev.jin.learninghub/EBWebView/Default/Local Storage/leveldb` 를 함께 읽어라.**                                                                                            |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | 같은 항목 — 레일 조립 화면 하나를 은퇴시켰다                                                                                              | **모션 어휘 두 마디가 발화 0으로 남았다**(`shift` A-17 · `deny` A-18 — 그 화면이 유일한 소비처였다). knip 이 잡긴 했지만 knip 이 말한 것은 «export 가 안 쓰인다» 이지 «어휘에 발화가 없다» 가 아니다 — 어휘 집행자(불변식 ⑥ `VOCAB`)는 **어휘가 줄어드는 방향을 안 본다.** 교훈: **화면을 은퇴시킬 때 그 화면이 유일 소비처였던 «어휘»(모션·토큰·상태 이름)를 함께 세라.** 안 세면 없는 마디로 키프레임을 지을 수 있는 상태가 남는다.                                                              |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I010(상시 인입구) — 「개강 1회뿐」을 화면 문구·뷰 이름 문제로 보고 착수했다                                                               | **막고 있던 것은 문구가 아니라 `if (!item) return` 한 줄**이었다. 학사 눈금은 **학기의 속성**인데 과목 게이트가 그 위에 얹혀 있어서, 「4/6 휴강」을 붙여 넣고 버튼을 눌러도 아무 일이 안 일어났다(에러도 없다). 교훈: **「입구가 좁다」류 항목은 UI 를 고치기 전에 적용 경로의 early-return 부터 읽어라** — 그 한 줄이 화면 전체의 용도를 좁히고 있을 수 있고, 조용해서 화면으로는 안 보인다.                                                                                                      |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | 스냅샷 6장을 좁혀 재생성하려고 `npm run e2e -- -g … --update-snapshots=all` 을 썼다                                                       | **아무것도 재생성되지 않았고 전량이 그냥 다시 돌았다**(3분). `e2e` 스크립트는 `playwright test && playwright test -c motion` 이라 `npm run … --` 의 인자가 **뒤쪽 명령에만** 붙는다. 교훈: **좁혀 재생성할 때는 npm 스크립트를 경유하지 말고 `npx playwright test -g "…" --update-snapshots=all --workers=1` 을 직접 불러라.** (전량은 `npm run e2e:update` 가 맞다 — 그건 두 설정 각각에 플래그가 박혀 있다.)                                                                                     |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I028(하루의 한 칸 타입 통합) — 원장이 적은 «`done?` 유무가 유일 차이»를 믿고 병합 설계를 짜려 했다                                        | 스키마를 실제로 겹쳐 보니 **차집합이 6**이었다(Task 전용 다섯 · PlanEvent 전용 하나). 그리고 그 처방의 근거였던 «일정만 가용시간을 깎는다»는 `PlanEventSchema` 주석이 **2주 넘게 낡아 있었다** — 2026-08-07 의 N-1 이 시각 박힌 과제도 같은 축에 넣었다. 교훈: **원장의 «유일한 차이» 주장은 세어서 확인하라.** 리뷰 시점의 판단은 그 시점의 주석을 읽고 만들어지므로, 주석이 낡으면 처방도 함께 낡는다.                                                                                           |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I001(원장이 계획의 입력이 된다) — 새 lib 을 지으려고 파일을 팠다                                                                          | **이미 있었다**(`lib/ledgerSeed.ts` · 2026-07-31). 규율도 옳았다(«자동으로 안 찍는다»). 틀린 것은 **소비처가 하나**(`importVaultSubject`)라는 것이었고, 임포트는 1회인데 원장은 계속 자라므로 손으로 만든 과목엔 그 다리가 영원히 안 놓인다. 교훈: **「밖의 것이 앱에 안 들어온다」류를 만나면 먼저 `grep` 으로 「그걸 하는 함수가 이미 있나」와 「그 함수를 부르는 곳이 몇 개인가」를 세라.** 이 회차에서만 같은 형태가 **셋**이었다(I001 임포트 1회 · I010 개강 1회 · I013 이름이 없어 못 맞춤). |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I053(전공 밖 레인) — 보류 사유가 「표면이 틀렸다」인 줄 알았다                                                                            | 실제로는 **저장 자리**였다. `weekAlloc` 안에 두면 칸 하나가 `ensureWeekAlloc` 을 타고 그 주를 managed 로 승격시키지만, **최상위 키 하나**로 빼면 `ROW_SLICES` 를 안 지나 `settings` 한 행이 되고 새 표도 D1 DDL 도 0이다. 교훈: **「이 값을 어디에 두나」가 「이 값을 보여줄 것인가」보다 먼저다** — 이 저장소의 매퍼는 «선언 안 된 키는 자동으로 settings» 라는 확장점을 이미 갖고 있고, 그것을 모르면 멀쩡한 항목이 스키마 변경으로 보인다.                                                      |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I001 반영 줄을 붙이고 `-g "ledger"` 로 e2e 17장을 돌렸다 — **전부 통과**                                                                  | 통과의 뜻이 «안 깨졌다»가 아니라 **«한 번도 렌더된 적 없다»** 였다: 기본 픽스처의 과목 이름(미적분…)이 원장 픽스처(통신이론…)와 안 맞아 그 줄이 **정의상** 안 그려진다. 교훈: **새 UI 가 「데이터가 맞을 때만」 뜨는 종류면, 통과한 스냅샷 수는 커버리지의 증거가 아니다** — 픽스처를 파생시켜(`LEDGER_APPLY_SEED`) 렌더되는 상태를 먼저 잠가야 §15-4 를 지킨 것이다. ⚠ 기본 SEED 를 고치면 무관한 베이스라인이 통째로 움직이므로 **파생**이 답이다.                                               |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I024·I025·I026 — 「표면이 너무 많다」류 셋을 한 묶음으로 착수하려 했다                                                                    | **셋 중 하나만 참이었고, 나머지는 세는 법이 틀렸다.** I024 의 「하루 표면 일곱~아홉」은 근거로 인용한 주석의 «여섯 표면»이 실은 **한 화면의 부품 여섯**이었고(components 를 screens 로 셈), I026 의 「절반이 `null`」은 맞지만 이 저장소에서 `null` 은 **정직한 선언**이라 위반이 아니다. 교훈: **「N이 너무 많다」로 시작하는 항목은 N의 *단위*부터 확인하라** — 화면인가 부품인가, 선언인가 위반인가. 단위가 틀리면 처방은 옳은 것을 지운다.                                                     |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I026 을 재려고 `primary: null` 화면에서 `⇧G` 를 열어 봤다                                                                                 | **그 기능 자체에 결함이 있어 측정이 성립하지 않았다** — `readouts` 가 `primary` 분기 **안에** 중첩돼, 대표 수가 없는 아홉은 보조 수를 갖고 있어도 사과문만 떴다. 즉 «이 기능이 값을 내는가»를 그 기능이 자기 수를 가린 채로 재고 있었다. 교훈: **자기 검증 조건을 코드에 적어 둔 기능은, 그 조건을 재기 전에 그 코드가 조건을 공정하게 재는지부터 확인하라.**                                                                                                                                      |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I025 처방(«하위화면 문법을 라우트 하나로 재설계»)대로 새 문법을 지으려 했다                                                               | 최싼검증(도달성 표)을 실제로 찍으니 **문법은 이미 옳았고 12개 중 2개에만 적용돼 있었다.** `?view=` 로 접힌 화면이 로스터 밖이면 이름·⌘K·방문 집계 **셋을 한꺼번에** 잃는데, 그 셋은 각각 다른 감사(H-12·Q-22·I034)에서 따로 고쳐진 적이 있어 «이미 해결된 문제»로 보였다. 교훈: **같은 결함이 축마다 따로 고쳐졌다면, 그건 공통 원인이 안 고쳐졌다는 신호다** — 여기선 «로스터 등재»가 그 원인이었고 이제 불변식 ⑱ 이 막는다.                                                                      |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | I029 — 사용자 승인이 있으니 바로 착수하려 했다                                                                                            | 실측이 처방의 **방향**을 반증했다: 회계 축(44코스 · 학점·성적 전량 기입)은 **살아 있고**, 죽어 있는 것은 그 위의 **학기 국면 축**(학기 3개 중 `startDs` 0개 → `activeSemester` 가 늘 null)이었다. 처방대로 `Course` 를 `Item` 에 얹으면 과목 목록이 **5행 → 44행**이 된다. 교훈: **승인은 «그 방향»에 대한 것이다** — 실측이 방향을 뒤집으면 승인은 이월되지 않고, 반대 방향은 새로 물어야 한다.                                                                                                   |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | C043(저널 델타가 UTC 날짜로 센다)을 고치고 짝 테스트를 붙였다                                                                             | **`TZ=UTC` 에서는 이 부류의 판별력이 0이다** — 결함이 있는 옛 코드로 되돌려 재니 UTC 는 8/8 통과, KST/LA 매트릭스는 양방향 모두 실패했다                                                                                                                                                                                                                                                                                                                                                           | 개발 머신(KST)과 CI(UTC)가 **서로 다른 것을 재면서 둘 다 «전량 녹색»** 을 보고할 수 있다. 시간대 의존 테스트는 «어느 시간대에서 도는가»가 검사의 일부다                                                                                                                                                                                                                                                                                                                     |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | C051(타입 인지 비동기 린트) 위반 45곳에 린트가 준 **줄·열**로 `void` 를 자동 삽입했다                                                     | 넷은 속성 이름 앞에 붙어 파싱이 깨졌고(`void onClick:`), **셋은 파싱을 통과했다**: `run: void A.resetAll` 은 함수를 즉시 버리고 `undefined` 를 넘긴다 — `run?` 이 옵셔널이라 타입도 안 잡아 ⌘K 액션 셋이 무동작이 될 뻔했다                                                                                                                                                                                                                                                                        | 자동 수정은 **위치가 아니라 표현식**을 봐야 한다. 그리고 «파싱이 깨진 것»보다 «파싱을 통과한 것»이 위험하다 — 전자는 즉시 보이고 후자는 조용하다                                                                                                                                                                                                                                                                                                                            |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | 같은 항목 — `setInterval(void refresh, …)` 도 같은 형태였다                                                                               | 이쪽은 **타입이 잡았다**(`undefined` 는 `() => void` 에 대입 불가)                                                                                                                                                                                                                                                                                                                                                                                                                                 | 같은 실수가 한 곳에서는 타입에, 다른 곳에서는 아무 데도 안 걸렸다. 차이는 **그 자리가 옵셔널인가** 하나다                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | C057(뷰 콜백 래칫)의 임계를 실측 최댓값 **16** 으로 못박았다                                                                              | `npm run format` 이 C036 의 한 줄 호출을 5줄로 접어 **20**이 됐고, 방금 세운 래칫이 스스로 빨간불이 됐다                                                                                                                                                                                                                                                                                                                                                                                           | 크기 래칫의 임계는 **포맷 뒤**의 실측이어야 한다. 그리고 이 실패는 래칫이 실제로 동작한다는 증거이기도 하다                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | C052(TZ 매트릭스)를 `vitest.config.ts` 의 `test.projects` 로 넣으려 했다                                                                  | 하위 프로젝트가 루트의 `resolve.alias` 를 **상속하지 않아** 18파일이 `Cannot find package '@/lib/…'` 로 통째로 죽었다                                                                                                                                                                                                                                                                                                                                                                              | vitest `projects` 는 프로젝트마다 `resolve`·`server.fs` 를 줘야 한다. 그리고 커버리지 래칫의 분모를 흔들지 않으려면 **별도 설정 파일**이 낫다                                                                                                                                                                                                                                                                                                                               |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | C047(루트 리다이렉트) 뒤 `server` 테스트에서 「해시 붙은 번들」 케이스가 깨졌다                                                           | 그 케이스는 `/index.html` 을 읽었는데, 자산 워커가 그것을 `/` 로 **307 정규화**하고 있었고 `dispatchFetch` 가 그 리다이렉트를 **조용히 따라가고** 있었다 — 그 사실은 어디에도 안 적혀 있었다                                                                                                                                                                                                                                                                                                       | 테스트가 «우연히 통과하던 경로»에 기대고 있으면, 무관해 보이는 라우팅 변경이 그것을 드러낸다. 리다이렉트를 재려면 `redirect: 'manual'` 이 필요하다                                                                                                                                                                                                                                                                                                                          |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | C059(GuideView §③ 삭제) — 문서 절 하나를 지우는 일이라 여겼다                                                                             | **테스트 둘이 깨졌다**: `icons.test.ts`(`discovery` 글리프의 마지막 소비처가 사라져 「죽은 아이콘 0」 위반) · `guideTab.test.tsx`(「세 축」 단언)                                                                                                                                                                                                                                                                                                                                                  | 잔재 삭제의 파급은 «그 파일»이 아니라 «그 파일이 마지막으로 붙들고 있던 것»으로 퍼진다. 두 래칫이 둘 다 시끄럽게 잡았다 — 그게 그것들이 있는 이유다                                                                                                                                                                                                                                                                                                                         |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | C035 짝 테스트의 `.ics` 픽스처 제목을 「3주 보강」으로 지었다                                                                             | 「보강」이 **학사 눈금 어휘**라 그 이벤트가 단발 눈금으로 착지했고, `unparsed` 가 0이 되어 **고쳐진 것처럼 보였다**                                                                                                                                                                                                                                                                                                                                                                                | 파서 픽스처의 **텍스트가 다른 갈래를 밟을 수 있다**. 「버렸다」를 재려면 어느 갈래에도 안 걸리는 중립적 입력을 써라                                                                                                                                                                                                                                                                                                                                                         |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | C033 처방대로 `Tab` 에 `k: TabKey` 유니온을 주려 했다                                                                                     | **그런 타입이 없다** — `TabMeta.key` 는 `string` 이고 `TABS` 는 `as const` 가 아니다. 만들려면 로스터를 읽는 곳 전부가 딸려 온다                                                                                                                                                                                                                                                                                                                                                                   | 「타입 에러가 되게 하라」는 처방은 **그 타입이 실재하는지** 먼저 확인해야 한다. 없으면 같은 보장을 게이트 층(불변식)에 두는 것이 범위를 지키는 길이다                                                                                                                                                                                                                                                                                                                       |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | C048 처방대로 `lib/contentSearch` 가 `shell/tabs` 에서 경로를 읽게 하려 했다                                                              | `boundaries` 가 `lib → lib` 만 허용한다 — 그 경계는 **옳다**(순수 규칙이 셸을 끌면 폰 번들·테스트가 함께 무거워진다)                                                                                                                                                                                                                                                                                                                                                                               | 레이어를 뚫는 대신 **테스트가 둘 다 본다**는 사실을 쓴다. 테스트는 레이어 밖이라 «두 곳의 값이 같은가»를 잴 수 있는 유일한 자리다                                                                                                                                                                                                                                                                                                                                           |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | 파이썬 한 줄 패치로 파일을 고치며 `open(p,'w')` 를 썼다                                                                                   | 인코딩 에러가 나면 **`open('w')` 이 이미 파일을 비운 뒤**라 `test/quickCapture.test.ts` 가 0바이트가 됐다                                                                                                                                                                                                                                                                                                                                                                                          | 텍스트를 **먼저 인코딩하고** 바이트로 쓴다(`open(p,'wb').write(s.encode())`). 그리고 힙 도구 대신 `git checkout --` 으로 즉시 복구되는지 확인해 두어라                                                                                                                                                                                                                                                                                                                      |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | C034 로스터 대조를 짜며 TS 호출 이름을 정규식으로 뽑았다                                                                                  | `call('…')` 만 잡고 **`call<T>('…')` 의 제네릭을 못 받아** «Rust 에만 9건» 이라는 거짓 드리프트를 냈다                                                                                                                                                                                                                                                                                                                                                                                             | 크로스언어 대조는 **양쪽 추출이 비어 있지 않은지**를 먼저 단언해야 한다. 그리고 관용구가 하나라고 가정하지 마라(여기선 `core.invoke` 직접 호출이 둘 더 있었다)                                                                                                                                                                                                                                                                                                              |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | C054(수렴 property)에 원장 처방대로 `fast-check` 를 넣으려 했다                                                                           | 상태 공간이 «연산 6개의 순열 = 720» 이라 유한하고 작았다 — **전수 열거가 랜덤 100표본보다 강하다**(축소도 불필요). 그리고 모델을 TS 로 다시 쓰면 «내 모델이 내 모델과 같다»를 증명하게 된다                                                                                                                                                                                                                                                                                                        | 새 의존을 들이기 전에 **상태 공간의 크기**를 재라. 그리고 property test 의 값은 생성기가 아니라 **무엇을 실행하느냐**에 있다 — 여기선 Node 내장 `node:sqlite` 로 `contract.ts` 의 진짜 SQL 을 돌렸다(새 의존 0)                                                                                                                                                                                                                                                             |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | C041(드레인 되읽기)을 고치며 회차마다 되읽기만 뺐다                                                                                       | 되읽기를 빼면 **DB 는 병합-후 · 기준선은 병합-전**이 되어, 그 사이의 flush 가 받아온 행을 되돌리는 upsert 를 만든다(C-1 이 고친 바로 그 형태)                                                                                                                                                                                                                                                                                                                                                      | 성능 수정이 **안전 장치의 부수효과**를 함께 걷는지 봐라. 여기서는 되읽기를 빼되 «기준선 무효화 + 병합 창»은 회차마다 그대로 세워야 했다                                                                                                                                                                                                                                                                                                                                     |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | C067(비동기 실패를 보는 층) — 「전 화면을 한 바퀴 돌며 센다」는 최싼검증을 그대로 했다                                                    | **0건**이 나왔는데, 그 0은 「없다」가 아니라 **「이 순회는 안 누른다」**였다 — 대상인 `onClick={async …}` 69곳은 **눌러야** 거부가 난다                                                                                                                                                                                                                                                                                                                                                            | 「N건인가」를 재기 전에 **그 N이 발생하는 조건**을 확인하라. 감시는 _열어 보는_ 층이 아니라 **누르는 층**(트랙 A 전량)에 달아야 했고, 그러자 69곳이 한 번에 덮였다                                                                                                                                                                                                                                                                                                          |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | 같은 항목 — 실측이 0이라 전역 `unhandledrejection` 핸들러(원장의 처방)를 만들려다 멈췄다                                                  | 항목 자신이 _"0이면 표면을 안 만들어도 되고"_ 라 적어 뒀다                                                                                                                                                                                                                                                                                                                                                                                                                                         | 없는 실패를 위해 표면을 만들면 그게 이 저장소가 반복해 만든 **「있는 것처럼 보이는 배관」**이다. 처방을 실행하기 전에 **그 처방의 발동 조건**을 다시 읽어라                                                                                                                                                                                                                                                                                                                 |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | C057 래칫(뷰 콜백 줄 수)을 세우고 C068 로 규칙을 `lib` 으로 내렸다                                                                        | 그 자리에 남긴 **2줄짜리 근거 주석** 때문에 래칫이 20 → 17 밖에 안 내려갔다 — **근거를 적는 쪽이 벌을 받는 형태**                                                                                                                                                                                                                                                                                                                                                                                  | 크기 래칫은 **주석·빈 줄을 걷고** 세라(불변식 ⑰이 같은 이유로 `strip` 한다). 걷고 재니 22 → 14 였다                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | 같은 래칫 — 임계를 실측 최댓값으로 못박았다                                                                                               | `npm run format` 이 한 줄 호출을 5줄로 접어 **방금 세운 래칫이 스스로 빨간불**이 됐다                                                                                                                                                                                                                                                                                                                                                                                                              | 크기 래칫의 임계는 **포맷 뒤**의 실측이어야 한다. 그리고 「임계가 최댓값에 붙어 있다」를 함께 단언하면 이 부류를 두 번 다 잡는다 — 그 케이스를 지우지 마라                                                                                                                                                                                                                                                                                                                  |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | 트랙 A 전량에 비동기 실패 감시를 달고 213케이스를 돌렸다                                                                                  | **0건** · 다만 `visual.spec.ts` 의 `settings · dark` 가 1회 flaky 로 잡혔다                                                                                                                                                                                                                                                                                                                                                                                                                        | 감시 유무와 무관하게 재현되지 않았다(각각 6·8회 반복 통과). 새 감시를 단 회차의 flaky 는 **감시 없이도 재현되는지** 먼저 갈라라 — 안 그러면 새 장치를 의심하며 시간을 쓴다                                                                                                                                                                                                                                                                                                  |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | V023 — `tabs.ts` 주석이 「도달성: ⌘K · `/guide` 딥링크 · 찾기 버튼」이라기에 `rg "'/guide'"` 로 확인했다                                  | **0건**이 나와 「주석이 거짓」이라 읽을 뻔했다. 실제로는 그 라우트가 **생성된다**: `App.tsx:173` 이 `role:'view'` 로스터 행에서 `<Route path={'/' + t.key}>` 를 만든다                                                                                                                                                                                                                                                                                                                             | **생성되는 것을 리터럴로 찾으면 없다고 나온다.** 「이 경로가 있나」를 물을 때 라우트 **생성기**부터 읽어라 — 이 저장소는 로스터에서 라우트·팔레트·리다이렉트를 전부 파생시킨다                                                                                                                                                                                                                                                                                              |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | 같은 항목 — `MiniHud:58` 이 「전 저장소에 `inert` 0건」이라는데 실측은 3건이었다                                                          | 그 문장이 **과거시제**였다(_"…0건이었다"_). 같은 커밋의 `State.tsx` 가 그 뒤에 `inert` 를 들였다                                                                                                                                                                                                                                                                                                                                                                                                   | 「주석이 사실과 다르다」류를 받으면 **시제**부터 확인하라. 이 저장소의 주석은 대부분 «그때 그랬다»의 기록이고, 현재형으로 읽으면 멀쩡한 근거가 결함으로 올라온다                                                                                                                                                                                                                                                                                                            |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | V032(TS 문자열 속 생 hex) — 리뷰 실측 「3파일」을 그대로 믿고 검사기를 짰다                                                               | 주석을 걷으니 **2파일**이었다. 매치 대부분이 _"그때 그 값이 틀렸다"_ 를 적는 **근거 문단**이었다                                                                                                                                                                                                                                                                                                                                                                                                   | 「hex 가 N곳」류의 수는 **주석 포함인지** 먼저 물어라. 그리고 검사기는 주석을 걷어야 한다 — 안 걷으면 옛 값을 인용하는 근거가 위반이 되어, 이 저장소가 다섯 번 못박은 역인센티브를 다시 만든다                                                                                                                                                                                                                                                                              |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | V033 — 「`ThemeProvider` 의 폴백 근거가 `lib/ledger.ts` 와 정면 충돌」이라 한쪽이 틀린 줄 알았다                                          | 충돌은 진짜였지만 답은 「둘 중 하나가 틀렸다」가 아니라 **「근거가 틀렸다」**였다: 코드는 둘 수 있고(지우면 `content=""`) 이유만 거짓이었다(`useEffect` 안이라 토큰이 이미 서 있다)                                                                                                                                                                                                                                                                                                                | 「A 와 B 가 충돌한다」를 받으면 **셋째 가능성**을 보라 — 코드는 옳은데 *왜 그런지*가 틀린 경우다. 그때 처방은 삭제가 아니라 **근거 정정 + 진짜 판단의 만료일 예약**이다                                                                                                                                                                                                                                                                                                     |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | V025(`dist/` 가 커밋돼 있다)를 고치려고 `.gitignore` 부터 열었다                                                                          | `git ls-files dist` → **0** · `git log -- web/dist` → **이력 없음** · `.gitignore` 에 이미 있었다. **한 번도 추적된 적이 없다**                                                                                                                                                                                                                                                                                                                                                                    | 「X 가 커밋돼 있다」류는 `git ls-files` **한 줄**로 먼저 확인하라. 이 항목은 근거를 안 재고 올라왔고, 안 재보고 고쳤으면 있지도 않은 것을 지우는 커밋이 됐다                                                                                                                                                                                                                                                                                                                |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | V026(카드 래칫 「여유 0이라 막힌다」)을 완화하려 했다                                                                                     | 실측은 **21/25 = 여유 4** — 방향이 반대였다. 코드 축의 삭제들이 표면 넷을 걷은 뒤였다                                                                                                                                                                                                                                                                                                                                                                                                              | 래칫 항목을 받으면 **지금 값을 먼저 재라.** 「막힌다」와 「헐겁다」는 처방이 정반대이고, 둘 다 같은 문장(「여유가 N이다」)에서 나온다                                                                                                                                                                                                                                                                                                                                       |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | V029(문서 1.7MB 삭제 · `로드맵.md` 685KB 포함)를 처방대로 실행하려 했다                                                                   | `로드맵.md` 의 **인바운드 참조가 71건**(19개 문서)이었다 — 지우면 그 인용이 전부 매달린다                                                                                                                                                                                                                                                                                                                                                                                                          | 문서 삭제 전에 **참조를 세라**(`rg -l <이름>`). 그리고 「완료」를 스스로 선언한 것만 지워라 — `예약봉투-3일검증.md` 는 그 선언이 없는 **가설 문서**였고, 지웠으면 살아 있는 아이디어가 사라졌다                                                                                                                                                                                                                                                                             |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | 같은 항목 — 발주서 둘을 지우고 끝낸 줄 알았다                                                                                             | `결정로그.md`·`로드맵.md` 의 **네 곳**이 그것을 「정본」이라 가리키고 있었다                                                                                                                                                                                                                                                                                                                                                                                                                       | 삭제의 파급은 파일이 아니라 **그것을 정본이라 부르던 문장**으로 간다. 지운 뒤 `rg` 한 번 + 복구 명령(`git show <SHA>:<경로>`)을 그 자리에 남겨라                                                                                                                                                                                                                                                                                                                            |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | V023 — `tabs.ts` 주석이 「`/guide` 딥링크로 닿는다」기에 `rg "'/guide'"` 로 확인했다                                                      | **0건**이었지만 주석이 맞았다 — 그 라우트는 `App.tsx:173` 이 `role:'view'` 로스터 행에서 **생성**한다                                                                                                                                                                                                                                                                                                                                                                                              | **생성되는 것을 리터럴로 찾으면 없다고 나온다.** 이 저장소는 라우트·팔레트·리다이렉트를 전부 로스터에서 파생시킨다 — 「이 경로가 있나」는 **생성기**부터 읽어라                                                                                                                                                                                                                                                                                                             |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | V024 검사를 「워드마크 조각이 한 줄인가」로 짰다                                                                                          | `MARK_PART` 가 `whitespace-nowrap` 이라 조각은 **원리적으로 줄바꿈하지 않는다** — 폭이 없으면 넘친다. **영원히 초록인 검사**였다                                                                                                                                                                                                                                                                                                                                                                   | 새 검사를 짜면 **그 단언이 거짓이 될 수 있는 경로**를 먼저 만들어 보라. 여기서는 폭을 `1ch` 로 조여 넘침을 확인하고 나서야 관계가 옳아졌다                                                                                                                                                                                                                                                                                                                                  |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | C025 검사를 「그 파일이 그 키를 언급하는가」로 짰다                                                                                       | 단일 문자(`'q'`)가 `params.get('q')` 같은 곳에 흔해 **검출력이 0**이었다 — 없는 키 `Q` 를 심어도 통과했다                                                                                                                                                                                                                                                                                                                                                                                          | 「언급한다」는 관계가 너무 약할 때가 있다. 키 관용구 **다섯**을 읽는 추출기로 바꿔야 잡혔고, 그중 둘(`k: ' '` · `*_KEY = 'm'`)은 **오탐이 나서야** 알았다 — 관용구 목록은 실측으로만 모인다                                                                                                                                                                                                                                                                                 |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | 프로브로 파일을 바꿨다가 `git checkout -- <파일>` 로 되돌렸다                                                                             | 그 파일에 있던 **다른 회차의 수정**(C051 의 `void resetAll()`)이 함께 날아갔다 — 게이트가 잡았다                                                                                                                                                                                                                                                                                                                                                                                                   | `git checkout --` 는 프로브만이 아니라 **그 파일의 모든 변경**을 되돌린다. 프로브는 «심고 되돌리는» 스크립트 쌍으로 짜고, 되돌린 뒤 `git diff --stat <파일>` 로 남은 변경이 그대로인지 확인하라                                                                                                                                                                                                                                                                             |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | P023~P027(성능 팩)을 「고칠 것」으로 보고 시작했다                                                                                        | 다섯 중 **셋이 기각/측정완료**로 끝났다: 히트맵 `<i>` **191개** · flush 재직렬화 **6.1ms/10k행**(디바운스의 1.5%) · 서버 본문 두 번 읽기 **512KB 에서 2.8ms**                                                                                                                                                                                                                                                                                                                                      | 성능 지적은 **먼저 재고** 판단하라. 이 다섯 측정은 전부 10분 안에 끝났고(순수 함수 벤치 · 실 산출물 파싱 · roundtrip 하네스), 그 10분이 없어서 네 항목이 몇 주를 열린 채 있었다                                                                                                                                                                                                                                                                                             |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | D024 에서 에코 억제 키의 이름공간 구분자로 NUL(`�`)을 썼다                                                                                | **불변식 ⑮**(소스에 원시 NUL 금지)이 잡았다 — 파이썬 한 줄 패치에서 `\\u0000` 이 실제 NUL 바이트가 돼 파일에 박혔다                                                                                                                                                                                                                                                                                                                                                                                | 구분자는 **인쇄 가능한** 것으로 고르라(`tomb#`). 그리고 이스케이프를 포함한 패치는 쓴 **뒤에** 바이트로 확인하라(`grep -c $'\\x00'`)                                                                                                                                                                                                                                                                                                                                        |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | D020(스키마 핸드셰이크)을 「헤더 1줄」로 보고 착수하려 했다                                                                               | 프로토콜은 실제로 싸다(양쪽이 `src-tauri/migrations/` 를 **공유**한다 — `wrangler.jsonc` 의 `migrations_dir`). 그런데 **「오류가 아닌 경고」 채널이 없다**(지금은 `PermanentPushError` = 실패 뒤뿐)                                                                                                                                                                                                                                                                                                | 「1줄이면 된다」는 제안은 **그 값을 받을 자리**가 있는지 먼저 보라. 없으면 만드는 것은 표면이고, 그건 1줄이 아니다 — 그리고 표면 없이 프로토콜만 넣으면 그게 이 저장소가 반복해 지운 배관이다                                                                                                                                                                                                                                                                               |
| 2026-08-06~19                                                                                                                                                                                                                                                                                                                               | gitleaks 스캔이 도는 줄 알았다(CI `secret-scan` 잡 · 「버전 핀 + 체크섬 대조」)                                                           | **도입 이래 0회 돌았다.** `-o gitleaks.tar.gz` 로 이름을 바꿔 받아 놓고 `sha256sum -c` 에 **원래 이름이 적힌 줄**을 먹였다 — 그 도구는 줄에 적힌 경로를 열므로 «그런 파일이 없다»로 죽었다. 대조 자체는 옳았고 **이름만** 어긋나 있었다                                                                                                                                                                                                                                                            | **체크섬 대조는 파일명까지 계약이다.** ⚠ 그리고 이번 회차에서 같은 함정의 **다른 얼굴**을 또 만났다: cargo-deny 의 `.sha256` 은 해시 **한 덩어리만**(대문자·파일명 없음) 담아 `sha256sum -c <파일>` 이 원리적으로 안 된다 — 줄을 직접 조립해야 한다. **받아서 한 번 돌려 보기 전엔 워크플로에 적지 마라**                                                                                                                                                                   |
| 2026-07-19~08-01                                                                                                                                                                                                                                                                                                                            | `cloud-server` 잡이 인증·입력검증 층을 지켜 준다고 믿었다                                                                                 | **13일·8런 연속 빨간불**이었다(`Cannot find module 'zod/mini'` 외 16). 원인은 설계 그 자체 — server 는 web 의 zod 스키마를 **문자 그대로 공유**하는데 CI 는 `server/` 만 설치했다. 즉 «server 만 설치하면 된다»는 전제가 구조와 모순이었다                                                                                                                                                                                                                                                         | **«CI 엔 있는데 로컬엔 없다»는 곧 «아무 데도 없다»** — 그리고 그 잡이 _빨간 채로_ 있으면 «있다»조차 아니다. 이 저장소가 네 번 물린 형태이고, 그래서 지금은 `gate.mjs` 가 `server verify`·`cargo test`·`tauri:fmt/clippy` 를 전부 로컬에서도 돌린다                                                                                                                                                                                                                          |
| 2026-08-01                                                                                                                                                                                                                                                                                                                                  | v0.3.0 을 배포했다 — 매니페스트도 서명도 유효했다                                                                                         | `/updates/…exe` 가 **1433 B SPA 폴백 HTML** 을 줬다. `release:stage` 를 건너뛴 배포라 **매니페스트가 가리키는 파일이 없었고**, v0.2.0 사용자는 업데이트를 제안받고 minisign 검증에 실패했다. ⚠ 정적 자산은 `run_worker_first` 밖이라 **워커를 안 탄다** → `app.onError` 에도 Workers Logs 에도 0줄이다                                                                                                                                                                                             | **순서를 계약이 아니라 의존으로.** 그게 `predeploy` 훅이고, 이번 회차에 그 **짝인 `postdeploy`** 도 붙였다(O011) — 배포 후 도달 확인이 「⚠ 건너뛰지 마라」라는 문장으로만 지켜지고 있었다. ⚠ 상태코드로는 원리적으로 판정 불가다(폴백이 무엇을 물어도 200)                                                                                                                                                                                                                  |
| 2026-08-20~22                                                                                                                                                                                                                                                                                                                               | CI 가 번들 예산·스모크·a11y 를 지켜 준다고 믿었다                                                                                         | **이틀간 셋 다 skipped 였다.** `test:coverage` 하나가 죽자 뒤 5스텝이 통째로 건너뛰어졌고, 그 실패는 UI 회귀가 아니라 **무관한 Node 버전 차이**였다(`node:sqlite`). 같은 형태를 C2 가 이미 진단했는데 그때 처방(폭포 차단)은 `shell-build` 에만 갔다                                                                                                                                                                                                                                               | **skipped 는 통과가 아닌데 화면에서 초록으로 읽힌다.** 서로 독립인 축은 각자 돌게 하라(`if: !cancelled()`). ⚠ 그리고 «처방을 짝에도 적용했는가»를 물어라 — 이 회차 지적 Major 열 중 **여덟**이 그 형태였다(R3)                                                                                                                                                                                                                                                              |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | O010 — 볼트 감시가 죽는 것을 테스트로 잡으려고 감시 중인 폴더를 지웠다                                                                    | **`Path::exists()` 가 계속 `true` 를 줬다.** Windows 는 열린 핸들이 있는 디렉터리를 지우면 «삭제 보류» 상태로 두고 **이름은 남긴다** — 테스트가 5초 타임아웃으로 죽었고, 처음엔 코드가 아니라 테스트가 이상한 줄 알았다                                                                                                                                                                                                                                                                            | **«있나»가 아니라 «아직 읽을 수 있나»를 물어라**(`read_dir(dir).is_err()`). 그게 이 감시의 유용성 그 자체이기도 하다 — 못 읽는 폴더의 감시는 살아 있어도 쓸모가 없다. ⚠ 그리고 그 판정은 **연속 2회**를 요구한다: 일시적 잠금으로 감시를 걷으면 스스로 낫는 상태에 사용자를 부른다                                                                                                                                                                                          |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | O001 — 리포트가 준 `keyIdOf` 스니펫을 공개키·서명 **양쪽에** 그대로 썼다                                                                  | 멀쩡한 키를 **불일치라 신고했다.** 리포트는 공개키에 `.pop()`, 서명에 `[1]` 을 썼는데 그걸 `.pop()` 하나로 일반화한 것이 원인이다 — minisign 공개키 블록은 **2줄**(주석+키)인데 서명 블록은 **3줄**이다(끝이 `trusted comment: timestamp:… file:…`)                                                                                                                                                                                                                                                | **리포트의 코드는 처방이지 정답이 아니다**(§3 «처방을 그대로 베끼지 마라»). 그리고 실물 바이트로 한 번 돌려 보지 않았으면 **반대 방향의 거짓 경보**를 게이트에 심을 뻔했다 — 그 형태가 더 나쁘다(진짜 사고 때 «또 오탐이겠지»가 된다)                                                                                                                                                                                                                                       |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | O006 — `freshness.mjs` 로 미배포 델타를 재고 「0커밋」을 얻었다                                                                           | 실제로는 **15커밋**이었다. `git log -- web/src` 를 `web/` 에서 실행해 경로가 cwd 기준으로 풀렸고(`web/web/src`), git 은 **말없이 0을 줬다**                                                                                                                                                                                                                                                                                                                                                        | **재는 축이 자기 자신을 못 재면 그 파일 전체가 장식이다.** 새 계기판을 만들면 «지금 알고 있는 참값」과 한 번 대조하라 — 여기서는 리포트가 이미 «15커밋 · 14일»을 실측해 뒀고, 그게 없었으면 0을 그대로 믿었다                                                                                                                                                                                                                                                               |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | O015(데스크톱에 부팅 1회 동기화가 없다)를 고치려고 `StorageGuard` 를 열었다                                                               | **재현되지 않았다.** `installSyncTriggers` 가 설치 직후 `run()` 을 부르고 있었고(`syncController.ts:411` · 주석까지 «설치 즉시 1회 — 열었다 = 최신을 보고 싶다»), `git log -S` 로 그것이 **2026-07-24** 부터임을 확인했다 — 리뷰보다 한 달 전이다                                                                                                                                                                                                                                                  | 「트리거 넷이 전부 전이 이벤트다」류는 **그 함수의 끝까지 읽어라.** 트리거 «등록» 목록만 보면 등록 뒤의 즉시 호출이 안 보인다. ⚠ 재현 불가는 실패가 아니라 정상이고, 그 자체가 리뷰 품질의 신호다                                                                                                                                                                                                                                                                           |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | U041 — a11y 대비 미측정 래칫의 30초 타임아웃을 더 큰 상수로 올리려 했다                                                                   | 30초가 틀린 값이 된 **이유**가 「로스터가 자랐다」였다 — 상수를 상수로 바꾸면 같은 실패를 뒤로 미룰 뿐이다. 로컬 15.7초 · CI 2코어에서 초과                                                                                                                                                                                                                                                                                                                                                        | **고정 숫자가 틀렸을 때 더 큰 고정 숫자는 처방이 아니다.** 로스터 길이에서 유도하라(`TABS.length * 화면당_예산`) — 이 저장소가 손베낌으로 반복해 물린 형태의 시간판이다                                                                                                                                                                                                                                                                                                     |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | 운영 축 전반 — 「기대 주기」 게이트(`freshness`)를 게이트에 넣었다                                                                        | 즉시 **세 축이 빨간불**이었다(배포 14일/15커밋 · CI 미해결 빨간불 · 봇 활동 0). 셋 다 참이고 셋 다 이 세션이 코드로 못 닫는다(릴리스·push·외부 설정)                                                                                                                                                                                                                                                                                                                                               | **시간 축 게이트는 켜는 순간 빨갛다 — 그게 정상이고 그때 끄면 영영 안 돌아온다.** 처방은 `                                                                                                                                                                                                                                                                                                                                                                                  |                                                                                                                                                                                                                                                                                  | true` 가 아니라 **사유+재검토일 원장**이다(`audit-allowlist` 와 같은 관용구 + 해소되면 지우라고 강제하는 역래칫). ⚠ 그 원장이 없으면 이 부류 게이트는 도입 2주 안에 무력화된다 |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | O007 — 프런트 로그 다리를 트랙 B 로 검증하려고 `<데이터폴더>/learning-hub.log` 를 읽었다                                                  | **비어 있었다.** 「다리가 죽었다」로 읽고 원인 셋(플러그인·ACL·부팅 배선)을 뒤지려던 참에 폴더를 열어 보니 실물은 **`러닝허브.log`** 였다 — `TargetKind::Folder { file_name: None }` 의 기본값은 DB 이름이 아니라 **`productName`** 이다. 다리는 처음부터 멀쩡했다                                                                                                                                                                                                                                 | **산출물 파일 이름을 짐작하지 마라 — 폴더에서 찾아라.** ⚠ 이 오진의 방향이 특히 나쁘다: 「멀쩡한 것을 고장으로」 읽으면 멀쩡한 코드를 뜯게 되고, 그 과정에서 진짜 회귀를 심는다. ⚠ 짝으로 배운 것: 로그 폴더는 **SD-6 override 밖**이었다(`app_log_dir()` 은 식별자 기준) — 검증이 사용자의 실제 로그를 만질 뻔했고, `paths.rs::log_dir` 이 그것을 막는다                                                                                                                   |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | O018 — `cargo install cargo-deny` 를 「버전 핀 + 릴리스 바이너리 + 체크섬 대조」로 바꾸고 **로컬에서 실물로 한 번 돌려** 검증했다         | 러너에서 죽었다. 내려받기·체크섬·tar 레이아웃은 전부 맞았는데(`…tar.gz: OK` 가 러너 로그에도 찍혔다) **`-C` 대상만** 안 맞았다: `$USERPROFILE` 은 윈도 경로라 GNU tar 가 `C:` 를 원격 호스트로 읽어 `C\:\Users\runneradmin/.cargo/bin: Cannot open` 으로 죽는다. 로컬 시험은 `-C` 를 **POSIX 임시폴더**로 줬었다                                                                                                                                                                                   | **「실물로 한 번 돌렸다」의 «실물»에 환경 차이가 포함되는지 물어라.** 여기서 검증한 것은 «내려받기·대조·압축 해제»였고 안 잰 것은 «그 러너의 경로 문법»이었다. ⚠ 처방은 `cygpath -u`(`$CARGO_HOME`·`$USERPROFILE` **둘 다** 윈도 경로다). ⚠ 그리고 이 실패가 **폭포로 다섯을 skip 시켰다**(`tauri:deny`·`tauri:test`·번들·e2e) — 같은 회차에서 `build-test` 에 폭포 차단을 넣으면서 `shell-build` 에는 안 넣은 것이 즉시 청구됐다(R3 를 나 자신에게 적용 못 했다)           |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | 운영 축 실행이 Dependabot 을 들이고 푸시했다 — 「실행 흔적이 남는다」가 목적이었다                                                        | 흔적은 남았는데 **계정이 과금 차단됐다**: 봇이 브랜치 일곱을 밀자 `web-ci` 가 **PR 하나당 2런**(브랜치 `push` + `pull_request`)으로 발화했고, 잡 애노테이션이 _"The job was not started because recent account payments have failed or your spending limit needs to be increased"_ 를 냈다. ⚠ 같은 회차의 리포트가 «30일 2,141 과금분 / Free 한도 2,000분»을 이미 쟀지만 청구 API 스코프가 없어 **`[추측]`으로 남겨 둔** 축이다                                                                    | **자동화를 들일 때 「그것이 무엇을 트리거하나」를 함께 세라.** 이 저장소는 PR 0 · 브랜치 1 이라 `push` 에 `branches` 필터가 없는 사각이 **한 번도 청구된 적이 없었고**, 봇을 넣는 순간 그 사각이 곧 비용이 됐다. ⚠ 처방 둘: `push: branches: [master]`(중복 제거) + PR 에서 `shell-build` 제외(전체 과금분의 **73%** · windows 2배 과금 · npm 범프는 Rust 를 안 바꾼다). ⚠⚠ 그리고 **`[추측]` 이라 적어 둔 비용 축이 가장 먼저 터졌다** — 「못 쟀다」는 「괜찮다」가 아니다 |
| 2026-08-22                                                                                                                                                                                                                                                                                                                                  | `freshness.mjs` 의 **역래칫**(해소된 보류 항목을 지우라고 강제)을 세우고 «작동한다»고 여겼다                                              | **한 번도 안 울렸다.** `나쁨 = 늦음                                                                                                                                                                                                                                                                                                                                                                                                                                                                |                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | r.즉시실패`인데`즉시실패`가 «사유 문자열 또는`null`» 이라, 건강한 행에서 `나쁨`이`false` 가 아니라 **`null`** 이 된다. 역래칫은 `본[이름] === false`로 비교하므로`null`은 안 걸린다 — CI 가 실제로 녹색이 된 뒤에도 **낡은 보류 항목이 그대로 통과**했고, 게이트는`✅` 를 찍었다 | **`                                                                                                                                                                            |     | `로 만든 불리언에`=== false`를 쓰지 마라.** 피연산자 하나라도`null`·`undefined`·`''` 이면 결과가 그 값 그대로 나온다(`Boolean()` 으로 좁혀라). ⚠⚠ 그리고 이건 **집행자 자신이 「0건인데 아무것도 안 재는 검사」가 된** 형태다 — 이 저장소가 반복해 물린 그 부류를, **그 부류를 막으려고 만든 파일이** 재현했다. 새 래칫을 세우면 **해소 경로를 한 번 밟아 봐라**(여기서는 CI 를 녹색으로 만들고 실제로 빨간불이 뜨는지) |
| 2026-08-23                                                                                                                                                                                                                                                                                                                                  | U042 — 본문 샷을 `#main` 으로 좁히고 **크롬 잠금**(전체 페이지 샷 2장)을 세워 레일 커버리지를 지켰다고 여겼다                             | **그 잠금이 아무것도 안 잠갔다.** 레일을 실제로 흔들어 보니(라벨 두 줄) `chrome-dark`·`chrome-light` **둘 다 통과**하고 엉뚱하게 `shortcuts-help` 하나만 깨졌다. 원인은 `maxDiffPixelRatio: 0.005` 가 **이미지 전체에 대한 비율**이라는 것 — 1280×720 에서 임계가 4,608px 이고, 레일 변화는 전체 페이지 샷 안에서 **희석돼 임계 아래로** 떨어진다                                                                                                                                                  | **작은 것을 지키려면 작게 찍어라.** 크롬 잠금을 `nav[aria-label]`·`header` 로 좁히자 같은 교란에서 즉시 발화했다. ⚠ 이 저장소엔 같은 논리가 이미 있었다 — `motion.spec` 의 맥동이 «자기 클립»을 쓰는 이유가 정확히 이 희석 문제다(R3: 관용구는 있었고 한쪽에만 적용돼 있었다)                                                                                                                                                                                               |
| 2026-08-23                                                                                                                                                                                                                                                                                                                                  | 같은 항목 — 첫 교란으로 레일 라벨을 길게 바꿔 「레이아웃이 밀리는가」를 재려 했다                                                         | **레일이 라벨을 잘라내(truncate) 레이아웃이 안 밀렸다.** 그런데 `shortcuts-help` 가 깨져서 «교란이 통했다»고 오독할 뻔했다 — 그건 레이아웃이 아니라 **치트시트의 텍스트**가 바뀐 것이었다                                                                                                                                                                                                                                                                                                          | **교란이 의도한 것을 실제로 일으켰는지 확인하라.** 「무언가 깨졌다」는 「내가 겨눈 것이 깨졌다」가 아니다. 결국 유효한 교란은 레일 행 높이(`py-2 → py-4`)였고, 그때서야 숫자가 나왔다                                                                                                                                                                                                                                                                                       |
| 2026-08-23                                                                                                                                                                                                                                                                                                                                  | 같은 항목 — 교란을 걸어 둔 채 `--update-snapshots` 로 크롬 베이스라인을 만들었다                                                          | **깨진 상태가 정답으로 굳을 뻔했다.** 두 줄짜리 레일이 베이스라인이 된다는 뜻이다. 커밋 전에 알아채고 원복 → 재빌드 → 재생성했다                                                                                                                                                                                                                                                                                                                                                                   | CLAUDE.md 가 적은 _«`--update-snapshots` 는 깨진 결과를 정답으로 굳힌다»_ 는 **실험 중에 가장 위험하다** — 교란과 재생성이 같은 세션에 섞이기 때문이다. **교란은 반드시 원복하고 빌드까지 되돌린 뒤 재생성한다**                                                                                                                                                                                                                                                            |
| 2026-08-23                                                                                                                                                                                                                                                                                                                                  | Dependabot PR 이 `knip` 에서 깨졌기에 **의존성 범프가 뭘 부쉈나**를 찾으려 했다                                                           | 부순 게 없었다 — 범프 목록에 **`knip` 자신**이 있었다(`6.27.0 → 6.32.2`). 새 버전이 «배럴이 재수출만 하고 아무도 그 경로로 안 쓰는 이름» 여덟을 새로 잡았다. ⚠ 처음엔 「죽은 export」로 읽고 지우려다, 참조를 세어 보니 **여덟 다 참조가 5~22건**이었다 — 죽은 게 아니라 **소비처가 원본 모듈에서 직접 가져가고 배럴 입구만 남아돌던 것**                                                                                                                                                          |
| **린터가 깨지면 「내 코드가 나빠졌나」 전에 「린터가 올라갔나」를 먼저 보라.** 범프 목록에 그 도구가 있으면 그게 1순위 용의자다. ⚠⚠ 그리고 **`unused export` 를 「참조 0」으로 읽지 마라** — knip 이 말하는 것은 «이 **입구**로 들어오는 소비처가 없다» 이지 «이 이름이 안 쓰인다»가 아니다. 참조를 세지 않고 지웠으면 멀쩡한 코드를 지웠다 |
| 2026-08-23                                                                                                                                                                                                                                                                                                                                  | Dependabot PR 을 로컬 재현하려고 `npm ci` 를 돌렸다                                                                                       | **node_modules 가 반쯤 지워진 채 죽었다**(`EPERM: unlink lightningcss…node` · `.bin` 0개 · 패키지 76개만 남음). 원인은 **2026-08-21 부터 떠 있던 `vite preview`(PID 22604 · 포트 4199)** 가 그 네이티브 바이너리를 잡고 있던 것. ⚠ 처음엔 «내가 남긴 잔재»로 보고했는데 프로세스 생성 시각이 이번 세션보다 **앞섰다** — 확인하고 정정했다                                                                                                                                                          | **`npm ci` 는 지우고 시작하므로, 실패하면 트리가 「전」이 아니라 「반쯤」이 된다.** 복구는 `npm install`(지우지 않고 채운다). ⚠⚠ 그리고 **떠도는 dev 서버를 치우는 축이 없다** — 트랙 B 는 `ensureNoStrayShell()` 로 exe 를 매번 정리하는데 preview 서버엔 그 짝이 없다(→ O035). ⚠ 증상이 `EPERM` 이라 「권한 문제」로 읽히고, 그러면 원인에서 멀어진다 — Windows 에서 이 오류는 대개 **파일을 쥔 프로세스**다                                                              |
| 2026-08-23                                                                                                                                                                                                                                                                                                                                  | O035 — 떠도는 preview 를 거두는 `globalSetup` 을 짜면서 주석에 «우리가 방금 띄운 것은 안 죽인다(이 모듈이 webServer 앞에 돈다)» 라 적었다 | **적어만 놓고 구현하지 않았고, 가정도 틀렸다.** Playwright 는 `globalSetup` 보다 `webServer` 를 **먼저** 띄운다 → 첫 실행이 **자기 서버를 죽여** 4케이스가 통째로 실패했다                                                                                                                                                                                                                                                                                                                         | **주석이 보장을 말하면 그 보장을 코드가 지고 있는지 확인하라.** 그리고 «앞에 돈다」 같은 순서 가정은 **재라** — 여기서는 프로세스 생성 시각을 실행 시작과 비교하는 것으로 바꿨다(가정이 필요 없어진다)                                                                                                                                                                                                                                                                      |
| 2026-08-23                                                                                                                                                                                                                                                                                                                                  | 같은 항목 — 그 시각 비교를 `CreationDate` 로 하려 했다                                                                                    | `ConvertTo-Json` 이 그것을 **`/Date(1787292233691)/`**(Microsoft 전용 형식)로 내고 `Date.parse` 는 **`NaN`** 을 준다. 「시각을 못 읽으면 안 죽인다」 가드에 **전부 걸려 아무것도 안 했다** — 안전한 방향으로 실패했지만 **재는 것이 0**이었다                                                                                                                                                                                                                                                      | **안전하게 실패하는 가드도 「아무것도 안 재면」 검사가 아니다.** 이 세션에서만 같은 부류를 **세 번** 만났다(역래칫의 `=== false` · 크롬 잠금의 희석 · 여기). 새 가드는 **양방향으로 밟아라** — 걸려야 할 것이 걸리는지, 안 걸려야 할 것이 안 걸리는지                                                                                                                                                                                                                       |
| 2026-08-23                                                                                                                                                                                                                                                                                                                                  | 같은 항목 — epoch 변환을 `[int64]($_.CreationDate-(Get-Date '1970-01-01Z'))` 로 짰다                                                      | `Get-Date '1970-01-01Z'` 가 **로컬로 파싱**돼 기준선이 시간대만큼(KST **9시간**) 밀렸다. 그러면 모든 프로세스가 «이 실행보다 나중»으로 보여 가드가 **또** 전부 걸러낸다                                                                                                                                                                                                                                                                                                                            | PowerShell 에서 epoch 로 바꿀 땐 **`[System.DateTimeOffset]::new(x).ToUnixTimeMilliseconds()`** 를 써라(Kind 를 존중한다). ⚠ 그리고 시각 계산은 **현재 시각과 대조해 자릿수를 확인**하라 — 9시간 오차는 값만 봐서는 그럴듯해 보인다                                                                                                                                                                                                                                         |
| 2026-08-23 | Dependabot 이 낸 `server-deps` 묶음 PR 이 `cloud-server → verify` 에서 깨져 **마이그레이션 대상**을 찾으려 했다 | 대상이 **알파**였다. `@cloudflare/vitest-pool-workers 0.22.0` 이 **전이 의존**으로 `miniflare 5.…-alpha` 를 끌어오고, 그 메이저가 `new Miniflare()` 생성자를 바꿔(`workers` 배열 요구) `assets.test.ts` 가 `ERR_VALIDATION` 으로 죽는다. ⚠ **PR 제목·직접 의존성 목록에는 miniflare 가 없다** — 락파일 diff 를 봐야 보인다 | **깨진 의존성 PR 을 볼 땐 「무엇이 깨졌나」보다 「무엇이 새로 들어왔나」를 먼저 보라 — 특히 전이 의존의 프리릴리스.** 여기서 옳은 처방은 마이그레이션이 아니라 **핀**이었다: 그 잡이 지키는 것이 인증·입력검증 층의 실물 검증이라, 알파 런타임 위에 올리면 «녹색이 무엇을 뜻하는가»가 흐려진다(TS 7·ESLint 10 과 같은 부류). ⚠ 핀에는 **재시도 조건**을 함께 적는다 — 안 적으면 그게 영구 면제가 된다 |
