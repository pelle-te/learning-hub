# 판례 — 러닝허브

`/실행`이 항목을 닫을 때 append 한다. **명령·렌즈팩 문서에는 적지 않는다** —
거기 쌓이면 규율 준수 자체가 작업이 되어 실제 사고 예산을 잡아먹는다.

한 줄 형식: `날짜 · 무엇을 하려다 · 뭐에 물렸나 · 교훈`

| 날짜       | 하려던 것                                                                                                 | 물린 것                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 교훈                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------- | --------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-20 | 옛 명령·에이전트 은퇴                                                                                     | 보관소 `.claude/_은퇴-*/` 를 만들었다가 지웠다 — «참고용 보존본» 은 잔재를 만드는 합리화다(검색·스캔 경로에 남아 계속 비용을 낸다)                                                                                                                                                                                                                                                                                                                                                                 | **보관소를 만들지 마라. git 이 이력이다.** 복구: `git show f1af4f8:.claude/commands/감사.md` · `git show ed358dc:.claude/agents/web-reviewer.md` · `bd31741:.claude/agents/web-ideator.md` · `86bc12d:.claude/commands/게이트.md` ⚠ **hub 은 서브모듈** — hub 저장소에서 찾는다                                                                                                                                   |
| 2026-08-21 | `/리뷰 데이터` 의 §진보에서 「최고 수준엔 있고 우리에겐 없는 것」을 세려 했다                             | `time-travel`(하이픈·소문자)로 grep 해 0건을 얻고 **「Time Travel 없음」·「복원 훈련 없음」·「키 회전 절차 없음」 셋을 격차로 올렸는데 셋 다 이미 있었다** — 실물 표기가 `Time Travel`(공백·대문자)이었고, `restoreDrill.test.ts` 와 `릴리스.md:25-30` 도 있었다                                                                                                                                                                                                                                   | **부재를 주장할 땐 `-i` + 표기 변형 2개 이상.** 한 패턴 0건은 `[측정]`이 아니라 `[추측]`이다. 이 실패는 §진보에서 특히 비싸다 — `ⓓ=없음` 은 «이미 있는 것을 다시 만들자»가 되어 `/실행`이 중복을 짓는다                                                                                                                                                                                                           |
| 2026-08-21 | 같은 회차에서 「지우는 경로가 없는 데이터」를 찾으려 했다                                                 | 코드만 읽어서는 **원리적으로 안 보였다** — 실 SQLite 를 열자 `docs` 표에 은퇴한 화면이 남긴 **3행 51,793 B** 가 있었고 읽을 수도 지울 수도 없는데 이미 D1·폰까지 밀려 있었다                                                                                                                                                                                                                                                                                                                       | **데이터 축은 실행 중인 저장소를 열어야 한다.** ⚠ 반드시 **임시 복사 후 읽기 전용** — 이 저장소엔 검증이 검증 대상을 망가뜨린 전력이 있다(트랙 B 가 실 DB 의 `updated_at` 을 0 으로 깎았다)                                                                                                                                                                                                                       |
| 2026-08-21 | D001(폰 마이그레이션 비원자)의 재현 — 리포트가 든 예 `009` 로 재현 스크립트를 짰다                        | **재현되지 않았다.** 009 는 마지막에 `summaries_v9` 를 `summaries` 로 rename 해서 **우연히 재실행 가능**하다. 실제로 브릭되는 것은 `CREATE TABLE` 로 **끝나는** 007·008·010·011 쪽이었다(`table route_visits already exists` 실측)                                                                                                                                                                                                                                                                 | **결함의 부류가 맞아도 리포트가 든 예가 맞는 건 아니다.** 재현 단계를 건너뛰고 그 예로 회귀 테스트를 썼다면 **아무것도 안 잠그는 녹색 테스트**가 남았을 것이다 — 이 회차에서 실제로 한 번 그렇게 쓸 뻔했다                                                                                                                                                                                                        |
| 2026-08-21 | D010 — `cargo deny` 를 게이트에 넣고 D016(event-listener 5.4.1 · RUSTSEC-2026-0221)이 잡히는지 확인했다   | **`advisories ok` 가 나왔다.** 그 crate 가 실 빌드 그래프 안인데도(`-s` 로 재확인: 0 errors/warnings/notes). 원인은 `informational = "unsound"` 인 권고가 **v2 기본에서 리포트되지 않는 것** — `[advisories] unsound = "all"` 을 명시해야 한다(`unmaintained` 와 같은 enum)                                                                                                                                                                                                                        | **도구를 들였으면 「그 도구를 들인 계기」로 한 번 겨눠 봐라.** 안 겨눴으면 «게이트 있음»이라 적힌 채 정확히 그 부류를 못 잡는 게이트가 남는다 — 이 저장소가 세 번 물린 «있는 줄 알았는데 안 돌고 있었다» 의 변종이다                                                                                                                                                                                              |
| 2026-08-21 | D019 — JS 소비처 0인 `global-shortcut` 권한 3종 + `core:window:allow-show` 삭제                           | 아무것도 안 깨졌다. 릴리스 exe + 트랙 B 프로브로 `capabilities.hotkey === true`·`hotkeyError === null` 확인                                                                                                                                                                                                                                                                                                                                                                                        | **Tauri 권한(capabilities)은 웹뷰 `invoke` 만 통제한다 — Rust 가 부르는 경로는 ACL 을 안 탄다.** 그래서 "Rust 가 쓰니까 권한도 필요하겠지"는 틀린 추론이고, 그 추론으로 켜 둔 권한의 유일한 사용자는 **침해된 웹뷰의 임의 JS** 다                                                                                                                                                                                 |
| 2026-08-21 | D002 — pull 관용을 열 개수 축까지 넓혔다                                                                  | 기존 테스트 하나(`cloudClient.test.ts` "아는 테이블의 내용이 계약과 다르면 여전히 던진다")와 **정면 충돌**했다. 그 테스트는 뒤집기 전 정책을 정확히 인코딩하고 있었다                                                                                                                                                                                                                                                                                                                              | **게이트가 빨개졌을 때 «내 수정이 깼다»와 «옛 계약을 갱신해야 한다»는 다르다.** 후자면 테스트를 고치되 **뒤집힌 이유를 그 자리에 적는다** — 안 적으면 다음 회차가 옛 근거를 읽고 되돌린다                                                                                                                                                                                                                         |
| 2026-08-21 | `/리뷰 ux` 의 §진보에서 표본(Linear·Vercel)의 CSS 를 받아 `font-size` 고유값을 셌다                       | 표본이 **27종·79종**으로 나왔다 — 우리(28)보다 많다. 그 수만 보면 「우리는 양호」라는 **정반대 결론**이 나온다. 원인은 제품 사이트라 **서드파티 CSS 가 섞인** 것이고, 실제로 비교 가능했던 것은 **토큰 «정의» 층**이었다(`--radius-4/6/8/12` vs `--radius-swatch/chip/rail-chip`)                                                                                                                                                                                                                  | **표본의 «총량»은 거의 항상 비교 불가다. 비교하는 것은 «규율의 형태»다.** 여기서 나온 것은 개수가 아니라 **이름 짓는 법**이었다 — 값으로 이름 지으면 off-ladder 값은 이름을 못 지어 사다리가 유한해지고, 호출부로 이름 지으면 사다리가 컴포넌트 수만큼 자란다                                                                                                                                                     |
| 2026-08-21 | 같은 회차에서 「레일이 720px 에서 잘린다」를 Major 로 올리려 했다                                         | **배포 기본 창은 1440×900**(`src-tauri/tauri.conf.json:17`)이라 기본 상태에서는 다 들어간다. 720 은 **Playwright 뷰포트**(`devices['Desktop Chrome']`)였고, 그것을 사용자 창으로 읽었다                                                                                                                                                                                                                                                                                                            | **베이스라인이 보여 주는 것은 「사용자가 보는 화면」이 아니라 「테스트가 보는 화면」이다.** 창 크기를 근거로 삼기 전에 `tauri.conf.json` 의 `width`·`minWidth` 를 열어라. ⚠ 다만 발견이 사라진 것은 아니고 **좁아졌다** — `minHeight` 가 600 이고 검증망이 그 대역(720)에서 돈다는 사실은 남는다                                                                                                                  |
| 2026-08-21 | 같은 회차에서 반픽셀 `--fs-*` 5칸을 「자기 규약 위반」으로 올리려 했다                                    | `stylelint.config.js:17-22` 에 **근거 있는 예외**가 있었다 — 정수로 일괄 **상향** 했더니 월 캘린더 `.cell`(고정 행 높이 + `overflow:hidden`)에서 과목 칩이 통째로 잘린 실사고                                                                                                                                                                                                                                                                                                                      | **예외의 «범위»를 재라 — 근거가 있다고 전부 덮는 것이 아니다.** ① 그 예외는 _리터럴_ 에 대한 것인데 C-7 이 리터럴을 전부 **사다리 칸으로 승격**시켰다(같은 파일이 「사다리에 대한 규약」이라 못박은 그 자리로 들어갔다) ② 실패 모드는 **상향에만** 있다 — `[재현]` 레일에서 올려도 +3px·생략 0건이고, **내림으로 고정하면 그 실패가 원리적으로 불가능하다**. 뒤집는 것이 아니라 **방향을 못박는 것**이 처방이었다 |
| 2026-08-21 | U021 — 워드마크가 `schedule`·`alloc` 에서 네 줄로 무너지는 것을 고치고 **곧바로 재측정**했다              | 값이 안 바뀌었다(h1 높이 80px 그대로). 원인은 코드가 아니라 **측정 대상**이었다: 트랙 A 는 `vite preview` 로 **`dist` 를 서빙**하는데 `npm run build` 를 안 돌렸다                                                                                                                                                                                                                                                                                                                                 | **트랙 A 로 무언가를 재기 전에 빌드하라.** 절대규칙 #1(«셸이 prebuilt dist 를 로드한다»)은 앱 실행에만 해당하는 줄 알기 쉬운데, **e2e 도 같은 dist 를 본다** — 안 그러면 «고쳤는데 안 고쳐졌다»가 되고, 그 다음 수순은 멀쩡한 처방을 의심하는 것이다                                                                                                                                                              |
| 2026-08-21 | U023 — 「320px 에서 가로 스크롤」을 고치려고 넘치는 요소를 찾았다                                         | 범인이 본문이 아니라 **하단 탭바**였고, 더 나쁜 사실이 함께 나왔다: 그 바는 **390px(폰 베이스라인)에서도 탭 7개를 화면 밖에 두고 스크롤조차 안 됐다**. 즉 「확대 시 리플로우」로 접수된 것이 실제로는 **상시 도달성 결함**이었다                                                                                                                                                                                                                                                                   | **넘치는 것과 못 닿는 것을 가려라.** 문서 폭만 재면 `position:fixed` 안의 넘침은 안 잡힌다(뷰포트 스크롤에 안 실린다) — 그래서 이 결함은 리플로우 지표에서 **부분적으로만** 보였다. 게이트를 세울 때 «넘쳤는가»와 «닿는가»를 각각 물어라                                                                                                                                                                          |
| 2026-08-21 | U016 — 숙달 램프를 색각이상에서도 갈리게 하려고 «명도 단조성 + 양 끝 대비 + 면 대비» 셋을 계약으로 걸었다 | **단조성은 원리적으로 불가능했다.** sRGB 휘도는 빨강(낮음)→노랑(최고)→초록(중간)이라 0°→120° 램프의 중간이 항상 봉우리다. 셋을 다 걸면 통과하는 조합이 **하나도 없다**                                                                                                                                                                                                                                                                                                                             | **계약을 걸기 전에 그 계약이 만족 가능한지 먼저 풀어 봐라.** 여기서는 격자 탐색 한 번(2분)이 «어떤 계약을 포기해야 하는가»를 알려 줬고, 그 덕에 포기한 이유를 테스트 머리주석에 근거와 함께 적을 수 있었다 — 못 지킬 계약을 걸어 두면 다음 사람이 그것을 «지킬 수 있는데 안 지킨 것»으로 읽는다                                                                                                                   |
| 2026-08-21 | 여러 항목에서 «현재 위치로 데려간다»(`scrollIntoView`)를 붙였다                                           | **컴포넌트 테스트 70건이 통째로 죽었다** — jsdom 에 그 메서드가 없다. 제품 코드는 멀쩡한데 게이트가 빨갛다                                                                                                                                                                                                                                                                                                                                                                                         | **없는 API 는 환경이 채운다 — 제품 코드에 `typeof … === 'function'` 가드를 심지 마라.** 그 가드는 곧 «왜 방어하지?»가 되고, 진짜 이유(테스트 환경의 한계)는 아무 데도 안 남는다. `test/_setup.ts` 가 그 자리다                                                                                                                                                                                                    |
| 2026-08-21 | U031 — 손으로 적힌 빈 상태 로스터를 `export const TABS_EMPTY = TABS` 별칭으로 파생시켰다                  | `knip` 이 **중복 export** 로 잡았다                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **게이트가 더 나은 형태를 가리킬 때가 있다.** 이름이 둘이면 다음 사람이 «둘은 다르다»로 읽는다 — 별칭을 지우고 소비처가 원본을 쓰게 하는 쪽이 옳았다                                                                                                                                                                                                                                                              |
| 2026-08-21 | U030 — 두 화면의 셋업 안내를 `components/State` 로 옮겼다                                                 | 옮기고 나니 **빈 상태 스냅샷 넷이 타임아웃**으로 죽었다. `State` 의 제목이 `<div>` 라 그 화면들의 `#main` 안에 **머리글이 하나도 없었고**, 회귀망의 「본문이 떴는가」 단언이 `h2` 를 찾고 있었다                                                                                                                                                                                                                                                                                                   | **검사가 멈춘 자리가 제품의 구조 결함일 수 있다.** 단언을 느슨하게 하는 것이 첫 반응이지만, 여기서는 **제목에 `role="heading"` 을 주는 것**이 옳았다(SR 머리글 탐색이 18개 빈 화면에서 함께 살아났다). ⚠ `<h2>` 태그로 바꾸는 것은 오답이다 — 언레이어드 전역 `h2 {}` 가 18곳의 조판을 통째로 바꾼다                                                                                                              |
| 2026-08-22 | I039 — 「번호는 같은데 내용이 다른 마이그레이션」을 잡으려고 `PRAGMA table_info` 해시 가드를 만들려 했다  | **탐지는 이미 있었다.** sqlx 가 SHA-384 로 매 부팅에 대조하고, `db.rs` 자신이 _"주석 한 글자만 바뀌어도 기존 DB 를 못 연다"_ 고 적어 뒀다. 없던 것은 **보고**다 — 그 실패가 `load()` Err → C1 조용한 폴백으로 흘러 «뜨는데 데이터가 옛날 것»이 되고, `is_downgrade` 는 이 경우 거짓이라 명시적 화면이 안 떴다                                                                                                                                                                                      | **「없다」고 적힌 발견을 만들기 전에 「있는데 안 들리는 것 아닌가」를 물어라.** 해시를 새로 만들었다면 같은 사실에 판정자가 둘이 되고, 둘은 반드시 갈린다. 이 회차에서 같은 형태가 **넷**이었다(I039·I041·I038·I023) — 발상 축의 «없는 것»은 자주 «탐지는 있고 끝이 없는 것»이다                                                                                                                                  |
| 2026-08-22 | I041 — 「leech 표식은 있는데 자료를 바꾸러 갈 자리가 없다」를 고치려 했다                                 | **표식조차 안 그려지고 있었다** — `leech` 를 읽는 화면이 저장소에 0개(`spacedReview` 가 A-4 에서 _"UI 는 이걸 다른 문구로 그려야 한다"_ 고 적어 둔 조건이 한 번도 충족된 적 없다). 그리고 러너 배지가 `chapterCopy().badge` 를 **손으로 재현**하고 있었다(폰은 `copy.badge` 사용 = 이미 갈릴 준비 완료)                                                                                                                                                                                            | **주석이 「UI 가 …해야 한다」고 적으면 그 소비처를 grep 해서 확인하라.** 판정만 흐르고 끝이 없는 층은 게이트가 원리적으로 못 잡는다(계산은 옳고 테스트도 녹색이다). H14 가 본문에서 고친 「말이 두 벌」이 **배지 축에 남아 있던 것**도 같은 방식으로만 보인다                                                                                                                                                     |
| 2026-08-22 | I020 — 복습 due 에 챕터별 ±1일 흔들기를 넣었다                                                            | **유닛 3건이 깨졌다.** fixture 가 사다리 칸(16일)에 **정확히** 앉아 있어서, 그 챕터의 흔들기 부호를 그대로 검사하게 됐다                                                                                                                                                                                                                                                                                                                                                                           | **경계값 fixture 는 「경계를 재는 케이스」에만 둬라.** 저 셋이 재려던 것은 완료/미래 필터와 경과일이었고 경계는 우연이었다 — 한 칸 밖으로 옮기면 의도가 그대로 남는다. ⚠ 반대로 **정말 경계를 재는 케이스는 그대로 둔다**(riskOf 직접 호출 5건은 안 건드렸다)                                                                                                                                                     |
| 2026-08-22 | I030 — 점검 트래픽을 원장에서 빼는 「플래그 한 줄」을 처방대로 부울로 만들려 했다                         | 부울이면 **켜 놓은 것을 잊어 관측이 조용히 영원히 멈춘다.** 그러면 다음 판정자가 그 0 을 「안 쓴다」로 읽는데, 그건 이 원장이 막으려는 바로 그 순환이다 — 노브가 자기 목적을 재현하는 형태                                                                                                                                                                                                                                                                                                         | **관측을 끄는 노브에는 만료를 붙여라.** 날짜 스코프면 자정에 스스로 꺼지고, 켜져 있다는 사실이 리드아웃에 날짜로 보인다. ⚠ 그리고 **행이 없어도 스위치는 그린다** — 안 그러면 첫날부터 켠 사용자는 끄는 문에 도달할 수 없다                                                                                                                                                                                       |
| 2026-08-22 | I033 — 폰 원장을 판정자에게 가져오려고 처방(«백업 왕복 1회»)대로 폰 백업을 데스크톱에서 가져오려 했다     | `importJSON` 은 `loadState` 로 **앱 상태를 교체**한다. 폰 백업으로 그걸 하면 폰 데이터가 PC 를 덮는데, 앱 데이터의 정본은 **클라우드 동기화**다 — 두 정본이 서로를 덮는 경로를 손으로 여는 셈                                                                                                                                                                                                                                                                                                      | **「그 파일을 가져온다」와 「그 파일의 한 조각을 합친다」는 다른 동작이다.** `_obs` 만 꺼내 upsert 하는 별도 입구를 파는 것이 옳았다(방문 수 `MAX` 규칙은 `lib/observations` 가 이미 소유하고 있었다)                                                                                                                                                                                                             |
| 2026-08-22 | I049 — 트레이·알림을 지운 뒤 `check:tokens` 가 **쓰고 있는 토큰**(`--color-learning`)을 고아로 잡았다     | 검출기의 주석 제거 정규식이 `<Cmd>exports/*.txt</Cmd>` 의 **글롭을 주석 시작으로** 읽어 `GuideView` 의 절반을 공백으로 지우고 있었다 — 그 구간의 소비가 통째로 안 보였다                                                                                                                                                                                                                                                                                                                           | **문자열 속 `/*` 를 주석으로 읽지 마라 — 앞 글자를 봐라.** 이 부류는 «다른 소비처가 하나라도 있으면 조용하다»: 실제로 그 화면이 삭제되기 전까지 몇 달을 지나갔다. 고친 형태는 `(^                                                                                                                                                                                                                                 | [^A-Za-z0-9_])\/\*` 다(진짜 주석은 줄머리·공백·`{` 뒤에 온다) |
| 2026-08-22 | I044~I052 실행 뒤 트랙 A 넉 장(`settings`·`integrations` × 2테마)이 **병렬에서만** 빨개졌다               | 처음엔 웹폰트 스와프로 읽고 `fonts.load`·`fonts.ready`·레이아웃 안정 대기를 차례로 넣었는데 **전부 안 닫혔다.** 프로브가 그 가설을 죽였다 — 폰트는 두 조건에서 동일했고(`check` 참 · 항목 높이 38px 동일) 갈린 것은 레일 nav 의 `scrollTop`(40↔43) 같은 서브픽셀 상태였다                                                                                                                                                                                                                          | **베이스라인을 만드는 조건과 재는 조건이 다르면 그 차이만큼은 원리적으로 못 닫는다.** 이 저장소는 `e2e:update` 를 `--workers=1` 로 못박아 두고 검사는 `fullyParallel` 로 돌리고 있었다 — 평소엔 `retries:1` 이 흡수했고, 레일이 한 줄 줄자(I048) 표면화됐다. 처방은 임계 완화(드리프트를 삼킨다)도 재시도(어긋남을 남긴다)도 아니라 **조건을 맞추는 것**이었다                                                    |
| 2026-08-22 | 같은 회차에서 `settings · light` 한 장이 재시도까지 실패했다                                              | diff 를 보니 **네이티브 폼 컨트롤(체크박스·날짜 입력 아이콘)이 한쪽에만 그려져** 있었다 — 폰트도 레이아웃도 아니고 «아직 다 안 그렸다» 였다                                                                                                                                                                                                                                                                                                                                                        | **«정착했나»를 물을 때 `readyState` 를 빠뜨리지 마라.** rAF 두 번은 _스크립트가 만든_ 변화만 넘기고, 브라우저가 네이티브 위젯을 그리는 시점은 그 뒤일 수 있다                                                                                                                                                                                                                                                     |
| 2026-08-22 | I048 — 「`role:'retired'` 어휘를 지운다」를 문자 그대로 실행하려 했다                                     | 그 항목이 규정한 두 갈래(레일에 선다 / 없다) 중 **어느 쪽도 지금 고를 수 없었다**: 레일에 올리려면 자기 라우트·로더가 필요하고(W9·A-16·A-19 를 되돌리는 IA 변경), 로스터에서 빼면 H-12(이름)와 I034(집계)가 **함께 회귀**한다                                                                                                                                                                                                                                                                      | **어휘를 지우는 것과 그 어휘가 붙은 것을 옮기는 것은 다른 결정이다.** 여기서 지울 수 있었던 것은 «영원히 유예»라는 **뜻**이고(그래서 `view` = 확정된 배치), 자리 이동은 데이터가 붙는 항목(I025)의 몫이다. 부분 실행이면 **무엇을 안 했는지와 왜인지를 닫기 노트에 적는다**                                                                                                                                       |
| 2026-08-22 | I036(저장 엔진 하나로) — 승인을 받았으니 바로 착수하려다 **최싼검증부터** 돌렸다                          | `enableBrowserDb()` 를 켜고 트랙 A 를 돌리니 시각 스냅샷은 **1장**만 깨졌는데(원장이 걱정한 비용) `plan.spec` 이 **쓰기가 아예 안 남아** 실패했다 — 트랙 A 의 시드가 `localStorage` 에 심기는데 정본이 OPFS 로 바뀌면 그 시드가 통째로 무시된다                                                                                                                                                                                                                                                    | **승인은 착수 허가지 전제 확인이 아니다.** 이 항목의 전제(«트랙 A 가 OPFS 위에서 돈다»)는 거짓이었고, 진짜 비용은 «깨진 스냅샷 수»가 아니라 **시딩 기질 재작성**이다. 5분짜리 프로브가 그것을 말해 줬다 — 안 돌렸으면 스냅샷 209장을 재생성한 뒤에 알았을 것이다                                                                                                                                                  |
| 2026-08-22 | I032(관측 원장 축소) — 「`idle_spells` 0행」을 근거로 지우려 했다                                         | 같은 원장의 `idleVerdict` 가 스스로 _"관측 N일 — 3일 미만이면 판정하지 말 것(0 은 「없다」가 아니다)"_ 라 적어 두고 있었다. 그리고 이 항목의 전제는 «I030 뒤에도» 인데 **I030 이 그날 착지했다**                                                                                                                                                                                                                                                                                                   | **항목의 전제에 「무엇 뒤에」가 적혀 있으면 그 무엇이 언제 끝났는지를 봐라.** 선행이 방금 끝났으면 그 항목은 아직 **잴 수 없는 상태**이지 열려 있는 상태가 아니다. 보류에 사유를 적어 두면 다음 회차가 같은 0을 다시 근거로 쓰지 않는다                                                                                                                                                                           |
| 2026-08-22 | I053(배분판의 「전공 밖」 레인) — 「데이터 계약 0」이니 안전하다고 보고 착수하려 했다                     | 계약은 정말 0이었다(스케줄러는 `weekly.forEach` 로 실제 과목만 훑으므로 합성 sid 를 무시한다). 그런데 **쓰는 쪽**이 걸렸다: `setAllocCell` → `ensureWeekAlloc` 이 그 주를 **managed 로 승격**시킨다 — 「교양 2시간」을 적으면 그 주 배치가 통째로 자동에서 수동으로 바뀐다                                                                                                                                                                                                                         | **「읽는 쪽 계약 0」과 「쓰는 쪽 부작용 0」은 다른 말이다.** 항목이 «데이터 계약 0» 이라 적었을 때 검토된 것은 읽기 방향뿐이었다. 새 값을 기존 저장 구조에 얹을 때는 **그 구조를 쓰는 함수가 무엇을 함께 하는지**를 봐라                                                                                                                                                                                          |
| 2026-08-22 | I027(레일을 줄인다) — 「무엇을 접었는지가 곧 답」이라기에 접기 원장을 읽으러 갔다                         | **읽을 곳이 실 DB 가 아니었다.** UI 취향은 `settings` 표가 아니라 WebView2 의 `lh_ui_v1`(로컬스토리지 leveldb)에 산다 — 앱 데이터와 UI 를 다른 저장소에 두는 것이 이 저장소의 계약이라, 실 DB 만 열면 «키가 없다 → 기본값이다»를 **추론**하게 되고 그건 실측이 아니다. 교훈: **UI 노브의 실사용을 재려면 `%LOCALAPPDATA%/dev.jin.learninghub/EBWebView/Default/Local Storage/leveldb` 를 함께 읽어라.**                                                                                            |
| 2026-08-22 | 같은 항목 — 레일 조립 화면 하나를 은퇴시켰다                                                              | **모션 어휘 두 마디가 발화 0으로 남았다**(`shift` A-17 · `deny` A-18 — 그 화면이 유일한 소비처였다). knip 이 잡긴 했지만 knip 이 말한 것은 «export 가 안 쓰인다» 이지 «어휘에 발화가 없다» 가 아니다 — 어휘 집행자(불변식 ⑥ `VOCAB`)는 **어휘가 줄어드는 방향을 안 본다.** 교훈: **화면을 은퇴시킬 때 그 화면이 유일 소비처였던 «어휘»(모션·토큰·상태 이름)를 함께 세라.** 안 세면 없는 마디로 키프레임을 지을 수 있는 상태가 남는다.                                                              |
| 2026-08-22 | I010(상시 인입구) — 「개강 1회뿐」을 화면 문구·뷰 이름 문제로 보고 착수했다                               | **막고 있던 것은 문구가 아니라 `if (!item) return` 한 줄**이었다. 학사 눈금은 **학기의 속성**인데 과목 게이트가 그 위에 얹혀 있어서, 「4/6 휴강」을 붙여 넣고 버튼을 눌러도 아무 일이 안 일어났다(에러도 없다). 교훈: **「입구가 좁다」류 항목은 UI 를 고치기 전에 적용 경로의 early-return 부터 읽어라** — 그 한 줄이 화면 전체의 용도를 좁히고 있을 수 있고, 조용해서 화면으로는 안 보인다.                                                                                                      |
| 2026-08-22 | 스냅샷 6장을 좁혀 재생성하려고 `npm run e2e -- -g … --update-snapshots=all` 을 썼다                       | **아무것도 재생성되지 않았고 전량이 그냥 다시 돌았다**(3분). `e2e` 스크립트는 `playwright test && playwright test -c motion` 이라 `npm run … --` 의 인자가 **뒤쪽 명령에만** 붙는다. 교훈: **좁혀 재생성할 때는 npm 스크립트를 경유하지 말고 `npx playwright test -g "…" --update-snapshots=all --workers=1` 을 직접 불러라.** (전량은 `npm run e2e:update` 가 맞다 — 그건 두 설정 각각에 플래그가 박혀 있다.)                                                                                     |
| 2026-08-22 | I028(하루의 한 칸 타입 통합) — 원장이 적은 «`done?` 유무가 유일 차이»를 믿고 병합 설계를 짜려 했다        | 스키마를 실제로 겹쳐 보니 **차집합이 6**이었다(Task 전용 다섯 · PlanEvent 전용 하나). 그리고 그 처방의 근거였던 «일정만 가용시간을 깎는다»는 `PlanEventSchema` 주석이 **2주 넘게 낡아 있었다** — 2026-08-07 의 N-1 이 시각 박힌 과제도 같은 축에 넣었다. 교훈: **원장의 «유일한 차이» 주장은 세어서 확인하라.** 리뷰 시점의 판단은 그 시점의 주석을 읽고 만들어지므로, 주석이 낡으면 처방도 함께 낡는다.                                                                                           |
| 2026-08-22 | I001(원장이 계획의 입력이 된다) — 새 lib 을 지으려고 파일을 팠다                                          | **이미 있었다**(`lib/ledgerSeed.ts` · 2026-07-31). 규율도 옳았다(«자동으로 안 찍는다»). 틀린 것은 **소비처가 하나**(`importVaultSubject`)라는 것이었고, 임포트는 1회인데 원장은 계속 자라므로 손으로 만든 과목엔 그 다리가 영원히 안 놓인다. 교훈: **「밖의 것이 앱에 안 들어온다」류를 만나면 먼저 `grep` 으로 「그걸 하는 함수가 이미 있나」와 「그 함수를 부르는 곳이 몇 개인가」를 세라.** 이 회차에서만 같은 형태가 **셋**이었다(I001 임포트 1회 · I010 개강 1회 · I013 이름이 없어 못 맞춤). |
| 2026-08-22 | I053(전공 밖 레인) — 보류 사유가 「표면이 틀렸다」인 줄 알았다                                            | 실제로는 **저장 자리**였다. `weekAlloc` 안에 두면 칸 하나가 `ensureWeekAlloc` 을 타고 그 주를 managed 로 승격시키지만, **최상위 키 하나**로 빼면 `ROW_SLICES` 를 안 지나 `settings` 한 행이 되고 새 표도 D1 DDL 도 0이다. 교훈: **「이 값을 어디에 두나」가 「이 값을 보여줄 것인가」보다 먼저다** — 이 저장소의 매퍼는 «선언 안 된 키는 자동으로 settings» 라는 확장점을 이미 갖고 있고, 그것을 모르면 멀쩡한 항목이 스키마 변경으로 보인다.                                                      |
| 2026-08-22 | I001 반영 줄을 붙이고 `-g "ledger"` 로 e2e 17장을 돌렸다 — **전부 통과**                                  | 통과의 뜻이 «안 깨졌다»가 아니라 **«한 번도 렌더된 적 없다»** 였다: 기본 픽스처의 과목 이름(미적분…)이 원장 픽스처(통신이론…)와 안 맞아 그 줄이 **정의상** 안 그려진다. 교훈: **새 UI 가 「데이터가 맞을 때만」 뜨는 종류면, 통과한 스냅샷 수는 커버리지의 증거가 아니다** — 픽스처를 파생시켜(`LEDGER_APPLY_SEED`) 렌더되는 상태를 먼저 잠가야 §15-4 를 지킨 것이다. ⚠ 기본 SEED 를 고치면 무관한 베이스라인이 통째로 움직이므로 **파생**이 답이다.                                               |
