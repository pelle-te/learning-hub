# 판례 — 러닝허브

`/실행`이 항목을 닫을 때 append 한다. **명령·렌즈팩 문서에는 적지 않는다** —
거기 쌓이면 규율 준수 자체가 작업이 되어 실제 사고 예산을 잡아먹는다.

한 줄 형식: `날짜 · 무엇을 하려다 · 뭐에 물렸나 · 교훈`

| 날짜       | 하려던 것                                                                                               | 물린 것                                                                                                                                                                                                                                                                     | 교훈                                                                                                                                                                                                                                                                            |
| ---------- | ------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-20 | 옛 명령·에이전트 은퇴                                                                                   | 보관소 `.claude/_은퇴-*/` 를 만들었다가 지웠다 — «참고용 보존본» 은 잔재를 만드는 합리화다(검색·스캔 경로에 남아 계속 비용을 낸다)                                                                                                                                          | **보관소를 만들지 마라. git 이 이력이다.** 복구: `git show f1af4f8:.claude/commands/감사.md` · `git show ed358dc:.claude/agents/web-reviewer.md` · `bd31741:.claude/agents/web-ideator.md` · `86bc12d:.claude/commands/게이트.md` ⚠ **hub 은 서브모듈** — hub 저장소에서 찾는다 |
| 2026-08-21 | `/리뷰 데이터` 의 §진보에서 「최고 수준엔 있고 우리에겐 없는 것」을 세려 했다                           | `time-travel`(하이픈·소문자)로 grep 해 0건을 얻고 **「Time Travel 없음」·「복원 훈련 없음」·「키 회전 절차 없음」 셋을 격차로 올렸는데 셋 다 이미 있었다** — 실물 표기가 `Time Travel`(공백·대문자)이었고, `restoreDrill.test.ts` 와 `릴리스.md:25-30` 도 있었다            | **부재를 주장할 땐 `-i` + 표기 변형 2개 이상.** 한 패턴 0건은 `[측정]`이 아니라 `[추측]`이다. 이 실패는 §진보에서 특히 비싸다 — `ⓓ=없음` 은 «이미 있는 것을 다시 만들자»가 되어 `/실행`이 중복을 짓는다                                                                         |
| 2026-08-21 | 같은 회차에서 「지우는 경로가 없는 데이터」를 찾으려 했다                                               | 코드만 읽어서는 **원리적으로 안 보였다** — 실 SQLite 를 열자 `docs` 표에 은퇴한 화면이 남긴 **3행 51,793 B** 가 있었고 읽을 수도 지울 수도 없는데 이미 D1·폰까지 밀려 있었다                                                                                                | **데이터 축은 실행 중인 저장소를 열어야 한다.** ⚠ 반드시 **임시 복사 후 읽기 전용** — 이 저장소엔 검증이 검증 대상을 망가뜨린 전력이 있다(트랙 B 가 실 DB 의 `updated_at` 을 0 으로 깎았다)                                                                                     |
| 2026-08-21 | D001(폰 마이그레이션 비원자)의 재현 — 리포트가 든 예 `009` 로 재현 스크립트를 짰다                      | **재현되지 않았다.** 009 는 마지막에 `summaries_v9` 를 `summaries` 로 rename 해서 **우연히 재실행 가능**하다. 실제로 브릭되는 것은 `CREATE TABLE` 로 **끝나는** 007·008·010·011 쪽이었다(`table route_visits already exists` 실측)                                          | **결함의 부류가 맞아도 리포트가 든 예가 맞는 건 아니다.** 재현 단계를 건너뛰고 그 예로 회귀 테스트를 썼다면 **아무것도 안 잠그는 녹색 테스트**가 남았을 것이다 — 이 회차에서 실제로 한 번 그렇게 쓸 뻔했다                                                                      |
| 2026-08-21 | D010 — `cargo deny` 를 게이트에 넣고 D016(event-listener 5.4.1 · RUSTSEC-2026-0221)이 잡히는지 확인했다 | **`advisories ok` 가 나왔다.** 그 crate 가 실 빌드 그래프 안인데도(`-s` 로 재확인: 0 errors/warnings/notes). 원인은 `informational = "unsound"` 인 권고가 **v2 기본에서 리포트되지 않는 것** — `[advisories] unsound = "all"` 을 명시해야 한다(`unmaintained` 와 같은 enum) | **도구를 들였으면 「그 도구를 들인 계기」로 한 번 겨눠 봐라.** 안 겨눴으면 «게이트 있음»이라 적힌 채 정확히 그 부류를 못 잡는 게이트가 남는다 — 이 저장소가 세 번 물린 «있는 줄 알았는데 안 돌고 있었다» 의 변종이다                                                            |
| 2026-08-21 | D019 — JS 소비처 0인 `global-shortcut` 권한 3종 + `core:window:allow-show` 삭제                         | 아무것도 안 깨졌다. 릴리스 exe + 트랙 B 프로브로 `capabilities.hotkey === true`·`hotkeyError === null` 확인                                                                                                                                                                 | **Tauri 권한(capabilities)은 웹뷰 `invoke` 만 통제한다 — Rust 가 부르는 경로는 ACL 을 안 탄다.** 그래서 "Rust 가 쓰니까 권한도 필요하겠지"는 틀린 추론이고, 그 추론으로 켜 둔 권한의 유일한 사용자는 **침해된 웹뷰의 임의 JS** 다                                               |
| 2026-08-21 | D002 — pull 관용을 열 개수 축까지 넓혔다                                                                | 기존 테스트 하나(`cloudClient.test.ts` "아는 테이블의 내용이 계약과 다르면 여전히 던진다")와 **정면 충돌**했다. 그 테스트는 뒤집기 전 정책을 정확히 인코딩하고 있었다                                                                                                       | **게이트가 빨개졌을 때 «내 수정이 깼다»와 «옛 계약을 갱신해야 한다»는 다르다.** 후자면 테스트를 고치되 **뒤집힌 이유를 그 자리에 적는다** — 안 적으면 다음 회차가 옛 근거를 읽고 되돌린다                                                                                       |
