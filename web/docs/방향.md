# 방향 — 무엇을 이루려 하는가 (발산의 1차 앵커)

> **상태: 1차 확정** (2026-07-31 생성 + 같은 날 사용자 응답 반영).
> ⚠ **"비어 있음"이 여기서는 미완성이 아니라 _답_ 이다** — §1(목표·마감 없음)·§3(다섯 줄 전부
> "그냥 안 함")은 사용자가 실제로 그렇게 답한 결과이고, 그 공백 자체가 발산의 재료다. 다만
> **§2 순서만 의도적으로 미해결**이다(억지로 정하면 다음 발산이 자신 있게 틀린 곳을 판다).

## 왜 이 문서가 있나 (지우지 말 것)

`/개선 발산` 은 오래 **제품 목적 문장**("볼트·Anki·일과를 한눈에 보는 로컬 학습 대시보드")에
앵커링했다. 그런데 그 문장은 **현 구현의 요약**이다 — 요약을 앵커로 쓰면 정의상 현 구현 밖으로
나갈 수 없고, 실제로 나가지 못했다(2026-07-31 진단: 직전 발산 산출 24건 중 **없던 능력 1건**,
7건은 그냥 `/감사` 항목, 나머지는 있는 화면의 재배치).

**앱은 목적이 아니라 수단이다.** 이 문서는 그 목적을 적어 둔다. 발산은 여기서 시작한다:
_"이 여정의 어느 구간에 앱이 아예 없나."_

⚠ 이건 **로드맵이 아니다.** 로드맵은 "다음에 뭘 만드나"(항목), 이 문서는 "왜 만드나"(목적).
항목을 여기 적지 마라 — 그러면 6개월 뒤 또 하나의 낡은 백로그가 된다.

## 1. 목표 (기한과 함께)

**현재 값은 비어 있고, 그것이 이 절의 결론이다** (2026-07-31 사용자 응답: _"1,2는 구축해놓고
생길 때 입력하면 바로 되게"_).

- **최상위 / 이번 학기 / 다음 마감** — 지금 확정된 값 없음.
- ⚠ **여기서 요구되는 것은 값이 아니라 _자리_ 다.** 사용자는 "목표를 정해 오겠다"가 아니라
  **"생겼을 때 넣으면 바로 도는 상태"** 를 요구했다. 즉 이 절의 공백은 **앱이 채워야 할 미충족**이지
  문서의 미완성이 아니다.
- ⚠ 특히 **다음 마감**: 앱 전체가 **주 단위 배분**으로 돌아가는데(README 배분 엔진) 시험은
  **D-day 역산**이다. 시험일 하나를 넣었을 때 배분·복습·오늘 화면이 **함께 뒤집히는가**가 이 축의
  실제 질문이고, 지금은 그 입력을 받는 자리 자체가 없다.

### 1-b. 이 절이 만드는 설계 원칙 — **빈 자리를 먼저 만든다 (단, 폐기 조건과 함께)**

이 앱은 지금까지 **이미 있는 데이터를 보여주는** 도구였다(볼트·Anki·일과 — 전부 밖에서 생긴 것).
§1·§3 의 답이 가리키는 방향은 반대다: **아직 없는 입력을 받을 자리를 만드는** 것.

- ⚠⚠ **그러나 이건 금도금과 한 끗 차이다.** "안 하고 있다"가 *도구가 없어서*인지 *필요가 없어서*인지는
  **관측으로 구분되지 않는다.** 빈 슬롯만 늘리면 그건 개선이 아니라 비대화이고, 이 제품에서
  "한눈에"를 깨는 방향이다.
- **그래서 규율:** 이런 안은 ① **가장 싼 자리**부터(새 탭이 아니라 기존 표면 한 줄) ② **폐기 조건을
  함께 적는다**(`N주 동안 비어 있으면 은퇴`) ③ **넣었을 때 무엇이 즉시 달라지는지**가 있어야 한다 —
  입력만 받고 아무것도 안 바뀌면 그건 서식이지 기능이 아니다.
- 이 셋이 개선루브릭 **§신규형 트랙**의 `전제 / 가장 싼 검증 / 참이면 무엇이 달라지나` 와 같은 자다.

## 2. 성공의 정의 — 무엇이 되면 이 앱은 다 한 것인가

**넷 다** (2026-07-31 사용자 응답):

- (a) **매일 아침 앱을 열면 오늘 뭘 할지 결정이 끝나 있다** — 계획 도구로서의 완성
- (b) **내가 뭘 모르는지 앱이 나보다 먼저 안다** — 진단 도구로서의 완성
- (c) **시험 전날 무엇을 버릴지 앱이 말해 준다** — 판단 도구로서의 완성
- (d) **앱을 안 열어도 학습이 굴러간다** — 개입 도구로서의 완성

⚠ **넷 다 고른 것의 의미와 대가.** 의미: 이 앱은 대시보드가 아니라 **학습 운영체제**를 지향한다 —
7각도를 전부 유지할 근거가 된다. 대가: **각도 우선순위 신호가 0**이라, 발산이 넷을 고르게 파면
어느 축도 완성에 못 닿는다. 그래서 한 축을 더 둔다:

- **순서 — 미해결로 남긴다** (2026-07-31 사용자 응답: _"3은 애매하고"_). 억지로 정하지 않는다 —
  근거 없이 정한 순서는 다음 발산이 **자신 있게 틀린 곳을 파게** 만든다.
  - **잠정 기준**은 코드가 실제로 투자한 순서다: **(a) ≫ (b) > (d) > (c)** — 배분·배치 엔진이 가장
    두껍고 · CBMS/백지복습이 그다음 · 폰/핫키가 얇고 · **무엇을 버릴지는 코드에 없다.**
  - **언제 결정이 강제되나**: 두 안이 _서로 다른 축을 완성시키는데 둘 다는 못 할 때_. 그때 이 절로
    돌아와 정한다. 그전까지 발산은 7각도를 고르게 돌린다.
- ⚠ 관찰: **(c) 가 유일하게 "빼기"** 축이다. 나머지 셋은 더 잘 보여주거나(a·b) 더 자주
  닿는(d) 것이라 지금 구조 위에 쌓을 수 있지만, (c)는 앱이 **무엇을 하지 말라고 말하는** 것이라
  현재 어디에도 없다. 신규성이 가장 클 후보 = (c).

## 3. 지금 앱 밖에 있는 것 (7번 각도의 씨앗)

_"이 일을 지금 종이 · 머릿속 · 다른 앱 · 그냥 안 함 중 무엇으로 하나?"_

**다섯 줄 전부 "그냥 안 함"** (2026-07-31 사용자 응답: _"4는 지금은 안 하고 있어, 근데 구현해놓고
생기면 작성했을 때 되게"_).

| 단계                   | 지금 어떻게    | 비고 (⚠ 2026-08-01 발산이 코드로 **세 줄을 정정**했다)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 시험 D-day 역산        | **그냥 안 함** | ⚠ **정정**: ~~앱은 주 단위 배분만 안다~~ → **절반만 맞다.** `Item.deadline` 은 스키마·입력 UI(`SubjectDefinition.tsx:267`)·**스케줄러 정렬 키**(`engine.ts:89,120` `_dlIdx`=EDF)·오늘 급함 점수(`focusState.ts:112-114`)·캘린더 칩까지 **이미 배선돼 있다.** 없는 것은 *자리*가 아니라 **① 범위(챕터 N까지) ② 초과의 반대편 가지** ⚠⚠ **2026-08-02 재정정 — "그냥 안 함"의 이유가 바뀌었다**: 씨앗 때 시험일을 비운 것은 회피가 아니라 **정답**이었다(방학이라 존재하지 않는 값 · `deadline` 은 `z.optional` 이고 엔진이 없을 때를 전부 명시 처리한다 — 임의 값은 **가짜 마감 압력 + 초과 경고**를 만들었을 것이다). 즉 이 줄은 지금 *도구가 없어서*가 아니라 **값이 아직 세상에 없어서** 비어 있다. **개강 전까지 이 줄로 읽기 A/B 를 판정하지 말 것** |
| 무엇을 **버릴지** 결정 | **그냥 안 함** | 앱은 더하기만 제안한다. ⚠ **정밀화**: (c)축은 "코드에 없다"가 아니라 **분기 하나가 잘려 있다** — `engine.ts:446-447` 이 부족분을 이미 계산하고 유일한 처방이 `주당 시간↑`(사용자가 할 수 없는 것)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 오답의 **원인** 분류   | **그냥 안 함** | ⚠ **정정**: ~~CBMS 는 오답을 세지 원인을 안 센다~~ → **부정확.** `CBMS_INFO`(`methodology.ts:106`)가 개념/경계/수학/실수/시간 **5분류 + tip** 을 갖고 `mistakes.ts` 가 `topCode` 를 집계한다. 진짜 결함은 **그 코드가 스케줄러·복습 큐 어디에도 안 닿는다**는 것(grep 0건) — "안 센다"가 아니라 **"세는데 아무것도 안 바뀐다"**                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 슬럼프·컨디션          | **그냥 안 함** | ⚠ **정정**: ~~기록하는 곳이 없다~~ → **앱은 이미 안다.** `adherenceFactor`(`priority.ts:70`)가 최근 14일 이행률로 **계획 용량을 0.5~1.0배로 실제로 깎는데 그 사실이 어느 화면에도 없다.** 미충족은 "기록할 곳이 없다"가 아니라 **"앱이 자기가 아는 것을 말하지 않는다"** — 즉 기분 일지를 **안 만들어도 되는** 축이다                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 학기 단위 회고         | **그냥 안 함** | 주간 리뷰까지만 있다. ⚠ **주기가 6개월이라 §1-b 폐기 조건("N주 비면 은퇴")을 _적용할 수 없는 유일한 줄_** — 검증 불가능한 안은 열지 않는다. 그리고 다른 줄이 축적한 데이터가 회고의 재료를 자동으로 만든다(버린 것·보정계수의 학기별 변화) → **1순위가 아니라 부산물**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

> ⚠⚠ **다섯 줄이 같은 종류가 아니다(2026-08-01 발견).** 셋은 _도구가 없는_ 것이고 둘은 ***도구가
> 있는데 말을 안 하거나(④) 첫 입력의 보상이 음수인(①)* 것**이다. 후자는 "빈 자리를 만든다"가 아니라
> **"이미 만든 자리를 살린다"** 이므로 §1-b 의 비대화 위험이 **정의상 0**이다 — 먼저 열 후보가 여기서
> 갈린다. 이 저장소는 "빈 슬롯을 만들지 마라"를 배웠지만 **이미 만든 빈 슬롯이 왜 비어 있는지**는
> 아직 안 물었다.

⚠⚠ **이 표를 읽는 두 가지 방법이 있고, 어느 쪽인지는 아직 모른다.**

- **읽기 A(기회):** 다섯 다 **도구가 없어서** 안 하는 것이다 → 자리를 만들면 하게 된다.
  7번 각도 규율이 _"'그냥 안 함'이 가장 값이 크다"_ 고 적은 것이 이 읽기다.
- **읽기 B(경고):** 다섯 다 **필요가 없어서** 안 하는 것이다 → 자리를 만들어도 영원히 빈다.
  그러면 그건 미충족 해소가 아니라 **비대화**다.
- **읽기 C(2026-08-01 신설 · 실측):** 다섯 다 **앱 자체가 아직 안 쓰이고 있어서**다 → 그러면 A 도 B 도
  못 가른다. ⚠⚠ **이 읽기가 지금 참이다.** 실물 DB 실측(사본 · 원본 무접촉): **과목 1개**(이름이
  기본값 `새 과목` · **챕터 0** · **deadline 빈 문자열**) · `completions`·`records`(CBMS·백지·보충)·
  `summaries`·`week_alloc` **전부 0행** · 일과 9블록 중 **'공부' 블록 0** · 클라우드 마지막 pull
  2026-07-31(낡은 사본이 아니다).
- ⚠⚠ **읽기 C 가 참인 동안 §1-b 의 폐기 조건은 작동하지 않는다** — 어떤 자리를 만들어도 N주 뒤 비어
  있을 것이고, **그 공백은 그 기능에 대한 증거가 아니다.** 즉 A/B 를 가르려면 **씨앗 하나**(실제 과목 +
  챕터 + 시험일)가 물리적 선행이다. 그것 하나가 로드맵의 관측 대기 6건·CBMS 표본·`p_eff` 를 **동시에**
  푼다(로드맵이 이미 _"앱을 한 번 연다"_ 로 적은 그 상류).

> ### ⚠⚠ 읽기 C 갱신 — **씨앗이 들어왔고, 절반만 풀렸다** (2026-08-02 · 커밋 `e07074f`)
>
> 위 실측은 **하루짜리였다.** 다음 날 실 앱에 씨앗이 들어갔다: **과목 4 · 챕터 51 · 312h**
> (`기초 수학` 30 · `과학기술과 법` 14 · `선형대수` 6 · `회로이론` 1) + 클라우드 push 확인.
> **그러나 읽기 C 가 죽은 것이 아니라 축이 갈렸다** — 셋 중 하나만 해소됐다:
>
> | 축          | 상태                                                                                                     |
> | ----------- | -------------------------------------------------------------------------------------------------------- |
> | **챕터 축** | ✅ **해소** — 51챕터가 실재한다. 챕터 수를 전제하던 검증은 이제 돌릴 수 있다                             |
> | **마감 축** | ❌ **해소 불가** — 방학이라 시험일이 *아직 안 정한 값*이 아니라 **존재하지 않는 값**이다(아래 표 ① 참조) |
> | **사용 축** | ❌ **학기 대기** — 매일 여는 상태가 아니다. 방문·이행률·CBMS 표본을 쓰는 검증은 **여전히 못 잰다**       |
>
> ⚠ **그래서 §1-b 의 폐기 조건은 축마다 다르게 적용된다.** 챕터 축에 걸린 자리는 이제 "N주 비면 은퇴"가
> 의미를 갖지만, 마감·사용 축에 걸린 자리는 **개강 전까지 그 공백이 여전히 증거가 아니다.** 발산이
> 신규형을 낼 때 `가장 싼 검증`을 **"지금 종이로 되나 / 개강해야 되나 / 표본이 더 필요한가"** 로
> 갈라 적는 이유가 이것이다(2026-08-02 5회차가 그 열을 도입했다).

- ⚠ **둘은 관측으로 구분되지 않는다** — 그래서 §1-b 규율(가장 싼 자리 · 폐기 조건 · 넣으면 즉시
  달라지는 것)이 **이 표의 모든 항목에 무조건 적용**된다. 다섯 줄을 "확정된 할 일"로 읽지 말 것:
  이건 **가설 목록**이고, 각 줄은 자기 검증을 달고 나와야 한다.
- ⚠ 발산이 이 표를 씨앗으로 쓸 때 **다섯 개를 다 열지 말 것.** 가장 싼 하나를 열어 실제로 채워지는지
  본 뒤 다음을 판단한다(읽기 A/B 를 가르는 유일한 방법이 그것이다).

### 3-b. ⚠⚠ 위 다섯 줄은 **전부 학기 중반의 것이다** (2026-08-02 · 5회차 발산의 여정 실측)

`/개선 발산` 5회차의 각도 7 이 이 질문을 **화면이 아니라 여정으로** 다시 물었다 — 교재를 고르는 것부터
학기가 끝난 뒤까지 **28단계**를 늘어놓고 각 단계에 판별 질문을 걸었다. 결과: **앱이 덮는 것은 3단계**
(과목·챕터 입력 · 주당 배분 · 매일 블록 실행)이고, **셋 다 학기 중반의 실행 구간**이다.

**공백은 양 끝에 몰려 있다** — 지금 §3 표의 다섯 줄은 그 사이 구간만 본 것이다:

- **입구(1~6단계)** — 수강 과목 결정(졸업요건×시간표×부하) · 방학의 선수 결손 되살리기 ·
  강의계획서에서 **평가 비중·시험일·범위** 추출 · 교재 확보 · 주차↔챕터 매핑
- **출구(20~27단계)** — 시험 전날 한 장 요약 · **시험 직후 회수**(막힌 문항이 휘발하기 전) ·
  성적 ↔ 내 공부 방법 귀속 · 학기 종료 시 챕터·카드 처분 · **방학 유지 복습**(안 하면 복귀 첫날 밀림 벽)

> ⚠ **이것이 §1 의 공백과 같은 것을 다른 각도에서 말한다.** §1 은 _"목표를 넣을 자리가 없다"_ 고 적고
> 여기서는 _"학기라는 그릇이 없다"_ 가 된다 — 그리고 **코드가 그것을 확인한다**: `Semester`
> (`schema.ts:171`)는 `id·name·courses` 뿐이라 **날짜가 없고**, 과목이 두 우주에 산다
> (`Course` = 학점·성적 회계 / `Item` = 챕터·주당시간·마감 실행 · **서로를 전혀 참조하지 않는다**).
> 씨앗 커밋이 발견한 _"관측 대기의 정체는 학기 대기였다"_ 는 이 공백의 증상이다.
>
> ⚠ **그래도 §1-b 규율은 그대로 적용된다** — 입구·출구가 비었다는 것이 곧 채워야 한다는 뜻은 아니다.
> 이 여정 표는 **사용자 확인을 받은 것이 아니라 일반적 공학 전공 여정**이므로, 각 줄은 여전히
> **가설**이고 자기 검증(반나절)을 달고 나와야 한다.

## 4. 제약 (바뀌지 않는 것)

- **1인 · 로컬 우선** — 앱 데이터만 클라우드, 볼트·도구·AI 는 PC. 협업·수익화·성장은 이 제품의
  문제가 아니다(발산에서 검색조차 하지 않는다).
- **기기** — 데스크톱(Tauri 셸)이 본체, 폰은 지하철 5분짜리 보조.
- **볼트 파이프라인이 상류다** — 허브는 지금까지 볼트를 **읽기만** 했다. 쓰기는 repo 경계를 넘는다.

## 5. 갱신 규칙

- **학기가 바뀌거나 목표가 바뀌면 여기부터 고친다.** 낡은 방향은 없는 방향보다 나쁘다 —
  발산이 자신 있게 틀린 곳을 판다.
- ⚠ **§1·§3 이 채워지면(실제 시험일이 생기고, 다섯 줄 중 하나를 실제로 쓰기 시작하면) 그때가 가장
  큰 갱신이다** — 그 순간 §3 의 "읽기 A vs B"가 관측으로 갈린다. 빈 채로 N주가 지난 자리는
  §1-b 폐기 조건대로 은퇴시키고 **그 사실을 여기 적는다**(실패한 가설도 방향의 일부다).
- ⚠ **§3 은 사실 대장이 아니라 _가설 목록_ 이다** — 2026-08-01 발산이 코드로 **세 줄을 정정했다**
  (①·③·④). 다음 발산도 이 표를 읽기 전에 **코드로 한 번 대조하라**: 씨앗이 된 문장이 낡으면 그
  발산은 자신 있게 틀린 곳을 판다.
- ⚠⚠ **갱신은 발산이 스스로 하지 않는다**(`/개선 발산` §5 폐루프 차단 · 2026-08-01 신설). 4회차가
  자기 산출로 이 문서를 고쳤고, 그러면 **3회차가 쓴 앵커를 4회차가 읽고 4회차가 고친 앵커를 5회차가
  읽는** 구조가 된다(시스템이 자기 출력을 입력으로 먹으며 회차마다 분산이 준다). 발산은 불일치를
  발견하면 **리포트에 "방향 갱신 제안"으로 적고 멈추고**, 반영은 **사용자 확인 뒤 별도 편집**이다.
  - 아래 2026-08-02 갱신은 그 절차를 밟았다: 5회차가 제안 → **사용자가 "지금 반영해줘"로 승인** → 반영.
    "사실 정정이니 괜찮다"는 승인 없이 쓸 수 있는 사유가 **아니다**(그게 정확히 4회차의 논리였다).
- 마지막 갱신: **2026-08-02**(사용자 승인 · 읽기 C 를 3축으로 분해 + §3 표 ① 재정정 + §3-b 여정
  입구/출구 신설 · `/개선 발산` 5회차 제안).
  앞선 갱신: 2026-08-01(§3 세 줄 정정 + 읽기 C 신설 · 4회차 P 시리즈) ·
  2026-07-31(초안 생성 + 사용자 응답 1차 반영)
