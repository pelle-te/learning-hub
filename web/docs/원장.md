# 원장 — 러닝허브 · "다음 뭐"의 단일 출처

개설 2026-08-20. 이전 발견은 아카이브 문서에 있고, **여기는 새 회차부터 채운다.**

**열린 것만 있는다.** 닫힌 것은 `원장-아카이브.md` 로 나간다.
`/리뷰`(모든 축)가 append 하고, **`/실행`만 닫는다.**

ID: `C`코드 `P`성능 `U`ux `D`데이터 `V`규약 `I`발상 + 3자리. 번호 재사용 금지.
상태: `열림` `진행` `닫힘` `기각` `재개방` `보류(사유)`

> 2026-08-21 ux 축 1회차의 U001~~U038 은 같은 날 `/실행`이
> **전량 종결**했다(재현 불가 0건 · 처방과 다르게 고친 것 14건) — 닫기 노트는 `원장-아카이브.md`.
> 그 앞 회차(데이터 축 D001~~D019)도 같은 날 종결됐다.
>
> ⚠ **다음 ux 회차가 읽을 것 셋**(아카이브 회차 총평의 요지):
> ① 실측이 리포트와 갈린 곳이 셋 있었다 — 리포트의 수치는 **재는 범위**와 함께 읽어라.
> ② U019 는 «중복»이 아니라 **다른 계약**이었다(`em` vs `px`) — 다시 열지 마라.
> ③ 이 회차가 세운 집행자 일곱이 다음 회차의 `[측정]` 바닥이다(`렌즈-오버레이.md §0-B`).
>
> 아래 발상 항목 셋(D020~D022)은 심각도가 없고 **열린 채로 남는다.**

## 열린 항목

| ID   | 발견일     | 축   | 심각도 | 비용 | 상태 | 제목                                                               | 근거                                                                                                                                                                          |
| ---- | ---------- | ---- | ------ | ---- | ---- | ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| U039 | 2026-08-21 | ux   | Nit    | S    | 열림 | `--fs-*` 사다리에 24px 칸이 없다(`.ds-roNum` 만 생 px 으로 남았다) | U034 실행 중 발견 — 22 ↔ 26 사이라 반올림하면 위계가 붙거나 카드 높이가 바뀐다. «칸을 더할 것인가»는 디자인시스템 결정                                                        |
| V027 | 2026-08-21 | 규약 | Nit    | S    | 열림 | 불변식 ⑧·⑪·⑬이 아직 **지역 파일 워커**를 판다(⑪은 지역 `strip` 도) | ⑩은 2026-08-21 에 닫혔다(거기만 **줄 주석을 안 걷어** 오탐 위험이 실재했다). 남은 셋은 주석 제거가 이미 옳아 **보일러플레이트 중복일 뿐** — 그래서 Minor 에서 Nit 으로 내렸다 |

## 축 밖 이월 (다음 회차의 입력 · 미검증)

| ID   | 발견일     | 축   | 상태 | 제목                                                                                                  |
| ---- | ---------- | ---- | ---- | ----------------------------------------------------------------------------------------------------- |
| V023 | 2026-08-21 | 규약 | 열림 | 주석·머리주석 다섯이 사실과 다르다(`tabs.ts:432,183` · `GlanceMode:41` · `MiniHud:58` · `Subject:17`) |
| V024 | 2026-08-21 | 규약 | 열림 | 시각 회귀가 「지금과 같은가」만 물어 워드마크 4줄 붕괴를 정답으로 굳혔다                              |
| V025 | 2026-08-21 | 규약 | 열림 | `dist/` 가 커밋돼 있고 게이트가 그 위에서 돈다                                                        |
| V026 | 2026-08-21 | 규약 | 열림 | 카드 표면 래칫 25/25 — 여유 0이라 다음 리팩터가 즉시 막힌다                                           |
| C023 | 2026-08-21 | 코드 | 열림 | `contentSearch` 의 목적지 어휘가 4종으로 갈렸다                                                       |
| C024 | 2026-08-21 | 코드 | 열림 | 크롬 `action` 이 단수라 「호스트로 돌아가기」와 「주 동사」가 슬롯을 다툰다                           |
| C025 | 2026-08-21 | 코드 | 열림 | `useKeymapDoc` 의 「선언과 구현이 두 곳」 구조가 실제로 청구됐다(U033)                                |

> ⚠ **ux 회차가 이 표의 일부를 이미 먹었다**(2026-08-21 · 중복 착수 방지):
> · **V023 의 다섯 중 둘이 닫혔다** — `Subject:17`(「⌘K 챕터 히트가 자기 자리에 착지한다」)은
> U011 이 **참으로 만들었고**, `a11y.spec.ts` 머리주석의 거짓 한 줄은 U015 가 정정했다.
> 남은 셋은 `tabs.ts:432,183` · `GlanceMode:41` · `MiniHud:58` 이다.
> · **V024 는 그대로 열려 있다.** U021 이 그 사각의 **한 사례**(워드마크)를 고쳤을 뿐,
> 「시각 회귀가 지금과 같은가만 묻는다」는 게이트 설계 문제는 남는다 — 다만 이 회차가
> 포커스·확대·창 크기 축에 케이스를 세워 **그 사각의 넓이는 줄었다**(오버레이 §1-C 참조).
> · **C025 의 사례(U033)는 닫혔지만 구조는 남는다** — 없는 키를 광고하는 것을 막는 집행자는 아직 없다.
> | P023 | 2026-08-21 | 성능 | 열림 | 히트맵 셀 `<i>` 수백 개 · `HEAT_CAP=60` 의 DOM 비용 |

## 발상 항목 (심각도 없음)

| ID   | 발견일     | 상태 | 제목                                                      | 표면                | 전제                                          | 최싼검증                                                                | 근거                                                                            |
| ---- | ---------- | ---- | --------------------------------------------------------- | ------------------- | --------------------------------------------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| D020 | 2026-08-21 | 열림 | 스키마 버전 핸드셰이크 — 프로토콜에 `version` 이 0회      | 동기화 계약         | 클라이언트 배포는 동시에 안 일어난다          | 헤더 `x-hub-schema` 1줄 + 서버가 `serverSchema` 를 돌려주고 클라가 배너 | [진보](리뷰/2026-08-21-데이터/리포트.md#5-진보--표본에는-있고-우리에겐-없는-것) |
| D021 | 2026-08-21 | 열림 | Expand/Migrate/Contract 순서 규약(제거는 앱→DB)           | 마이그레이션 README | 11개가 전부 확장 전용이라 «아직 안 지웠을 뿐» | README 세 줄 · 기계 0                                                   | [진보](리뷰/2026-08-21-데이터/리포트.md#5-진보--표본에는-있고-우리에겐-없는-것) |
| D022 | 2026-08-21 | 열림 | 유실 창을 수치로 — 「N일 전」이 아니라 「N건이 로컬에만」 | 백업 배너           | 백업의 값은 «있다»가 아니라 «무엇을 잃나»     | 배너 문구 하나 · 질의는 아웃박스가 이미 센다                            | [진보](리뷰/2026-08-21-데이터/리포트.md#5-진보--표본에는-있고-우리에겐-없는-것) |

---

## 발상 축 1회차 (2026-08-22) — `I001`~`I053`

> 리포트: [`리뷰/2026-08-22-발상/리포트.md`](리뷰/2026-08-22-발상/리포트.md) · 앵커: [`앵커.md`](리뷰/2026-08-22-발상/앵커.md)
> **심각도·비용·티어가 없다**(발상 축은 커널 §심각도에서 면제). 닫는 것은 `/실행` 뿐이다.
>
> ⭐ **먼저 읽을 것 셋** — 이 표의 항목 대부분이 이 셋에 매달려 있다:
> ① **학습 데이터 표가 전부 0행이다**(실 DB 실측 · `앵커.md §3-b`). 「입력이 왜 안 들어오나」가
> 이 회차의 중심이고, 그걸 안 풀면 나머지는 빈 화면을 더 예쁘게 만드는 일이다.
> ② **관측이 자기 근거를 파괴하고 있다**(`앵커.md §3-a` · 리포트 §4-E). `I030` 이 여럿의 선행이다.
> ③ **상충 셋을 메인이 판정했다**(리포트 §5) — `I023`(LLM) · `I030~I032`(관측) · `I033`(폰).
> 그 판정을 안 읽고 개별 항목만 집으면 **반대 방향 둘을 동시에 착수하게 된다.**
>
> ⚠ **`D020`(스키마 버전 핸드셰이크)은 새로 열지 않는다 — 이번 선례 해부가 그 항목의 근거를 보강했다**:
> 밖의 대응 메커니즘은 배포 플랫폼의 _version locking_(`?dpl=`·`x-deployment-id`)이고, 그 대가는
> **옛 배포를 살려 두는 보존 창**이다. 우리 형태(자동 갱신 폰 + 수동 갱신 exe 가 같은 D1)가 정확히
> skew 의 정의라는 것이 이번에 `[측정]`으로 확인됐다(출처: vercel.com/docs/skew-protection · 2026-08-22).

### A. 밖에서 이미 일어난 학습을 받는다 (묶음 A)

| ID   | 발견일     | 상태 | 제목                                                                             | 표면                                       | 전제                                                       | 최싼검증                                                                     | 근거                                                                                        |
| ---- | ---------- | ---- | -------------------------------------------------------------------------------- | ------------------------------------------ | ---------------------------------------------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| I001 | 2026-08-22 | 열림 | 정본 원장(`_챕터원장.json` 5단계)이 계획 엔진의 입력이 된다                      | 원장↔과목 결합 + 「반영」 승인 줄          | 원장 `slug`/`chapter_id` 가 앱 과목·챕터와 맞물린다        | 원장을 열어 `items` 와 손으로 대조 · 매칭률 <60%면 조인 설계가 먼저          | 리포트 §4-A                                                                                 |
| I002 | 2026-08-22 | 열림 | Anki 를 「몇 장 남았나」가 아니라 **「오늘 몇 장 했나」**로 읽어 완료를 파생한다 | 기존 화면 · Rust 변경 0 가능               | 덱→과목 귀속이 리뷰 로그 해상도에서도 된다                 | AnkiConnect 에 `findCards {"query":"rated:1"}` 한 번 · 이어 `rated:7` 분포   | 리포트 §4-A · ⚠ `getNumCardsReviewedByDay` 는 **이미 부른다**(날짜별 총량) — 없는 것은 귀속 |
| I003 | 2026-08-22 | 열림 | 파이프라인 세션이 학습 노동으로 계상된다(`진행.py` 상태 + 블록)                  | 새 표면(파이프라인 축) + `alloc` 세션 종류 | `진행.py list` 출력이 안정적 · 주당 유의미한 시간을 먹는다 | 그 도구 1회 실행 + 최근 2주 커밋으로 주당 챕터 수                            | 리포트 §4-A                                                                                 |
| I004 | 2026-08-22 | 열림 | 기록의 방향을 뒤집는다 — 「계획대로 했나」가 아니라 **「무슨 일이 있었나」**     | 새 표면(하루 되짚기 한 장)                 | 사후 30초 입력이 사전 계획 준수보다 채워진다               | 3일간 종이에 하루 한 줄 · 3일 다 적히면 참                                   | 리포트 §4-A · 사전형은 실측 0행으로 이미 반증                                               |
| I005 | 2026-08-22 | 열림 | 실제 소요를 되돌려 넣는다(`actualMin` 이 집중 세션 완주에만 실린다)              | 기존 화면(하루/오늘) + 폰                  | 사후에 ±15분으로 말할 수 있다                              | `completions` 채움률 — **분모가 0이라 이미 증명됨**. 나머지는 I004 에 얹는다 | 리포트 §4-A                                                                                 |

### B. 파이프를 양방향으로 (묶음 B)

| ID   | 발견일     | 상태 | 제목                                                                     | 표면                                           | 전제                                                     | 최싼검증                                                                                | 근거                                                                                            |
| ---- | ---------- | ---- | ------------------------------------------------------------------------ | ---------------------------------------------- | -------------------------------------------------------- | --------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| I006 | 2026-08-22 | 열림 | 「집중 시작」이 **대상을 실제로 연다**(챕터 노트·교재·덱)                | today 히어로 CTA + 새 권한·커맨드              | 블록 대상이 파일로 지목 가능하다                         | 오늘~2주 블록 중 **유일 노트 파일로 해석되는 비율** · <60%면 매칭이 먼저                | `[측정]` capabilities 12종에 `opener` 없음 · 소스 grep 0                                        |
| I007 | 2026-08-22 | 열림 | `sources/` 교재를 앱이 처음으로 본다(챕터가 몇 쪽·몇 절인지 안다)        | 새 산출물 + `items`/`schedule` 분량 축         | 교재 md 절 구조가 챕터명과 맞물린다                      | 교재 하나의 챕터별 문자 수 vs 실측 소요의 상관 · 없으면 안 전체가 무너진다              | `[측정]` `sources` 참조 **0건**                                                                 |
| I008 | 2026-08-22 | 열림 | `.ics` 를 **거꾸로 구독**한다(학교 시간표·학사일정 인입)                 | `integrations` 한 줄 + `icsParse`              | 학교가 `.ics` 를 준다                                    | 포털에서 `.ics` 받아 본다 · 안 되면 죽고 I010 만 남는다                                 | `[측정]` `ics.ts` 는 빌더만 · 파서 0                                                            |
| I009 | 2026-08-22 | 열림 | 오답·막힘이 **Anki 카드 초안**이 된다(앱→Anki 쓰기 경로 0)               | 새 표면(초안 큐 · 승인만) + `addNote` 중계     | `addNote` 가 이 덱 구조에서 돈다 · 별도 덱 분리 가능     | 테스트 덱에 `addNote` 1회. 후보 공급은 I011·I004 선행                                   | `[측정]` `addNote                                                                               | createDeck | guiAddCards` **0건** |
| I010 | 2026-08-22 | 열림 | 학기 중 **상시 인입구**(휴강·연기·범위 변경) — 지금은 개강 1회뿐         | `SyllabusIntake` 를 깔때기로 개편 + today 배지 | 계획을 바꾸는 공지가 월 1회 이상 온다                    | 지난 학기 공지 10건을 **기존 파서**에 먹여 `unparsed` 비율                              | 리포트 §4-B · `syllabusIntake` 머리주석이 「자주 틀린다」고 자인                                |
| I011 | 2026-08-22 | 열림 | 종이/사진이 인입구가 된다 — 앱 전체에 **이미지 필드가 0개**              | 새 표면(캡처 인입) + 폰 캡처바 + 파일 저장     | 사진 한 장이 텍스트 4칸보다 자주 남는다                  | 지난 2주 사진첩의 「공부 사진」 수 vs `records` 0행                                     | ⚠ **D1 계약이 전부 불투명 JSON** 이라 바이너리가 그 관용구를 처음 깬다(비용을 깎지 않고 적는다) |
| I012 | 2026-08-22 | 열림 | 시험 직후 **회수 시트가 폰에** 있다(지금은 데스크톱에만)                 | 폰 다섯째 뷰(창 열릴 때만)                     | 시험 종료 시각이 앱에 있다 · **I033 선행**               | `Item.exams` 채움 건수와 시각 정보 유무                                                 | `recallWindows` 가 자기 전제와 어긋난 표면에 있다                                               |
| I013 | 2026-08-22 | 열림 | 수업이 과목의 **일부**가 된다 — 지금은 「가용시간을 깎는 장애물」        | 새 표면(과목 안 수업 축) + 수업 직후 폰 한 탭  | 과목당 30초 1회 연결을 한다                              | 실 DB `routine` 값에 과목명이 이미 들어 있나 · 강의시간 합 ÷ 자습 목표 합               | `[측정]` `RoutineBlockSchema` 에 `sid` 없음                                                     |
| I014 | 2026-08-22 | 열림 | **시험 결과가 들어올 자리** — `Exam` 에 결과 필드 0개                    | 새 표면(시험 결산) · `records` 경로면 D1 DDL 0 | 점수와 틀린 문항 번호를 돌려받는다                       | 지난 시험 하나의 챕터별 실점 vs 같은 시점 `chapterStrength` **순위 상관** · 같으면 폐기 | `[측정]` `ExamSchema` 4필드 · 앱의 숙달 신호가 외부 정답지를 한 번도 안 만난다                  |
| I015 | 2026-08-22 | 열림 | **문제풀이가 세션 유형**이 된다(지금은 「공부 아닌 시간」으로 명시 제외) | 새 표면(세트 단위 원장)                        | 번호만으로 나중에 그 문제를 찾을 수 있다 · 세트 1건 30초 | 한 세트를 그 형식으로 옮겨 적어 본다 · 30초 넘으면 형식이 틀렸다                        | `[측정]` `SessionType` 5종에 없음 · `choreWeeklyH` 주석이 명시 제외                             |
| I016 | 2026-08-22 | 열림 | 「모의시험」에 **내용과 결과**를 준다(지금 `chapters:[]` 빈 껍데기)      | I014 의 그릇 + I015 의 재료                    | 재료가 있다(I015·I011 선행)                              | 지금 손에 있는 재료로 모의 1회를 실제로 만들어 본다                                     | `[측정]` `engine.ts` 가 `sid:'mock'`·`chapters:[]` 를 꽂는다                                    |
| I017 | 2026-08-22 | 열림 | 수업 중 캡처가 **지금 시각의 블록으로 자동 귀속**된다                    | 폰 캡처바 개편 · 새 표면 0                     | 시간표가 앱에 들어 있다(I008 사실상 선행)                | 최근 backlog 중 과목 태그를 가진 비율                                                   | 앱은 그 시각에 어느 수업인지 **이미 안다**(today 흐름이 그린다)                                 |

### C. 미래를 보는 층 (묶음 C — 밖의 표본이 이 자리를 셋 갖는다)

| ID   | 발견일     | 상태 | 제목                                                                         | 표면                                                   | 전제                                                                | 최싼검증                                                            | 근거                                                                                      |
| ---- | ---------- | ---- | ---------------------------------------------------------------------------- | ------------------------------------------------------ | ------------------------------------------------------------------- | ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| I018 | 2026-08-22 | 열림 | **시뮬레이션 하네스** — `simulate(state, patch)` 로 what-if 를 묻는다        | `lib/scheduler/simulate.ts` + 기존 화면 미리보기 한 줄 | `schedule()` 이 부작용 없이 반복 호출된다(주석: 회귀로 동결된 코어) | 시험 날짜만 바꿔 20회 실행 시간 측정 · <50ms 슬라이더 / <500ms 버튼 | `[측정]` 순수 함수인데 소비처 전부 1회 실행 · `shortfalls` 가 경고에서 멈춘다             |
| I019 | 2026-08-22 | 열림 | **부하 곡선** — 사다리를 바꾸면 4주 뒤 어떻게 되는지 먼저 본다               | 복습 설정 곁 · 노브는 곡선 뒤에                        | 현재 챕터 카탈로그로 일별 복습 수를 투영할 수 있다                  | 곡선 두 개를 그려 본다(현행 vs 한 칸 늘림)                          | 선례 M14(Anki/FSRS) · ⓒ **정직하게 「더 잊는다」를 판다**                                 |
| I020 | 2026-08-22 | 열림 | 복습 due **평탄화** — 같은 날 시작한 챕터가 영원히 같은 날 몰린다            | `spacedReview` 내부 · UI 0                             | ±1일 흔들기가 사다리 의미를 안 깬다                                 | 현재 카탈로그로 일별 due 히스토그램 · 몰림이 없으면 폐기            | `[측정]` `scheduler/` 에 fuzz·balance **0건** · ⚠ UI 는 «총량은 안 준다»를 그대로 적는다  |
| I021 | 2026-08-22 | 열림 | 규칙 변경의 **카나리** — 스케줄러 규칙이 바뀌면 전 과목에 100% 즉시 적용된다 | 규칙에 `variant` 태그                                  | 절반 분할이 학습을 망치지 않는다                                    | 과목 절반(또는 홀수 주)에만 새 규칙 · 4주 뒤 비교                   | 선례 M2 · ⚠ 표본이 작으면 비율이 안 맞는다(문서가 자인) → 승격 판정은 사람이              |
| I022 | 2026-08-22 | 열림 | **변경 저널** — flush 때 이미 만드는 pre-image 를 표에 남겨 시간축을 만든다  | 표 하나 + `write.ts` 한 줄                             | 하루 저널 바이트가 SQLite 로 감당된다                               | `pushUndo` 에 `preImageBytes` 카운터 · 하루 실사용                  | `[읽음]` pre-image 는 이미 전량 조립돼 있다 · 종단 질문이 `day_signals` 5정수에 갇혀 있다 |

### D. 표면을 줄인다 (묶음 D — **신설의 짝**)

| ID   | 발견일     | 상태 | 제목                                                                         | 표면                               | 전제                                                             | 최싼검증                                                                            | 근거                                                                                                                      |
| ---- | ---------- | ---- | ---------------------------------------------------------------------------- | ---------------------------------- | ---------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| I024 | 2026-08-22 | 열림 | **하루를 한 자리로** — 지금 하루를 말하는 표면이 일곱~아홉                   | `/day/:ds` 로 수렴 · 오버레이 제거 | 오늘과 어제가 같은 화면의 매개변수(앱이 `altRoute` 로 이미 인정) | 「오늘 학습」과 `/day/오늘` 을 나란히 놓고 **같은 질문에 다른 답을 주는 칸**을 센다 | 수렴 ① · `DayBufferOverlay` 머리주석의 자기 자백                                                                          |
| I025 | 2026-08-22 | 열림 | 하위화면 문법을 **라우트 하나**로(지금 네 벌)                                | 레일 + 각 feature 자체 바          | 뷰로 접힌 13이 실제로는 독립 화면이다                            | 경로+쿼리 전 조합에 ①이름 ②⌘K ③링 도달을 표로 찍는다                                | `[측정]` `role:'lens'`·`?view=`·`useUI.schedView`·로컬 `useState` · **첫 조각: `degree` 만 5라우트**                      |
| I026 | 2026-08-22 | 열림 | `primary`(화면당 단 하나의 수) 계약을 **축소**한다 — 절반이 `null`           | 상단 크롬 축소 + `⇧G` 제거         | 대표 수는 화면 안 맥락에서 더 잘 읽힌다                          | `primary` 를 숨기고 트랙 A · **그 수가 화면 안 어디에도 없는** 화면을 센다          | `[측정]` `primary: null` **10** / 비-null 15(조건부 포함) · `GlanceMode` 가 **자기 검증 조건을 적어 두고 아무도 안 셌다** |
| I027 | 2026-08-22 | 열림 | **레일을 줄인다**(접기 노브는 처방이 아니라 자백)                            | 레일 + 설정 한 절 제거             | 15줄이 줄여야 할 수다                                            | `railLayout` 이 기본값이 아닌가 · **무엇을 접었는지가 곧 답**                       | `[측정]` 상위 1개가 32%, 하위 둘이 각 1회                                                                                 |
| I028 | 2026-08-22 | 열림 | 「하루의 한 칸」 타입을 **하나로**(지금 다섯 스키마)                         | 캘린더 입력부 + 스키마 통합        | 다섯의 차이가 타입이 아니라 필드다                               | 다섯 스키마 필드를 겹쳐 교집합/차집합 · 차집합 ≤3이면 확정                          | **첫 조각: `Task`+`PlanEvent` 병합**(`done?` 유무가 유일 차이)                                                            |
| I029 | 2026-08-22 | 열림 | ⟳ **`degree` 회계 축 축소** — `Course` 를 지우고 `Item` 에 `credits`·`grade` | 레일 한 줄 + 화면 다섯             | 학점·GPA 의 정본은 학교 포털이다                                 | `?view=` 별 방문은 못 세므로 → **`courses[]` 중 `itemId` 붙은 비율**                | 수렴 ④ · `[측정]` 2,215줄 · ⚠ **승인 경계**(리포트 §8)                                                                    |

### E. 관측을 정직하게 (묶음 E — `I030` 이 여럿의 선행)

| ID   | 발견일     | 상태 | 제목                                                             | 표면                           | 전제                                      | 최싼검증                                                                              | 근거                                                                 |
| ---- | ---------- | ---- | ---------------------------------------------------------------- | ------------------------------ | ----------------------------------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| I031 | 2026-08-22 | 열림 | 관측 **질의층 + 표본충분성 판정**(`observationsReady(question)`) | `lib/` 모듈 하나 · 표면 0      | I030 이 먼저 · 표본이 찬 질문이 최소 하나 | 실 DB 에 SQL 넷을 던져 **결정을 바꿀 만큼 선명한 것이 몇 개인가** · 0이면 아직 이르다 | 읽는 곳이 «관측이 살아 있음을 보여 주는 화면»뿐 · 행동을 바꾸는 곳 0 |
| I032 | 2026-08-22 | 열림 | 관측 원장 **축소**(`route_visits` 하나만 남긴다)                 | 표 셋 + 백업 목록 + 설정 4블록 | I030 뒤에도 나머지 셋이 결정을 못 낸다    | `idle_spells` **0행** · `day_signals` 8행 값 거의 전부 0                              | ⚠ I031 과 **반대 방향** — 리포트 §5-B 판정을 먼저 읽어라             |

### F. 그 밖의 없는 것

| ID   | 발견일     | 상태 | 제목                                                                         | 표면                                         | 전제                                       | 최싼검증                                                                                   | 근거                                                                                                                 |
| ---- | ---------- | ---- | ---------------------------------------------------------------------------- | -------------------------------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------- |
| I035 | 2026-08-22 | 열림 | 과목에 **id 바인딩**을 준다 — 지금 이름 부분문자열로 조인한다                | 과목 카드 「연결」 줄(`Item.source` 가 반쪽) | 이름 흔들림이 표기가 아니라 정체성 문제다  | `subjectJoin` 을 실데이터로 · **matched 중 gap>0 건수**가 0이 아니면 즉시 참               | `subjectMatch.ts` 가 자인: _"그 규칙은 `masteryNeed` 를 통해 배분을 구동한다"_ · **첫 조각: 매칭기를 제안기로 강등** |
| I036 | 2026-08-22 | 열림 | ⟳ **저장 엔진을 하나로**(브라우저도 OPFS SQLite)                             | 표면 0 · 층이 사라진다                       | 트랙 A 가 OPFS wasm SQLite 위에서 돈다     | dev 진입점에서 `enableBrowserDb()` 켜고 트랙 A 1회 · 깨진 스냅샷 수 = 비용                 | ⚠ **승인 경계**(리포트 §8) · `phone.spec.ts` 가 이미 OPFS 를 검사한다                                                |
| I037 | 2026-08-22 | 열림 | ⟳ **충돌을 구조적으로 불가능하게**(폰은 append-only)                         | 표면 넷이 사라진다                           | 폰의 같은 행 편집이 드물다                 | D1 에서 같은 `(table,key)` 를 두 기기가 같은 날 쓴 건수 · 0이거나 전부 `completions` 면 참 | ⚠ **승인 경계** · **첫 조각: `toggleDone` 을 단조 결합으로**                                                         |
| I038 | 2026-08-22 | 열림 | **세대 백업 + 위험 작업 직전 스냅샷**(지금은 사고 이후 회수만)               | 설정 한 줄(복원=파일 고르기)                 | SQLite 파일 복사로 충분(WAL 체크포인트 후) | `semesterClose` 직전 복사 1회를 손으로 해 본다                                             | 선례 M18 · ⚠ 밖의 한계 문구도 함께 적는다(같은 디스크다)                                                             |
| I039 | 2026-08-22 | 열림 | **스키마 해시 가드** — 번호는 같은데 내용이 다른 경우를 못 잡는다            | 부팅 경로 · 기존 다운그레이드 화면 재사용    | `PRAGMA table_info` 해시가 안정적          | 부팅에 해시 계산 + `meta` 저장 한 번                                                       | 선례 M7 · ⚠ 과민성이 대가(무해한 변경도 버전을 올린다)                                                               |
| I040 | 2026-08-22 | 열림 | 「**오늘은 빼기**」 — 미루기가 하나뿐이라 영구 포기와 하루 미룸이 같은 버튼  | 복습 화면 동사 하나 추가                     | 두 시제의 배타성을 타입으로 못박을 수 있다 | 하루짜리 빼기를 한 주 써 본다                                                              | 선례 M19 · `reviewHold` 는 「중지」에 해당하고 **묻기 축이 없다**                                                    |
| I041 | 2026-08-22 | 열림 | leech 의 **처방 선택지** — 표식은 있는데 자료를 바꾸러 갈 자리가 없다        | leech 표식 곁 행동 셋                        | leech 판정이 실제로 뜬다(데이터 필요)      | 현재 카탈로그에 leech 가 몇 개인가                                                         | `spacedReview.ts` 가 자인: _"처방이 간격이 아니라 자료다"_                                                           |
| I042 | 2026-08-22 | 열림 | **캡처 트리아지** — 판정 없이 백로그로 직행한다                              | 캡처 착지처를 「미분류」로 · 나가는 길 셋    | 판정 노동이 새 백로그가 되지 않는다        | 미분류 칸을 한 주 운영 · 안 비면 그 자체가 답                                              | 선례 M9 · ⚠ **스누즈가 이 앱엔 없다**(`reviewHold` 는 자동 만료 없음)                                                |
| I043 | 2026-08-22 | 열림 | **임시 학습 세트**(검색 결과로 세션 · 기본 preview 모드)                     | `contentSearch` 결과에 버튼 하나             | preview 모드가 사다리를 안 건드린다        | 한 번 만들어 돌려 본다                                                                     | 선례 M17 · ⚠ 매뉴얼이 _"반복 사용에 부적절"_ 을 함께 문서화한다                                                      |
| I053 | 2026-08-22 | 열림 | 배분판이 **「전공 밖」 레인**을 갖는다(⛔ **데이터 계약 0** — 손으로 놓는다) | `alloc` 보드 레인 하나                       | 교양축이 유의미한 시간을 먹는다            | `survey/` 산출물 최근 2주 생성 건수 × 건당 눈대중 · 주 2h 미만이면 한 줄 표시              | ⚠ 부모 규약의 «계약 0» 과 부딪히므로 **분모만 정직하게** 형태로만 성립(리포트 §7)                                    |

### 🧹 순수 은퇴 후보 (신설의 짝 · 같은 저울)

| ID   | 발견일     | 상태 | 제목                                                                  | 지우면 나빠지는 것                           | 대체 경로                               | 근거                                                                                            |
| ---- | ---------- | ---- | --------------------------------------------------------------------- | -------------------------------------------- | --------------------------------------- | ----------------------------------------------------------------------------------------------- |
| I044 | 2026-08-22 | 열림 | `graph`(학습 구조도) **2,249줄** — 은퇴 화면인데 파일도 폴더도 그대로 | 과목-챕터의 _공간적_ 배치                    | `items` 목록 · 선수 관계는 텍스트 한 줄 | 스냅샷 실물이 **노드 5개**를 화면 가득 그린다 · 「한눈에」의 반례                               |
| I045 | 2026-08-22 | 열림 | 앰비언트 WebGL 오로라 + 필름 그레인 **373줄**                         | 배경이 CSS 그라데이션이 된다(폴백 이미 구현) | `FALLBACK_BG` 3줄                       | ⚠ **Windows TDR 실사고 전력** — 장식이 사용자에게 실제 피해를 줬고 그걸 막느라 코드가 더 커졌다 |
| I046 | 2026-08-22 | 열림 | 게이미피케이션 3종(스트릭·18주 히트맵·최장 연속 트로피)               | 동기부여 장치                                | 그 자리를 「밀린 것 수」에 준다         | `[측정]` `completions` 0행 → 실물에서 **항상 0** · 0을 크게 그리는 것은 조망이 아니다           |
| I047 | 2026-08-22 | 열림 | 명사 주소 셋(`/chapter/*`·`/week/:ws`·`/exam/:sid`) — **관측된 0**    | 알림·⌘K 가 그 명사를 못 가리킨다             | `subject` 만 남긴다                     | `[측정]` `routeKey` 로 정상 집계되는데 5키 전부 0행                                             |
| I048 | 2026-08-22 | 열림 | `role:'retired'` **어휘 자체** — 「영원히 유예」의 도구가 됐다        | 옛 URL 리다이렉트                            | 두 갈래만: 레일에 선다 / 없다           | `tabs.ts` 가 이미 그 진단을 적고 **어휘는 남겼다** · I034 와 짝                                 |
| I049 | 2026-08-22 | 열림 | 말 걸기 채널 넷의 중복(레일 배지·작업표시줄·OS 알림·트레이 상주)      | 앱을 안 열면 모른다                          | 레일 배지 하나                          | `[측정]` 셋이 **글자 그대로 같은 식**을 쓰고, 그 식의 입력이 전부 0행                           |
| I050 | 2026-08-22 | 열림 | `.ics` **살아있는 구독 피드** + 인증 없는 공개 GET                    | 캘린더에서 학습 블록이 사라진다              | 1회 내보내기(`files.rs`)                | `[측정]` `week_alloc` 0행 = 지금 발행되는 것은 **빈 캘린더** · 유일한 무인증 표면               |
| I051 | 2026-08-22 | 열림 | 실시간 poke 소켓(`live.rs`+DO+클라 = 648줄)                           | 다른 기기 편집이 최대 5분 늦다               | 폴링 + 포커스 복귀 pull                 | `[측정]` `tombstones` 0 · 동기화할 편집이 8일간 0건                                             |
| I052 | 2026-08-22 | 열림 | `telemetry`(클라 221줄 + 서버 스키마) — **약함**                      | 폰 브라우저 스택이 안 남는다                 | `ErrorBoundary` 가 화면에 스택을 그린다 | 사용자=개발자 1인 · ⚠ 근거가 다른 항목보다 약하다(I033 결과에 매달린다)                         |

### 축 밖 이월 — 발상 축 1회차가 본 것 (미검증 · 다음 회차의 입력)

| ID   | 발견일     | 축     | 상태 | 제목                                                                                                 |
| ---- | ---------- | ------ | ---- | ---------------------------------------------------------------------------------------------------- |
| V029 | 2026-08-22 | 규약   | 열림 | 문서 1.7MB — 아카이브·끝난 발주서가 살아 있고 CLAUDE.md 가 **경고문 넷**으로 그것을 막는다           |
| V030 | 2026-08-22 | 규약   | 열림 | `e2e:motion` 의 감도 바닥 vs 불변식 ⑥ — 정적 집행자가 이미 막는 것을 스냅샷 4장이 겹쳐 막는가        |
| C026 | 2026-08-22 | 코드   | 열림 | 루트 `/`(Workers 오리진)가 문이 아닌데 열린다 → localStorage 로 갈라진다                             |
| C027 | 2026-08-22 | 코드   | 열림 | `/degree?view=*`·`/find?view=guide`·`/items?view=sheet` 가 로스터에 없어 **호스트 이름으로 굳는다**  |
| C028 | 2026-08-22 | 코드   | 열림 | `state.retrievals`·`_ankiFile`·`_icsExport` — 쓰기만 있고 소비처 0(주석이 그 사실을 적는다)          |
| C029 | 2026-08-22 | 코드   | 열림 | `schedView`(일/주/월)가 URL 이 아니라 영속 토글 — N-12 가 명사 주소를 만든 근거가 여기엔 미적용      |
| C030 | 2026-08-22 | 코드   | 열림 | `lib/` 90+ 모듈 중 **25개가 소비처 1곳** — 「축이 늘면 모듈이 는다」의 실측치                        |
| C031 | 2026-08-22 | 코드   | 열림 | `tools.rs` 의 `vault-health`/`healthy` 판정이 틀린 값을 낸다고 **그 파일이 스스로 적어 뒀다**        |
| C032 | 2026-08-22 | 코드   | 열림 | `AmbientCanvas` 12fps 루프가 컨텍스트 손실 후 계속 도는지 정지 프레임 게이트가 검증 불가             |
| D023 | 2026-08-22 | 데이터 | 열림 | `meta.present` 가 슬라이스 11개를 선언하는데 **실제 행 0** — 선언과 실물의 불일치를 아무도 안 잡는다 |
