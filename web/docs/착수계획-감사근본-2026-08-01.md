# 착수계획 — `/감사 근본`(2026-08-01) 결과

> ## ✅ **전량 실행 완료(2026-08-01)** — 이 문서는 이제 **이력**이다
>
> Critical 2 · High 23 · D 5 · P 4 · F4 판정 3 · F5 추출 1 · 원장·게이트 패리티까지 **§5 순서대로
> 전부 닫혔다.** 승인 4건(H3 ⌘Z 캡처 범위 · F4-① 스키마 · P5+P6 불변식 ③-b · P1/P2 표본)은 모두
> **권장안으로 승인**받아 출하했다.
>
> - **실행 기록의 정본**은 `로드맵.md#2026-08-01-감사-근본` 이다.
> - **설계 판정**(F4 무효화 3 · 판단 2 · 설계 판단 3)은 `결정로그.md#전제-재검증--감사-근본-판정-2026-08-01`.
> - 아래 본문은 **착수 당시의 발견·근거·측정**이다. 상태(⬜/✅)를 여기서 다시 읽지 말 것 — 그건
>   로드맵이 소유한다(같은 사실을 두 곳에 두면 반드시 갈린다).
>
> **⚠ 실행 중 뒤집힌 것 3 — 다음 세션이 이 문서를 인용하기 전에 알아야 한다:**
>
> 1. **brace-expansion 의 셋째 근거가 틀렸다**(§4 원장). _"기록된 GHSA 는 더 이상 근거가 아니고
>    현재는 다른 신규 권고"_ → **재측정 결과 같은 `GHSA-mh99-v99m-4gvg`** 이고 제목만 바뀌었다
>    ("ReDoS" → "DoS via unbounded expansion length"). 나머지 둘(사유가 삭제된 항목을 가리킴 ·
>    해소조건이 틀림 — 실측 뿌리 **넷**)은 맞았고, 권고 id 대조 처방도 그대로 값어치가 있다.
> 2. **P3(`graph` ⌘K 누락)은 완화된 불변식 ③-b 가 못 잡는다.** 그 불변식은 `TABS` 를 순회하는데
>    `graph` 는 **배열에서 통째로 빠진** 경우다. 그 구멍은 `palette.ts` 의 손수 놓은 `act:graph`
>    항목이 막는다 — 둘은 다른 것을 지킨다(불변식 주석에 명시해 뒀다).
> 3. **F5 는 "12 → 6" 이 맞았고, 오라클도 예측대로 prop 개수였다.** 다만 추출로 함께 사라진 것이
>    셋 더 있다(초안 리셋 비교 렌더 setState · `cancelGrade` 효과 · `coach.id` 태깅) — `key` 리마운트가
>    **손으로 배선한 수명 규율 셋을 구조로 대체**했다.
>
> **⚠ 성능 264ms 도 닫혔다(2026-08-01).** 원인은 후보 셋 중 **React `lazy`+`Suspense`** 였다 —
> 폴백 커밋 뒤의 노출 억제 `setTimeout(≈248ms)`. **콜드 부팅 337ms → 114ms.** ⚠ 1차 처방(모듈
> 캐시 덥히기)은 **실측으로 기각**됐다(`React.lazy` 는 캐시에 있어도 첫 렌더에서 반드시 throw).
> 상세·가드는 로드맵의 그 항목이 소유한다.

> **이 문서의 목적**: 감사가 채팅에서 휘발되지 않게 한다. 새 세션이 **이 파일 하나만 읽고** 착수할 수
> 있어야 한다 — 근거를 다시 캐지 말 것(아래 §0이 재발견 비용이 큰 사실을 모아 둔다).
>
> **범위**: 허브 전체(web + server + src-tauri + 게이트 + CI + 배포 실물). 17렌즈 + F1~F6 팬아웃 8 +
> 메인 직독. **커버리지 17/17 완결.**
> **기준 커밋**: `acab94e`(미푸시) · 작업트리에 0.4.0 버전 범프 + `e2e:update` 수정이 미커밋.
>
> ⚠ **이 감사는 게이트를 한 번도 안 돌렸다**(팬아웃 중 실행 금지 규율). 착수 후 §5가 완료 계약.

---

## 0. 재발견 비용이 큰 사실 (다시 조사하지 말 것)

- **`records.value`·`ds_map.value` 는 서버에 불투명하다.** `001_initial.sql:20-29` 가 `value TEXT`
  이고 `rows.ts:428,443` 이 `JSON.stringify/parse` 한다. 서버 `TABLE_COLS`(`index.ts:76`)는 그 안을
  **한 번도 안 본다** → **이 슬라이스들의 스키마 변경은 D1 DDL 0 · 서버 zod 0.**
- **CI 는 hub 를 독립 repo 로 체크아웃한다**(`pelle-te/learning-hub`). `atelier/{knowledge,pipeline}`
  형제 폴더가 **원리적으로 없다**. `testkit::real_workspace()` = `workspace::infer()` 가 그걸 요구한다.
- **배포 오리진은 SPA 폴백이라 `/updates/*` 는 무엇을 물어도 200 이다.** 상태코드로 판정 불가 —
  **content-type 을 봐야 한다**(런북 §401 이 `/health` 로 이미 같은 함정을 기록했다).
- **`npm run budget` 총합 축 656.1/660** 중 **133.5 KB gz(20.3%)가 폰 전용 SQLite 글루 2종**이다.
- **`Icon.tsx:23` 의 `name?: string`** 이고 `ICON_PATHS` 는 `Record<string,string>` — 타입이 못 잡는다.
  (손으로 1회 스캔: 현재 **미정의 참조 0건**. 아직 안 물렸다.)
- **실 DB 규모**(`%APPDATA%/dev.jin.learninghub/learning-hub.db`, 180 KB): `route_visits` **2행** ·
  `completions`·`records`·`summaries`·`week_alloc`·`tombstones` **전부 0** · `docs` 3 · `day_signals` 1.
  → **사용률로 자를 수 있는 탭은 0개다.** 씨앗이 선행이다.

### 증거 등급 — 무엇을 내가 직접 확인했나

새 세션이 재확인 우선순위를 정하는 데 쓴다.

- **[확인]**(메인이 파일을 직접 열거나 명령을 실행): C1 · C2 · H1 · H3 · H4 · H14 · H17 · H21 · H22 ·
  H23 · D2 · D3 · D4(부분) · P4 · F4 3건 전부 · F5 최상위 후보 · 원장 3종 · 패리티 표.
- **[전언]**(팬아웃 결과 · 구조는 대조했으나 실행 재현은 안 함): H2 · H5 · H6~H13 · H15 · H16 ·
  H18 · H19 · H20 · P1(실 DB 조회는 에이전트) · P1(성능 264ms 실측 하네스).
- **[추정 · 강등]**: P4 의 "⌘Z 히스토리에 빈 항목을 민다" — `rows.ts:222` 가 무변경 행을 건너뛰므로
  pre-image 가 안 쌓일 공산이 크다. **착수 시 확인할 것.** P4 본 발견은 그와 무관하게 성립한다.

---

## 1. 🔴 Critical — 먼저 닫는다

### C1. 업데이터가 인스톨러 대신 SPA HTML 을 준다 `[측정]`

```
GET /updates/러닝허브_0.3.0_x64-setup.exe → 200 · text/html · 1433 B  ← dist/index.html 과 같은 크기
GET /updates/zzz-nonexistent.exe          → 200 · text/html          ← 아무 이름이나 같다
GET /updates/latest.json                  → 200 · 2368 B (v0.3.0 · 서명 유효)
```

매니페스트는 살아 있고 서명도 유효한데 **가리키는 파일이 없다.** 2026-08-01 배포가
`npm run release:stage` 를 건너뛰었다(`npm run budget` 의 오염 축이 `dist/updates/` 에 exe 0개를
확인해 준다 — 두 번째 증거). v0.2.0 사용자는 0.3.0 을 제안받고 **1433바이트 HTML 로 minisign 검증
실패**한다. 2026-07-25 감사가 세운 "관측↔전달" 짝의 **전달 절반이 무효**다.

**처방 3단**

1. `cd web && npm run build && npm run release:stage && cd ../server && npx wrangler deploy`
   (릴리스.md §2-4 가 이미 이 순서를 계약으로 적어 뒀다 — 지켜지지 않았을 뿐이다)
2. 런북 §배포후확인에 **content-type 검사** 추가:
   `curl -I …/updates/<exe> | grep -qi 'application/octet-stream'`
   ⚠ **200 을 보면 안 된다** — SPA 폴백이 항상 200 을 준다.
3. 재발 방지: `server/package.json` 에 `predeploy: cd ../web && npm run release:stage` 를 걸어
   **순서를 계약이 아니라 의존으로** 만든다.

### C2. CI 13일·8런 연속 전패 — 시각 회귀 실행 0회 `[측정]`

마지막 녹색 **2026-07-19**. 최신 런 `30596112863` 4잡 중 3잡 실패, **독립 원인 3개**:

| 잡           | 실패                                             | 원인                                                                                                            | 처방                                                                                                                               |
| ------------ | ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| shell-build  | `cargo test` 6건                                 | `testkit::real_workspace()` 가 형제 폴더 요구 (→ §0)                                                            | CI 에 최소 픽스처 워크스페이스 생성 **또는** `LEARNING_HUB_TEST_WORKSPACE` env 주입. ⚠ `#[ignore]` 는 "조용한 skip 금지" 규율 위반 |
| cloud-server | `verify` — `Cannot find module 'zod/mini'` 외 16 | `server/tsconfig` 가 `../web/src/lib/*` 를 include 하는데 그 잡은 `server/` 만 `npm ci`                         | 잡에 `npm ci --prefix ../web` 추가                                                                                                 |
| secret-scan  | 체크섬 대조                                      | `curl -o gitleaks.tar.gz` 로 **이름을 바꿔 받고** `sha256sum -c` 는 원래 이름을 찾는다 → **도입 이래 스캔 0회** | 원래 파일명으로 받는다                                                                                                             |

**가장 나쁜 귀결**: shell-build 에서 `cargo test` 다음이 전부 `skipped` 다 — `tauri:build` ·
**`npm run e2e`(시각 회귀 174케이스)**. 2026-07-25 감사가 _"UI 안전망이 누가 로컬에서 기억하는가에
걸려 있다"_ 고 진단해 CI 에 넣은 그 스텝이 **진단된 상태 그대로 남아 있다.**

**부수**: 커밋 **10개 미푸시**(v0.3.0 릴리스 · ⌘Z · 아이콘 포함) — 어제 출하분은 CI 를 아예 안 탔다.

**F6 판정**: 트랙B→`cargo test` 재배치(2026-07-20)는 **옳았다**(8케이스 중 7.5가 GUI 고유). 검토되지
않은 것은 _"싼 층"_ 이 **"CI 가 도달할 수 없는 층"** 이기도 했다는 점이다.

---

## 2. 🟡 High — 파일·라인·처방

> ⚠ **패턴 경고**: H5 와 D2 는 같은 형태다 — **어제 감사(2026-07-31)가 한 축만 고치고 대칭 축을
> 안 봤다.** 이번 착수에는 _"수정한 축의 반대편을 명시적으로 센다"_ 를 절차로 넣을 것.

### 동기화·⌘Z (어제 출하분)

- **H1** `syncController.ts:138` · `undoController.ts:38` — `await applyPull(...)` 이 `try` **밖**이라
  `finally { endMergeApply() }` 에 도달 못 한다. `merge.ts:180` 계약이 명시한 두 closer 중
  `runSync` 만 바깥 finally 로 덮여 있어 구멍이 안 보였다. 굳으면 이후 flush 가 전부
  `deferred`(=`ok:true`)로 반환돼 **경고 없이 그 세션이 하나도 저장되지 않는다.**
  → `await` 를 `try` 안으로(각 1줄).
- **H2** `ConflictsNotice.tsx:27` · `phone/ConflictsView.tsx:41` · `App.tsx:221` · `palette.ts:193` —
  `.catch` 가 없어 확인창까지 누른 파괴적 동작이 **토스트 0 · 라이브리전 0** 으로 끝난다.
  ⌘Z 는 `popUndo()` 가 적용 **전에** 실행되므로 **누를 때마다 스택이 한 칸씩 조용히 파괴된다.**
  → `peek → apply → pop` 으로 바꾸거나 실패 시 `pushUndo` 복구 + 호출부 4곳에 `.catch`+토스트.
- **H3** `db/docs.ts:116` — `docSet` 은 `writeRows` 가 아닌 별도 `runExclusive(execDb)` 경로라
  **pre-image 가 안 잡힌다.** 독후감 작성 후 ⌘Z → **10분 전 챕터 편집이 되돌아가고** 토스트는
  "직전 편집을 되돌렸어요"라 말한다. `docs` 는 `contract.ts:98` 로 이미 `OUTBOX_TABLES` 에 있어
  **기계는 복원 가능하다 — 빠진 것은 캡처뿐.** 최소 조치는 스택 배리어.
- **H4 + P1(성능)** — **드레인이 양방향 모두 안 돈다.**
  · push: `run.ts:103` 의 `res.sent >= MAX_BATCH_ITEMS` 가 `capBatch` 의 **스탬프 그룹 경계** 때문에
  거의 항상 거짓(400건 그룹이면 `sent=400`). 5,348행에 사용자 트리거 14회 필요.
  · pull: **`drainPull` 이 존재하지 않는다**(grep 전수). 다음 회차는 `applyMerged` 의 부수효과로만
  걸린다(`AFTER_EDIT_MS` 1200ms) → 두 번째 기기 온보딩에 27라운드 ≈ 30초 UI 갈림.
  → `capBatch` 의 `oversized`/`upto<fence` 를 `PushResult` 에 실어 판정 + **push 와 대칭인
  `drainPull` 신설**(상한 포함).
- **H5** `useApp.ts:180-195` — **어제 H10 이 만든 경로다.** 그 주석이 `unavailable` 을 제외한 이유로
  "뜨거운 루프"를 명시해 놓고, 같이 넣은 `writeFailed` 축엔 그 방어가 없다. `writeFailed` 원인 중
  **되읽기 불일치(매퍼 드리프트)는 고쳐질 때까지 영구히 참** → 시간당 9,000회 전량 쓰기.
  `setSaveFallback(r.unavailable)` 이라 **배너도 안 뜨고 `persistFallback` 도 안 타서 그 세션 편집의
  사본이 어디에도 없다.** → ① 지수 백오프(400ms→30s) ② `writeFailed` 도 `persistFallback` +
  `markDbFallback()` ③ `setSaveFallback` 조건을 `!r.ok && !r.skipped` 로 넓힘.
  (`fallback.ts` 머리주석이 이미 _"사용자에게 참인 사실은 '내가 방금 한 편집이 정본에 못 갔다'"_
  라 적었다 — **그 원칙이 코드보다 넓다.**)
- **H6** `write.ts:307` `if (!wrote.touched.length) return _last;` — `touched` 는 upsert 만 담는다
  (`rows.ts:188` 이 그 사실을 적었다). 직전이 `unavailable` 이면 **성공한 삭제가 실패로 보고**되고,
  직전이 `ok` 면 **삭제 반영을 한 번도 확인하지 않고** "일치"라 말한다.

### 실패 표면화

- **H7** `tauri.ts:266-287` — `vaultScan`·`onVaultChanged` 가 예외를 null/no-op 로 접어 볼트 실패가
  **"볼트가 비었다"로 보인다.** `capabilities.ok` 는 참이라 콜드 게이트 문구도 안 뜬다. 감시 스레드
  사망은 `vault.rs:289` 로그뿐이고 `capabilities` 는 **hotkey 오류만** 싣는다. 같은 파일이
  `artifactRead`(`:329`)엔 정반대 판단을 명시해 뒀다. → `capabilities` 에 `vaultWatchError` 를
  hotkey 와 **같은 형태로**(Rust 1필드 + 프런트 1줄).
- **H8** `AnkiPanel.tsx:112` 빈 catch + `lastAuto` 는 성공 시에만 갱신 → Anki 를 꺼도 하루 뒤까지
  `↻ 09:14 갱신` 을 보여 준다. H23 이 `useCollectTool` 에서 고친 계열의 미적용 사본.
- **H9** `exitMini` 가 `setMiniWindow` 반환값을 버린다 → `setResizable` 실패 시 **전체 앱이 320×92
  창에 감금되고 재시작 외 탈출이 없다.**

### UX·a11y (axe 밖)

- **H10** `App.tsx:225` 게이트가 `isTyping() || useOverlay.palette` **둘뿐**. 같은 스토어의
  `help`(`useOverlay.ts:25`)도, `shell/modal` 도, `/mini` 도 안 본다. 리스너는 **캡처 단계**라
  다이얼로그보다 먼저 돈다 → `?` 치트시트를 열고 거기 적힌 `g`+키를 누르면 **뒤에서 탭이 바뀐다.**
  `전체 초기화` 확인창이 다른 화면 위로 옮겨 앉는다. → "떠 있는 층이 하나라도 있으면 단일키 무시"를
  **파생 셀렉터 하나**로.
- **H11** `/mini` 에서 `]`·`g` 로 나가면 `exitMini()` 가 안 불려 창이 알약 크기로 남는다(H10 게이트에
  `/mini` 포함으로 함께 닫힌다).
- **H12** `useListCursor` — 리스너가 `window`, `cursor` 가 blur 로 안 지워지고, `enabled` 옵션을
  **소비처 5곳 전부 안 넘긴다**. `/journal` 은 두 목록이 동시에 `j`/`k` 를 잡아 하이라이트가 두 줄에
  뜨고 이후 `d` 가 **보이는 포커스와 다른 목록**을 지운다. → 동사 실행 전
  `root.contains(document.activeElement)` + `enabled` 실배선.
- **H13** 트레이 길이 편집(−/＋)이 **키보드로 도달 불가**(W13 이 `tabIndex={-1}` 로 뺐는데 스테퍼만
  대체 동사 없음). WCAG 2.1.1 → `useTrayCursor` 에 `e` 추가(어휘 확장 아님).

### 설계·중복

- **H14** 볼트 질의가 **5벌**이고 서로 다르다 — 그리고 `actions.ts:568` 주석이 그 사실을 **거꾸로**
  적었다(_"`Mistakes.tsx:88` 이 같은 폴백"_ → 실제로는 `chapter || subject` 로 정반대):
  `actions.ts:570` `[subject,chapter].filter(Boolean).join(' ')` · `Review.tsx:602` `subject+' '+chapter`
  · `Mistakes.tsx:118` `chapter||subject` · `ReviewRun.tsx:262-263` `cbms.name+' '+chapter` / `subject+' '+chapter`.
  → `lib/utils.ts` 에 `vaultQuery(subject?, chapter?)` 하나.
- **H15** `actions.ts:508-742` — 팔레트 검색엔진 **235줄**(복잡도 38)이 "데이터·백업" 모듈에 산다.
  store 결합은 **한 줄**(`const s = st().state`)이고 나머지 94줄은 순수 랭킹. 이미 비용을 내는 중:
  `test/contentSearch.test.ts` 가 순수 함수를 검사하려고 zustand 시드 + `vi.mock` 을 한다.
  → `lib/contentSearch.ts(q, state, reads, limit)` + shell 에 1줄 어댑터.
- **H16** `agoLabel` 3벌 — `Atlas.tsx:63`(로컬 `agoLabel(days)`)이 `lib/utils.ts:282`
  (`agoLabel(epochMs, now)`)를 **그림자로 덮는다**. 둘 다 number 하나로 호출 가능해서, Atlas 가
  `@/lib/utils` 를 import 하는 날 `agoLabel(3)` 이 **"56년 전"** 을 그리고 **타입은 통과한다.**
  `CloudCard.tsx:44 relTime` 은 세 번째 사본 → 같은 `syncedAt` 을 레일은 "어제", 설정은 "36시간 전".

### 보안·수명·시간·디자인

- **H17** `cloud.rs:109-112` — 머리주석이 _"무한 스트림이 오면 메모리가 터진다"_ 고 약속했으나
  `res.bytes().await` 로 **전부 버퍼링한 뒤** 8MB 를 검사한다. **OOM 은 검사 전에 난다.**
  `news.rs:259` 동일. `server/src/index.ts:216-227` 이 **정확히 옳은 형태**를 이미 갖고 있다
  (청크를 세다 넘으면 `cancel()`) → 그걸 Rust 로 옮긴다.
- **H18** `Markets.tsx` 는 드로어 닫기에서만 AI 스트림을 취소 → 탭을 떠나면 `ollama_cancel` 이 안 나가
  Rust 가 최대 120초 생성을 계속 돈다. → **`useAiStream` 자체에 언마운트 abort**(호출부 둘을 한 번에).
- **H19** 전 저장소 `webglcontextlost` **0건** — Windows TDR·sleep/resume 에서 배경이 **영구히 빈 채**로
  남고 rAF 는 계속 돈다. 정지 프레임 게이트가 원리적으로 못 본다.
- **H20** 자정 롤오버가 화면마다 갈린다 — `today`(30s)·`schedule`(60s)는 되고
  `items`·`journal`·`alloc`·`SubjectDefinition` 은 안 된다. **더 나쁜 것은 쓰기**: `Today.tsx` 의
  `RitualCard` 는 오버레이라 자기 틱이 없어, 열어 둔 채 자정을 넘겨 체크하면 **어제 날짜 키에 쓴다.**
  → `useTodayISO()`(다음 자정까지 `setTimeout` 1개 · 60초 폴링 아님).
- **H21** `lib/utils.ts:136-142 BLOCK_TYPES` 5종 생 hex 가 **실제 렌더 색**이다
  (`layout.ts:143` → `WeekCalendar.tsx:359` `--seg` → `tw.css:301-308` 파생 전부 · `SkeletonPanel.tsx:167`).
  주석이 스스로 _"딥블랙과 조화"_ 라 적어 **다크 전용**임을 자백하는데 라이트에서 그대로 쓰인다.
  stylelint 는 CSS 만, `check-tokens.mjs` 는 `var(`/`getPropertyValue(` **두 문법만** 보므로 원리적 사각.
  → `tokens.css` 에 테마별 5토큰 + 맵을 `'var(--block-sleep)'` 문자열로(그 순간 검사 범위 안으로 들어온다).
- **H22** `iconPaths.ts` 머리주석이 집행자로 지목한 **`scripts/check-icons.mjs` 가 존재하지 않는다.**
  → **약속된 스크립트보다 나은 처방**: `as const satisfies Record<string,string>` +
  `export type IconName = keyof typeof ICON_PATHS` + `Icon.tsx` 의 `name?: IconName`.
  **게이트 단계 0개 추가로 typecheck 가 전부 잡는다.**
- **H23** 번들 총합 **656.1/660**(잔여 3.9 KB) — 다음 기능이 게이트를 깬다. 그중 20.3%가 폰 SQLite
  글루라 **데스크톱만 만지는 변경이 폰 wasm 예산 때문에 막힌다.** → 총합 축을 플랫폼별로 가른다
  (**한도만 올리면 축이 죽는다**). 어느 쪽이든 `bundle-budget.mjs` 머리주석에 사유를 남길 것.

### 어제 수정의 반쪽 · 제품 정합

- **D2** `WeekCalendar.tsx:211,221` 이 `t.color`·`ev.color`(굳은 hex)를 읽는다 —
  `DayPlanner.tsx:195 segColor` 는 `sid` 로 재파생하는데. H13(어제)이 _"스키마 필드는 남기되 **읽지
  않는다**"_ 라 못박고 **writer 만 닫았다.** → H13 이전 태스크는 주 뷰/일 뷰 색이 다르고 노브 교체가
  주 뷰에 영원히 도달하지 않는다. `ev.color` 는 현재 writer 0이지만 `lib/events.ts:56` 이 **입력으로
  받고** `schema.ts:231` 이 저장하므로 pull·가져오기가 값을 실어 오면 즉시 같아진다.
  → 두 reader 를 `sid` 재파생으로 + `addEvent` 의 `color` 입력 드롭.
- **D3** `Review.tsx:780` `<span aria-hidden="true">⚖ </span>` — **살아남은 이모지 1건.**
  `eslint.config.js:148` 셀렉터가 BMP 글리프를 **손목록**으로 드는데 U+2696 이 없다. 같은 구멍에
  `⚡`(26A1)·`⭐`(2B50)·`❗`(2757)·`⌛`(231B) 가 열려 있다. `acab94e` 의 _"다시 못 들어오게 막았다"_ 가
  **부분적으로만 참**이다. → `iconPaths` 에 `balance` + 셀렉터에 최소 2696 추가 +
  **목록에 구멍이 있다는 사실을 그 주석에 적는다**(손목록 방식 자체는 유지가 옳다).
- **D4** `Icon.tsx:22` 주석 _"`.ic` 가 언레이어드라 `!` 가 필요"_ 는 **거짓** — `.ic` 는
  `global/base.css:59` = **base 레이어**라 유틸리티가 이미 이긴다(`acab94e` 가 파일을 옮기며 C-7 이전
  문장을 그대로 가져왔다). 디자인시스템.md 는 맞게 적혀 있어 **문서끼리 어긋난다.**
- **D5** `ThemeProvider.tsx:7 META.light='#ffffff'` vs `tokens.css:649 --bg:#fafbfc` — **다크는 일치,
  라이트만 어긋난다** → 라이트에서 OS 크롬 틴트에 얇은 이음매. 같은 파일에 파생 관용구가 이미 있다
  (`Settings.readAccentPreviews`) → `getComputedStyle(root).getPropertyValue('--bg')`.
- **D6** `ds.css:78 .ds-canvas` 소비처 **0**인데 디자인시스템.md §4 는 "표면 레지스터는 넷"이라 선언.
  (`ds-rule` 54 · `ds-well` 20 · `ds-frame` 3) → 지우고 셋으로 줄이거나 쓸 자리를 지정.
- **P3** `palette.ts:24` 가 `orderedTabs()`(=`TABS`)에서만 탭 명령을 만드는데 P-19 가 `graph` 를 TABS
  에서 뺐다 → **⌘K 에 "학습 구조도"가 없다.** `tabs.ts:207` 의 _"도달성 손실 0"_ 이 거짓(딥링크는 맞고
  ⌘K 는 아니다). → `{kind:'act', to}` 한 줄 + 그 문장 정정.
- **P4** `Items.tsx:213-227 recolorAll` — **구조적 no-op.** `s.color = colorForId(s.id)` 는
  `refineItemColors`(`utils.ts:95-97`)와 문자 그대로 같고 그 함수는 진입 3경로 전부에서 이미 돈다
  (`useApp.ts:72,85,250`). 주석이 든 유일한 존치 사유("옛 백업 가져오기")도 `:250` 이 덮는다.
  title 문구 _"새 팔레트 **순서**로"_ 는 2026-07-24 에 죽은 배열-팔레트 어휘의 화석.
  → **삭제(15줄 + 버튼).** ⚠ 착수 시 "⌘Z 오염" 여부만 확인(§0 참조).

---

## 3. 🏗️ 근본 판정 (F1~F6)

### F4 — 전제가 무효화된 것 3건 (이 감사의 핵심 산출)

| 메커니즘                           | 옛 근거(인용)                                                                                                 | 무엇이 변했나                                                                                                                                                                                                  | 판정                                                                                                                                                                                                                                                                         |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **다운그레이드 가드**              | `db.rs:194` _"지금은 안전하다(**릴리스 0건**)… 첫 업데이트를 내는 순간 무장된다"_                             | **v0.2.0·v0.3.0 출하**(`81a47e0`) + 0.4.0 범프 대기                                                                                                                                                            | **유지(코드) · 주석만 정정** — `db_version_guard` 는 `boot.ts:133` → `main.tsx:94 DowngradeScreen` 까지 **종단 배선 확인됨**. 문장만 낡았고, 그게 "아직 안 급하다"로 읽힌다                                                                                                  |
| **근본이관 ① 챕터 단위 실패 기록** | 로드맵 1238: _"스키마 추가가 D1·서버 strict zod·폰까지 번진다 — `ds_map` 슬라이스로 실을 수 있는지가 갈림길"_ | **갈림길 해소**(§0) — `blankResults` 는 `records` 슬라이스이고 `value` 는 불투명                                                                                                                               | **재설계 승인 가능.** `BlankResultSchema`(`schema.ts:117-124`)에 `chapter` **한 줄**. **D1 DDL 0 · 서버 zod 0 · 별도 `reviewMiss` 맵 불필요.** 선례: `conf`·`at` 이 같은 방식. 남은 위험은 로드맵이 이미 적은 둘(과목당 최근 하나 규율 · 넘김≠실패)                          |
| **근본이관 ② 되먹임 루프**         | 로드맵 2232: _"repo 경계를 넘는 **첫 쓰기**다"_                                                               | **쓰기가 필요 없다** — `지식엔진.py:840-843` 이 `atelier/러닝허브_백업.json` 을 **자동 탐색**하고 허브 `exportJSON` 이 `exportSnapshot`(런타임 캐시만 제외 = `items`·`blankResults`·`cbms` 포함)을 이미 만든다 | **보류(코드 0줄)** — 막힌 것은 **파일명 하나**(앱은 `러닝허브_{startDate}.json`). 다만 통계 전제는 여전히 미충족(`EVIDENCE_MIN=1.0` · 챕터당 ~20건 · CBMS 0행) → **①이 실질 선행이라는 로드맵 판단은 유효.** 처방: 엔진 후보 목록에 앱 파일명 패턴 추가 + **① 출하 후 재개** |

### F1·F2 — 도마에 올렸으나 옳았던 것 (유지)

- **저장 백엔드 3분할** — 유지. localStorage 폴백을 없애면 dev 와 트랙A 시각 베이스라인이 함께 죽는다.
- **엔트리 2개** — 유지. 폰 32개 import 전량 합법, §13-0 결론이 지켜지고 있다.
- **레이어 5분할 + boundaries** — 유지, **비용이 0에 수렴**: 우회 3경로 전수(동적 import 7 · type-only 0
  · `boundaries` disable 0) → **위반 0건.** "이 규모에 과하다"는 반론이 측정으로 기각된다.
- **LWW + 툼스톤 + fence** — 유지. 서버가 권위 시계를 쥐고 클램프(M1)하고, 클라이언트 병합과 서버 SQL
  타이브레이크(`>=`/`<=`)가 문자 단위로 대칭. 1인·소수기기·행 슬라이스에서 CRDT 는 불투명 `value`
  컬럼에 키별 메타데이터를 얹는 **순손실**.
- **`lib/tauri.ts` 625줄 분할 — 불필요.** 호출부 21곳 매핑 결과 15개 커맨드 계열이 각 3~15줄 무로직
  파사드. 불변식 I2 가 응집 근거이고 갈라도 재수출이 필요하다. H7 의 분할 조건("부팅 경로가 무거운
  표면을 끌 때")은 유효하나 **지금 성립 안 함.**

### F5 — 컴포넌트 재설계 후보

측정(전체-src · 임계 15): 지목 12파일 중 **초과 3개뿐** — `TodaySignature:273`=66 ·
`actions.ts:648`=38 · `DayPlanner:135`=31. **나머지 9개의 긴 줄 수는 이미 분해된 조각의 나열이다.**

| 대상                                                                       | 뺄 것                          | 인터페이스                                                                                              | 판정                                                                                                                              |
| -------------------------------------------------------------------------- | ------------------------------ | ------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| `ArticlePractice.tsx:376 SummaryEditor`                                    | 요약 초안 + AI 코치            | **12 props → 6**, `onSave` 불투명. 초안 리셋은 `key={sel.id}`(부모가 **371행에서 이미 그 수법을 쓴다**) | **후보 · 최우선**                                                                                                                 |
| `TodaySignature.tsx:956-1031`                                              | 인출 슬롯 + 정답 공개 상태기계 | **5 props** · 내부 상태 2개가 부모 지역변수를 하나도 안 본다                                            | 후보                                                                                                                              |
| `ReviewRun.tsx:350-443`                                                    | P-2 인라인 CBMS                | `useMissLog(today)` + `MissBar`(3) · 세션 상태기계와 공유 상태 0                                        | 후보                                                                                                                              |
| `actions.ts:648 contentSearch`                                             | 6개 도메인 스캔                | 순수 함수 6 + cap 드라이버                                                                              | 후보(=H15)                                                                                                                        |
| `Graph.tsx:258-610`                                                        | 검색·줌 오버레이               | **2 props**                                                                                             | 후보(소)                                                                                                                          |
| `CommandPalette.tsx:265-340`                                               | 명사→동사 2단 선택             | 반환 8(경계선) · `sel` 이 `Command.Dialog` 제어값이라 샌다                                              | 후보(약)                                                                                                                          |
| **`TodaySignature.tsx:273` 본문(66)**                                      | —                              | —                                                                                                       | **후보 아님**: 60+ 지역변수를 히어로 그리드가 공유 → 떼면 20-prop = 응집→결합 교환                                                |
| `AllocBoard.tsx:183` 드래그                                                | —                              | —                                                                                                       | **후보 아님(과잉추출)**: 상태 2·핸들러 4에 **로직 0**. `useTimeboxDnd` 가 정당했던 이유(MIME 직렬화·좌표→분·`resolveSlot`)가 없다 |
| `Graph` 캔버스 · `DayPlanner` · `Review`/`Settings`/`Degree` · `atlasData` | —                              | —                                                                                                       | **후보 아님**(이미 분해 / 데이터 부피)                                                                                            |

**요지**: 줄 수는 이 렌즈의 오라클이 아니었다. 진짜 신호는 **드릴된 prop 개수**였다.

### F6 — 검증 재배치

트랙B 8케이스는 7.5건이 GUI 고유 → 재배치는 옳았다. 역효과는 C2. 추가 발견:

- `db.rs` 의 `skip!` 매크로가 **실제로는 조용하다** — cargo 가 통과 테스트 stderr 를 캡처해 CI 로그에
  `SKIP` 0회인데 `ok` 로 표시된다. → `--nocapture` 또는 `#[ignore]`+명시 카운트.
- `gate.mjs` 요약기가 `/error|fail/` **첫 매칭 줄**만 200자로 잘라 **증상을 보고하고 원인(panic 사유)을
  버리며, 첫 실패에서 break** 해 C2 의 독립 원인 3개 중 하나만 보여 준다.

---

## 4. 🧹 규약 위생 · 원장 · 패리티

### 문서↔코드 드리프트

| 위치                                                                                                     | 왜 오도하나                                                                                                                                                           |
| -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CLAUDE.md:125`                                                                                          | `e2e:update` 를 `--update-snapshots=all --workers=1` 이라 적음 — 작업트리에서 **설정 둘을 도는 형태로 바뀜**. 정본은 `package.json`                                   |
| `web/package.json`(미커밋)                                                                               | `"e2e:update"` 들여쓰기 2칸 → **`format:check` 실패 = 현재 `verify` 빨간불** `[측정: prettier --check]` → `npm run format`                                            |
| `db.rs:194` · `actions.ts:568` · `iconPaths.ts` 머리주석 · `cloud.rs`/`news.rs` 머리주석 · `Icon.tsx:22` | 각각 F4-① · H14 · H22 · H17 · D4                                                                                                                                      |
| `useCollectTool.ts:43`                                                                                   | `cancel()` 호출부 **0개**이고 셸에선 원리적으로 무동작(`api.ts:116` 이 signal 을 명시적으로 무시) — UI 에 붙이는 순간 **조용한 무반응**. → 죽은 경로 제거 + 주석 정정 |

### 기한부 판단 원장

| 항목                            | 만료       | 판정                                                                     |
| ------------------------------- | ---------- | ------------------------------------------------------------------------ |
| react-router / -dom [high/prod] | 2026-09-01 | **유효**(RSC 경로 부재 재확인 — `BrowserRouter` 만 · 폰은 라우터 미탑재) |
| **brace-expansion [high/dev]**  | 2026-10-01 | **사유노후 3중 · 즉시 조치** ↓                                           |
| `server` 허용 `[]`              | —          | 유효(빈 것이 내용) · 기준일 주석만 갱신                                  |
| `a11y.spec.ts` 알려진위반 `{}`  | —          | **유효 · 정상 종결**(집행이 `accentContrast.test.ts` 계산으로 승격)      |

**brace-expansion** `[측정]` — ① 사유가 _"위 `eslint` 항목의 논증이 그대로 적용된다"_ 인데 **그 항목은
2026-08-01 에 삭제됐다**(같은 파일 `//체인` 주석이 삭제를 기록한다) → **사유가 허공을 가리킨다**
② 해소조건 "ESLint 10 승격"이 틀렸다 — `npm ls` 가 **뿌리 넷**(jsx-a11y·sonarjs·typescript-eslint·
vite-plugin-pwa)을 보여 준다 ③ 기록된 권고 `GHSA-mh99-v99m-4gvg` 는 더 이상 근거가 아니고 현재 걸린
것은 **다른 신규 권고**이며 `fixAvailable: true`.
→ **구조적 원인**: `audit-gate.mjs:122` 가 패키지명으로만 매칭하고 **`권고` 필드를 한 번도 비교하지
않는다** — 같은 패키지의 새 CVE 가 옛 사유 아래로 조용히 흡수된다. 처방: `npm audit fix` 후 항목 삭제
**+ 게이트에 권고 id 대조 추가**(사문·사유노후 한 계열이 기계 검출 가능해진다).

### 로컬↔CI 패리티

| 단계                                         | 로컬                              | CI                    | 판정                                                                                                                                        |
| -------------------------------------------- | --------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| verify 7종·audit·build·budget·e2e:smoke·a11y | ✓                                 | ✓                     | 대칭                                                                                                                                        |
| codegen:check · e2e:shell                    | ✓                                 | ✗                     | **의도적**(근거 기재됨)                                                                                                                     |
| **e2e 시각·motion·phone**                    | ✓                                 | 선언만 · **실행 0회** | **사고**(C2)                                                                                                                                |
| **`cargo test`**                             | ✓                                 | 상시 6건 실패         | **사고**(C2)                                                                                                                                |
| **`tauri:fmt`·`tauri:clippy`**               | **✗**(`gate.mjs` 에 없음)         | ✓(그 CI 가 빨간불)    | **사고** — Rust 린트가 사실상 무게이트                                                                                                      |
| **`server verify`·`server audit`**           | **✗ — 어떤 로컬 게이트에도 없다** | ✓(실패 중)            | **사고 재발** — 2026-07-20 에 "CI 에 없다"를 고쳤는데 지금은 **로컬에 없다**. 인증을 다루는 유일한 층이 **양쪽 어디에서도 검증되지 않는다** |
| pre-commit(husky)                            | web 스테이징만                    | —                     | `src-tauri/`·`server/` 만 고친 커밋은 훅을 안 탄다 — 절대규칙 #5 의 범위 확장과 어긋남                                                      |
| secret-scan                                  | ✗                                 | 상시 실패             | 도입 이래 실행 0회                                                                                                                          |

---

## 5. 📋 실행 순서 · 완료 계약

### 순서 (되돌리기 쉬움 · 노력 낮음부터 · 앞 항목이 뒤를 안 막는다)

1. **C1** 재배포 + content-type 확인 + `predeploy` 배선
2. **C2** 3잡 각각 (cargo 워크스페이스 주입 · web 의존 설치 · gitleaks 파일명)
3. **미커밋 정리**: `npm run format`(package.json) + `CLAUDE.md:125` 문구
4. **H1**(각 1줄) · **H2**(peek→apply→pop + `.catch` 4곳) · **H5**(백오프+폴백+배너)
5. **P4** 삭제 · **D3**(이모지+셀렉터) · **D4**·**D5**(각 1줄) · **P3**(한 줄) — 전부 30분 이하
6. **H22**(`IconName` 타입 집행) · **H21**(블록색 토큰화) · **D2**(주 캘린더 reader 제거)
7. **H10~H13**(오버레이 게이트 셀렉터 1개 + `enabled` 배선 + `e` 동사)
8. **H3**(docs pre-image) · **H4+드레인 대칭** · **H6**(삭제 검증)
9. **H7·H8·H9·H14·H16·H17·H18·H19·H20** · **H15**(`contentSearch`→lib)
10. **F4-①** `chapter` 한 줄 — **승인 필요(코어)** · 서버 비용 0 확인됨
11. **F5** `SummaryEditor` 추출 · **H23** 예산 축 분리
12. 원장 정리 + `audit-gate` 권고 대조 · `server verify`·`tauri:fmt/clippy` 를 `gate.mjs` 에

**승인 선행**: 10(스키마) · 8의 H3(⌘Z 캡처 범위) · P5+P6(불변식 ③-b 완화 = 설계 변경).

### 판단 선행 — 코드가 아니다

- **P1/P2 — 2026-08-12 은퇴 판정의 전제가 깨져 있다.** `route_visits` **2행 · 키 1개 · 날짜 1일**인데
  `tabs.ts:250-270` 이 사전등록한 규칙은 _"8/12 에 합계 ≥3 이면 유지, 미만이면 삭제"_ 다. 그날 그 값은
  **0이 되고, 그 0은 아무것도 말하지 않는다**(앱이 두 번 열렸을 뿐). `visits.ts` 머리주석이 스스로
  경고한 순환(_"자명한 0 을 근거로 쓰면 그건 관측이 아니라 순환"_)이 자기 판정에 발동한다.
  → **날짜가 아니라 표본에 건다**(`관측일 ≥10 && 총방문 ≥30` 미만이면 _"판정 불가 — 표본 부족"_).
  ⚠ 그대로 실행하면 **표본 0을 근거로 탭을 지운다** — 스냅샷·a11y 에서 두 번 물린
  _"녹색이 '회귀 없음'이 아니라 '안 쟀음'을 뜻하는"_ 형태와 같다.
- **P5+P6 — IA 원천 둘이 다른 말을 한다.** `atlas`·`markets`·`discovery` 가 `group:'discover'` 인데
  실제 호스트는 `settings`(`SUBTAB_GROUPS`) → `GROUP_LABELS.discover` 는 **한 번도 렌더되지 않는다**.
  불변식은 "라벨이 존재하는가"만 봐서 못 본다. 그 결과 설정 세그먼트가 **7개**(앱 최장)이고 그중 학습
  상태 소비 0이 셋(`guide` 는 import 가 `Icon`+`usePageChrome`+`react` **셋뿐**이라 데이터 의존 0).
  **뿌리는 불변식 ③-b**(_"lens 는 반드시 어느 세그먼트 그룹에 속한다"_) — 라우트·⌘K·`g` 가 이미 도달을
  보장하는데도 "매일 안 볼 것"을 매일 보이는 바에 세우는 압력이 됐다(tabs.ts:365-368 이 자인).
  → ③-b 를 _"라우트·⌘K·세그먼트 중 **최소 하나**"_ 로 완화하면 **P5·P6 이 한 번에 풀린다.**

### 🔬 검증 사각 — 게이트로 못 닫는 것

| 무엇                  | 사각              | 닫는 법                                                       |
| --------------------- | ----------------- | ------------------------------------------------------------- |
| C1 업데이터 전달      | ③ 실 백엔드 IO    | **content-type** 검사(자동화 가능) — 상태코드로는 원리적 불가 |
| H1 병합창 교착        | ⑥ 비결정 타이밍   | 방어 *논리*로만 확인                                          |
| H10~H13 키보드 전이   | ①⑤                | e2e 키보드 케이스 신설(axe 는 렌더된 기본 상태만 본다)        |
| H19 WebGL 손실        | ⑤ 실 브라우저 API | `WEBGL_lose_context` 로 강제                                  |
| H20 자정 쓰기         | ⑥                 | `_today` 시드로 유닛                                          |
| H21·D2 라이트/주뷰 색 | ① 상태 의존 시각  | **실렌더 1회**(§15-4)                                         |
| 성능 264ms            | ⑤                 | 릴리스 exe + 트랙B 하네스에 Performance 트레이스              |

**성능 미해결 1건**: 콜드 부팅에서 스켈레톤→콘텐츠 사이 **264ms 가 CPU 도 네트워크도 아니다**
(`hub:app` 91→콘텐츠 369ms · 3회 재현 · **네트워크 요청 0 · CPU 샘플 91.6%가 `(idle)` · 라우트 무관**).
`budget` 의 ②축이 지키는 것과 실제 TTI 병목이 **다르다** — 청크를 쪼개도 KB 를 깎아도 안 움직인다.
후보: React 19 `lazy`+`Suspense` 커밋 레인 / `__vitePreload` 의 CSS `load` 대기 / `StrictMode`.
**원인 특정이 처방보다 먼저다.**

### 완료 계약 (기억으로 적지 말 것 — 이 목록이 정본)

```
cd web && npm run gate      # verify + audit(SCA) + build + budget + e2e(a11y·phone 포함)
                            #   (+ cargo 있으면 tauri:check → cargo test → tauri:build → e2e:shell)
cargo test --manifest-path src-tauri/Cargo.toml --lib   # 셸을 만졌으면
cd server && npm run verify                             # server/ 를 만졌으면 (gate 밖)
```

동기화 코어를 만졌으면 **릴리스 exe 로 트랙B 1회**(dev 는 CSP·오리진 결함을 못 본다 ·
`tauri:build:fast` 로 NSIS 생략).
⚠ 베이스라인 재생성 대상은 "실패한 것"이 아니라 **"그 UI 가 보이는 스냅샷 전부"** (소스 역산).

### 착지 (문서 반영 — 아직 안 했다)

| 무엇                                     | 어디                                                                                          |
| ---------------------------------------- | --------------------------------------------------------------------------------------------- |
| C1·C2 · H1~~H23 · D2~~D6 · P3·P4         | `로드맵.md` Now/Next(출하하면 ✅ 로)                                                          |
| **F4 무효화 3건**                        | `결정로그.md` — `[메커니즘 → 옛 근거 → 무효화된 전제 → 새 판정]`                              |
| **근본이관 ①②**                          | `로드맵.md:1238,2232` 의 ⬜ 를 **판정 완료로 갱신**(①=승인 가능·서버비용 0 / ②=보류·코드 0줄) |
| F5 후보 6 / **후보아님 6**               | `로드맵.md` Next — **후보아님도 재발굴 방지로 함께 적는다**                                   |
| brace-expansion · `audit-gate` 권고 대조 | **원장 갱신**                                                                                 |
| P1·P2(8/12 판정 전제) · P5+P6(③-b)       | `결정로그.md`(판단) + 승인 후 실행                                                            |
