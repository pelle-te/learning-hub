# 코드 축 1회차 — 리포트 (2026-08-22)

> 범위: 전체(`web/src` · `src-tauri/src` · `server/src` · `web/scripts` = **355파일 74,295줄**).
> 하위축 넷(실패·구조·표면·성능)을 서로 블라인드로 팬아웃하고, 메인이 **모든 발견의 파일을 직접 열어** 확인했다.
> 코드는 고치지 않았다.

## 1. 총평

**통과선(SQLite·Go stdlib·React 급 리뷰어가 approve할 수준)에 대해, 이 저장소의 «있는 코드»는 가깝고 «없는 검사»는 멀다.**

도구 층은 실제로 비어 있다 — `knip` 0 · `madge --circular` 0 · `jscpd` **0.25%** · `gitleaks` 0 ·
단언 없는 테스트 **0/2110**. 리스너·타이머 누수 0, `server/src` 의 인증·입력검증에서 새로 지적할 것 0,
Rust 경로 경계(`safe_join`·화이트리스트·RAII 캡)도 0. 이건 2026-08-20 전면 리뷰(46건 전량 처리)의 성과이고
**그 성과는 유지되고 있다.**

이번 회차가 찾은 것은 그 층 **아래**다. 결함 18건 중 열은 하나의 형태를 공유한다 —
**「선언은 있고 집행자가 없다」가 아니라, 「집행자는 있는데 자기 문법 밖을 못 본다」.**
불변식 ⑰은 `<b>…</b> 탭` 을 막는데 `GuideView` 는 같은 뜻의 `<Tab>` 컴포넌트를 써서 **없는 탭 다섯을
여덟 번** 가리킨다. 불변식 ⑭는 「테스트만 쓰는 모듈」을 파일 단위로 막는데 export 단위는 못 본다.
`knip.jsonc` 는 자기가 못 보는 축을 알고 있었고 그 집행자를 세웠는데, 세운 집행자가 한 겹 굵었다.

두 번째 형태는 **비동기 실패의 관측 채널이 셋 다 닫혀 있다**는 것이다. 타입 인지 린트를 켜 보면
`no-floating-promises` **34** · `no-misused-promises` **80** 이 나오는데(실측), 저장소는 그 규율을
**손으로** 지키고 있다(`void` 표기 62곳). 전역 `unhandledrejection` 핸들러는 **0**(I052 가 어제 텔레메트리를
걷었다), 데스크톱 e2e 는 `pageerror` 를 실패로 보지 않는다. 즉 이 세 층 어디에도 «비동기가 조용히 죽었다»가
안 걸린다.

⚠ **이 회차의 범위 밖**: 돌려서 재야 아는 것(렌더 프로파일·번들·실런타임)은 성능 팩 · 문서·게이트 구성의
드리프트는 규약 팩 · 스키마·동기화 계약의 데이터 수명은 데이터 팩. §8 에 제목만 넘겼다.

## 2. 커버리지

| 항목           | 값                                                                                                                         |
| -------------- | -------------------------------------------------------------------------------------------------------------------------- |
| 대상           | **355파일 74,295줄**(대장 참조 · 생성 코드·스냅샷·`node_modules`·`dist` 제외)                                              |
| 하위축         | 4/4 전부 돌렸다(실패·구조·표면·성능)                                                                                       |
| 실제로 연 파일 | 축1 38 · 축2 40 · 축3 26 · 축4 27 + 메인 31 = **중복 제외 약 120 / 355 (34%)**                                             |
| 기계 스캔      | cloc · knip · madge · jscpd · osv-scanner · gitleaks · eslint(cc) · churn · **타입인지 린트 프로브** — 산출 원문은 `스캔/` |
| 게이트 실행    | **0회**(읽기 전용 · 병렬 세션이 도는 중이면 게이트가 flaky 를 결함으로 읽는다는 규율)                                      |
| 실물           | 실 SQLite 를 **읽기 전용 사본**으로 열어 18표 행수를 쟀다(쓰기 0)                                                          |

**`[미열람]` 중 큰 덩어리** — 다음 회차의 첫 입력이다:

- `web/src/features/` **80파일 중 약 70** — 축1(실패)이 1개, 축3(표면)이 7개만 열었다. 폼 검증·낙관적
  업데이트 롤백·키보드 경로의 상태 경합이 전부 여기 있다.
- `src-tauri/src` 17 중 **`db.rs`(793) · `tools.rs`(619) · `ollama.rs`(474) · `hotkey.rs` · `notify.rs` ·
  `updater.rs` · `paths.rs`** 미열람. `updater.rs` 는 서명·설치 경로라 **보안 표면인데 못 봤다**.
- `web/scripts/` **10파일 전량** — 「검사가 실제로 도는가」의 근거가 여기 있는데 못 봤다.
- `lib/scheduler/*` 6 중 3 · `spacedReview.ts`(839) · `methodology.ts`(759) · `persistence.ts`(615).

**⚠⚠ 이 회차가 스스로 틀린 자리 — 축 추천이 오도됐다**

`/리뷰` 는 «코드 축은 리뷰 이력이 없다»를 근거로 이 축을 추천했다. **거짓이다.**
전면 코드 리뷰가 **2026-08-20 에 실제로 돌았고**(46건 · 전량 처리 · 근본 원인 R1~R3), 그 산출물은
`web/docs/리뷰/` 가 아니라 **저장소 루트의 `REVIEW.md`(1,032줄) + `review-manifest.md`(667줄)** 에 있다.
축 추천이 리뷰 디렉터리만 나열하므로 그 회차가 **존재하지 않는 것으로 세어졌다.**
→ 회차 중반에 발견해 **재지적 필터를 그 문서로 다시 걸었다**(§7). 이번 발견 중 REVIEW.md 와
겹치는 것은 **0건**이다(`toISOString`·`RRULE`·`readTouched`·`readRows`·`DOC_KEYS`·`schedView` 전부 0히트).
그 문서의 **§확인하고 버린 가설**은 이 회차가 실제로 사용했다 — 축4의 `C032`(캔버스 컨텍스트 손실) 기각이
그 절과 독립적으로 같은 결론에 도달했다.

## 3. 지적 목록

### Major

#### C033 · [표면] `GuideView` 의 `<Tab>` 이 **없는 탭 다섯을 여덟 번** 가리킨다 — 불변식 ⑰이 그 문법을 못 본다

- 위치: `web/src/features/find/GuideView.tsx:45`(`Tab` 컴포넌트) · `:203,206,214,242,312,319` · 집행자 `web/test/invariants.test.ts:1440`
- 증거: **[측정]** — `<Tab>` 이름 전수 대 `shell/tabs.ts` 의 `label` 전수 대조.
  죽은 이름: `발견`(3) · `읽을거리`(2) · `탐구 수집`(1) · `증시 동향`(1) · `증시`(1) = **8회**.
- 실패 시나리오: 사용자가 ⌘K → 「이 시스템이 할 수 있는 것」 §③ 을 열면
  _"허브 `읽을거리`·`증시 동향` 탭을 열면 갱신"_ · _"허브 `발견` 탭에서 후보를 승격/기각"_ 을 읽는다.
  그 이름을 ⌘K 에 치면 **0 히트**다 — 다섯 탭은 P10 W4 에서 **제거**됐고(`features/registry.tsx:26` 이
  _"은퇴가 아니라 제거"_ 라 명시) 그 축 자체가 2026-08-21 에 은퇴했다. 불변식 ⑰은 정확히 이 형태
  (_"따라갈 수 없는 처방"_)를 막으려고 U009 가 세운 것인데, 관용구가 `/<b>(…)<\/b>\s{0,3}탭/` 이라
  **같은 뜻의 전용 컴포넌트를 원리적으로 못 본다.** 오버레이 §1-B 의 「막힌다/통과한다」 표 그대로다.
- 수정: 관용구에 둘째 문법을 넣되, 근본은 **문자열 대신 키를 받는 것**이다.

  ```tsx
  // features/find/GuideView.tsx — 문자열은 개명을 못 따라온다
  import { TABS, type TabKey } from '@/shell/tabs';
  function Tab({ k }: { k: TabKey }) {
    const t = TABS.find((x) => x.key === k);
    if (!t || t.role === 'view') throw new Error(`살아 있는 탭이 아니다: ${k}`);
    return <span className={TAB}>{t.segLabel ?? t.label}</span>;
  }
  ```

  ```ts
  // test/invariants.test.ts — ⑰의 관용구를 두 문법으로
  const 관용구 = /<b>([^<>{}]{1,14})<\/b>[^\S\n]{0,3}탭|<Tab>([^<>{}]{1,14})<\/Tab>/g;
  ```

  §③(`:190~250`)과 표의 두 행(`:309`·`:316`)은 이름을 고쳐도 **가리킬 화면이 없다** → 삭제(§6 잔재).

- 비용: M

#### C034 · [구조] Rust 커맨드 로스터가 **TS↔Rust 두 벌 손유지**인데 대조하는 자리가 0이고, 드리프트는 `catch` 가 삼킨다

- 위치: `web/src/lib/tauri.ts:64`(`call`) ↔ `src-tauri/src/lib.rs:107-155`(`generate_handler![…]`) · 삼키는 자리 `web/src/lib/tauri.ts:143-150`
- 증거: **[측정]** — 양쪽 기계 추출 대조: 현재 **일치**(드리프트 0). 대조 검사기 부재:
  `rg -c "generate_handler|invoke_handler" web/test web/scripts web/e2e` → **0건**.
  트랙 B(`e2e-shell/shell.spec.ts:124-150`)는 24 중 **6개만** 부르고 그 목록도 손으로 적혀 있다.
  같은 형태의 검사기는 **한 상수만** 잠근다(`test/shellContract.test.ts:48-51` 이 `notify.rs` 를 파싱해
  `NOTIFY_CLICK` 하나를 대조하고, 그 파일 머리주석이 스스로 _"검사가 계약의 한쪽 끝만 보고 있으면 그건
  계약 검사가 아니다"_ 라 적는다).
- 실패 시나리오: Rust 에서 `db_version_guard` 를 개명하거나 모듈 정리로 등록에서 뺀다
  (**선례가 있다** — P10 W4 가 `news.rs`·`research.rs` 를 실제로 삭제했다). 프런트는

  ```ts
  try {
    return await call('db_version_guard', undefined, DbGuardZ);
  } catch {
    return null;
  }
  ```

  라 **`null` 로 삼킨다.** 결과: C2 다운그레이드 가드와 `drifted` 판정(그 스키마 주석이 _"비어 있지 않으면
  DB 를 열어선 안 된다"_)이 통째로 사라진 채 앱이 **정상으로 뜬다** — 신버전이 적용한 DB 를 구버전이 열고
  "뜨는데 데이터가 옛날 것"이 된다. 이때 `verify`·`cargo test`·트랙 A 는 물론 **트랙 B 도 녹색**이다.
  `checkShape` 는 응답 *모양*만 보므로 이름 드리프트를 원리적으로 못 본다.

- 수정: 이미 그 일을 하는 파일(`test/shellContract.test.ts`)에 케이스 둘 — `generate_handler![…]` 블록을
  파싱한 집합과 `tauri.ts` 의 호출 이름 집합을 **양방향** 대조. 파싱 실패는 `throw`(조용한 skip 금지).

  ```ts
  it('프런트가 부르는 커맨드가 전부 generate_handler! 에 등록돼 있다', () => {
    expect(
      [...called()].filter((c) => !registered().has(c)),
      '개명·제거는 catch 로 삼켜져 무증상으로 기능만 죽는다',
    ).toEqual([]);
  });
  it('등록된 커맨드에 프런트 호출부가 있다 — 지워진 기능의 배관 탐지', () => {
    expect(
      [...registered()].filter((c) => !called().has(c)),
      'P10 W4 의 news·research 형태',
    ).toEqual([]);
  });
  ```

  ⚠ 추출 정규식은 `ollama_run`/`ollama_cancel` 처럼 `core.invoke` 를 직접 쓰는 둘도 잡아야 한다(실측).

- 비용: S

#### C035 · [실패] `.ics` 의 `RRULE:FREQ=WEEKLY;COUNT=n`(유한 반복)이 **영구 일과 블록**이 된다

- 위치: `web/src/lib/icsParse.ts:150-170`(`weeklyDays`) · 착지 `web/src/features/degree/CalendarIntake.tsx:89`
- 증거: **[읽음]** — 그 함수가 읽는 것은 `FREQ`·`INTERVAL`·`BYDAY` 셋뿐이다. `rg "COUNT=" web/src` → **0건**.
  (`UNTIL` 은 **의도적으로 받는다** — 학기 종료 표기이고 `test/icsParse.test.ts:48` 에 픽스처가 있다.)
- 실패 시나리오: 포털이 3주짜리 보강을 `RRULE:FREQ=WEEKLY;BYDAY=WE;COUNT=3` 으로 내보낸다.
  `weeklyDays` 가 `[3]` 을 돌려주고 `lectureToBlock` 이 **요일·시각만 있고 종료 개념이 없는 `RoutineBlock`**
  을 일과에 꽂는다(`icsParse.ts:256-257` — 그 타입에 종료일 필드가 없다) → 3주 뒤부터 **없는 수업이 매주
  수요일 19:00–20:00 을 영구히 먹는다.** 가용시간이 조용히 줄고 스케줄러가 그만큼 전부를 누른다.
  이 함수는 **두 줄 위에서 `INTERVAL !== 1` 을 정확히 같은 논거로 거부한다** —
  _"격주 수업을 매주로 펴면 없는 수업이 매주 시간을 먹는다 — 가용시간을 깎는 쪽이라 조용히 틀리면 계획
  전체가 눌린다."_ 같은 축의 한 갈래만 빠졌다.
  ⚠ 하위축은 Minor 로 냈다(사용자가 지울 수 있다). **메인이 Major 로 올린다** — 잘못된 값을 만들고,
  원인이 화면에 안 보이며, 재인입이 정상 사용(I010)이라 **반입마다 유령 블록이 하나씩 쌓인다.**
- 수정:

  ```ts
  if (interval !== 1) return null;
  /* ⚠ `COUNT=n` 은 **유한 반복**이다 — 상시 일과 블록으로 옮기면 위 INTERVAL 과 똑같이
     «없는 수업이 매주 시간을 먹는다». `UNTIL` 은 반대다(학기 시간표의 정상 표기). */
  if (parts.has('COUNT')) return null;
  ```

  짝 테스트: `COUNT=3` 이벤트가 `lectures` 0 · `unparsed` 1 이 되는지.

- 비용: S

#### C036 · [구조] 학사 눈금 적용 규칙 11줄이 두 인입구에 **글자 그대로** 복붙돼 있고 테스트가 0이다

- 위치: `web/src/features/degree/CalendarIntake.tsx:92-102` ↔ `web/src/features/degree/SyllabusIntake.tsx:100-110`
- 증거: **[측정]** — `diff` **차이 0줄**(11줄 완전 동일 · 메인이 직접 확인). jscpd 가 이 쌍을 두 번 잡았다.
  테스트: `rg -c "CalendarIntake|SyllabusIntake" test e2e` → **0건**(`test/syllabusIntake.test.ts` 는
  `lib/syllabusIntake.ts` **파서**만 본다). 즉 이 규칙은 **컴포넌트를 렌더하지 않으면 도달 방법 자체가 없다.**
  같은 두 파일에 토스트 판정(5줄)과 `row` 렌더 헬퍼(13줄 완전 동일)가 추가로 복제돼 있다.
- 실패 시나리오: 중복 판정 키가 `(ds, kind)` 다. 같은 날 **보강 두 건**(1교시·3교시)이 오면 지금도 한 건만
  들어간다 → 키에 `label` 을 더하는 수정이 온다. 고치는 사람은 자기가 연 화면 하나만 고치고, 그 뒤
  `.ics` 경로는 두 건 · 붙여넣기 경로는 한 건이 들어간다 — **같은 학기에 인입 경로에 따라 다른 눈금 집합**이
  남는다. `CalendarIntake.tsx:83-84` 가 강의 블록에 대해 이미 그 실패를 겪고 주석으로 못박아 뒀다.
  게이트는 전량 녹색이다(테스트가 0이므로).
- 수정: **읽기가 사는 곳으로 쓰기를 내린다** — 읽기는 이미 한 곳이 소유한다(`lib/semester.ts:261 marksOf`).

  ```ts
  // lib/semester.ts — marksOf 옆
  /** 학사 눈금을 학기에 넣는다. 같은 (ds,kind) 는 안 넣는다 — 학기 중 재인입(I010)이 정상 사용이라
   *  중복 방지가 이 규칙의 본체다. @returns 실제로 들어간 건수(호출부 토스트 카운터). */
  export function applyMarks(target: Semester, incoming: readonly Omit<AcademicMark,'id'>[]): number { … }
  ```

  두 호출부가 `n.mark = applyMarks(target, 고른것)` 한 줄로 같아진다. `row` 헬퍼는
  `features/degree/shared.tsx` 로(선례: `features/day/shared.tsx`).

- 비용: M (옮기기 S + 새 유닛 테스트 M)

#### C037 · [구조] `addAnki` 쌍둥이가 남았다 — 같은 자리의 짝은 H22 가 **이미 모았다**

- 위치: `web/src/features/integrations/AnkiPanel.tsx:143-153` ↔ `web/src/features/items/VaultImport.tsx:80-90`
- 증거: **[측정]** — `diff` 결과 11줄 중 **차이는 람다 인자 이름 한 글자뿐**(`(s)` vs `(x)`). 테스트 0건.
  바로 위 형제는 이미 고쳐져 있다: `VaultImport.tsx:77-79` 와 `VaultPanel.tsx:149-152` 가 둘 다
  `shell/actions.ts:577 importVaultSubject` 를 부르며 _"종전엔 28줄 사본 둘이었다 · H22"_ 라 적고,
  `shell/index.ts:92` 는 _"입구가 둘이지만 규칙은 하나다(H22)"_ 라 못박는다.
- 실패 시나리오: 중복 판정이 `items.some(x => x.name === 'Anki: ' + name)` 이다. 접두를 떼거나
  `source:'Anki'` 기준으로 바꾸면 고친 화면 하나만 바뀐다 → **과목 탭에서 덱을 넣은 뒤 연동 탭에서 같은
  덱을 넣으면** 중복 판정이 빗나가 `mode:'daily'` 항목이 둘 생기고, 스케줄러가 그 `dailyMin` 을
  **두 번 예산에 넣는다** — 매일 배정 시간이 조용히 두 배가 된다. 타입·린트·번들 전부 통과한다.
- 수정: H22 가 만든 자리 옆에 짝을 만든다 — `shell/actions.ts` 에 `importAnkiDeck(name, mins)`,
  `shell/index.ts` 에서 `importVaultSubject` 옆에 재수출, 두 컴포넌트의 지역 `addAnki` 삭제.
- 비용: S

#### C038 · [구조] 자정 걸침 분할 규칙이 스케줄러 안에 **세 벌**이고, 셋은 산문으로만 이어져 있다

- 위치: `web/src/lib/scheduler/windows.ts:82` · `windows.ts:124` · `web/src/lib/scheduler/layout.ts:133`
- 증거: **[측정]** — `rg "1440\]" web/src` → 그 셋이 전부 동일한 `s <= e ? [[s,e]] : [[s,1440],[0,e]]`.
  결합 선언은 주석뿐이다: `layout.ts:131` _"freeWindows 분할과 **동일 규칙**"_ ·
  `windows.ts:118` _"**수면과 동일 규칙**"_.
- 실패 시나리오: **이 부류는 이미 한 번 청구됐다** — `windows.ts:68-69` 가
  _"옛 구현은 sleep[0]만 보고 … `23:00–07:00` 한 칸을 통째로 놓쳐 **심야에 공부를 배정하는 버그**가 있었다"_
  라 적어 뒀고, `utils.ts:168` 도 같은 계열을 경고한다. 다음 번은 이렇게 온다: `s === e` 를 「종일」로 읽어야
  하는 요구가 오면 `windows.ts` 의 두 벌은 함께 고쳐지고 `layout.ts` 의 셋째는 다른 파일·다른 관심사라
  남는다 → **스케줄러는 그 시간을 점유로 빼는데 타임라인은 폭 0 으로 그린다**(비어 보이는데 아무것도
  안 배정되는 칸). 셋 다 지역 상수라 컴파일러가 못 본다.
- 수정: `toMin` 이 사는 곳에 한 벌.

  ```ts
  // lib/scheduler/windows.ts — toMin 곁
  /** 블록 [start,end] → 분 구간들. 자정을 걸치면 둘로 쪼갠다.
   *  ⚠ **한 벌**인 것이 요점이다 — 사본이 갈리면 스케줄러가 배정한 날과 타임라인이 그린 날이 달라진다. */
  export function minuteSegments(start: string, end: string): [number, number][] {
    const s = toMin(start),
      e = toMin(end);
    return s <= e
      ? [[s, e]]
      : [
          [s, 1440],
          [0, e],
        ];
  }
  ```

- 비용: S

#### C039 · [표면] `*At` 접미사가 **네 뜻**이다 — 같은 값이 한 홉 건너 `seenAt` ↔ `seenDs` 로 갈렸다

- 위치: `web/src/lib/uiState.ts:111` · `web/src/store/useUI.ts:112-117` · `web/src/app/RailSidebar.tsx:144,149` · `web/src/lib/since.ts:40,54,57` · `web/src/lib/schema.ts:178`
- 증거: **[측정]** — `*At` 필드 선언 28건 전수: epoch ms **24** · `HH:MM` 1(`schema.ts:178 doneAt`) ·
  `YYYY-MM-DD` 1(`uiState.ts:111 seenAt`) · 0-based 배열 인덱스 2(`resume.ts:184` · `reviewQueue.ts:287`).
  반면 `Ds` 접미사는 **31어휘·600여 회 전량** `YYYY-MM-DD` 로 지켜진다.
  그리고 `uiState.ts:110` 이 스스로 _"⚠ 날짜만 담는다(시각 아님)"_ 라 적는다 —
  **작성자가 이름이 거짓임을 알고 개명 대신 주석을 달았다.**
- 실패 시나리오: `since.ts:57` 이 `pick(state).filter(ds => ds > seenDs)` 로 **문자열 비교**한다.
  두 번째 「지난번 이후」 소비처(폰·새 화면)를 다는 사람이 `ui.seenAt` 을 보고 이 저장소의 지배적
  관용구(`*At` = epoch ms — 같은 파일 `since.ts:40` 이 _"`at`(epoch ms)뿐이라 ISO 로 맞춘다"_ 라 적는다)를
  적용하면:
  · `Date.now()` → `"2026-08-22" > "1756…"` 이 **항상 참** → 레일 배지가 전 이력을 영구히 「새 것」으로 센다.
  · `toISOString()` → `"2026-08-22" > "2026-08-22T09:00:00.000Z"` 가 **거짓** → 오늘 것이 통째로 안 세어진다.
  둘 다 zod(`z.record(z.string(), z.string())`)를 통과하고 게이트가 녹색이다.
  `doneAt` 은 더 예약된 형태다 — 스키마 주석이 _"지금은 아무 화면도 이 값을 안 그린다"_ 라,
  다른 세션이 `new Date(entry.doneAt)` 로 읽는 것이 **계획돼 있다**(→ `Invalid Date`).
- 수정: `seenAt` → `seenDs`(저장 키 개명 · `sinceCount` 가 `!seenDs → null` 이라 1회 공백이 무해하다 —
  `navSurface` 가 N-6 에서 쓴 무마이그레이션 형태) · `schema.ts:178 doneAt` → `doneHm`(작성자가 `nowHm()`).
- 비용: S

#### C040 · [표면] `PACE_HORIZON_MAX = 546 // 18개월 — 페이스/마감 경로` 가 **페이스 경로에 대해 거짓**이고, 진짜 손잡이는 이름 없는 `26` 이다

- 위치: `web/src/lib/scheduler/engine.ts:113`(`let weeksNeed = 8`) · `:118`(`Math.min(weeksNeed, 26)`) · `:131`(`PACE_HORIZON_MAX = 546`)
- 증거: **[읽음]** — `endByPace = mondayOf(start) + weeksNeed*7 + 6` 이고 `weeksNeed ≤ 26` 이므로
  `endByPace − start ≤ 188일`. 546 은 `lastDL`(시험·마감)이 546일 밖일 때만 걸린다 →
  **페이스 경로에서 546 은 결코 도달하지 않는다.**
- 실패 시나리오: 「300시간짜리 과목을 주 2시간」인 사용자의 계획이 26주에서 잘려 그 뒤 챕터가 배치되지
  않는다. 고치러 온 사람은 같은 함수 안에서 **유일하게 이름과 근거가 붙은 손잡이**인 `PACE_HORIZON_MAX`
  (주석: `페이스/마감 경로`)를 올린다 → **아무 변화가 없다.** 13줄 위의 벌거벗은 `26` 이 진짜 손잡이인데,
  이 파일에서 **유일하게 이름도 주석도 없는 수**다(바로 아래 두 상수는 각각 여러 줄짜리 근거를 단다).
- 수정: `PACE_WEEKS_MIN = 8` · `PACE_WEEKS_MAX = 26` 으로 이름을 주고, 546 의 주석을
  **「마감(시험) 경로 전용 — 페이스 쪽은 `PACE_WEEKS_MAX` 가 먼저 자른다」**로 정정.
  ⚠ **26 을 올리는 것이 처방이 아니다** — 그 상한이 의도인지는 별개 판단이고, 이 건은 «손잡이가 거짓말한다»다.
- 비용: S

#### C041 · [성능] `applyPull` 이 **드레인 라운드마다 정본 전량을 되읽는다** — pull 이 DB 크기에 대해 제곱이다

- 위치: `web/src/lib/cloud/merge.ts:190`(`const back = await readRows()`) · 루프 `web/src/lib/cloud/run.ts:175-212`
- 핫패스: 기기 온보딩 · 전량 복구 · 오래 끊긴 뒤 복귀. `MAX_PULL_DRAIN` 이 도는 유일한 경로이고
  그 경로의 존재 이유가 «큰 동기화»다(H4). 일상 편집은 1회차로 끝나 비용 0.
- 증거: **[읽음]** + 저장소가 스스로 적은 실측 상수. `run.ts:196` 이 _"`applyPull` 은 **매번 정본 전량을
  다시 읽어 오므로** 마지막 것이 곧 누적 결과다"_ 라고 **그 사실을 근거로 쓴다** — 즉 버려지는 회차의
  전량 읽기가 설계에 알려져 있다. 규모 상수: `sqlite.ts:215` _"2년 근사 상태에서 IPC 946KB + JS 9ms"_ ·
  `write.ts:298` _"10,380행/0.67MB"_ · `run.ts:129` _"27라운드 × 1.2초 ≈ 30초"_.
  ⚠⚠ **[측정] 오늘 실 DB 는 총 24행**이다(메인이 읽기 전용 사본으로 확인:
  settings 17 · docs 3 · ds_map 2 · meta 1 · runtime_cache 1 · `records`/`completions`/`summaries`/
  `week_alloc`/`tombstones` **전부 0**). **지금 이 비용은 관측되지 않는다** — 아래 배수는 저장소가
  스스로 설계 목표로 적은 규모에서의 값이다.
- 실패 시나리오(입력 규모 → 배수): 한 회차 상한이 `MAX_BATCH_ITEMS=500` 이므로 행 N 을 받는 라운드는
  ≈`N/500+1`, 라운드마다 DB 전량 D행을 읽는다 → **O(N·D/500)**, 전량 온보딩(N≈D)이면 **O(D²/500)**.
  D=10,380(저장소 기록치)이면 22라운드 × 10,380 = **228,360 행-읽기 ≈ 21MB IPC**, 실제로 필요한 것은
  마지막 1회뿐 → **22배**. D=100,000이면 **201배**(선형이 아니라 제곱이라 데이터 10배면 낭비 100배).
  m-7(2026-08-20)이 같은 루프에서 `rowsToState` 를 지연 게터로 빼 절반을 없앴는데 **그 아래
  `readRows()` 는 그대로 남았다.**
- 수정: 되읽기·기준선 세우기를 **드레인 밖으로 한 번**만 낸다(`applyPull` 에 `readback?: boolean`,
  루프 뒤 `refreshBaseline()`). 창(`beginMergeApply`)은 회차마다 그대로 열어 C1/C-2 가드를 더 넓게 닫는다.
- ⚠ **되돌리기 비싼 코어다**(오버레이 §2-데이터). 착수하면 `cd server && npm run test:roundtrip`(실 workerd
  - 실 D1) + 드레인 케이스가 게이트이고, 특히 «드레인 중간 실패»(`run.ts:223` 형태 ⓐ)와 «되읽기 실패»(ⓑ)가
    이 변경으로 **한 갈래로 합쳐진다** — 부수 이득이자 검증해야 할 지점.
- 비용: M

#### C042 · [잔재·Major] `docs.ts` 가 **존재하지 않는 파일**에 값의 소유를 맡기고, `DOC_KEYS` 의 세입자가 다시 0이다

- 위치: `web/src/lib/db/docs.ts:48-53`(`DOC_KEYS = ['ics:feed']` · _"값의 형태는 `lib/icsFeed.ts` 가 소유한다"_)
- 증거: **[측정]** — `ls web/src/lib/icsFeed.ts` → **없음**(I050 이 어제 `.ics` 살아있는 피드를 라우트째 걷었다).
  `docGet`/`docSet` 실호출자는 `sidecars.ts` 둘뿐이고 그 키는 `LOCAL_EXTRA_KEYS = ['lh_ui_v1']` 라
  **`DOC_KEYS` 밖**이다 → 전량 localStorage 로 흐른다.
- 실패 시나리오: 같은 파일 `:44` 가 **빈 배열 상태의 대가**를 이미 기술해 뒀다(_"`DocKey` 가 `never` 라
  … 지금 이 파일은 동작상 얇은 KV 래퍼이고 … 조용히 죽은 배관이 이 저장소가 반복해 만든 형태다"_).
  그런데 `:48` 이 **✅ 로 「세입자가 돌아왔다」**고 선언한 채 남아 있어, 인수인계받은 사람은 **정반대 상태**를
  읽는다 — 살아 있는 D1 동기화 KV 가 있다고 믿고 그 위에 기능을 얹는다. 낡은 주석이 사실과 다르게
  말하는 형태라 커널 §잔재 규정에 따라 **Major**(없는 문서보다 나쁘다).
- 수정: `DOC_KEYS` 를 `[] as const` 로 되돌리고 `:44` 문단을 정본으로 복원 · `:48~53` 의 ✅ 문단은
  **삭제**(배관 자체는 그 문단의 근거대로 존치). ⚠ 실 DB `docs` 표에 **행 3개**가 남아 있는 것은
  데이터 축 소관이다(§8).
- 비용: S

### Minor

#### C043 · [실패] `since.ts` 의 저널 델타가 **UTC 날짜**로 세는데 비교 대상은 **로컬 날짜**다

- 위치: `web/src/lib/since.ts:44` · 소비처 `web/src/app/RailSidebar.tsx:13,149`
- 증거: **[재현]** — `TZ=Asia/Seoul` 에서 `2026-08-22 08:00 KST` →
  `toISOString().slice(0,10)` = `'2026-08-21'`(안 세어짐) vs `iso(local)` = `'2026-08-22'`(세어짐).
  `utils.ts:207` 이 _"`toISOString()`은 UTC라 KST 등에서 하루가 밀린다"_ 라고 **같은 함정을 이미 적고**
  `iso()` 를 제공하는데, `COUNTERS.journal` 한 줄만 그 규약 밖이다.
- 실패 시나리오: `seenDs` 는 `todayISO()` = **로컬** 이다. KST 00:00–09:00 에 쓴 3문장 요약은 전날 날짜가
  붙어 `ds > seenDs` 를 통과하지 못한다 → 레일의 「새 것」 배지가 **안 뜬다**. 그리고 `seenAt` 이 이미
  전진했으므로 **다음 날에도 영영 안 뜬다.**
  ⚠ `test/since.test.ts:45` 가 못 잡는 이유: 픽스처가 `Date.parse('2026-08-03')`(UTC 자정)이라 UTC/로컬
  표현이 **우연히 같다**.
- 수정: `.map((x) => (x.at ? iso(new Date(x.at)) : ''))` — 테스트도 로컬 오전 시각 픽스처로.
- 비용: S

#### C044 · [실패] 동기화가 도는 중에 한 편집은 트리거가 **버려진다**(재큐 없음)

- 위치: `web/src/store/syncController.ts:287-307`(`installSyncTriggers` 의 `run`)
- 증거: **[읽음]** — `const run = () => { if (_running) return; … }`. `syncSoon`(`:249`)은 타이머가 뛰는 순간
  `_editTimer = null` 로 자기를 소비하고 `_activeRun()` 을 부르므로, 그 호출이 `_running` 에 막히면
  **그 편집의 예약은 어디에도 남지 않는다.** 폴백 폴링은 없다 — `:368` 이 _"`pollMs` 는 호환을 위해 남았고
  데스크톱은 더 이상 주지 않는다(W24)"_.
- 실패 시나리오: 두 번째 기기 온보딩처럼 드레인이 도는 동기화(같은 파일이 _"27라운드 × 1.2초 ≈ 30초"_ 라
  실측을 적어 뒀다) 중에 편집한다 → 1.2초 뒤 `syncSoon` 이 뛰지만 즉시 반환 → 남은 트리거는
  `focus`(**최소 간격 5분**) · `online`(전이 이벤트라 안 옴) · 다음 편집뿐. 이 파일 머리주석이 결함으로 지목한
  _"편집→다른 기기 반영이 최대 5분 늦었다"_ 가 이 구간에서 그대로 재현된다. **유실이 아니라 지연**이다
  (워터마크 기반이라 다음 스캔이 반드시 집는다) — 그래서 Minor.
- 수정: `_again` 플래그로 도는 중에 온 트리거를 **한 번으로 접어 뒤에 잇는다**(`finally` 에서 재호출).
- 비용: S

#### C045 · [성능] `readTouched` 의 첫-키 `IN` 이 `records`·`ds_map` 에서 **슬라이스 전량 인출**로 퇴화한다

- 위치: `web/src/lib/db/sqlite.ts:242-246` · 열 정의 `web/src/lib/db/rows.ts:141-143`
- 핫패스: `writeAndVerify` → `runWrite` 의 `probes.length <= 400` 가지 — 즉 **모든 일반 편집 flush**
  (400ms 디바운스). 드래그 중이면 초당 2~3회.
- 증거: **[측정]** — `records.cols[0] = 'slice'`(카디널리티 **9**) · `ds_map.cols[0] = 'slice'`(**4**).
  나머지 넷은 안전하다(`completions`=`ds` · `week_alloc`=`wk` · `summaries`=`sid`).
  ⚠ 그 자리 주석(`sqlite.ts:221`)은 _"과다 인출의 상한은 «이번에 손댄 서로 다른 첫 키 수»"_ 라 적는데,
  그건 **바인딩 개수의 상한이지 반환 행 수의 상한이 아니다.** 그리고 문제가 되는 두 표가 하필
  «성장 무제한 슬라이스만 행으로 쪼갠다»로 선정된 표 전부다.
  ⚠ **[측정] 오늘 실 DB**: `records` 0행 · `ds_map` 2행 → **현재 실비용 0**(그래서 낮은 쪽으로 분류).
- 실패 시나리오: 120일치 `dayPlans` 가 쌓인 상태에서 한 날의 블록 하나를 드래그 → touched 1행(883B)인데
  질의는 `WHERE slice IN ('dayPlans')` → **120행 106KB**, 400ms 마다. `records/cbms` 500행이면 **500배**.
  H6 이 시간축 표본(20회당 1회)을 행축 매회로 바꾼 근거가 _"손댄 행만 보면 O(변경행)"_ 이었는데
  이 두 표에서는 **O(슬라이스 전량)** 이라 그 전제가 성립하지 않는다.
- 수정: 둘째 키도 `IN` 으로 건다(곱집합이라 **과소 인출이 원리적으로 불가능**하고 호출부가 완전키로
  조회하므로 잉여 행은 무해하다). 행 값 튜플은 쓰지 않는다 — 그 주석이 sqlx 경로 미검증을 이유로
  거부한 문법이고 여기서는 필요 없다. 바인딩 최악 400+400=800 으로 `rows.ts:404` 의 여유선과 같다.
- 비용: S

#### C046 · [구조] 정체성 해시가 **넷 · 알고리즘 둘** — SSOT 를 선언한 주석에 집행자가 없고 이미 세 번 어겨졌다

- 위치: `web/src/lib/utils.ts:94`(`hash32` · SSOT 선언) · `lib/semesterFingerprint.ts:50`(`hash01`) · `lib/mistakes.ts:126` ↔ `lib/retrieval.ts:20`(`hashStr` 상호 사본)
- 증거: **[측정]** — `rg "Math.imul" web/src` → **4곳**. `utils.ts:88-92` 가
  _"정체성에서 값을 파생할 때는 이걸 쓴다 … 새 해시를 또 만들면 파생마다 다른 근거를 갖게 된다"_ 라
  규약을 선언하는데 `rg "Math.imul|hash32" web/test/invariants.test.ts eslint.config.js` → **0건**.
  `hash01` 은 비트 사본이다(상수 `2166136261`·`16777619` = `0x811c9dc5`·`0x01000193`).
- 실패 시나리오: `hash32` 는 `chapterFuzz`(복습 due 를 ±1일 흔든다)와 `colorForId` 를 동시에 먹인다.
  분포 문제로 승수를 바꾸는 변경이 오면 — `utils.ts` 는 _"노브만 바꾸면 다음 부팅에 전부 갱신된다"_ 를
  근거로 그런 변경을 **장려한다** — `semesterFingerprint` 만 옛 알고리즘에 남아 두 화면이 같은 id 에 대해
  다른 이야기를 시작한다. 어느 검사도 울지 않는다.
  Minor 인 이유: 넷 다 결정론적이고 **현재 출력이 갈린 곳은 없다.** 청구되는 것은 다음 변경의 비용이다.
- 수정: `hash01` 을 `(hash32(id) % 10000) / 10000` 으로(비트 동일 실측 — 스냅샷 재생성 불필요) ·
  `hashStr` ×31 을 `utils.rotateSeed` 한 벌로 · **불변식 ⑲**(`Math.imul` 이 `lib/utils` 밖에 없다)로 잠근다.
- 비용: S

#### C047 · [구조] 루트 `/`(Workers 오리진)가 **데스크톱 SPA 를 연다** — 거기서 한 편집은 영원히 동기화되지 않고, 테스트가 그 동작을 축복한다

- 위치: `server/wrangler.jsonc:49`(`not_found_handling: "single-page-application"`) · 잠그는 케이스 `server/test/assets.test.ts:102`(_"`/` 가 index.html 을 준다"_) · 가드 부재 `web/src/main.tsx`
- 증거: **[측정]** — `main.tsx` 에 오리진·호스트 판정이 **0건**. 저장 경로 조건은 `isSqlitePrimary()` 인데
  브라우저의 데스크톱 엔트리는 그것을 켜지 않으므로 localStorage 백엔드로 뜬다.
- 실패 시나리오: 사용자가 폰이 아니라 클라우드 URL **루트**를 연다(북마크·주소 자동완성·공유 링크).
  완전히 동작하는 것처럼 보이는 앱이 뜨고 편집이 localStorage 로 간다. **아웃박스는 SQLite 만 훑으므로
  그 편집은 영원히 올라가지 않는다** — CLAUDE.md 가 명시한 바로 그 실패("폰 편집이 아웃박스에 안 걸려
  영원히 동기화되지 않는다")가 **문이 하나 더 있어서** 성립한다. 화면에 아무 표시가 없다.
  원장 `C026` 의 재확인이자 실물 근거 보강이다.
- 수정(택일 · 되돌리기 쉬운 쪽부터): ① 데스크톱 엔트리가 `tauri://` 가 아닌 오리진에서 뜨면
  **배너 하나**를 띄운다(`app/StorageBanner` 가 이미 그 형상을 갖는다) ② 또는 `/` 를 `/phone` 으로
  리다이렉트하고 데스크톱 SPA 는 명시 경로에만 둔다(그러면 `assets.test.ts:102` 의 단언을 그 계약으로
  바꾼다 — **테스트가 옛 계약을 지키고 있다는 것이 이 건의 절반이다**).
- 비용: S(①) / M(②)
- ⚠ 브라우저 localStorage 폴백 자체는 **의도된 것**이다(dev 루프 + 트랙 A 전량이 그 위에서 돈다) —
  지우자는 제안이 아니다. 지적은 **프로덕션 오리진에 그 문이 열려 있고 아무도 안 말한다**는 것이다.

#### C048 · [표면] `contentSearch` 의 목적지가 다섯 자리에서 **네 관용구**로 조립되고 셋은 로스터와 연결이 없다 (원장 `C023` 확인)

- 위치: `web/src/lib/contentSearch.ts:80`(지역 `itemAnchor`) · `:88` · `:98` · `:116`(`'/day'`) · `:135`(`/review#…`) · `:154`(`'/mistakes?sid=' + …`)
- 증거: **[읽음]** — 메인이 직접 확인. 네 관용구(헬퍼 / 헬퍼+템플릿 / 생 리터럴 / `+` 연결)이고
  `itemAnchor` 는 **함수 안 지역 화살표**라 밖에서 재사용도 안 된다.
- 실패 시나리오: 이 저장소는 실제로 탭 키를 갈아 왔다(`journal`→`day` · `graph` 은퇴 · `guide` 삭제 후 부활).
  `'/day'`·`'/review'`·`'/mistakes'` 가 개명되면 셋은 **컴파일도 린트도 불변식도 통과한 채** 남고,
  `App.tsx` 의 `<Route path="*" element={<Navigate to="/today"/>}/>` 가 ⌘K 히트를 전부 오늘 화면으로
  착지시킨다 — 사용자에겐 "⌘K 가 엉뚱한 데로 간다"이고 불변식 ⑱은 `?view=` 만 보므로 사각이다.
- 수정: 목적지를 한 자리에 모으고 탭 경로는 **로스터에서 읽는다**(`TabKey` 유니온이면 개명이 타입 에러가
  된다 — 그게 이 수정의 값이다).
- 비용: S

### Nit

#### C049 · [표면] `tabs.ts:466` 의 「`guide` 행이 여기 있었다 — 삭제됐다」가 **오늘 아침 거짓이 됐다**

- 위치: `web/src/shell/tabs.ts:466-474`(삭제 주석) vs `:385`(`key: 'guide'` · `role: 'view'` · 살아 있다)
- 증거: **[측정]** — `git log -S"key: 'guide'"` → `2e28465`(2026-08-22 09:51 · I025)가 그 행을 되살렸고
  삭제 주석(W4 · 2026-08-07)은 갱신되지 않았다.
- 실패 시나리오: 다음 사람이 `?view=` 화면을 은퇴시킬 때 한 파일에서 **정반대의 두 선례**를 만난다.
  아래(옛것)를 따르면 불변식 ⑱이 잡는데, 그때 사람은 **면제 사유를 적어 통과시키는 쪽**으로 간다
  (⑱이 면제 문법을 갖고 있다). 게이트가 잡으므로 Nit.
- 수정: 삭제 주석에 «그러나 I025 가 `role:'view'` 로 되등재했다 — 지금의 선례는 «지운다»가 아니라
  «`role:'view'` 로 내린다»다» 한 문단 추가.
- 비용: S

#### C050 · [잔재] `eslint.config.js` 가 **존재하지 않는 파일**에 `max-lines: 'off'` 면제를 걸고 있다

- 위치: `web/eslint.config.js:250-254`(`src/lib/atlasData.ts` 면제) · 근거 문장 `:175`
- 증거: **[측정]** — `ls web/src/lib/atlasData.ts` → 없음(P10 W4 · 2026-08-07 에 `survey/` 로 이사).
  **같은 파일 `:518` 이 그 이사를 스스로 적는다** — 한 설정 파일 안의 자기모순.
- 실패 시나리오: 지금은 무해하지만 ① `:175` 의 _"실측 최댓값 724(면제된 `atlasData.ts` 제외)"_ 라는
  근거 문장이 사문이 됐고 ② 누가 그 경로에 새 파일을 만들면 **크기 래칫이 조용히 꺼진다.**
- 수정: override 블록 삭제 + `:175` 괄호 삭제.
- 비용: S

## 4. 근본 원인 (3개)

### R1 — 집행자가 **자기 문법**을 막고 **같은 뜻의 다른 표기**를 못 본다

2026-08-20 회차의 R1(_"선언하고 집행자를 안 붙인다"_)에 대한 처방은 실제로 이행됐다 — 이 저장소에는
불변식 18개 + `check-tokens.mjs` + `boundaries` + 래칫 둘이 있고 전부 돈다. **이번 회차가 찾은 것은 그
다음 층이다**: 세운 집행자들이 한 겹 굵다.

| 집행자          | 막는 것                       | 통과하는 것                                | 이번 회차                             |
| --------------- | ----------------------------- | ------------------------------------------ | ------------------------------------- |
| 불변식 ⑰        | `<b>이름</b> 탭`              | `<Tab>이름</Tab>`(같은 뜻 전용 컴포넌트)   | **C033** — 죽은 이름 8회              |
| 불변식 ⑭        | 테스트만 쓰는 **모듈**        | 테스트만 쓰는 **export**                   | `retrievalStats`·`slowButPassing`(§6) |
| 불변식 ⑱        | `?view=` 주소                 | `to:` 생 리터럴·`+` 연결                   | **C048**                              |
| `shellContract` | 상수 **하나**(`NOTIFY_CLICK`) | 커맨드 이름 **24개**                       | **C034**                              |
| 타입(`DbRows`)  | 슬라이스 리터럴 두 벌         | 규칙 복붙(`applyMarks`·`addAnki`·자정분할) | **C036·C037·C038**                    |

⭐ 오버레이 §1-B 가 ux 축에서 세운 명제(_"검사기를 볼 때 「무엇을 막나」가 아니라 「같은 결과를 내는 다른
표기가 있나」를 물어라"_)가 **코드 축에서도 그대로 참이다.** 그리고 §1-A 의 「손으로 적은 목록」은 코드
안에서 여전히 살아 있다 — C034 의 커맨드 로스터가 그 형태이고, 지금 **참인 이유가 사람 기억뿐**이다.

### R2 — 비동기 실패가 **어느 층에도 안 걸린다**

셋이 동시에 비어 있다:

1. **타입 인지 린트가 꺼져 있다** — `eslint.config.js:28` 이 `tseslint.configs.recommended`(타입 비인지)다.
   켜 보면 `no-floating-promises` **34** · `no-misused-promises` **80**(실측 · 프로브 14초).
2. **규율은 손으로 지킨다** — `void <expr>` 표기가 **62곳**. 즉 규약은 알려져 있고 표기까지 도입돼 있는데
   집행자가 없어 **34곳이 표기 없이 샌다**(§1-A 의 형태 그대로).
3. **런타임 관측이 0** — 전역 `unhandledrejection` 핸들러가 **저장소 전체에 0건**이다.
   `bootFallbackScreen.ts:11` 은 _"텔레메트리는 `unhandledrejection` 으로 그 사실을 서버에 보냈지만"_ 이라
   적지만 그 층은 어제 은퇴했고(I052 · 커밋이 _"⚠ 대가: 폰의 렌더 밖 부팅 실패가 다시 침묵이다"_ 라
   스스로 적었다), 데스크톱 e2e 는 `pageerror` 를 검사하지 않는다(`phone.spec.ts` 만 본다).

그리고 실패를 삼키는 관용구가 그 위에 있다 — `dbVersionGuard` 의 `catch { return null }`(C034)이 대표다.
**은퇴는 정당한 결정이었지만, 그 결정이 남긴 구멍을 메우는 값싼 대체물(린트 · e2e 한 줄)이 안 들어왔다.**

### R3 — **새 표면은 규칙을 컴포넌트 안에 두고 테스트를 안 붙인다**

마지막 코드 리뷰(2026-08-20) 이후 `web/src` 에 **신규 16파일**이 들어왔다(`.ics` 인입 · 학기 인입 ·
백업 · 마이그레이션 · 관측). 이번 결함 중 셋이 그 창에 있고(**C035·C036** + C049), 그 셋의 공통점은
**규칙이 `.tsx` 의 `mutate((st) => {…})` 콜백 안에 산다**는 것이다 — 그러면 렌더 없이는 도달 불가라
테스트가 0이 되고(실측 `CalendarIntake`·`SyllabusIntake` 테스트 **0건**), 두 번째 인입구가 생기면
복붙이 유일한 선택지가 된다.

실측 분포: `.tsx` 안 다중행 `mutate` 콜백 **44개 중 6줄 초과 8건**, 최댓값 **23줄**(`CalendarIntake.tsx:80`).
이 저장소는 이미 처방을 알고 있다 — H22 가 `importVaultSubject` 로 같은 형태를 한 번 접었다(C037 은
그 짝이 안 접힌 것이다).

## 5. ⭐ 진보 — 최고 수준엔 있고 우리에겐 없는 것

**표본은 실제로 열었다**(기억 인용 아님):

- SQLite 테스트 철학 — <https://sqlite.org/testing.html> (확인일 2026-08-22)
- Go 코드 리뷰 관행 — <https://go.dev/wiki/CodeReviewComments> (확인일 2026-08-22)
- React 기여 가이드 — <https://legacy.reactjs.org/docs/how-to-contribute.html> (확인일 2026-08-22)

### 셀 수 있는 것으로 나란히

| 원시값              | 표본                                                       | 우리                                              | 차이                      |
| ------------------- | ---------------------------------------------------------- | ------------------------------------------------- | ------------------------- |
| 테스트:본체 줄 수   | SQLite **92,053 : 155.8 KSLOC ≈ 590:1**                    | 23,325 : 39,756 = **0.59:1**                      | 1000×                     |
| 단언 없는 테스트    | —                                                          | **0 / 2,110**                                     | 우리가 건강하다           |
| 커버리지 종류       | statement · branch · **MC/DC 100%**                        | statement·branch 래칫                             | MC/DC 없음                |
| 이상 상황 주입      | OOM · I/O 오류 · 크래시 · **중첩 실패**                    | 「완전 불가」 2케이스만                           | 부분                      |
| 퍼저                | AFL · OSS-Fuzz · dbsqlfuzz · jfuzz                         | **0**                                             | 없음                      |
| 오류 무시 금지 집행 | Go: _"Do not discard errors using `_`"_ (리뷰어 의무)      | 타입 인지 린트 **off** → floating 34 · misused 80 | **없음**                  |
| PR 필수 게이트      | React: prettier·lint·flow·test·**`test --prod`** + 번들 봇 | verify 8단계 + budget 4축 + a11y + 트랙 A/B       | 우리가 더 많다(단 아래 ⓐ) |

### 해부 — `ⓓ = 없음` 먼저

| 메커니즘                             | ⓐ 왜 *의무*인가                                                                                                                       | ⓑ 없으면 나는 사고                                                                                                          | ⓒ 그 대가                                                                                                                      | ⓓ 우리                                                                                                                                                | ⓔ 우리 규모의 최소형                                                                                                                       |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| **타입 인지 비동기 린트**            | Go 가 «오류를 `_` 로 버리지 마라»를 리뷰어 의무로 두는 이유와 같다 — 비동기 실패는 **표현식 하나로 사라지고** 사람 눈으로는 안 보인다 | R2 전체. 그리고 «`void` 표기 62곳»이 규약을 **알고 있음에도** 34곳이 샌다                                                   | 린트가 타입 정보를 요구해 느려지고(**실측 14초/src**), 정당한 fire-and-forget 마다 `void` 를 손으로 붙여야 한다                | **없음**(`tseslint.configs.recommended`)                                                                                                              | `no-floating-promises`·`no-misused-promises` **둘만** 켜고 나머지 타입 인지 룰은 끈 채 `verify` 에 — 114건은 대부분 `void`/`catch` 한 글자 |
| **시간대·로케일 테스트 매트릭스**    | 시간 정확성은 «한 TZ 에서 통과»가 증명이 아니다 — 같은 코드가 UTC 에선 맞고 UTC+9 에선 틀린다                                         | **C043 그 자체.** 그리고 그 테스트는 UTC 자정 픽스처라 **영원히 초록이다**                                                  | CI 시간 배수 · TZ 의존 스냅샷이 있으면 그쪽을 먼저 TZ-불변으로 고쳐야 한다                                                     | **없음**(`rg "process.env.TZ" web/` → **0건** · 개발자 머신 시간대에 의존)                                                                            | `vitest --project` 둘(`TZ=UTC` · `TZ=Asia/Seoul`)을 **날짜 파일에만**(`utils`·`since`·`ics*`·`spacedReview`·`semester`·`dayPhase`)         |
| **크로스언어 계약 대조**             | Rust↔TS 는 **컴파일러가 원리적으로 못 잇는 유일한 경계**다                                                                            | **C034.** 개명이 `catch` 로 삼켜져 다운그레이드 가드가 무증상 소실                                                          | 생성기(`tauri-specta`)를 쓰면 Rust 타입이 TS 형태를 좌우해 프런트 리팩터가 Rust 를 건드린다 — 지금의 «비차단 경고» 정책과 충돌 | **없음**(이름은 생 문자열 · 반환만 손유지 zod)                                                                                                        | **생성기가 아니라** `shellContract.test.ts` 의 로스터 양방향 대조 두 케이스                                                                |
| **모델 기반 수렴 property test**     | LWW+툼스톤의 명세는 «규칙이 맞나»가 아니라 «임의 연산 순서에서 두 기기가 같은 상태로 수렴하나»다                                      | 동점 규칙이 한쪽만 뒤집혀도(`>=` ↔ `>`) 손코딩 케이스는 전부 통과하고 특정 인터리브에서 **영구 분기**한다(H3 실사고가 그것) | 최소 반례 축소에 시간이 들고, 시드 고정을 안 하면 flaky 로 읽힌다                                                              | **부분** — `test:roundtrip` 이 실 workerd·D1 로 왕복하지만 **고정 시나리오 12건**                                                                     | 시드 고정 `fast-check` 로 «op 20개를 두 기기에 무작위 배분 → 임의 순서 push/pull → 두 로컬 DB 가 같다» 100케이스                           |
| **질의 수를 계약으로 못박는 테스트** | 복잡도는 코드를 읽어야만 보이고 읽는 사람이 매번 바뀐다                                                                               | **C041·C045 둘 다** «리뷰가 발견할 때까지 몇 달»이었다. m-7 이 같은 루프의 **절반만** 고치고 나머지를 남긴 것이 증거        | `Db` 경계에 계측이 상주하고, 정당한 리팩터가 질의 모양을 바꿀 때마다 테스트가 깨진다                                           | **없음** — 성능 회귀망은 `budget`(번들 4축) 하나이고 **런타임 질의·순회 축은 0**                                                                      | 카운팅 `Db` 스텁 하나 + 단언 둘(«N페이지 pull → `readRows` 정확히 1회» · «1행 편집 → 반환행 ≤ touched×2»)                                  |
| **저장 경계 결함 주입**              | SQLite 가 «N번째 I/O 에서 실패시킨다»를 전수로 도는 이유: 부분 실패 경로는 **주석에만 안전하다고 적혀 있다**                          | `writeRows` 중간 실패 · `readTouched` 실패 · `batchDb` 폴백의 비원자 중단이 **실행된 적이 없다**                            | 페이크 어댑터 유지비 + 실 SQLite 의 성질(친화성 강제변환)은 그 페이크로 못 잰다                                                | **부분**(`dbUnavailable`·`saveFailure` 가 「완전 불가」만)                                                                                            | `Db` 인터페이스가 이미 4메서드 — k번째 `execute` 에서 던지는 카운팅 데코레이터 + k 를 0..N 으로 훑는 루프                                  |
| **뷰 콜백 안 도메인 로직 래칫**      | 상태 변형 규칙이 뷰 안에 있으면 **렌더 없이는 도달 불가**가 되어 테스트가 구조적으로 0이 된다                                         | **R3 · C036 그 자체**(11줄 규칙 · 테스트 0 · 사본 2)                                                                        | 한 줄 변형도 파일을 건너야 해 초기 마찰이 오르고 과잉 추상화 유인이 생긴다                                                     | **없음** — `boundaries` 는 **import 방향만** 보고 변형 위치는 문서 규약뿐                                                                             | `.tsx` 안 `mutate(…)` 콜백 **본문 줄 수 래칫** — 실측 최댓값 **23**에서 시작해 내려가기만                                                  |
| **프로덕션 모드 테스트 실행**        | React 가 `yarn test --prod` 를 따로 요구하는 이유: dev 전용 코드(경고·불변식)가 prod 와 갈린다                                        | 갈린 분기가 있으면 유닛이 **한쪽만** 검증한다                                                                               | 테스트 실행이 두 배                                                                                                            | ⓐ **해당 없음으로 판명** — `rg "import.meta.env.(DEV\|PROD\|MODE)" src` → **0건**(분기 자체가 없다). 트랙 A 는 이미 `dist`(프로덕션 빌드) 위에서 돈다 | **최소형은 검사 추가가 아니라 그 0 을 잠그는 것** — 「`src/` 에 dev/prod 분기가 없다」 불변식 한 줄                                        |

⚠ **테스트:본체 비(0.59:1)를 격차로 읽지 마라.** SQLite 는 라이브러리이고 우리는 1인 데스크톱 앱이라
부류가 다르다(오버레이 §0-A 의 표본은 Linear·Vercel·Raycast 다). 여기서 가져오는 것은 **비율이 아니라
종류**다 — 「이상 상황 주입」·「MC/DC」·「퍼징」은 우리가 **한 종류도 안 가진** 축이고, 그중 우리 규모에
값이 있는 것은 **결함 주입 하나**다(위 표 ⓔ).

## 6. 🧹 잔재 — 지울 것

| 무엇                                                                                       | 언제 죽었나                             | 죽었다는 증거                                                                                                                                               | 지우면 깨지는 것                                                            | 복구                                                  |
| ------------------------------------------------------------------------------------------ | --------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | ----------------------------------------------------- |
| `docs.ts:48-53` 의 ✅ 「세입자가 돌아왔다」 문단 + `DOC_KEYS=['ics:feed']`                 | 2026-08-22(I050)                        | `lib/icsFeed.ts` **파일 없음** · `docGet/docSet` 실호출자의 키가 전부 `DOC_KEYS` 밖                                                                         | 없음(배관은 존치)                                                           | → **C042**                                            |
| `GuideView.tsx:190-250` §③ 「수집·발견」 절 + 표 두 행                                     | P10 W4(2026-08-07) · 축 은퇴 2026-08-21 | 부르는 탭 넷이 `TABS` 에 **0건** · `registry.tsx:26` 이 _"은퇴가 아니라 제거"_ · 부모 CLAUDE.md 가 _"발견 크로스워크는 삭제됐다"_                           | 없음                                                                        | `git show <이전>:web/src/features/find/GuideView.tsx` |
| `bootFallbackScreen.ts:11` · `App.tsx:63` 의 텔레메트리·`unhandledrejection` 언급          | 2026-08-22(I052)                        | `rg unhandledrejection web/src` → **주석 1건뿐** · `lib/telemetry.ts` 삭제됨(`7085303`)                                                                     | 없음 — 단 **R2 의 구멍을 이 주석이 가리고 있다**                            | `git show 7085303^:web/src/lib/telemetry.ts`          |
| `AmbientCanvas` 를 가리키는 주석 **5곳**                                                   | 2026-08-22(I045)                        | 컴포넌트 0개 · `ThemeProvider.tsx:58` 이 _"그 캔버스가 은퇴했다(I045)"_ 라 적는데 나머지 넷은 현재형                                                        | 없음                                                                        | 위 커밋                                               |
| `eslint.config.js:250-254` 의 `atlasData.ts` 면제                                          | P10 W4(2026-08-07)                      | 파일 없음 · 같은 파일 `:518` 이 이사를 적는다                                                                                                               | **크기 래칫의 조용한 구멍이 닫힌다**                                        | → **C050**                                            |
| `lib.rs:33-38` — 은퇴한 트레이 doc comment 와 `#[cfg(desktop)]` 이 `mod testkit` 에 붙었다 | 2026-08-22(I049)                        | `/// T-3 — 상주 트레이.` 다음 줄이 `#[cfg(desktop)]` 이고 그 사이 블록 주석을 지나 `#[cfg(test)] mod testkit;` 에 **둘 다** 적용된다(`mod tray;` 만 지웠다) | 데스크톱 빌드에서는 무해 — rustdoc 이 `testkit` 을 「상주 트레이」라 부른다 | 위 커밋                                               |
| `tabs.ts:448` `// 안내(guide) — … fill 없음`                                               | W4(2026-08-07)                          | 수식하던 행이 사라졌고 현 `guide` 행(`:385`)은 `role:'view'` 라 `fill` 필드가 없다                                                                          | 없음                                                                        | —                                                     |
| `quickCapture.ts:218-244` 의 `try/catch`                                                   | 처음부터                                | `try` 안이 `matchDate`·`matchChapter`·`matchSubject` 셋뿐이고 전부 정규식·배열만 쓰는 순수 함수                                                             | **삼키는 것이 0에서 «나중에 던지는 단계»로 바뀐다**                         | —                                                     |
| `components/hud/index.ts` — export 1개짜리 배럴                                            | —                                       | export 1(`HudFrame`) · 소비처 1 · 머리주석이 _"NeonTrack·Readout은 소비처가 사라져 제거됨"_                                                                 | 없음                                                                        | —                                                     |

⚠ **`retrievalStats`·`slowButPassing` 은 지우지 않는다 — 전제 재검증 결과다.** 프로덕션 소비처가 0이고
불변식 ⑭가 **모듈 단위**라 못 보지만, `retrievalLatency.ts:20-23` 이 _"소비처가 아직 없다 … 재는 것을 먼저
달고, 무엇을 그릴지는 표본이 쌓인 뒤에 정한다. 순환 대기를 끊는 것이 이 파일의 목적이다"_ 라고
**의도를 명시**한다. 그 전제의 현재 상태를 쟀다: **실 DB `records` 0행** — 즉 표본이 아직 안 쌓였고,
쓰기 비용도 아직 0이다. **판정: 유지.** 다시 볼 조건은 «`records` 가 100행을 넘겼는데 그리는 화면이
여전히 0» 이다(그때는 §1-A 의 형태가 된다). ⛔ **보관소로 옮기지 마라** — git 이 이력이다.

## 7. 유지 판정 — 도마에 올렸으나 옳았다 (다음 회차의 재작업 차단)

- **`RUNTIME_CACHE_KEYS`·`EPHEMERAL_ONLY_KEYS` 의 `_vaultScan`·`_ankiFile`**(쓰기 0·읽기 0) — 재지적 금지.
  `useRuntime.ts:26` 과 `persistence.ts:29-42` 가 논거를 적었고(_"지우면 볼트 경로가 `settings` 행이 되어
  D1 으로 나간다"_) `dbRows.test.ts:215-224` 가 잠근다. **죽은 코드가 아니라 가드의 타입 짝이다.**
  → 원장 `C028` 의 이 절반은 **기각**.
- **`DbRows` 빈 컨테이너 리터럴 2벌**(`rows.ts:536` ↔ `sqlite.ts:283` · jscpd 14줄) — `DbRows['arrays']` 가
  `Record<ArraySlice, OrdRow[]>` **총체 타입**이라 슬라이스를 더하면 **두 리터럴 다 컴파일 에러**다.
  집행자가 이미 타입이다. 헬퍼 추출은 줄 수 말고 사는 게 없다.
- **`ReviewRun.tsx:456` ↔ `phone/ReviewView.tsx:168`**(jscpd 9줄) — 사본은 **디스패치 3줄뿐**이고 판정은
  `lib/reviewQueue.cursorOp` 하나가 소유한다. C-6 의 «폰은 `lib/` 만 공유하고 화면은 따로» 계약대로다.
- **원장 `C030` 「`lib/` 25개가 소비처 1곳」 — 재측정 결과 결함 아님.** [측정] `lib/` **135모듈 중 프로덕션
  소비처가 정확히 1인 것 35개(25.9%)** — 원장의 「90+ 중 25」는 참이었고 **비율까지 유지된 채 자랐다.**
  대부분은 feature 하나의 순수 규칙을 테스트 가능하게 떼어낸 결과이고 그게 이 저장소의 규율 그 자체다.
  소비처 1 **이면서** 테스트 참조도 0 인 것은 **5개**뿐이고 그중 셋은 `scheduler/index.ts` 배럴로 테스트된다.
- **`OUTBOX_TABLES`** — 크로스언어 로스터의 **옳은 형태**: `server/src/index.ts:28` 이
  `web/src/lib/cloud/contract.ts` 를 **그대로 import** 한다(사본 0). C034 의 처방은 그것이 불가능한
  경계(Rust)에만 필요하다.
- **`server/src` 의 인증·입력검증** — 상수시간 비교 · 서명→만료 순서 · 등록 코드의 원자적 1회 소비
  (`used_at IS NULL` UPDATE) · 무상태 액세스 토큰 + 매 요청 폐기 확인 · https 강제 · CORS 닫힘 기본값 ·
  스트림 계량 413 · 2겹 레이트 리밋. `tbl` 이 `z.enum(TABLE_NAMES)` 라 SQL 인젝션 표면이 닫혀 있다.
  **새로 지적할 것 0건.**
- **리소스 수명** — `setInterval` 11곳 전량 짝 확인. 스캔이 센 차이(`addEventListener` 43↔40 등)는 전부
  **부팅 1회 설치**라 누수가 아니다. **안 걷히는 것 0건.**
- **Rust `unwrap/expect`** — 97건 중 테스트 밖은 `vault.rs:516,524` 둘뿐(`#[cfg(test)]` 경계로 확인).
  그 `Mutex<Option<String>>` 은 파일 밖에서 잠기지 않고 락 구간에 패닉 지점이 없어 poison 이 성립하지 않는다.
- **`lib/` 순수성** — 전역(`localStorage`·`window.`·`document.`)에 닿는 lib 모듈 21/135 인데 실제 접근은
  전부 IO 전담 모듈이고 스케줄러 코어에 **숨은 전역 0**.
- **원장 `C024`(크롬 `action` 단수가 「호스트 복귀」와 「주 동사」를 다툰다) — 재현 실패 → 기각.**
  `action:` 을 세우는 화면 넷 전부가 **주 동사**이고(`ReviewRun`·`Mistakes`·`Stats`·`ForecastView`),
  다투는 화면이 **0개**다. 호스트 안의 뷰인 `ForecastView` 조차 복귀 액션을 요구하지 않는다.
- **원장 `C031`(`tools.rs` 의 `vault-health`/`healthy` 판정이 틀린 값을 낸다) — 오독 → 기각.**
  `tools.rs:8-16` 은 그 판정이 **`serve.js` 의 삭제된 파서**에 있었고 _"실제 도구 출력에 대보니 이미 2개가
  틀린 값을 내고 있었다 … 그래서 옮기지 않는다"_ 라고 **이식하지 않은 이유**로 적은 것이다.
  `tools.rs` 안에 그 로직은 **존재하지 않는다**.
- **원장 `C032`(`AmbientCanvas` 12fps 루프) — 대상 부재 → 기각.** 컴포넌트가 0개다(I045). 남은 주석 5줄은
  §6 잔재. ⚠ 부수 확인: 2026-08-20 회차도 이미 _"컨텍스트 손실 시 루프 중단"_ 을 확인하고 기각했었다.
- `boundaries` 위반 0 · `madge --circular` 0 · `knip` 0 · `gitleaks` 0 — 도구 층에서 이미 0.
- `osv-scanner` 의 Rust 20건(gtk/rsa/rkyv 등)은 **전부 Windows 빌드 그래프 밖**이고 `deny.toml` 의
  `[graph] targets` 가 의도적으로 거른다 — 데이터 축 소관이고 재지적 금지.

## 8. 축 밖 이월 (제목만)

**성능 팩** — 돌려서 재야 아는 것

- `stateToRows` + `toTableData(next)` 의 매 flush 전량 재직렬화(m-5 가 절반만 없앴다)
- `readRows()` 의 행 수 대비 실측 곡선 — C041 의 배수를 시간으로 환산할 유일한 방법
- `plugin-sql` 순차 `execute` 폴백의 실 IPC 왕복 곡선(`coalesceStmts` 이후 실측 없음)
- 폰 워커(wasm+OPFS) 왕복 실지연 — `CALL_TIMEOUT_MS=30s` 가 「밀리초대」를 전제하는데 근거 없음
- `cloud/undo.ts:currentImages` 의 행마다 순차 `selectDb`
- `server/src/index.ts` 의 본문 계량이 정상 요청 본문을 두 번 읽는다

**규약 팩**

- ⭐ **리뷰 산출물이 두 곳에 있다** — 루트 `REVIEW.md`/`review-manifest.md` vs `web/docs/리뷰/<날짜>-<축>/`.
  이것 때문에 **이번 회차의 축 추천이 「코드 축은 이력 0」이라고 틀리게 말했다**(§2)
- TS 문자열 속 **생 hex** 는 아무 집행자도 안 막는다(`check-tokens.mjs` 는 `var(--x)` 만 본다 · 실측 3파일)
- `ThemeProvider.tsx:16` 의 hex 폴백 근거(_"SSR·초기 프레임"_)가 `lib/ledger.ts:51-63` 의
  _"'SSR 안전'은 이 앱에 없는 상황이다 · 폴백을 되살리지 말 것"_ 과 정면 충돌
- `V023` 잔여 셋 판정: `tabs.ts:432`(현 `:465`) **이미 참 — 닫아라**(⌘K·딥링크·버튼 셋 다 실재) ·
  `GlanceMode:41` **거짓**(오버레이 6종 → 실측 7 · 수를 고치지 말고 빼라) ·
  `MiniHud:58` **거짓 양성**(과거시제이고 `git log -S` 로 주석이 `State.tsx:215` 의 `inert` 보다 **먼저**임을 확인)
- `features:lib = 1.38:1`(18,971/13,701) 추세 해석

**데이터 팩**

- 실 DB `docs` 표에 **행 3개**가 남아 있는데 `DOC_KEYS` 는 그 키를 모른다(C042 의 데이터 쪽 절반 · `D023` 계열)
- `_merged` 에코 억제표가 툼스톤에는 안 걸린다(`outbox.ts:241-246`) — 받아온 삭제가 다음 배치에 되올라가는가

**ux 팩**

- `tools.rs` 의 `PYTHON` 환경변수가 설정 UI·README 어디에도 없다
