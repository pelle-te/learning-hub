# 메타 — 코드 축 1회차가 팩에 대해 배운 것 (2026-08-22)

1. **지시가 모호함 (가장 값진 항목)** — 커널 §원장 착지는 _"닫는 것은 `/실행`의 일이지 리뷰의 일이 아니다"_
   라고만 말하고, **이월된 「미검증」 항목을 리뷰가 실물로 반증했을 때** 무엇으로 표시할지 말이 없다.
   열린 채 두면 원장이 거짓말하고(다음 회차가 같은 것을 다시 판다), 닫으면 그 규율을 어긴다.
   → 이번엔 **`기각`(원장이 이미 정의한 상태)** 으로 내리고 근거를 회차 머리말에 남겼다(7건).
   **팩 개선 후보**: 「리뷰는 `열림`→`기각` 만 할 수 있다. `닫힘`은 `/실행` 전용」을 명문화.

2. **팩이 안 시켰는데 해야 했던 것 — 축 추천 근거를 검증하는 일.**
   `/리뷰 §0` 은 `<장부뿌리>/리뷰/` 를 나열해 「가장 오래 안 돈 축」을 고르라고 한다. 그대로 했더니
   **「코드 축은 이력 0」이라는 거짓 근거로 축을 추천**했다 — 실제로는 2026-08-20 에 전면 코드 리뷰가
   돌았고 산출물이 **저장소 루트 `REVIEW.md`** 에 있었다. 회차 중반에 발견해 재지적 필터를 다시 걸었다.
   **팩 개선 후보**(4저장소 공통일 가능성 높음): 축 추천 전에 `git log --diff-filter=A -- '*REVIEW*' '*리뷰*'`
   또는 장부뿌리 **밖**의 리뷰 산출물을 한 번 훑는다.

3. **거짓 양성을 유발한 지시** — 커널 §0단계-3 의 도구 표를 그대로 돌리면 **정당한 것이 대량으로 섞인다**:
   `jscpd` 14클론 중 실제 발견은 3(나머지는 타입이 이미 집행자이거나 계약상 의도된 사본) ·
   `osv-scanner` 20건은 **전부 빌드 그래프 밖**(`deny.toml` 이 의도적으로 거른다).
   팩은 _"도구 출력은 발견이 아니다"_ 라고 이미 경고하지만, **하위축 프롬프트에 목록을 그대로 실으면
   에이전트가 그것을 「할당량」으로 읽는다** — 이번엔 목록마다 «각각이 왜 그런지 설명하거나 빼라»를
   같이 적어 막았다. 그 문장을 팩이 갖는 편이 낫다.

4. **예산** — 축1(실패) 372k토큰/38파일 → 3건(Minor) · 축2(구조) 207k/40 → 6건(Major 4) ·
   축3(표면) 193k/26 → 5건(Major 3) · 축4(성능) 303k/27 → 2건(Major 1) + 이월 1건 기각.
   메인이 별도로 31파일 + 기계 측정 6종. **가장 비싼 축(실패)이 가장 적은 발견을 냈고 Major 는 0**인데,
   그 축이 「통과」로 낸 목록(보안 표면·리소스 수명·부분 실패)이 **실제로 이 저장소에서 가장 단단한 영역**
   이라 그건 축이 죽은 것이 아니라 **그 영역이 이미 두 번 감사된 것**이다(2026-07-20·2026-08-20).
   ⚠ 다음 회차가 실패 축을 또 0건으로 끝내면 그때는 하위축 정의를 의심하라 — 지금은 아니다.

5. **표본 대조 — 했다.** SQLite `testing.html` · Go `CodeReviewComments` · React 기여 가이드를
   **실제로 열어** 수치와 문장을 인용했다(URL·확인일 리포트 §5). `ⓓ=없음` 행 **6개**.
   ⚠ 배운 것: 표본의 원시값(테스트:본체 590:1)을 **격차로 읽으면 안 된다** — 부류가 다르면 비율이 아니라
   **종류**를 가져와야 하고, 그 판단을 팩이 아니라 오버레이 §0-A(표본 = Linear·Vercel·Raycast)가 줬다.
