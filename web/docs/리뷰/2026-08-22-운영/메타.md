# 메타 — 2026-08-22 · 운영 축 (팩 자신을 검증한다)

1. **팩이 시켰는데 못 한 것** — ① `[측정]` 을 끝까지 못 민 자리 하나: CI **과금분**은 잡
   타임스탬프에서 손으로 환산했다(`gh api …/timing` 이 `billable.*.total_ms = 0` 을 일관되게
   돌려주고 청구 API 는 `user` 스코프 부족 404). **`도구 없음`이 아니라 `권한 없음`이고, 팩에 그
   등급이 없다** — `[추측]` 으로 강등해 실었다. ② §잔재의 「실행 흔적 0」 판정을 Cloudflare 쪽에는
   못 걸었다(계정 접근 없음).

2. **팩이 안 시켰는데 해야 했던 것** — ⭐ **「자동 경로를 어떻게 열거하는가」를 팩이 안 준다.**
   §축1 은 _"각 자동 경로마다"_ 라고만 말하는데, 실제 발굴은 `.github/`·훅·`package.json` 밖에
   **네 층이 더** 있었다: `.claude/hooks/`(3개) · 앱 런타임 주기 경로(`setInterval`·`useEffect`·
   Rust `spawn` 전수 grep) · **기한부 원장**(만료가 곧 데드맨) · **관측 표본 임계**(`SAMPLE_MIN` 등
   폐기 조건 5종). 넷째는 이 저장소 고유이지만 앞 셋은 어느 저장소에도 있다 — 임무문에 손으로 적어
   넣었다.

3. **거짓 양성을 유발한 지시** — 브리핑에 내가 «`audit-allowlist.json`(3개)» 라 적었더니 축1 이
   **파일 3개를 항목 3개로 읽을 뻔했다**(실제 항목은 전부 0). 그 에이전트가 스스로 정정했다.
   → 교훈은 팩이 아니라 **내 임무문**의 결함이다: 수를 줄 때 **무엇의 수인지**를 붙여야 한다.
   팩 쪽 거짓 양성은 0건.

4. **예산이 어디로 갔나** — 신선도 ~14분 / 8건 · 관측 ~10분 / 6건 · 비용 ~15분 / 6건 ·
   복구 ~11분 / 8건. **네 하위축 전부 유효**(0건 하위축 없음). 메인은 §0 실측(CI·git·blob)과
   §2 실물 확인에 가장 많이 썼고, **주장 12개를 직접 다시 재서 전부 성립**했다(기각 0).

5. **표본 대조가 됐나** — **됐다.** 4개 출처를 WebFetch 로 실제로 열었고 URL·확인일을 달았다.
   `ⓓ=없음` 행 **3개**(데드맨 · 알림 종료조건 · 자동화 존재증명) — 이 절은 실패하지 않았다.

---

## 라우팅

### → `web/docs/렌즈-오버레이.md` (이 저장소에만 참 · 승인 후 이 회차가 적는다)

- ⚠⚠ **§2 「축별 오버레이」에 운영 절이 없다.** 다섯(코드·성능·ux·데이터·규약)만 있어 이 회차는
  팩 기본값 + §0-A 한 줄로 돌았다. 넣어야 할 것: **자동 경로를 세는 다섯 층**(위 2번) ·
  **📌 이미 0인 것**(CI `continue-on-error` 0 · `|| true` 0 · 조용한 skip 0 — 재발견 금지) ·
  **📌 이미 통과인 것**(Cloudflare 비용 구조 · `updater.rs` 수동 전용 · 볼트 백업/Anki 신선도가
  이 축의 모범이며 다른 경로의 처방은 그것을 베끼면 된다).
- ⭐ **새 렌즈 후보: 「상한은 있는데 하한이 없다」** — §1-A(손으로 적은 목록은 표류한다) ·
  §1-B/§1-D(집행자가 값이 아니라 문법을 막는다)와 같은 층위의 명제다. 이 저장소의 시간 상수는
  거의 전부 «너무 자주 돌지 않게 하는 상한»이고, 그래서 «늦었다»를 판정하는 술어가 없다(실측 4/31).
  ⚠ 이건 운영 축 전용이 아니다 — 임계·래칫·주기가 나오는 모든 축에서 물을 수 있다.
- **§0-B 도구 표에 추가할 것 없음** — 이 회차는 도구를 들여오지 않았다(리뷰는 정지가 규율).
  단 `/실행` 이 `ship-gap.mjs`·번들예산 축 ⑤ 를 세우면 그때 **어디에 걸렸는지와 함께** 적어야 한다.

### → `web/docs/판례.md` (이 저장소가 물린 함정 · 이 회차가 적는다 → 그러나 **`/실행` 이 닫을 때**)

`O034` 가 그것이다. 이 저장소의 운영 사고 넷(gitleaks 도입 이래 0회 · `cloud-server` 13일 상시
빨간불 · 배포가 SPA 폴백 HTML 을 준 것 · e2e 174 skipped)이 **전부 코드 주석에만** 있어서,
이 회차가 재지적 필터를 걸 대장이 없었다. ⛔ 리뷰는 판례를 안 적는다 — 원장 항목으로 넘긴다.

### → `<킷>/개선후보.md` (모든 저장소에 참일 것 같음 · **후보로만** · ⛔ 팩을 직접 고치지 마라)

1. **증거 등급에 `[권한없음]` 이 필요하다.** `[측정]` 을 시도했는데 **도구는 있고 권한이 없어서**
   못 잰 경우가 이 회차에 둘 있었다(GitHub 청구 · Cloudflare 대시보드). 지금 규율대로면 `[추측]` 인데,
   그건 «안 재 봤다»와 «재려 했으나 접근이 막혔다»를 같은 픽셀로 그린다 — 후자는 **사람이 5초에
   확인할 수 있다**는 정보를 담고 있어서 값이 다르다. 최소형: 등급을 늘리지 말고 `[추측]` 옆에
   **«검증 방법»을 «누가 어디서»까지 적으라**는 한 줄로 족할 수도 있다(크기 래칫 고려).
2. **§잔재의 「실행 흔적 0」 판정에 «설정만 있고 한 번도 안 돌았다» 형태가 빠져 있다.** 팩의 세 증거
   (참조 0 · 실행 흔적 0 · 대체됨)는 **한때 살았던 것이 죽는** 경우를 상정한다. Renovate 는
   **태어난 적이 없다** — 참조는 있고(`ci.yml` 이 인용한다) 대체물도 없다. 그런 것은 「지울 것」이
   아니라 **「살릴 것인가 지울 것인가를 판정할 것」**이고, 처방의 모양이 다르다.
   ⚠ 출처: hub 2026-08-22 운영 회차. **n=1 이므로 일화다** — 다른 저장소에서 같은 형태가 나오면 그때 팩으로.
