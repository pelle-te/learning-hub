# 읽음 대장 — 2026-08-22 · 운영 축

**범위**: 전체(`hub` 저장소). 운영 축의 대상 표면은 소스 전량이 아니라 **자동 경로 · 관측 경로 ·
비용을 내는 것 · 복구 절차**다. 아래가 그 확정 목록이고, 이 회차는 **전부**를 상대한다.

**대상 확정 방법**: ① `.github/` 전수 ② `git config core.hooksPath` → `.husky/` 전수
③ `package.json` 셋(root·web·server)의 스크립트 전수 ④ `web/scripts/*.mjs` 전수
⑤ 관측·동기화·백업 lib 전수(grep `observations|telemetry|visits|perf|backup|snapshot|sync`)
⑥ `src-tauri/src/` 중 자동 경로·알림·업데이터·경로 ⑦ `server/` 전량 ⑧ 런북 문서 둘.
⛔ 제외: `node_modules`·`web/dist`·`src-tauri/target`·생성 파일(`artifacts.gen.ts`)·`test-results`.

| 경로                                                 |    줄 | 역할 (운영 관점)                                        | 읽음     |
| ---------------------------------------------------- | ----: | ------------------------------------------------------- | -------- |
| `.github/workflows/ci.yml`                           |   291 | CI 4잡(build-test·secret-scan·cloud-server·shell-build) | 메인     |
| `.github/renovate.json`                              |    74 | 의존성 갱신 자동화 설정(주 1회 · automerge patch+minor) | 메인     |
| `.husky/pre-commit`                                  |    16 | web/ 변경 시 lint-staged·typecheck·test                 | 메인     |
| `package.json`(root)                                 |    27 | tauri 빌드·fmt·clippy·test·deny · SCA                   | 메인     |
| `audit-allowlist.json`(root)                         |     8 | SCA 원장(루트 트리 · 비어 있음)                         | 메인     |
| `web/audit-allowlist.json`                           |     — | SCA 원장(web · 비어 있음)                               | 메인     |
| `server/audit-allowlist.json`                        |     — | SCA 원장(server · 비어 있음)                            | 메인     |
| `src-tauri/tauri.conf.json`                          |    56 | 업데이터 엔드포인트·pubkey · NSIS · CSP                 | 메인     |
| `src-tauri/src/updater.rs`                           |   114 | 업데이트 확인·설치(사용자가 누를 때만)                  | 메인·축4 |
| `src-tauri/src/notify.rs`                            |   177 | OS 알림(무엇이 언제 우는가)                             | 축2      |
| `src-tauri/src/lib.rs`                               |   216 | 커맨드 등록·부팅                                        | 축2·축4  |
| `src-tauri/src/paths.rs`                             |   130 | 데이터 폴더(E2E 격리 포함)                              | 축4      |
| `src-tauri/deny.toml`                                |    88 | Cargo 공급망 게이트 설정                                | 축1      |
| `web/package.json`                                   |   117 | 게이트·e2e·budget 스크립트 정의                         | 메인·축3 |
| `web/scripts/gate.mjs`                               |   204 | 로컬 원커맨드 게이트(`ALL` 배열이 정본)                 | 축1·축3  |
| `web/scripts/audit-gate.mjs`                         |   219 | SCA — 만료·사문화를 실패로 만드는 기한부 원장           | 축1      |
| `web/scripts/release-verify.mjs`                     |   149 | 릴리스 산출물 검증                                      | 축4      |
| `web/scripts/stage-release-assets.mjs`               |    83 | 배포 직전 인스톨러를 dist/updates 로                    | 축4      |
| `web/scripts/bundle-budget.mjs`                      |   327 | 번들 예산 4축                                           | 축3      |
| `web/scripts/compiler-ratchet.mjs`                   |   115 | React Compiler 래칫                                     | 축1      |
| `web/scripts/report-debt.mjs`                        |   152 | 부채 추세(강제 없음)                                    | 축1      |
| `web/src/lib/observations.ts`                        |   156 | 관측 원장(표 단위)                                      | 축2      |
| `web/src/lib/observationsQuery.ts`                   |   110 | 관측 조회                                               | 축2      |
| `web/src/lib/visits.ts`                              |   310 | 방문 집계                                               | 축2      |
| `web/src/lib/perf.ts`                                |   100 | 성능 관측                                               | 축2      |
| `web/src/lib/backup.ts`                              |    45 | 백업                                                    | 축4      |
| `web/src/lib/snapshots.ts`                           |   129 | 스냅샷                                                  | 축4      |
| `web/src/lib/sync.ts` · `syncLedger.ts`              | 63·95 | 동기화 상태·원장                                        | 축2·축4  |
| `web/src/lib/cloud/client.ts`                        |   395 | Workers 클라이언트(요청 빈도의 근원)                    | 축3·축4  |
| `web/src/lib/cloud/outbox.ts`                        |   278 | 아웃박스(중간에 죽으면 재개?)                           | 축4      |
| `web/src/lib/cloud/run.ts`                           |   257 | 동기화 실행(조용한 포기 경로?)                          | 축2·축4  |
| `web/src/lib/db/write.ts`                            |   402 | 정본 쓰기 + 되읽기 대조                                 | 축2·축4  |
| `web/src/lib/db/boot.ts`                             |   236 | 부팅 읽기 + 1회 이관                                    | 축4      |
| `web/src/lib/bootFallbackScreen.ts`                  |    79 | 번들 밖 부팅 폴백                                       | 축2      |
| `web/src/features/integrations/TelemetryConsole.tsx` |   266 | 텔레메트리 화면                                         | 축2      |
| `web/src/features/settings/UpdateCard.tsx`           |   124 | 업데이트 확인 UI                                        | 축4      |
| `web/src/features/settings/VisitLedger.tsx`          |   300 | 방문 원장 화면                                          | 축2      |
| `server/package.json`                                |    39 | predeploy 훅(배포 전 판정)                              | 축4      |
| `server/wrangler.jsonc`                              |   116 | Workers·D1 바인딩 · observability 여부                  | 축2·축3  |
| `server/src/index.ts`                                |   889 | Worker 본체(5xx 가 어디에 남나)                         | 축2      |
| `server/src/auth.ts`                                 |   133 | 토큰                                                    | 축2      |
| `server/src/pull.ts`                                 |    72 | 페이지네이션(요청 수의 근원)                            | 축3      |
| `web/docs/릴리스.md`                                 |   186 | 배포·서명 런북                                          | 축4      |
| `web/docs/cloudflare-런북.md`                        |   760 | 호스트 실행 런북                                        | 축4      |
| `web/e2e/a11y.spec.ts`                               |     — | `알려진위반` 기한부 원장(데드맨의 한 형태)              | 축1      |

**합계 대상 ≈ 7,500줄 + CI 이력 40런.**

## 도구 산출 (원문은 `스캔/`)

| 무엇                       | 도구                                         | 산출                                      | 커버리지            |
| -------------------------- | -------------------------------------------- | ----------------------------------------- | ------------------- |
| CI 실행 이력 40런          | `gh run list --json`                         | `스캔/ci-runs.json`                       | GitHub Actions 전수 |
| 마지막 실패 런 로그        | `gh run view --log-failed`                   | `스캔/ci-32493848802-failed.log`(3,713줄) | 실패 잡 2개 전문    |
| 저자 분포                  | `git log --format='%an'`                     | 본문                                      | 503 커밋 전수       |
| 원격 브랜치·PR·이슈·릴리스 | `gh pr/issue/release list` · `git ls-remote` | 본문                                      | 전수                |
| 액션 최신 버전             | `gh api repos/*/releases/latest`             | 본문                                      | 핀 고정된 4종       |

⚠ **이 도구들이 원리적으로 못 보는 것**: ① GitHub **알림 설정**(워크플로 파일에도 API 에도 없다 —
계정 설정이다) ② Cloudflare 대시보드의 실제 요청 수·청구액(계정 인증 필요) ③ 사용자의 **실제 실행
빈도**(앱을 며칠에 한 번 여는가) ④ `src-tauri/.updater-key` 의 백업 존재 여부. 넷 다 사람만 답할 수 있다.
