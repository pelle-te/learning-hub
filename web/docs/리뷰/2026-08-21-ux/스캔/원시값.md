# 원시값 스캔 — 2026-08-21 · ux

메인이 직접 측정. 명령은 각 절에 적는다.

## 표본 (실제로 내려받아 셌다)

| 표본   | URL                                                          | 확인일     | CSS 크기  |
| ------ | ------------------------------------------------------------ | ---------- | --------- |
| Linear | https://linear.app/ (static.linear.app `_next/static/css/*`) | 2026-08-21 | 248,474 B |
| Vercel | https://vercel.com/ (`_next/static/immutable/chunks/*.css`)  | 2026-08-21 | 953,390 B |

## 반픽셀 font-size — 표본 0 / 우리 5

```
grep -ohE 'font-size:\s*[0-9]+\.[0-9]+px' <표본>.css | sort -u | wc -l
```

|                     | 반픽셀 font-size                                                                                      |
| ------------------- | ----------------------------------------------------------------------------------------------------- |
| Linear (248KB)      | **0**                                                                                                 |
| Vercel (953KB)      | **0**                                                                                                 |
| hub `--fs-*` 사다리 | **5** (`--fs-micro-plus:9.5` `--fs-caption:10.5` `--fs-note:11.5` `--fs-hint:12.5` `--fs-label:13.5`) |

⚠ hub `tokens.css:32` 자신이 «`--fs-*` : 정수 타이포 스케일(**반픽셀 금지**)» 이라 선언한다.

## 반경 사다리 — 이름이 무엇을 가리키나

|        | 반경 토큰 이름                                                                                                                                                            |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Linear | `--radius-4 --radius-6 --radius-8 --radius-12 --radius-16 --radius-24 --radius-32 --radius-circle --radius-rounded` — **값으로 이름 짓는다**                              |
| hub    | `--r-sm/--r-md/--r/--r-lg` + `--radius-{swatch,cell,drag,chip,blk,seg,chip-sm,chip-md,detail-x,detail-btn,rail,rail-chip,empty-glyph,palette}` — **호출부로 이름 짓는다** |

hub 실제 반경 값: 2·3·4·5·6·7·8·9·10·12·14·16·9999 = **13종**.

## 타이포 사다리

|        | 칸                                                                                                 |
| ------ | -------------------------------------------------------------------------------------------------- |
| Linear | `--text-{tiny,micro,mini,small,regular,large}-size` + `--title-{1,2,3}-size` = **9칸 · 의미 이름** |
| hub    | `--fs-*` 절대 px **25종** + em 3 + clamp 5 · 브리지 `--text-*` **50개**(다수가 호출부 이름)        |

## Tailwind 기본값 누수 (dist 실측)

브리지에 없어 Tailwind 기본이 실린 것 — 앱의 나머지가 px 인데 **이 셋만 rem**:

```
--text-3xl:1.875rem  --text-4xl:2.25rem  --text-base:1rem  --radius-xs:.125rem
```

소비처: `app/MiniHud.tsx:37`(4xl) · `features/degree/PhaseBoard.tsx:65`(3xl) ·
`features/integrations/TelemetryConsole.tsx:80`(3xl) · `text-base` 21곳 · `rounded-xs` 23곳.

## 잘 되어 있는 축 (파지 않는다)

- 모션: `--dur-fast/--dur/--dur-slow/--dur-cele/--draw/--tempo-*/--stagger` + ease 4종. 집행자 = `test/invariants.test.ts` 불변식 ⑥.
- 간격 임의값: 전 저장소 **1건**(`min-w-[2.5em]`).
- 생 hex: `stylelint` `color-no-hex` 가 막는다. 남은 것은 대부분 주석·JS 폴백.

## 레일 넘침 (실물 측정 · dev 서버 + Playwright)

| 뷰포트                                                                             | 레일 scrollHeight | clientHeight | 넘침     | 잘리는 항목                                   |
| ---------------------------------------------------------------------------------- | ----------------- | ------------ | -------- | --------------------------------------------- |
| 1366×768                                                                           | 805               | 768          | 37px     | `접기` 버튼                                   |
| **1280×720**(= `devices['Desktop Chrome']` = **전 시각 베이스라인 88장의 뷰포트**) | 805               | 720          | **85px** | **`연동 현황`(destination 탭) 전체** + `접기` |

`[재현]` `--fs-label` 13.5→14 · `--fs-caption` 10.5→11 로 올려도 레일은 **+3px**(805→808)이고
가로 생략(`text-ellipsis`) 발생 **0건** — 즉 반픽셀 예외의 근거인 «칩이 통째로 잘린다»는
레일에서는 성립하지 않는다.

---

⚠ **`스캔/all-css.txt`(표본 원본 220KB)는 커밋하지 않았다**(2026-08-21 `/실행`). 그 파일은
Linear·Vercel 이 배포한 **남의 CSS 를 그대로 받은 것**이라 이 저장소가 들고 있을 물건이 아니고,
이 회차가 실제로 인용한 것은 위 표(파생 수치)다. 다시 필요하면 그날의 절차를 반복한다 —
`curl` 로 두 사이트의 스타일시트를 받아 같은 정규식으로 센다(방법은 `메타.md` §5).
⚠ 그리고 그 대조에서 **총수는 비교 불가**였다는 것이 이 회차의 판례다(서드파티 CSS 가 섞인다) —
비교 가능했던 것은 **토큰 「정의」층의 이름 짓는 법**이었다.
