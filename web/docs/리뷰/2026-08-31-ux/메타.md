# 메타 — 2026-08-31 · ux 축 (팩 자신의 검증 · 다섯 줄)

1. **팩이 시켰는데 못 한 것** — 「대상 파일 전량」. 181파일 중 55만 열었다(`[미열람]` 126).
   사유는 `도구 없음`도 `해당 없음`도 아닌 **규모**다(ux 표면 40,630줄). 커버리지 절에 수로 적었고
   지적은 읽은 파일에만 냈다. ⚠ 팩은 「전량」을 요구하지만 **한 회차에 전량이 불가능한 축**에 대해
   무엇을 하라는 지시가 없다 — 구역을 회차 간에 나누는 규약이 없다(→ 킷 후보).
2. **팩이 안 시켰는데 해야 했던 것** — ⓐ **검증망 자신을 리뷰 대상에 넣은 것.** ux 팩의 하위축 넷은
   전부 «화면»을 말하는데 이번 회차 발견의 3분의 1이 `e2e/` 안에 있었다(로스터가 화면 계약이다).
   ⓑ **실패의 성격을 두 갈래로 가른 것**(베이스라인 미갱신 ↔ 갱신으로 안 사라지는 것). 안 갈랐으면
   ~30건을 결함으로 오보했다. ⓒ **은퇴 후 데이터 상태를 재현하는 별도 하네스**를 판 것 —
   저장소의 `boot()` 가 이미 죽은 아티팩트를 먹여서, 그대로 찍으면 «발생할 수 없는 화면»을 본다.
3. **거짓 양성을 유발한 지시** — ⓐ 「죽은 토큰을 정의 파일 기준으로 세라」. 그대로 하면 **275건**이
   나오는데 실질은 **0**이다(Tailwind v4 `@theme` 은 `--color-x` 를 `bg-x` 로, `--container-x` 를
   `max-w-x` 로 쓴다). 네임스페이스 역산을 붙여야 참이 된다 → 오버레이에 기록.
   ⓑ ⭐⭐ **팩이 아니라 내가 만든 거짓 양성 — 이 회차 최대의 교훈.** 게이트를 하위축 넷이 도는
   동안 돌려 **170 failed** 를 봤고, 그 수를 설명하려고 «레일 개편이 `#main` 폭을 바꿔 베이스라인이
   낡았다»는 **그럴듯한 서사**를 세웠다. 조용해진 뒤 재니 **6 failed / 102 passed** 였고 시각 표본은
   **통과**했다 — 서사도 수도 둘 다 틀렸다. 커널이 _"그럴듯한 서사는 변화가 아니다"_ 라고 못박은
   것이 **리뷰 자신의 측정에도** 적용된다는 것을 실측으로 배웠다. 팩엔 그 지시가 「전제 재검증」
   절에만 있고 **자기 계측**에는 없다 → 킷 후보.
4. **예산이 어디로 갔나** — 하위축 넷 병렬 각 ~~17분(도구 60~~101회). 발견: 흐름 6 · 상태 11 ·
   시각언어 11 · a11y 7 · **메인 직독 5**(`U043` 크래시 · `U045` 명령 실재 · `U049` 초록 오탐 ·
   `U051` `ready` 부재 · `masteryColor` 사문화). 0건 하위축 없음.
   ⚠ 메인의 **실물 확인**(스크린샷 9×2 + e2e 3회 + 대비 검산)이 이 회차 증거의 등급을 올렸다 —
   하위축 넷은 브라우저 금지였으므로 그들의 산출은 대부분 `[읽음]` 이었다.
5. **표본 대조가 됐나** — 됐다. Linear(CSS 54본)·Vercel(4)·Raycast(5)를 실제로 받아 같은 정규식으로
   셌고, W3C SC 2.4.11 · Primer primitives · Storybook · Vale · Playwright a11y 를 열었다.
   `ⓓ=없음` **3행**(토큰 이름 · 최근 본 것 · 상태 카탈로그 · SC 2.4.11 포함 4).

---

## 라우팅

- **이 저장소에만 참** → `렌즈-오버레이.md` **§2 「ux」 › 「이 축이 재는 법 — 세 줄」 · ✅ 사용자 승인 후 기록 완료**
  ⓐ 「죽은 토큰」은 Tailwind v4 네임스페이스 역산 없이는 275건의 거짓 양성을 낸다(재사용 스크립트
  `리뷰/2026-08-31-ux/스캔/_dead.mjs` 위치 명시).
  ⓑ **ux 축의 표면에 `e2e/` 로스터가 포함된다** — 화면 계약이 거기 산다(이번 발견의 3분의 1).
  ⓒ 「실패의 세 갈래」 — 워킹트리에 시각 작업이 떠 있으면 ①베이스라인 미갱신 ②갱신으로 안 사라지는 것
  ③**갱신이 거짓을 정답으로 굳힐 자리**로 가른다. **그 판정을 서사로 하지 말고 케이스 둘을 다시 돌려라**
  (실측 2.6초 · 가설보다 싸다).
- **이 저장소가 물린 것** → `판례.md`(`/실행` 이 닫으며 append)
  ⓐ **회차가 `verify` 만 돌리고 완료를 보고했다**(두 회차 연속). `verify` 는 화면을 안 띄운다.
  ⓑ **`ready` 가드에 `heading level 1` 을 쓰면 TopBar 워드마크에 걸려 「초록으로 다른 화면을 검사」한다.**
  ⓒ 게이트를 **서브에이전트가 도는 동안** 돌리면 flaky 를 결함으로 읽는다 — 이번 회차가 실제로
  그렇게 시작했고(CLAUDE.md 가 이미 경고한 것), 조용해진 뒤 다시 쟀다.
- **모든 저장소에 참일 것 같음** → `<킷>/개선후보.md` 에 **후보로만 append 완료**(2건 · 각 `n=1`)
  ⓐ **「전량이 불가능한 축」의 회차 간 구역 분할 규약이 없다.** 팩은 「샘플링 금지 + 전량」을 동시에
  요구하는데 큰 표면에서는 둘이 함께 성립하지 않아 회차가 조용히 표본으로 떨어진다(실측 55/181).
  ⓑ _**「그럴듯한 서사는 변화가 아니다」가 리뷰 자신의 계측에는 안 걸려 있다.**_ 그 문장은 §전제
  재검증에만 있고, 회차가 **자기가 만든 오염된 수**를 설명할 때는 아무 규율이 없다(이번에 물렸다).
  ⭐ pipeline 2026-08-29 의 「인용할 수치는 네가 다시 재라」와 **같은 병의 다른 얼굴**일 수 있다 —
  합칠 수 있으면 그 순간 **n=2** 다(개선후보 문서에 그 판정 요청을 함께 적었다).
  ⓒ 기록하지 않은 관찰: **하위축 에이전트에게 브라우저를 금지한 대가**가 증거 등급으로 청구된다
  (그들의 산출은 대부분 `[읽음]` 이었고, 이 회차의 `[재현]` 은 전부 메인이 만들었다). 규약대로
  작동한 것이라 후보로 올리지 않았다.
  ⛔ **팩은 직접 고치지 않았다**(4저장소 공유 · n=1 은 일화다).
