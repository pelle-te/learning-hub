# 읽음 대장 — `/리뷰 성능` 2026-08-27 (기준선 회차)

⚠ **이 축의 「대상」은 파일 집합이 아니라 「잰 것 + 그 수를 설명하려고 연 파일」이다.**
성능 팩의 단일 규율이 _"측정 없는 지적은 존재하지 않는다"_ 이므로, 커버리지의 단위도
**동선·지표**가 먼저고 파일은 그 뒤다. 아래 ①이 본체, ②는 그 수를 귀속시키려고 연 파일이다.

## ① 잰 것 (동선 × 계측기)

| 동선                                               | 계측기                                | 실물인가                                          | 원문                                                |
| -------------------------------------------------- | ------------------------------------- | ------------------------------------------------- | --------------------------------------------------- |
| 데스크톱 콜드 부팅(빈 상태)                        | chrome-devtools MCP · 1440×900        | **프록시**(Chrome + `vite preview` · SQLite 없음) | 트레이스는 폐기(아래 §비보존)                       |
| 데스크톱 콜드 부팅(시드 base/real/x10)             | 같음 + `PerformanceObserver`          | **프록시**                                        | —                                                   |
| 데스크톱 탭 전환 13탭 × 콜드/웜/웜2                | 같음                                  | **프록시**                                        | `스캔/tabswitch-real.json` · `tabswitch-x10.json`   |
| **실 셸 부팅(WebView2 + 실 SQLite)**               | 트랙 B 하네스(CDP) · 격리 데이터 폴더 | **실물**                                          | `스캔/shell-probe.json`                             |
| **실 셸 IPC 왕복 4종 × 20~50회**                   | 같음                                  | **실물**                                          | 같음                                                |
| **실 셸 부팅 웨이브 가드 6회**                     | `e2e-shell/shell.spec.ts` 실행        | **실물**                                          | 본문 F1                                             |
| 트랙 A 부팅 웨이브 가드 3회                        | `e2e/smoke.spec.ts` 실행              | 프록시                                            | 본문 F1                                             |
| **폰 콜드 로드(캐시 무효 · Fast 4G · 390×844)**    | chrome-devtools MCP                   | **프록시**(Workers 오리진이 아니라 preview)       | 본문 F2                                             |
| 번들 예산 5축                                      | `npm run budget`                      | 실물 산출물                                       | `스캔/budget.txt`                                   |
| 빌드 시간·플러그인 귀속                            | `npm run build`                       | 실물                                              | `스캔/build.txt`                                    |
| 첫 화면 글리프 수요 → 서브셋 슬라이스              | 정적 분석 + CDN `HEAD`                | 실물 산출물                                       | `스캔/subset-need.json` · `need-firstscreen-*.json` |
| 축1 하위축 전량(INP·CLS·CPU 4x·모션)               | Playwright(:4174)                     | 프록시                                            | `스캔/축1-*`                                        |
| 축2 하위축 전량(번들 귀속·서버 왕복·컴파일러 비용) | 벤치·roundtrip 하네스                 | 혼합                                              | `스캔/축2-*`                                        |
| 축3 하위축 전량(예산 우회로 실증)                  | `npm run budget` 조작→원복            | 실물                                              | `스캔/축3-*`                                        |

## ② 연 파일 (수를 귀속시키려고)

| 경로                                            | 줄       | 이 회차에서 무엇을 봤나                                                            |
| ----------------------------------------------- | -------- | ---------------------------------------------------------------------------------- |
| `web/scripts/bundle-budget.mjs`                 | 374      | 예산 5축의 **계량 단위**(gz · `dist/assets` · `.js/.css/.wasm`) — F2·F3·F4 의 자리 |
| `web/scripts/gate.mjs`                          | 215      | `budget` 이 `mode:'full'`, `e2e:shell` 이 `mode:'cargo'` — F1 이 왜 안 보이나      |
| `.github/workflows/ci.yml`                      | 466      | `build-test` 의 `checkout`(depth 기본) · 281행 «트랙 B 는 여기서 안 돌린다»        |
| `web/src/lib/perf.ts`                           | 100      | 부팅 웨이브 3마크의 계약 · `bootWave()` 가 음수를 0 으로 접는다                    |
| `web/e2e/smoke.spec.ts`                         | 132      | 트랙 A 의 50ms 임계와 그 근거 주석("0ms 대 · 5배 이격")                            |
| `web/e2e-shell/shell.spec.ts`                   | 495      | 트랙 B 의 같은 임계 — 여기서 깨진다(F1)                                            |
| `web/e2e/phone.spec.ts`                         | 159      | 5,000ms 는 임계가 아니라 **행 감지기**(P026 의 관측 지점)                          |
| `web/features/registry.tsx`                     | 129      | `warmTab`·`WARM_CAP_MS` — F1 의 대상 메커니즘                                      |
| `web/src/main.tsx`                              | 177      | `warmTab(pathname)` 호출 자리 · SD-7 부팅 순서                                     |
| `web/src/app/App.tsx`                           | 542      | `TabReady`(`firstData` 마크) · `app` 마크                                          |
| `web/src/lib/db/sqlite.ts`                      | 430      | `getDb` 메모이즈·실패 재시도 — 브라우저 스텁의 6회 호출이 **인공물**임을 확정      |
| `web/src/lib/tauri.ts`                          | 806      | `dbUrl`·`dbVersionGuard` 폴백 경로                                                 |
| `web/index.html` · `web/phone.html`             | 24 · 40  | **양쪽 다 폰트 `rel=preload`** — F2                                                |
| `web/src/styles/tokens.css`                     | 927      | `@font-face`(`font-display: swap` · 단일 파일)                                     |
| `web/vite.config.ts`                            | 273      | 엔트리 둘 · 매니페스트 · PWA                                                       |
| `web/e2e/_fixtures.ts`                          | 917      | `SEED`(**1,219 B · 과목 2 · 챕터 3**) · `boot` 의 invoke 스텁 계약                 |
| `web/e2e-shell/shellApp.ts`                     | 169      | 트랙 B 기동·격리(`LEARNING_HUB_E2E_DATA_DIR`)                                      |
| `web/playwright.config.ts` · `.shell.config.ts` | 113 · 39 | 4173 포트·떠도는 preview 청소(O038)                                                |
| `src-tauri/src/artifact.rs`                     | 207      | 산출물 5종의 실경로 → 실 크기(446 KB)                                              |

**[미열람]** — 이 축이 잴 이유를 못 찾아 안 연 것:
`web/src/features/**` 의 개별 화면 구현(80파일 · 탭 전환 수치가 임계 아래라 귀속 불필요) ·
`server/src/**`(축2 가 왕복으로 잼) · `src-tauri/src/*.rs` 의 나머지(IPC 왕복이 p50 1.1~3.3ms 로
설명됨) · `web/src/phone/**`(폰은 네트워크 축이 지배해 코드 축까지 안 갔다).
⚠ **읽지 않은 파일에 대해 지적하지 않았다.**

## §비보존 — 원시 트레이스를 안 남겼다 (의도)

`trace-cold-empty.json`(3.6 MB) · `trace-cold-real.json`(17.9 MB) 를 **지웠다.**
근거: 이 회차 자신의 발견(F5)이 **저장소 이력이 657/700 MiB(94%) · 잔여 43 MiB** 이고
증가의 86%가 e2e 스냅샷이라는 것이다. 21.5 MB 의 원시 트레이스를 커밋하면 **잔여의 절반**을
그 자리에서 쓴다. 재생성은 싸다(각 1분):
`chrome-devtools MCP performance_start_trace(reload:true, filePath:…)` — 시드 주입 절차는
`스캔/_mkseed.mjs` 와 본문 §부록에 남겼다.
