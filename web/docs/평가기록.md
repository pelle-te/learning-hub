# 평가 기록 (추세 누적)

> `/감사 <대상> 점수`(옛 `/평가`)·web-evaluator의 채점 결과를 여기 누적한다. **추세가 핵심**(절대값보다 델타). 루브릭은 `평가루브릭.md`.
> 형식: 날짜 · 범위 · 커밋 · 분기([feature]/[lib]) · 차원별 점수(N/A 명시) · 총점(%) · 약한 차원 · 사람 교차검증 여부.
> ⚠ 표 바로 뒤 요약은 **빈 줄로 분리한 불릿**으로(안 그러면 prettier가 표 행으로 흡수).

## 기록 형식 (복사해서 채움)

```
### YYYY-MM-DD · <대상> · [feature|lib] · <커밋 SHA>
| 차원 | 점수 | 증거(파일:라인·동작) |
|---|---|---|
| 1 정확성·견고성 | 4 | … |
| … | … | … |

- 합계: 25/30 (성능 N/A) = 83% · 약한 차원: a11y(3)
- 사람 교차검증: ☐ 미실시 / ☑ 실시 → (발견)
- 후속: (약한 차원 보강 → 게이트/린트 회귀로 승격)
```

---

## 누적 기록

<!-- 최신이 위로. -->

### 2026-07-18 · 계획 탭 전체(plan-host+schedule+alloc+items) · [feature] · 16dace1

| 차원            | 점수 | 증거(파일:라인·동작)                                                                               |
| --------------- | ---- | -------------------------------------------------------------------------------------------------- |
| 1 정확성·견고성 | 2    | 프로브 3건 재현: 첫 주 복사→그 주 new 블록 1→0 전멸(weekAlloc.ts:69+scheduler.ts:497 `if(alloc)`)  |
| 2 UX·사상       | 4    | 세그먼트별 단일질문 성립·EmptyState 1급. 착지가 영속 schedView 의존(PlanHost.tsx:11)               |
| 3 a11y          | 3    | 키보드대안·트랩·reduced-motion 5점급, `role="grid"` 계약 미이행(AllocBoard.tsx:159)·aria-current 0 |
| 4 DS 충실       | 3    | 하드코딩색 schedule 15 / alloc 0 / items 0. 라이트 스냅샷서 `#0b0d12` 이탈 실측                    |
| 5 경계·단순     | 3    | eslint boundaries exit0(위반 0). 주네비 3중 축자복제·spanOf 중복                                   |
| 6 성능          | 3    | budget PASS 382.5/500KB. Schedule()·Items() 컴파일러 bail 실측(진단 로거)                          |
| 7 테스트        | 3    | 653테스트 통과·lib Func 100%, UI Func 6~23%·동작 e2e 0건(커버리지 json 실측)                       |

- 합계: **21/35 (N/A 0) = 60%** · 약한 차원: 정확성(2)
- 사람 교차검증: ☐ 미실시
- 관통 진단: **순수 로직 100% 검증 / UI 배선 0% 검증** — 차원 1의 재현 결함 3건이 653개 테스트를 뚫고 생존한 구조적 원인.
- 세그먼트 편차: alloc·items 건강, **schedule이 D4·D5·D6·D7을 동시에 견인**(DayPlanner.tsx 874줄이 진앙).
- 후속: **보강 완료(2026-07-18, 같은 날)** — 아래 전건 적용 후 게이트 녹색(verify·build·budget 386.1/500·e2e 65/65).
  - **D1**: `weekAlloc[wk]={}` 계약을 `isWeekManaged` 단일 술어로 통일(scheduler `if(alloc)` → 비어있지 않을 때만 managed) · `copyPrevWeekAlloc`이 소스 비면 no-op+복사수 반환 · `removeSidFromAlloc`/`removeSidFromDayPlans`를 과목 삭제에 배선(고아 배분·블록·completion 키 청소) · `colSumMin(map,wd,validSids)` 고아 방어 · 리드아웃 분자/분모 집합 일치(200% 오염 종료) · `ddayInfo`로 D-0→D-DAY.
  - **D2**: "오늘 계획 짜기" 3개 진입로가 `setSchedView('day')`+`?ds=오늘`로 결정론 착지(일반 진입은 결정로그 v4 "주 뷰 착지" 존중) · 배분 보드 fill 프레임(`minmax(48px,1fr)` 행 신축) · 일 뷰 nav 빈 띠(`nowrap`+라벨 접기) · DayPlanner 빈 상태 5분기 진단(거짓 "완료 🎉" 제거) · 리드아웃 문법 통일.
  - **D3**: `role="grid"`(계약 미이행)→`role="table"`+`role="row"` · `button role="columnheader"` 오버라이드 제거 · `presentation`+onClick 3건 해소 · TimedCard Enter/Space(무효 `aria-pressed` 제거, 진짜 버튼) · `aria-current="date"` 4곳 · 죽은 `aria-label`에 role 부여.
  - **D4**: `#0b0d12`/`#fff` → `var(--panel)`/`var(--txt)`(분기 0건, 양 테마 AA 최저 5.36:1, 다크 채움 차이 ≤1.98/255) · 세션색 3종 → `--acc2`/`--warn`/`--violet` · `#b794f6` 저장 리터럴 제거(타입 플래그+토큰 파생). **선언부 하드코딩 색 계획 탭 전체 0건.**
  - **D5**: `useWeekOffset` 훅 추출(주 네비 3중 복제 해소+startDate 리베이스 버그) · `timeSpan` lib 이주(spanOf 중복 해소, 차등 23k케이스 일치) · `todayISO` 단일 출처 통일 · `weeklyItems`/`weekBudgetMin`/`weekAllocTotalMin`/`isUnschedulable` 술어 단일화 · `DOW_MON` 통일.
  - **D6**: `Schedule()` 컴파일러 bail 해소(`CompileError` → `CompileSuccess`, `_c(88)` 메모 슬롯 생성) · pointermove 커밋에 값 변화 가드(드래그당 수백 → 격자 통과 횟수).
  - **D7**: 690 테스트(+37) · **동작 e2e 3종 신설**(`e2e/plan.spec.ts` — 셀 입력→승격→새로고침 지속 / ⤵·Alt+↓ 배치 / `?ds=` 드릴다운, repeat-each=3 무flaky) · Functions 커버리지 55.73%→**57.22%**.
- 부수 발견(문서 갱신 필요): `persistence.ts:245 migrate()`가 `_today` 시드를 부팅 시 삭제 → localStorage 시드가 **무음 실패**한다(e2e는 `page.clock.setFixedTime` 사용). 프로토콜 문서의 "`_today` 고정" 안내가 낡음.
- ⚠ **보강이 회귀를 둘 냈다(6차 피드백에서 사용자가 잡음, 같은 날 되돌림)** — 배분 보드 행 신축·일 뷰 EmptyState 화면 대체. 둘 다 **감사 지적을 사용자 제약보다 위에 둔 것**이 원인(절대규칙 4 위반). 교훈: 루브릭 점수는 *진단*이지 목표함수가 아니다 — "빈 공간이 있다"가 곧 "채워야 한다"는 아니며, 이산 항목(매트릭스 행)과 연속량(시간축)은 신축 판단이 반대다. 상세·후속 수정은 `결정로그.md` 「계획 탭 v6」.

### 2026-07-09 · lib/retrieval · [lib] · c79cf76

| 차원           | 점수 | 증거                                                                        |
| -------------- | ---- | --------------------------------------------------------------------------- |
| B1 정확성      | 5    | retrieval.test.ts 5개 실행 통과·minAge 경계·결정성·null 방어                |
| B2 순수성·격리 | 5    | import=dayDiff+type뿐·Date.now/Math.random 0·완전 순수                      |
| B3 견고성      | 3    | 손상 날짜→dayDiff NaN→`NaN<minAge` false→전 요약 통과+ageDays NaN 무음 반환 |
| B4 테스트 커버 | 5    | export 2/2·4종 케이스(정상·경계·빈값·회귀)                                  |
| B5 단순        | 5    | 52줄·표면 최소·명료                                                         |

- 합계: 23/25 (N/A 0) = **92%** · 약한 차원: 견고성(3)
- 사람 교차검증: ☐ 미실시
- 발견 결함: 손상 날짜 입력이 NaN ageDays로 무음 통과(경계검증 부재). cross-day 회전 미테스트(B4 경미).
- → 보강 적용(2026-07-09): candidates에 `Number.isFinite(age)` 가드(retrieval.ts) → 무음통과 차단. retrieval.test에 NaN가드·cross-day 회전 회귀 2건 → **게이트 승격**.

### 2026-07-09 · features/stats · [feature] · c79cf76

| 차원            | 점수 | 증거(파일:라인·동작)                                               |
| --------------- | ---- | ------------------------------------------------------------------ |
| 1 정확성·견고성 | 4    | 테스트 22개 통과·빈상태 3중 가드                                   |
| 2 UX·사상       | 4    | 4컬럼 fill·리드아웃 3종·DetailDrawer 온디맨드·EmptyState 1급       |
| 3 a11y          | 4    | 마크마다 role=img+aria-label·드로어 포커스트랩·reduced-motion 이중 |
| 4 DS 충실       | 4    | CSS 하드코딩 hex 0·전역클래스 0(grep 실측)                         |
| 5 경계·단순     | 3    | boundaries eslint exit0·로직 lib 분리                              |
| 6 성능          | 4    | Stats 청크 10.37KB gzip(예산 내 실측)                              |
| 7 테스트        | 2    | phase4 스모크 1건뿐·드로어 차트 전량 미마운트                      |

- 합계: 25/35 (N/A 0) = **71%** · 약한 차원: 테스트(2) ≪ 경계·단순(3)
- 사람 교차검증: ☐ 미실시(모델 판단 — 추세 진단용)
- 발견 결함: ① compRate(605) 도메인 불일치 의심 ② 인출/추세 파생 3중복(표류 위험) ③ 히트맵/주별막대 키보드 도달 불가 ④ TSX 인라인 hex 폴백 9건
- → 보강 적용(2026-07-09):
  - ① **compRate = 버그 아님**(사람 검증): schedH=총 계획시간(planned일×dailyMin)이라 doneH÷totalSchedH=실제/총계획(100% 캡)은 방어 가능한 설계 → **미변경·재플래그 금지**(모델 판단을 사람이 정정한 사례).
  - ② 중복로직 → methodology `recallEvidence`/`cbmsTrendGlyph` dedup(RetrievalCard·메인·리드아웃 SSOT, 표류 위험 제거).
  - ③ WeeklyBars tabIndex+dead hex 폴백 토큰화. 히트맵 126셀 tabIndex는 탭스톱 과다로 **의도적 제외**(SR은 role=img+aria-label 접근). firing 폴백 `var(--ok/--muted,#hex)`는 스냅샷 영향이라 보류·플래그.
  - ④ 컴포넌트 테스트(statsTab: 빈상태·드로어 마운트) + recallEvidence/cbmsTrendGlyph 단위 → **게이트 승격**(테스트 2→~4 예상). 전 게이트 녹색.
