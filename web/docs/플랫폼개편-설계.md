# 플랫폼 개편 설계 v2 — Tauri 2 셸 + SQLite 데이터 모델

> 트리거: 사용자 요청(2026-07-19) — "근본적인 개발 도구·프레임워크·구현 메커니즘·설계 철학을 보고 더 좋은 방법이 있는데 안 쓰고 한 게 아닌지. 처음부터 갈아엎어도 된다" → 다각도 감사 후 "최고의 효율과 고점만 된다면 무엇을 써도 상관없다" → "전부 개편할 생각으로 계획서 다시 보고 꼼꼼하게. **기존에 있는 거랑 충돌하면 안 되고 일관성 있게.**"
>
> **v2 (2026-07-19)** — 문서·코드 4방향 전수 감사 반영. v1 의 **결함 3건을 정정**했다(§9). 특히 v1 은 (a) 1단계 완료 기준이 순환 논증이었고 (b) 데이터 영구 유실 경로를 못 봤으며 (c) "1단계 = 기능 변화 0" 전제가 성립하지 않았다.
>
> 이 문서는 *설계*다. 큰 결정은 `결정로그.md` 에 승격하고, 진행 상태는 태스크 목록이 소유한다. **수치는 전부 실측**이며 출처를 파일:줄로 남긴다.

---

## 0. 요약

구조적 결함이 두 뿌리에서 나온다 — **데스크톱 워크로드를 브라우저 샌드박스에서 돌리고 있고, 앱 데이터가 JSON 덩어리 하나다.** 프레임워크(React 19 · Vite · zustand)와 도메인 로직(`features/` 27,219줄 · `lib/scheduler/`)은 손대지 않고 **그 아래 플랫폼·데이터 모델·스타일 레이어만** 교체한다.

```
               지금                          목표                        단계
셸      브라우저 + .bat 2프로세스   →  Tauri 2 단일 바이너리            1
프런트  React19 · Vite · zustand   →  그대로 (무변경)                  —
스타일  CSS Modules 54개 + ds      →  Tailwind v4                      6
데이터  localStorage JSON 블롭 1개  →  SQLite 테이블                    2
볼트    메인스레드 풀스캔·수동버튼   →  Rust 증분 스캔 + notify 감시     3
백엔드  serve.js 816줄(무테스트)    →  Rust 커맨드 + Python sidecar     4
모바일  없음                       →  읽기 전용 뷰(방식 미결정)         5
```

---

## 1. 진단 (실측 2026-07-19)

### 1-1. 유지할 자산

React Compiler 실동작(`memo_cache_sentinel` 앱 청크 400여 개) · strict TS(`noUncheckedIndexedAccess`, `any` 0 · `@ts-ignore` 0) · `eslint-plugin-boundaries` error 강제 · zustand 전체구독 0건 · 23탭 코드 스플리팅 · `prefers-reduced-motion` 28파일 · `invariants.test.ts` Proxy 불변식 · 752 테스트.

### 1-2. 갭 8종과 뿌리

| #   | 갭                                                       | 뿌리        | 해소       |
| --- | -------------------------------------------------------- | ----------- | ---------- |
| ①   | Web Worker 0건 — 볼트 풀스캔·증분 없음·취소 불가         | 샌드박스    | 3단계      |
| ②   | localStorage 5MB 에 append-only 로그, `persist()` 미호출 | 데이터 모델 | 0-E, 2단계 |
| ③   | 아티팩트 8종 `as T` 캐스팅                               | 독립        | 0-B ✅     |
| ④   | 리서치 잡 인메모리 — 재시작 유실                         | 백엔드      | 4단계      |
| ⑤   | 잡 진행 = 폴링 + stdout 20KB 전량 재전송                 | 백엔드      | 4단계      |
| ⑥   | `jsx-a11y` 부재 / CSS 예산 부재                          | 독립        | 0-C ✅     |
| ⑦   | serve.js 816줄 테스트 0                                  | 백엔드      | 0-A        |
| ⑧   | 의존성 2 메이저 정체                                     | 독립        | 0-D ✅     |

### 1-3. 성장 관리 갭

400줄 초과 features 파일 **12개**(최대 `TodaySignature` 791) · 미사용 export **43개**(knip) · 인지복잡도 지적 **34건** · ReDoS 정규식 **2건**(sonarjs) · `features:lib` = **3.3:1**. → 0-F.

---

## 2. 비목표 (N)

- **N1. `features/` 27,219줄 재작성 금지.** 스케줄러 엔진·간격반복·일편집기 좌표·학위요건에 수년치 디버깅이 박혀 있고 주석이 실제 회귀를 기록한다. **갈아엎기가 고점을 내리는 유일한 경로.**
- **N2. 프런트 프레임워크 교체 금지.** React 19 + Vite + zustand. Next.js/RSC·Redux/Jotai 는 순손실(§10). **CSS 레이어만 예외** — 사용자 지시로 6단계 Tailwind 채택.
- **N3. Python 파이프라인 재작성 금지.** `pipeline/` 은 별도 repo 이고 Python 이 맞다. Tauri sidecar 로 계속 spawn.
- **N4. 다중 사용자·인증은 범위 밖.** ~~`127.0.0.1` 바인딩이 전제~~ → **v2 정정**: 5단계가 LAN 읽기 전용을 위해 `0.0.0.0` + 토큰을 요구하므로 N4 를 "**다중 사용자·계정 체계는 범위 밖. LAN 단일 사용자 읽기 전용은 5단계 예외로 허용**"으로 축소한다(v1 내부 모순 정정).

---

## 3. 불변식 (I) — 전 단계 공통

- **I1. 백업 호환.** `exportSnapshot` JSON 은 기존 백업을 읽을 수 있고, 내보낸 것은 기존 앱이 읽을 수 있어야 한다. 2단계의 최상위 제약. (`아키텍처.md:64` "persist 2계층 계약… 불변으로 유지"와 동일 — 그 문장을 승계한다.)
- **I2. 레이어 단방향.** `boundaries` error 유지. **Tauri `invoke` 호출은 `lib/` 가 소유**한다(React 무관 IO 의 정의) — `features/` 에 직접 박히는 것이 1·3·4단계의 가장 흔한 위반 경로다.
- **I3. 스케줄러 산출 동형.** `scheduler.test` 42케이스는 전 단계에서 그대로 통과.
- **I4. 게이트 녹색.** `verify` + `e2e` + `budget`(+ 1단계부터 `tauri build`). CI 동반 갱신.
- **I5. 규칙 #4 (명시 지시 보존).** `결정로그` 의 픽셀 결정(고정 행 높이·바탕 틴트 제거·44px 하한·칩 캡·"+N개 더")은 플랫폼 이행이 뒤집지 않는다.
- **I6 (신설). 픽셀 동일 계약.** 스타일 이행(6단계)은 "값은 이전과 픽셀 동일"을 계약으로 한다. — 이전 CSS Modules 전환(`결정로그.md:24`)이 이미 쓴 계약이므로 승계한다.

---

## 4. 이행 계획

원칙: **각 단계가 독립 릴리스 가능하고, 어디서 멈춰도 동작하는 앱이 남는다.** 빅뱅 금지.

### 0단계 — 플랫폼 무관 선행

|         | 내용                                            | 상태                                 |
| ------- | ----------------------------------------------- | ------------------------------------ |
| **0-A** | serve.js 회귀 테스트 (4단계 동등성 명세)        | ✅ `web/test/serve.test.ts` 26케이스 |
| **0-B** | zod 4 + 아티팩트 경계 파싱 + tasks 방어         | ✅ `5912e72`                         |
| **0-C** | a11y 린트 · CSS 예산 · Renovate                 | ✅ `86a527a`                         |
| **0-D** | 의존성 메이저(vite 8 · vitest 4 · boundaries 7) | ✅ `0bb023b`·`55fee36`               |
| **0-E** | **저장소 내구성 + 백업 범위 정정**              | ✅ **1단계 차단 해제**               |
| **0-F** | 품질 게이트(knip · sonarjs 튜닝 · max-lines)    | ✅ verify에 편입                     |
| **0-G** | 과목 색 파생 키 → **`item.id` 해시**            | ✅ 결정·구현(2026-07-19)             |

**0-A 상세.** 주석에만 있는 방어를 테스트로 잠근다: `readBody` UTF-8 청크 경계 · **1MB 초과 413 선전송**(로드맵 L-13) · 경로 traversal 403(형제 폴더 탈출 포함) · `Object.hasOwn` 프로토타입 가드 · 동시성 캡 증감/해제 멱등 · 화이트리스트 밖 도구/아티팩트 거부 · dash 접두 인자 거부 · **stdout 40KB→20KB 캡**(로드맵 L-12) · `killTree` 손자 정리 · SPA 폴백/캐시 헤더. **이 테스트가 곧 Rust 포팅의 동등성 명세다.**

**0-E 상세 — 1단계의 하드 차단 전제(v2 승격).**
`navigator.storage` API 를 한 번도 호출하지 않는다(`persist()`·`estimate()` 0회) → 저장소가 **best-effort 등급**이라 축출 가능.
① 부팅 시 `storage.persist()` ② `estimate()` 80% 사전 경고 ③ **`exportSnapshot` 범위 정정** — `atlas.notes`·`atlas.stars`(직접 쓴 진로 메모)·`lh:research-history`·`lh_ui_v1` 이 **어떤 백업에도 안 들어간다**.

> ⚠ **v1 은 이걸 "Tauri 후 무의미해질 보험"으로 적었다. 틀렸다.** §4-1단계 C5 참조 — Tauri WebView2 는 Chrome 과 **별개 저장소 오리진**이라 기존 데이터를 자동으로 못 읽는다. 이행은 반드시 **수동 export → import** 이고, ③이 완료되기 전에 1단계로 넘어가면 **아틀라스 메모가 영구 유실**된다.

**0-E 구현 결과 (2026-07-19 · 게이트 녹색).**

- ①② `lib/durability.ts`(`ensureDurableStorage` = `persisted()`→`persist()` 승격 + `estimate()` 병렬) + `app/StorageGuard.tsx`(부팅 1회). **승격 실패는 조용히 넘긴다** — 사용자가 할 수 있는 일이 없고(브라우저 참여도 휴리스틱) 매 부팅 경고는 무시를 학습시킨다. 행동이 필요한 **쿼터 80% 초과만** 토스트(내보내기 액션 동봉). `BootRecovery` 와 분리한 이유: 저기는 "부팅이 기본값으로 떨어졌을 때만" 도는 계약이라 정상 부팅에서 즉시 return 한다.
- ③ `lib/sidecars.ts` — 4개 키를 `_local` 사이드카로. **`_reads` 와 같은 관용구, 별개 층**(저기는 IDB 미러·전용 스키마를 가진 데이터 레이어, 여기는 평문 KV 값 통짜 보존). I1 호환: `_local` 은 **추가 키**라 구 앱이 무시하고, 구 백업엔 없어 `importLocalExtras` 가 no-op.
- **가져오기는 화이트리스트**(`LOCAL_EXTRA_KEYS`) + `Object.hasOwn`. 백업 파일은 신뢰 불가 입력이라 임의 키 쓰기를 허용하면 파일 하나로 `study_planner_v3` 를 덮을 수 있었다.
- ⚠ **설계에 없던 필수 발견 — 복원 후 되읽기.** 4개 키의 소비자(`Atlas` useState · `Control` useState · `useUI`)는 전부 **부팅 시 1회 read** 라, KV 만 복원하면 낡은 메모리가 다음 편집의 `writeJSON`/`flush` 에서 **방금 되살린 메모를 통째로 덮는다**(복원이 데이터 유실로 뒤집힌다). → `SyncMsg` 에 `kind:'local'` 추가, `Atlas`·`Control` 구독 + `useUI.reloadUI()` 명시 호출.
- 부수: 내보내기 페이로드를 `backupPayload()` SSOT 로 수렴 — 파일 내보내기와 볼트 백업이 각자 조립하던 탓에 사이드카가 한쪽에만 붙을 수 있었다.
- 테스트: `test/sidecars.test.ts`(왕복·화이트리스트·프로토타입·구 백업 no-op·쿼터 실패) · `test/durability.test.ts`(미지원·재요청 안 함·reject 흡수·quota 0).

**0-F 상세.** knip(오탐 제외: `lint-staged`=husky · `stylelint-config-standard`=stylelint 설정 · `schema.ts` 20개 스키마=동일 파일 `z.infer` 타입 출처 · **`e2e/`**) · sonarjs 튜닝(`cognitive-complexity` 래칫 + `super-linear-regex` **만**; `no-nested-conditional` 130 · `no-nested-assignment` 20 · `void-use` 18 · `no-nested-template-literals` 13 은 **끈다** — 노이즈 180이 신호 34를 묻는 게 도구 도입의 주 실패 모드) · `max-lines` 래칫. `features:lib` 비율은 **강제 없이 리포트 출력만**.

> ⚠ **max-lines 래칫은 6단계에서 반드시 깨진다** — Tailwind 는 CSS 를 JSX 로 옮겨 줄 수·줄 길이를 늘린다. 재기준선 시점을 §6에 명시.

**0-A 구현 결과 (2026-07-19).** `web/test/serve.test.ts` 26케이스. serve.js 말미를 `require.main === module` 로 감싸 **import 해도 리슨하지 않게** 하고 내부를 `module.exports` 로 열었다(테스트 표면 = 포팅해야 할 계약 목록).

- 잠근 것: readBody UTF-8 **1바이트씩** 흘려도 U+FFFD 0개 · 1MB 초과 **413 선전송 + 콜백 미호출**(L-13) · 상한이 UTF-16 길이가 아닌 **바이트** 기준 · `Object.hasOwn` 프로토타입 가드(runTool·artifact 양쪽) · TOOLS 11종과 `pipeline/_도구/*.py` 형태 · dash 접두 인자 거부 · publicJob 20KB 캡 + `proc` 미노출(L-12) · **killTree 손자 정리(실제 프로세스 트리로 검증)** · traversal 403(형제 폴더 탈출 포함) · SPA 폴백 · `/assets/` immutable vs index.html no-cache · gzip 협상 · Host 위조 403 · 비-로컬 Origin 403.
- 부수 리팩터: 라우터에 인라인돼 있어 테스트가 닿지 않던 인자 정제를 `toolExtraArgs(body)` 순수 함수로 분리.
- ⚠ **미커버 1건 — 동시성 캡 증감/해제 멱등.** `RUNNING`/`OLLAMA_RUNNING`/`EMBED_RUNNING` 이 라우터 클로저 안에 있어 단위 테스트가 닿지 않고, 실제 429를 만들려면 python·Ollama 를 실제로 점유해야 한다. 억지로 추출하면 3개 캡 사이트를 동시에 건드리는데 그 회귀를 잡을 테스트가 없다(테스트 없는 코드를 테스트 위해 고치다 깨뜨리는 자리). **4단계에서 Rust로 옮길 때 캡을 명시적 세마포어 타입으로 만들고 그때 테스트한다.**

**0-F 구현 결과 (2026-07-19).**

- **knip** — `knip.jsonc`(주석으로 예외 사유 기록 · stylelint.config.js 규약 승계). 미사용 파일·런타임 export·의존성 **0건**으로 깨끗했고 그 상태를 게이트로 잠갔다. `types` 규칙만 끔(schema.ts의 `z.infer` 타입 SSOT 규약 + 런타임 비용 0).
- **sonarjs** — `configs.recommended`(217규칙)를 **쓰지 않고** 규칙 2개만 켰다. 실측 249건 중 **181건이 스타일 취향**(no-nested-conditional 130 · no-nested-assignment 20 · void-use 18 · no-nested-template-literals 13)이라 신호(cognitive-complexity 34 · super-linear-regex 2)를 묻는다. v2가 예측한 수치가 실측과 정확히 일치했다.
- **super-linear-regex 2건은 실제로 고쳤다** — `vault.ts` 프론트매터(`\s*\n`의 `\s`가 개행을 포함해 뒤의 lazy `[\s\S]*?`와 겹침 → `[ \t]*\r?\n`로 분리, 덤으로 CRLF 볼트 인식) · `quickCapture.ts` 챕터 인식(무한 수량자 → `\d{1,4}` 상한).
- **래칫 2종** — `cognitive-complexity` 77(현재 최댓값), `max-lines` 730(주석·빈 줄 제외 최댓값). 둘 다 **내려가기만 한다**. 주석을 줄 수에서 뺀 이유: 이 저장소는 "왜"를 주석으로 남기는 게 규약인데 세면 규약을 지킬수록 게이트가 조여지는 역인센티브가 된다. `lib/atlasData.ts`(779줄 시드 데이터)는 예외.
- ⚠ **v2 수치 정정**: 설계가 적은 "TodaySignature 인지복잡도 1위(18)"는 틀렸다 — **실측 77**이다(2위 ArticlePractice 75 · vault.ts 51). 래칫 임계 근거가 바뀌므로 기록한다.
- `npm run report:debt` — features:lib 비율·레이어 규모·큰 파일을 **강제 없이** 출력(현재 features:lib = 2.12:1).

**0-G 결정 — 과목 색 파생 키 = `item.id` 해시 (사용자 결정 2026-07-19 · 구현 완료).**
원칙("색은 저장값이 아니라 파생물", `utils.ts:23-25`)은 옳고 유지한다. **키가 배열 인덱스인 것이 문제**다 — 삭제·재정렬 시 뒤따르는 모든 과목 색이 밀리고(`Items.tsx:194-195` 가 이미 인정, 즉시 재유도로 덮음), 9개째부터 색이 정확히 반복된다(팔레트 8색). 파생물인데 `it.color` 로 저장해 파생 로직이 4곳(`refineItemColors`·`makeItem`·`recolorAll`·`moveItem`).
**제안**: 키를 `item.id` 해시로 → 삭제·재정렬 불변, PALETTE 한 줄 교체=전탭 반영 유지, `color` 저장 불필요(2단계 스키마 단순화), 파생 4곳→1곳. 절대규칙 #3 문구를 메커니즘이 아닌 **원칙**으로 재서술.
**구현**: `lib/utils.ts` `colorForId(id)`(FNV-1a 32비트) 단일 파생점. `refineItemColors`·`makeItem`이 이걸 부르고, `moveItem`의 재정렬 보정은 **삭제**(순서가 색을 안 바꾸므로 보정할 게 없다), `recolorAll`은 의미가 바뀌어 "저장된 낡은 색을 현재 PALETTE로 다시 맞추는" 복구 버튼이 됐다(옛 팔레트 백업 가져오기 대비). `makeItem(itemCount, partial)` → `makeItem(partial)` 로 시그니처 축소(호출 8곳). 테스트 `web/test/itemColor.test.ts` 13케이스.

- ⚠ **id를 partial로 넘기는 호출부가 있다**(Items.tsx가 시트를 바로 열려고 id를 미리 만든다) → 색은 **spread 병합 뒤 최종 id**로 계산해야 저장 색과 부팅 재유도 색이 일치한다. 테스트로 잠갔다.

**감수한 트레이드오프 2종 (결정 시 인지됨)**

1. 목록의 **순서 있는 색 그라데이션 상실** — 팔레트 주석 ⑦⑧이 의도했던 것.
2. **색 중복** — 8색 해시라 과목 5개면 두 과목이 같은 색일 확률이 약 80%(생일 문제). 인덱스 방식은 8개까지 중복이 없었다. 8칸에서 "완전 불변 + 무충돌"은 **원리적으로 불가능**하다(충돌 회피가 다른 과목의 존재에 의존 → 삭제 시 색이 바뀌어 불변성이 깨진다). 실사용에서 거슬리면 **정공법은 PALETTE를 늘리는 것**(색을 늘리면 충돌 확률이 급감). `itemColor.test.ts`가 이 한계를 테스트로 문서화한다.

> ⚠ **부수 발견 — 시각 게이트가 색 회귀를 못 잡는다.** 0-G로 **과목 5개의 색이 전부 바뀌었는데 e2e 스냅샷 59장이 전부 통과**했다. `playwright.config.ts:29` 의 `maxDiffPixelRatio: 0.02` 와, PALETTE가 의도적으로 **단일 녹색 계열**이라는 점이 겹친 결과다(색 칩·세그가 전체 픽셀의 2% 미만). 임계를 낮추면 안티에일리어싱 노이즈로 flaky 해지므로 건드리지 않되, **6단계는 색 충실도를 스냅샷으로 검증할 수 없다**는 뜻이다 → §6단계 "조용히 깨지는 것" 목록의 4번째 항목으로 승격(아래).

---

### 1단계 — Tauri 셸 이사

**목표**: Tauri 2 셸이 현 `web/dist` 를 로드. `serve.js` 는 sidecar 로 유지.

**차단 전제(모두 충족 전 착수 금지)**

1. ✅ **0-E ③ 완료** — 백업 범위 정정(`_local` 사이드카). 미완이었다면 데이터 영구 유실.
2. **사용자 데이터 수동 이관 플로우 확정** — WebView2 ↔ Chrome 오리진 분리로 **자동 이관 경로가 물리적으로 없다**. 기존 앱에서 전체 내보내기 → Tauri 첫 실행 시 가져오기. 릴리스 노트 필수 항목.
3. **Rust 툴체인** — `rustc`/`cargo` 1.97.1 ✅ 설치됨. MSVC 빌드도구는 첫 `tauri build` 에서 확인.

**v2 신설 작업 — `WORK` 경로 결정 전략** ⚠
`serve.js:32-33` 이 `WORK = path.dirname(__dirname)` = `D:\atelier` 로 워크스페이스를 **실행 위치에서 추론**한다. 파이썬 도구 11종이 전부 이 cwd 로 돌고(`serve.js:157,203`), 아티팩트 6종 경로도 여기 기준(`serve.js:71-76`)이다.
Tauri 앱은 설치 경로에 놓이므로 **1단계에서 이미 이 추론이 깨진다**(sidecar serve.js 의 `__dirname` 이 설치 경로가 됨).
→ **워크스페이스 경로를 설정값으로 승격**: 최초 실행 시 폴더 선택 → 저장 · 기존 사용자 자동 추론 후 확인 · 폴더 이동 시 복구 UX. `gen-artifacts.mjs:34-37` 의 하드코딩 경로와 **한 곳으로 통합**할 기회.

> **따라서 "1단계 = 기능 변화 0"은 성립하지 않는다.** v1 의 이 표현을 **"프런트 회귀 0 + 셸/경로 설정 신규"**로 정정한다.

**v2 정정 — 완료 기준(순환 논증 제거)**
v1 은 "e2e 스냅샷이 100% 동일 동작을 증명한다"고 썼다. **성립하지 않는다** — Playwright 는 Chromium 으로 `vite preview`(`playwright.config.ts:31-36`)를 찍는데 Tauri 렌더러는 **WebView2** 다. 스냅샷 59장이 전부 녹색이어도 WebView2 에서 깨져 있을 수 있고, "무효화되는 도구로 무효화되지 않았음을 증명"하는 순환이다.
→ **e2e 2트랙 분리**:

- **트랙 A (기존 유지)** — Playwright + `vite preview`. 같은 `dist` 를 로드하므로 **프런트 회귀 검증에 계속 유효**. 비주얼 회귀 전용.
- **트랙 B (신설)** — `tauri-driver`(WebDriver, Windows 는 Edge WebDriver 경유 · **WebdriverIO 스택**) 로 셸 스모크 소수. 창 뜸 · 라우팅 · IPC 왕복 · 종료 시 flush.

**완료 기준**: 트랙 A 59장 무변경 · 트랙 B 스모크 통과 · `tauri build` 성공 · 수동 이관 플로우 1회 왕복 검증.

**스캐폴딩 완료 (2026-07-19) — 실행 검증은 미완**

- `src-tauri/` 신설: `Cargo.toml`(crate `learning-hub`) · `tauri.conf.json`(identifier `dev.jin.learninghub` · 1440×900 · `frontendDist=../web/dist` · `serve.js` 를 bundle resource 로) · `capabilities/default.json`(`core:default` + `dialog:allow-open` **만**).
- `src/workspace.rs` — **WORK 경로 승격**. 저장(`app_config_dir/workspace.json`) → 없으면 실행 위치에서 위로 올라가며 표지(`knowledge/`+`pipeline/`) 탐색 → 그래도 없으면 사용자 선택. `set_workspace` 는 **표지 없는 폴더를 거부**한다(엉뚱한 폴더를 저장하면 파이썬 도구 11종이 조용히 빈 결과를 내는 게 가장 진단하기 어려운 실패라서).
- `src/sidecar.rs` — `node serve.js` spawn(+`PYTHONIOENCODING`·`CREATE_NO_WINDOW`), TCP 헬스체크 대기, 창 파괴 시 정리. **`.bat` 4기능 흡수**: stale 서버 킬 → `single-instance` · 헬스체크 폴링 → `wait_until_ready` · `chcp 65001` 짝 → spawn env · Chrome `--app=` → WebView2.
- **`serve.js:33` 변경** — `WORK = process.env.LH_WORKSPACE || path.dirname(ROOT)`. 환경변수가 없으면 기존 추론 그대로라 **`node serve.js` 단독 실행·기존 `.bat` 경로의 동작은 동일**하다(회귀 0).
- 프런트 경계: `web/src/lib/tauri.ts` 가 **유일한 `invoke` 통로**(I2). `isTauri()` 로 브라우저를 1급 상태로 다뤄 dev·serve.js 실행 경로가 안 죽는다. 소비처는 `features/settings/WorkspaceCard.tsx`(Tauri 아니면 `null` 렌더).
- 게이트: `gate.mjs` 에 `tauri:check` 추가(**cargo 없으면 건너뜀** — web 만 만지는 작업에 Rust 툴체인을 요구하면 게이트가 진입장벽이 된다). 루트 스크립트 `tauri:dev|build|check|fmt|clippy`.
- 검증된 것: `cargo check` ✅ · `clippy -D warnings` ✅ · `cargo fmt` ✅ · web 게이트 5단계 ✅(verify·build·budget·e2e·tauri:check).
- ⚠ **아직 안 된 것(= 1단계 완료 기준의 나머지)**: `tauri build` 미실행 · **앱을 띄워본 적 없음**(WebView2 렌더·sidecar 실제 spawn·창 종료 정리 전부 미검증) · 트랙 B(`tauri-driver`) 미구축 · 수동 이관 플로우 미검증 · 아이콘이 Tauri 기본값.

> knip 이 `lib/tauri.ts` 를 "미사용 파일"로 즉시 잡아냈다(0-F 가 도입 당일 값을 했다) — 경계만 만들고 소비처를 안 붙이면 게이트가 막는다.

**부수 작업**: `러닝허브_실행.bat` 의 4기능 흡수 — 포트 8000 stale 서버 강제종료(`:11-17`) → `tauri-plugin-single-instance` · 조건부 재빌드(`:20-21`) → dev 모드 · 헬스체크 폴링(`:40-45`) → sidecar 준비 대기 · Chrome `--app=` 실행(`:48-57`) → WebView2 가 대체. `chcp 65001`/`PYTHONIOENCODING` 짝은 Rust spawn 에 승계.

---

### 2단계 — SQLite 데이터 모델 (최대 레버)

`AppState` 35슬라이스 → SQLite(`tauri-plugin-sql`). **I1(export/import JSON 계약) 유지**, 내부 저장만 교체. 양방향 검증 구간(JSON 경로와 SQL 경로 동시 수행 후 대조) 뒤 JSON 경로 제거.

**동시 해소**: 5MB 절벽 · 편집당 전량 재직렬화 · `selectors.ts` 수동 캐시 3종 · `migrate`/`sanitizeImported` 이중 유지 · 벡터 캐시 전량 재직렬화(`semantic.ts`).
**신규 획득**: FTS5 노트 검색 · sqlite-vec 벡터 인덱스.

**v2 신설 — 동기/비동기 계약 파괴** ⚠ 가장 위험
`persistence.ts:389` `storage.setItem` 은 **동기**인데 Rust `invoke` 는 **비동기**다. 그리고 `useApp.ts:136-141` 의 언로드 안전망이 `pagehide`/`visibilitychange` **동기 flush** 에 걸려 있는데, **Tauri 창 닫기에서 `pagehide` 발화가 보장되지 않는다**. 둘이 겹치면 400ms 디바운스 대기 중 편집이 유실된다.
→ `onCloseRequested` 훅 + 비동기 flush 완료 대기로 대체. **1단계에서 이미 필요**(셸이 바뀌는 시점).

**v2 신설 — 이관 표면 전수**
`localStorage` 직접: `kv.ts:19`(SSOT) · `shell/actions.ts:34,49,67,79,89,146,158` · **`AnkiPanel.tsx:32,97`(kv SSOT 우회 — 유일한 직접 접근점, 놓치기 쉬움)**.
KV 경유: `persistence.ts:369,377,389` · `useApp.ts:48-49` · `localStore.ts:11,21` · `uiState.ts:66,81,87,100-103` · `focusState.ts:41,54,55` · `reads.ts:80,88,93,105`.
IDB: `idb.ts` 전체 + 소비 10파일.

**v2 결정 필요 — IDB 미러의 처지**
`idb.ts:3-5` 가 존재 이유를 "**사이트 데이터 삭제**로 localStorage 전소 시 복구"라 명시한다. Tauri 에선 그 위협 자체가 소멸 → 미러 층 전체가 죽은 코드가 된다. 그런데 `BootRecovery.tsx` 전체 · `Settings.tsx:417,455`(복구 UI) · `palette.ts:144`(커맨드) · `actions.ts:180-200`(세대 백업)이 이 위협모델 위에 서 있다. **삭제 vs 유지 결정 필요.**

**부수**: `scheduler/priority.ts:16` 의 "localStorage 5MB 쿼터" 전제 주석 무효 · e2e 시딩 6곳(`visual.spec.ts:393-396,456-460,480-484,540-544` · `plan.spec.ts:72-77`, 키 `study_planner_v3`·`lh_ui_v1`)과 `plan.spec.ts:120` 의 영속 직접 검증 재작성.

---

### 3단계 — 볼트 Rust 이전 + 파일 감시

`lib/vault.ts` 메인스레드 풀스캔 → Rust `walkdir`+`rayon` + mtime 증분 + `notify` 감시.
**정정 포함**: 폴백 스캔이 깊이 2단 고정이라 **챕터 하위 폴더 노트가 조용히 누락**되던 결함(`vault.ts:116-148` vs 인덱스 경로의 임의 깊이 처리 — 두 경로가 서로 다른 볼트 구조를 지원하며 미문서화).

**v2 신설 — FSA 소비 표면 전수**: `fsAccess.ts:21,24-25,34,45,56,70` 정의 → `vault.ts`(11곳) · `anki.ts:79-80,98,100,104` · `knowledge.ts:92` · `VaultPanel.tsx`(6곳) · `AnkiPanel.tsx:14,54,59` · `VaultImport.tsx`(4곳) · 핸들 영속 `idb.ts:68-108`.
**모델 변경**: `FileSystemDirectoryHandle` 영속 → **경로 문자열** 저장. TanStack Query 키 `['vaultHandle']` 이 3파일에서 핸들 객체를 공유하므로 **동시 수정**. `queryVaultPermission`/`requestVaultPermission`(`vault.ts:181,185`)에 대응 개념이 Tauri 에 없으므로 **권한 UI 분기 제거**.

---

### 4단계 — serve.js 해체

0-A 테스트를 동등성 기준으로 파이썬 spawn·잡 모델·동시성 캡을 Rust 커맨드로. Ollama 는 `plugin-http` 직접. 잡 상태는 SQLite(④). `serve.js` 삭제.

**v2 정정 — `usePing` 은 폐기가 아니라 재정의** ⚠
v1 은 서버 소멸로 폴백 분기가 죽은 코드가 된다고 암묵 가정했다. **틀렸다.** 서버가 사라져도 ① Ollama 프로세스가 안 떠 있으면 AI 경로는 여전히 실패 ② 파이썬 산출물 "미생성" 상태는 그대로 ③ sidecar spawn 실패·취소·타임아웃 동일.
→ **`usePing` → `useCapabilities`(Ollama up? python up? vault 연결됨?)로 의미 재정의.** 없으면 `online` 이 상수 `true` 로 접히며 **10개 탭의 에러 UI가 통째로 무력화**된다(`TodaySignature`·`Control`·`Ledger`·`TelemetryConsole`·`Integrations`·`Atlas`·`Reads`·`Markets`·`Mastery`·`Review`).
**문구 교체**: `node serve.js로 켜면…` 하드코딩 4곳(`Markets.tsx:192`·`ArticlePractice.tsx:319`·`Goals.tsx:68`·`ArtifactGate.tsx:37`)이 전부 거짓이 된다.

**v2 신설 — 브라우저 API 정리**

- `anki.ts:42` — `fetch('http://localhost:8765')` AnkiConnect 직통. **Tauri origin `tauri://localhost` 는 AnkiConnect CORS 화이트리스트에 없어 깨진다.** `plugin-http` 경유 또는 AnkiConnect 설정 변경.
- `actions.ts:36-44` `download()` — `<a download>` 는 Tauri WebView 에서 무반응/위치 지정 불가. `save` 다이얼로그 + `fs.writeFile` 로. **내보내기 6경로 전부가 이 한 함수에 수렴**하므로 수정은 1곳.
- `useFocus.ts:47-48` · `FocusChip.tsx:52,54` `Notification` → `@tauri-apps/plugin-notification`(WebView 기본으로는 조용히 실패 가능).
- `OnlineStatus.tsx` — Tauri 에선 항상 online → **제거**.
- VitePWA 플러그인 + manifest 제거(단 5단계 후보ⓐ 채택 시 모바일 뷰 한정 부활 → **은퇴 시점은 5단계 결정에 종속**).
- BroadcastChannel(`sync.ts` · `useApp.ts:121,148-154`) — Tauri 단일 윈도우에서 **구독자 0** → `announce` no-op, `onSync` 미발화. 크래시는 없으나 `test/appSync.test.ts`·`test/sync.test.ts` 의 의미가 사라진다 → 삭제/유지 결정 필요.
- `api.ts:64` `AbortSignal.timeout(185000)` 은 "서버 캡 180s" 와 커플링 → Rust 캡과 재동기화.
- `Control.tsx:269` "새로고침해도 서버에서 계속 돌아가요" — Tauri 에선 앱 프로세스가 곧 잡 소유자라 모델·문구 재검토.

---

### 5단계 — LAN 읽기 전용 모바일 뷰

4단계의 Rust HTTP 확장. 폰이 PC 의 SQLite 를 직접 읽으므로 **정본 하나, 동기화 없음**. 모바일 UI·브레이크포인트는 이미 존재.

**v2 신설**: BroadcastChannel 은 **다른 프로세스라 도달 불가** — 라이브 갱신이 필요하면 SSE/WS 또는 Tauri 이벤트로 **별도 메커니즘**. `sync.ts` 를 "전송계층 추상화(local BC | server SSE)"로 리팩터하는 항목.

**보안**: `0.0.0.0` + 토큰 + **읽기/쓰기 라우트 분리**(4단계 Rust 설계 때부터 갈라둘 것). N4 축소(§2)와 짝.

**미결정 — PC 꺼진 상태 조회**: ⓐ **PWA 오프라인 캐시**(데이터 외부 유출 0 · 무료 · 무유지보수 / 한계=마지막 접속 시점 · 은퇴한 SW 를 이 용도로만 부활) vs ⓑ **클라우드 스냅샷 푸시**(어디서나 최신 / 대가=개인 학습 기록 사본이 제3자 인프라에 — **되돌리기 어려우므로 사용자 결정**). 배타적이지 않다(ⓐ 먼저, 부족하면 ⓑ). **5단계 착수 시 실제 불편을 보고 정한다.**

---

### 6단계 — CSS Modules → Tailwind v4

> **사용자 지시(2026-07-19)**. 원래 이 문서의 판단은 "안 함"이었다 — 규칙 #4 는 지시를 뒤집지 말라는 것이지 근거를 지우라는 게 아니므로 반대 논거와 완화책을 함께 남긴다.

**실측 난이도** (`*.module.css` 54개 · 8,053 선언)

| 구분                                       | 개수      | 적합도                 |
| ------------------------------------------ | --------- | ---------------------- |
| color·font-size·display·background         | 2,196     | ◎                      |
| padding·gap·border-radius·flex·align-items | 1,488     | ◎                      |
| `var(--토큰)` 참조                         | 2,159     | ○                      |
| calc/clamp/minmax · 그리드 배치            | 158       | ○                      |
| **복합·상태 셀렉터**                       | **1,904** | **✗ 난이도 전부 여기** |

~47% 는 기계적 치환. 마찰은 `.card:hover .cardTools` · `ds.card` 의 `nth-child` 스태거 · `focus-within` 툴바 같은 **관계형 스타일**에 몰려 있고, 이건 `group-*`/`peer-*`/`data-[]:` 로 **JSX 구조를 다시 짜야** 한다.

**v2 정정 — 수치·표면**

- 스냅샷은 66이 아니라 **59장**(`visual.spec.ts-snapshots/` 실측. 66은 _테스트 수_).
- `composes:` 는 2곳이 아니라 **11파일 20건**. Tailwind 에 대응물이 **없어 전부 수동 해체**. 목록: `ds.module.css:141` · `ui/Card:7` · `ui/Kpi:6,10,16,20` · `ui/Pill:3` · `ui/ProgressBar:5` · `alloc/AllocBoard:438,441,444` · `graph/Graph:318` · `degree/SeasonRoadmap:6` · `integrations/TelemetryConsole:6` · `journal/JournalStream:6` · `stats/Stats:342,345,349` · `markets/Markets:309` · `reads/Reads:663` · `settings/Settings:80,93,105` · `schedule/Schedule:126`.
- 전역 클래스는 27이 아니라 **43개(4파일)**. 그리고 `motion.css` 의 **View Transitions(`::view-transition-*`)는 유틸리티로 표현 불가** → **전역 CSS 는 0이 되지 않는다.** v1 의 종료 조건("`ds.module.css` 0줄")은 `global/` 을 안 다뤘다 → 종료 조건 재정의.

**v2 정정 — PALETTE 소재지(v1 오진)**
v1 은 "`tokens.css` 115변수를 `@theme` 로 옮기면 PALETTE 인덱스 파생이 산다"고 썼다. **`PALETTE` 는 `lib/utils.ts:21` 의 TS 배열이지 CSS 토큰이 아니다.** 결론(살아남는다)은 맞고 이유가 틀렸으며, 그 오진이 진짜 위험을 가렸다:
런타임 색은 **인라인 CSS 변수로 주입**된다 — `['--seg']` 4곳(`DayPlannerCards:51,293` · `DayPlanner:627` · `WeekCalendar:280`) · `['--sub']`(`AllocBoard:247`) · `['--tint']` 2곳(`ItemCard:63` · `SubjectSheet:189`). 동적 색은 **정의상 정적 클래스로 표현 불가**인데 v1 은 임의값을 error 로 차단하겠다고 했다 → **정면 충돌**.
→ **"런타임 CSS 변수 인라인 주입은 임의값 금지 룰의 명시적 예외"** 를 파둔다. `scheduler/engine.ts:416` 에 _"인라인 `--seg` 가 뷰의 `.mock` 토큰을 덮어 절대규칙 3을 깬다"_ 는 실제 회귀 기록이 이미 있다.

**v2 신설 — 조용히 깨지는 것 3종** (에러 없이 잘못 렌더 · 6단계가 "diff 눈 확인"에 의존하므로 특히 위험)

1. **`@theme` 트리셰이킹 + 폴백값** — `getComputedStyle` 로 토큰을 읽는 3곳(`AmbientCanvas:132-136` 4개 · `Graph.tsx:56-68` 8개 · `Settings.tsx:39` 1개)은 JS 에서만 읽으므로 Tailwind 가 **미사용으로 보고 제거**한다. `@theme static` 필수. 게다가 `@theme` 는 `--color-*` 네임스페이스 접두를 붙이므로 변수명이 바뀌는데, **세 파일 모두 폴백값이 있어 에러 없이 틀린 색으로 렌더**된다(`Graph.tsx:57` `|| fb`, `AmbientCanvas.tsx:133` `|| [0.02,…]`). → **"JS 에서 읽는 토큰 목록"을 `invariants.test.ts` 패턴으로 잠근다.**
2. **`Kpi` 후손 선택자 결합** — `Kpi.module.css:13-14` 주석대로 ds 의 `.kpi .v`/`.kpi .l` 이 성립하려면 부모·자식 **양쪽에 ds 클래스가 compose** 돼야 한다. Tailwind 로 옮기면 이 암묵 결합이 **컴파일 에러 없이 스타일만 빠진다**.
3. **시각 게이트가 색 회귀를 못 잡는다**(0-G에서 실증) — `maxDiffPixelRatio: 0.02` + 단일 계열 팔레트라 **과목 색 전량 교체가 스냅샷 59장을 통과**했다. 6단계는 색을 픽셀로 검증할 수 없으므로 **토큰→계산색 단위 테스트**(`invariants.test.ts` 패턴)로 잠글 것. 위 ①의 "JS에서 읽는 토큰 목록"과 같은 수단.
4. **stale dist 재사용** — `playwright.config.ts:34` `reuseExistingServer:!CI`. 별도 터미널에 preview 가 떠 있으면 **옛 dist 에 스냅샷을 찍는다**. 같은 파일 `:17-19` 가 SW stale 캐시는 막았는데 stale dist 는 안 막혀 있다. **6단계에서 stale 통과 = 검증망 소실.**

**v2 정정 — 전환 순서**
v1 은 "`골든/` 레퍼런스부터"라 했다. **재검토 필요** — 골든은 `features/today/` 이고 `TodaySignature` 는 **791줄 최장 · 인지복잡도 1위(18) · 결정로그 픽셀 결정이 가장 많이 박힌 곳**이다. 6단계 위험 3개를 동시에 최대치로 맞는다.
→ **더 작은 feature 로 규약을 먼저 굳히고 today 는 2~3번째.** 그리고 **`ds.module.css` 는 맨 마지막** — 공유 SSOT 라 건드리는 순간 59장이 전부 흔들려 "feature 단위 diff" 전략이 무너진다.

**v2 신설 — stylelint 대체 정밀화**
stylelint 가 강제하는 건 정확히 2개다: `media-feature-name-value-allowed-list`(`max-width` = 560/700/900 만, `stylelint.config.js:27-30`) · `color-no-hex`(`:35`, `tokens.css` 만 예외 `:40-41`). 배경(`:32-34`)에 **라이트 테마 대비 결함 실사고**(`--ok` 1.58:1 · CBMS 칩 2.16:1 · 순위 dot 1.14:1)가 기록돼 있다.

- 브레이크포인트: Tailwind 테마 정의분만 쓰면 자동 해결되나 `[max-width:820px]` 임의값은 **`better-tailwindcss` 의 임의값 룰을 명시적으로 켜야** 막힌다(룰명 수준으로 못박을 것).
- **반픽셀 예외 필수**: `stylelint.config.js:17-22` 가 "font-size 반픽셀 금지를 일부러 안 넣은 이유"(월 캘린더 `.cell` 고정 행 높이 + `overflow:hidden` 에서 0.5px 상향이 과목 칩을 통째로 잘라먹은 실사고)를 기록한다. Tailwind `text-*` 는 정수 사다리라 **9.5/10.5/11.5px 가 전부 임의값** → 임의값 차단 룰과 충돌. **예외 목록 필요.**

**순서 제약**: **1~5단계와 겹치면 안 된다.** 스냅샷 대량 재생성이 다른 단계의 판정을 무력화한다(0-D 에서 의존성 메이저를 1단계 앞으로 당긴 것과 같은 이유). 플랫폼 이행 완료 후 착수하며, CSS 는 Tauri 와 직교하고 1~4단계가 새 feature 를 만들지 않아 미루는 비용이 없다.

---

## 5. 순서 의존성

```
0-B ✅ ─┐
0-C ✅ ─┼─→ 0-A ✅ ───────────────────────────→ 4단계
0-D ✅ ─┘         (동등성 명세 = serve.test.ts 26케이스)
0-E ✅ ──────────> 1단계   ← 하드 차단 해제(백업이 4개 키를 담는다)
0-G ✅ ───────────────────→ 2단계, 6단계  ← 스키마·색 경로 확정(id 해시)
0-F ✅ (독립)                 ↑ max-lines·cognitive-complexity 는 6단계에서 재기준선

▶ **0단계 전 항목 완료(2026-07-19).** 다음 착수 = 1단계.

1 → 2 → 3
    └───→ 4 → 5
              6 (1~5 완료 후)
```

**재기준선이 필요한 게이트**: 커버리지 임계(1·2단계에서 Tauri 래퍼가 분모 증가 · vitest 2→4 선례 절차 재사용) · `max-lines`(6단계) · CSS 예산(6단계 하향) · **js 번들 예산(v1 미검토 — Tauri 후 근거가 "네트워크 전송"에서 "파싱 비용"으로 바뀌지만 예산 자체는 유지가 맞다)**.

---

## 6. 인프라 변경

**테스트**

- vitest 에 **`setupFiles` 가 아예 없다**(`vitest.config.ts`). Tauri IPC mock(`@tauri-apps/api/mocks` 의 `mockIPC`/`clearMocks`) 주입 훅이 0개 → `test/setup/tauri.ts` 신설. **node/jsdom 혼재라 환경별 분기 setup 필요**.
- e2e mock 재작성 **2회**: 2단계(localStorage 시딩 6곳) · 4단계(`page.route('**/api/artifact/*')` 5종 → **IPC 는 `page.route` 로 못 가로챈다** → `page.addInitScript` 로 `__TAURI_INTERNALS__.invoke` 스텁).

**CI** (`.github/workflows/ci.yml`)

- 현재 `ubuntu-latest` 단일. **잡 분리** 권장: ubuntu = 웹 게이트(lint·typecheck·test·budget, OS 무관이라 저렴) / **windows = Rust·`cargo test`·Tauri 빌드**. `killTree`(손자 taskkill /T) 같은 Windows 전용 로직은 ubuntu 에서 검증 불가.
- 배포 매트릭스는 **불필요**(Windows 단일 타깃). windows 러너는 2배 과금이므로 `Swatinem/rust-cache` 필수.
- visual.spec 은 CI 제외 유지(win32 베이스라인 · 러너 GPU 상이).
- `gate.mjs:14` 의 `steps` 에 `tauri build` 추가 — 없으면 게이트가 셸을 전혀 안 본다.

**자산**: `web/public/icon.svg` 1개뿐. Tauri 는 `.ico`(멀티해상도)·`.png` 다수·설치프로그램 아이콘 필요. `tauri icon` 이 생성하나 **1,272바이트 SVG 는 16×16 축소 시 뭉갤 수 있어 고해상도 PNG 마스터(1024²) 준비가 실제 작업**.

---

## 7. 문서 갱신 트리거

> 이 개편은 **기존 문서 다수를 무효화**한다. 단계 완료 시 아래를 함께 고치지 않으면 코드와 문서가 갈린다.

| 시점      | 대상                                                                                                                                                                                                                                                                                                                                                       |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **즉시**  | `결정로그.md:126` **Tailwind 기각 문구**(설계와 정면 모순 · 같은 날짜라 판별 불가 · 규칙 #4 관련) · `결정로그.md:20` zod 경계 검증 **사실 오류**(0-B 로 정정) · `로드맵.md:203` **EX-4 드롭**(0-E 와 모순 — "쿼터가 찰까"와 "저장소가 축출 가능 등급인가"는 다른 문제. 판단 번복 기록) · `디자인시스템.md:78` **무조건 `e2e:update`**(6단계 안전망 무력화) |
| **1단계** | `CLAUDE.md:6,9,10`(2프로세스·bat) · `:14` 규칙 #1(주체를 Tauri 로) · **`:18` 규칙 #5 범위**("web 변경만" → "web/ + src-tauri/" — 문자 그대로면 **자기 마이그레이션 첫 커밋을 막는다**) · `:26` build 문구 · `:65-66` · `아키텍처.md:68` · `README.md:10,11,13`                                                                                             |
| **2단계** | `아키텍처.md:55,57,62`(영속 열 전체) · `:64` I1 승격 · `기능추가.md:14` **"디스크 JSON 형태 불변"**(문자 그대로면 2단계를 금지) · `:7,:25` · `README.md:74,75`                                                                                                                                                                                             |
| **3단계** | `CLAUDE.md:9`(볼트 FSA) · `아키텍처.md:36` · `README.md:13`(Chrome 권장)                                                                                                                                                                                                                                                                                   |
| **4단계** | `아키텍처.md` **§4 전면** · `:83,:88` · `CLAUDE.md:14,25,65-66` · `결정로그.md:30,37`(supersede 주석)                                                                                                                                                                                                                                                      |
| **5단계** | `아키텍처.md:74`(보안·N4) · `:82` · `결정로그.md:31` · `CLAUDE.md:71`                                                                                                                                                                                                                                                                                      |
| **6단계** | `CLAUDE.md:31`(lint:css) · `:70`("CSS Module 전면 전환 **완료**" — 방향이 뒤집힌다) · `아키텍처.md:38`·§5 신설·§6(스냅샷 운용 명문화) · `결정로그.md:24,25` · 프로토콜 3종 CSS 지시 · `디자인시스템.md:3,38-44,59-66,74,76` · `골든/README.md:11,16,22`                                                                                                    |

**로드맵 정리**: 삭제 — `:52`(serve.js 헬퍼 · 버려질 코드 투자) · `:161` SD-1(4단계 흡수) · `:40` C-6(0-B 상위집합) · `:203` EX-4. 흡수 — `:121` X-8 → 0-F. 격상 — `:24` 배분보드 단위 테스트를 **6단계 선행조건**으로(6단계가 그 e2e 시각 스냅샷을 대량 재생성하므로).

**보존**: `설계도.md` 전체(자기격리 완료 · 단 `:12` "'의존성 0' 교리 폐기 → **적응 우선**"은 이번 개편의 **사상적 선례**라 역참조 가치) · `MIGRATION.md`(역사 기록 · 헤더에 "6단계에서 Tailwind 로 재이행" 포인터 1줄) · `결정로그` 의 뒤집힌 결정은 **삭제가 아니라 supersede 주석**(왜 그랬는지가 사라지면 이 저장소가 반복해 겪은 실패로 돌아간다).

---

## 8. 리스크

| 리스크                                            | 완화                                                                                |
| ------------------------------------------------- | ----------------------------------------------------------------------------------- |
| **데이터 영구 유실**(WebView2↔Chrome 오리진 분리) | 0-E ③ 을 1단계 하드 차단으로 · 수동 export→import 플로우 · 릴리스 노트              |
| 무테스트 백엔드를 다른 언어로 포팅                | 0-A 를 4단계 전제조건으로 강제                                                      |
| 2단계 데이터 유실                                 | I1 + 양방향 검증 구간 + 기존 다층 방어(rebase 큐·CORRUPT_KEY·IDB 2세대) 유지        |
| **동기 flush 계약 파괴**                          | `onCloseRequested` + 비동기 완료 대기(1단계)                                        |
| 언어 경계 2→3(TS↔Rust↔Python)                     | Rust 를 4구간(볼트 스캔·파일감시·python spawn/트리킬·잡 모델)으로 한정              |
| 6단계 조용한 파손                                 | `@theme static` + JS 읽기 토큰 불변식 테스트 · ds 를 **마지막**에 · stale dist 차단 |
| 단계 표류                                         | 각 단계 독립 릴리스. 0단계만으로도 ③⑥⑧⑦ 해소                                        |

---

## 9. v1 → v2 정정 이력

1. **1단계 완료 기준의 순환 논증** — "e2e 스냅샷이 기능 변화 0을 증명"은 성립 불가(Playwright=Chromium/preview vs Tauri=WebView2). → 2트랙 분리 + 완료 기준 하향.
2. **데이터 영구 유실 미인지** — 0-E 를 "보험"으로 취급했으나 실제로는 1단계 하드 차단 전제.
3. **"1단계 = 기능 변화 0" 불성립** — `WORK` 경로 추론이 설치 경로에서 깨진다. 경로 설정 항목이 1단계에 필수.
4. PALETTE 소재지 오진(`tokens.css` → 실제는 `lib/utils.ts:21` TS 배열) + 런타임 변수 주입이 임의값 금지 룰과 충돌.
5. `usePing` 폐기 가정 → **재정의**(`useCapabilities`).
6. 수치 정정: 스냅샷 66→**59** · `composes` 2→**20건/11파일** · 전역 클래스 27→**43** · calc/grid "복잡" 주장 철회(2%).
7. N4 내부 모순(5단계가 `0.0.0.0` 요구) → 범위 축소.
8. 6단계 전환 순서(골든 먼저 → 작은 feature 먼저, ds 마지막).

---

## 10. 탈락시킨 대안

- **원격 서버 + DB** — 내구성만이면 **앱이 소유한 로컬 파일**(2단계)이 더 안전하고 싸다. 그리고 원격으로 가도 **볼트는 여전히 로컬**이다(`knowledge/` 마크다운을 이 기계에서 Obsidian 으로 편집하고 `pipeline/` 파이썬이 직접 읽는다 — 동기하려면 Dropbox 를 직접 만드는 일). 네트워크 노출은 인증·세션·CSRF·TLS·백업 운영을 사용자 1명을 위해 세우게 한다.
- **Next.js/RSC** — 서버 유무와 무관하게 값이 없다: SEO 없음 · 낯선 방문자 없음 · 사용자 1명이라 SSR 이 아낄 TTFB 가 없음 · **렌더할 데이터가 서버에 없다**(소유권이 클라이언트).
- **OPFS + wa-sqlite(WASM) + Worker** — Rust 없이 SQLite·Worker 이득을 대부분 얻는 유력 후보. 탈락: 2프로세스 실행이 남고 **File System Access API 에 watch 가 없어 파일 감시가 불가능**(§4-3단계가 제품 성격 변경이라 판단한 기능).
- **Electron** — 무겁고 Node 런타임을 안고 간다.
- **SonarQube Server/Cloud** — 룰은 `eslint-plugin-sonarjs` 로 인프라 0에 얻는다(§0-F). 서버가 더 주는 건 대시보드·추세뿐이고 Cloud 는 소스가 제3자로 나간다. 추세가 필요해지면 나중에 얹어도 룰 엔진이 같아 재작업 없음.
- **전면 재작성** — N1. 고점을 내리는 유일한 경로.
- **Tailwind 를 "안 함"** — 원래 판단이었으나 **사용자 지시로 6단계 채택**. 근거 정정도 기록: 처음엔 "레이아웃 기하가 복잡해 Tailwind 가 약하다"고 했으나 실측하니 calc/clamp/grid 는 158개(2%)뿐이라 **그 논거는 틀렸다**. 실제 마찰은 관계형 셀렉터 1,904개다.
