/* ============================================================
   playwright.motion.config.ts — **모션 하네스 전용**(H4 · 2026-07-30 `/감사 근본`).

   ## 왜 설정을 갈랐나 — 감도의 짝을 구조로 만든다

   `e2e/motion.spec.ts` 는 임계를 기본 설정보다 **10배 조여** 쓴다(`threshold 0.05` ·
   `maxDiffPixelRatio 0.0005`). E24 실측으로 그래야 했다 — 기본 임계로는 실제 변경(등장
   500→340ms · 1.47배)이 **통과했다**. 그리고 그 파일은 조인 임계의 **짝이 `--workers=1`** 이라고
   스스로 적었다: 더 조이면 웹폰트 스와프·서브픽셀 렌더가 flaky 로 들어온다.

   그 짝이 종전에는 **`e2e:motion` 스크립트 문자열에만** 있었다. 게이트(`gate.mjs`)와 CI 는
   `npm run e2e` 를 돌리고, 그건 `fullyParallel` + 워커 무제한이다 → **하네스가 요구한 조건이
   실제로 도는 경로에서 성립하지 않았다.** 게다가 `retries: 1` 이 그 결과를 flaky-녹색으로
   가리므로, 깨져도 "간헐적"으로 보이고 아무도 원인을 안 본다.

   규율을 스크립트에 두면 흘러내린다는 것이 이 저장소의 반복된 결론이다(stylelint·번들 예산·
   커버리지 임계를 들인 근거와 같다). 그래서 `workers: 1` 을 **설정으로** 못박고, 기본 설정은
   이 파일을 `testIgnore` 로 뺀다.

   ⚠ **커버리지는 줄지 않는다** — `npm run e2e` 가 두 설정을 이어 돌린다(`package.json`).
   ⚠ 나머지 옵션은 전부 기본 설정을 상속한다. 여기서 감도를 조정하지 말 것 —
     "무엇을 보고 무엇을 못 보는가"의 실측 표는 `e2e/motion.spec.ts` 머리주석이 SSOT 다.
============================================================ */
import { defineConfig } from '@playwright/test';
import base from './playwright.config';

export default defineConfig({
  ...base,
  /* 이 설정은 모션 스펙**만** 본다(기본 설정이 같은 파일을 제외한다 → 둘이 겹치지 않는다). */
  testIgnore: undefined,
  testMatch: '**/motion.spec.ts',
  /* ⚠ 이 한 줄이 이 파일의 존재 이유다. 지우면 조인 임계가 부하에 흔들리고, `retries` 가
     그걸 녹색으로 덮어 하네스가 "결정적이지만 아무것도 안 재는" 상태로 돌아간다. */
  workers: 1,
});
