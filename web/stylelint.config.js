/* ============================================================
   러닝허브 CSS 규약 강제.

   왜 stylelint-config-standard를 통째로 켜지 않는가:
     ① CSS Modules의 composes/:global 등을 모르는 규칙이 대량 오탐을 낸다,
     ② 수백 건의 스타일 취향 경고가 섞이면 '진짜 규약 위반'이 그 속에 묻힌다.
   그래서 **문서에 이미 적혀 있던 규약만** 골라 기계가 지키게 한다.

   배경: 감사 결론이 "규율은 강제되는 축(린트·타입·훅)에서만 지켜졌고, 관습에 맡긴 축은
   전부 흘러내렸다"였다. 토큰 규약이 정확히 후자였다 — 문서엔 있는데 채택률이 한 자릿수였고,
   그 틈에서 라이트 테마 대비 결함이 났다. 이 파일이 그 축을 '강제되는 쪽'으로 옮긴다.

   포맷(들여쓰기·따옴표 등)은 prettier가 소유하므로 여기선 포맷 규칙을 켜지 않는다.
============================================================ */
export default {
  rules: {
    // ⚠ 'font-size 반픽셀 금지'는 여기 넣지 않았다.
    // tokens.css의 "반픽셀 금지"는 **--fs-* 토큰 사다리**에 대한 규약이지 모든 리터럴 금지가 아니다.
    // 실제로 기존 반픽셀 52곳을 정수 토큰으로 일괄 상향해봤더니, 월 캘린더 셀(.cell은 고정 행 높이 +
    // overflow:hidden)에서 0.5px가 커진 만큼 칩 행이 눌려 **과목 칩이 통째로 잘렸다**(시각 스냅샷이 잡아냄).
    // 조밀한 UI의 9.5/10.5/11.5px는 그 자리에 맞춰 조정된 값이다 — 규칙으로 일괄 처리할 대상이 아니라
    // 레이아웃을 함께 손볼 때만 건드릴 것.

    // 브레이크포인트는 합의된 3종만(560 좁음 · 700 셸 모바일 경계 · 900 2컬럼 해제).
    // 예전엔 11종이 흩어져 셸(700)과 콘텐츠(720~920)가 서로 다른 모드로 렌더되는 구간이 있었다.
    // 새 값이 필요하면 tokens.css의 브레이크포인트 절을 먼저 고치고 여기 추가할 것.
    'media-feature-name-value-allowed-list': {
      'max-width': ['560px', '700px', '900px'],
      'prefers-reduced-motion': ['reduce', 'no-preference'],
    },

    // 생 hex 금지 — 색은 tokens.css의 의미론 토큰(var(--*))에서만 온다.
    // 이 규칙이 없던 시절 라이트 테마에서 --ok 1.58:1 · CBMS 칩 2.16:1 · 순위 dot 1.14:1처럼
    // "정보가 아예 안 보이는" 결함이 났다(다크 전용 hex가 흰 패널 위에 그대로 놓여서).
    'color-no-hex': true,
  },
  overrides: [
    {
      // 토큰 정의 자체는 hex를 쓸 수밖에 없다(여기가 색의 출발점).
      files: ['src/styles/tokens.css'],
      rules: { 'color-no-hex': null },
    },
  ],
  ignoreFiles: ['dist/**', 'dev-dist/**', 'node_modules/**', 'coverage/**'],
};
